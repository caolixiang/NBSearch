# 今日目标

- 完成 Phase 180，修复附件删除后无法重传的问题，并锁住多附件请求顺序。
- 完成 Phase 181，调整用户消息附件的对话气泡结构，使文本与附件卡片分离并右对齐。
- 完成 Phase 182，本地复刻文件底板图标样式，用扩展名短标签替换当前附件卡片里的 Lucide 文件图标。
- 完成 Phase 183，用户消息中的图片附件显示真实缩略图，并把缩略图元数据随消息持久化。
- 完成 Phase 184，使用 `scripts/bump-version.sh` 升级到 `v0.1.5`，并提交、打 tag、push 触发发版。
- 完成 Phase 179，使用 `scripts/bump-version.sh` 将版本升级到 `v0.1.4`，并提交、打 tag、push 触发发版。
- 完成 Phase 178，为语音入口增加 `连接中` 中间态，形成 `正常 -> 连接中 -> 停止` 的三段式交互。
- 完成 Phase 177，语音面板默认声音切到 Ara，默认个性化切到 Assistant。
- 完成 Phase 176，点击“来源”只打开来源抽屉，不再滚动消息列表。
- 完成 Phase 175，取消语音态默认附件占位，仅下压文字，上传附件后再增高。
- 完成 Phase 174，将模型同步按钮图标替换为云下载箭头 SVG。
- 完成 Phase 173，调整语音态顶部预留空间与附件展示对齐，让文字区域下移。
- 完成 Phase 172，启动后静默检查并后台下载更新包，下载完成后在头部显示“立即更新”按钮。
- 完成 Phase 171，放开语音面板顶部文本输入，改为“`不方便说话，你也可以打字`”并支持直接输入。
- 完成 Phase 170，将语速滑条回退为普通双端点样式，移除当前不协调的胶囊化视觉。
- 完成 Phase 161，收窄侧边栏底部“设置”入口尺寸，保留默认展开侧边栏。
- 完成 Phase 162，收紧语音速度滑杆的高亮轨道，避免覆盖右侧下一个刻度。
- 完成 Phase 163，将侧边栏“设置”入口再收一小档，避免图标与文字仍显偏大。
- 完成 Phase 164，继续压缩侧边栏“设置”入口的整体体量，直至不再显眼。
- 完成 Phase 165，修正语音速度滑杆的 fill/thumb 拼接样式，贴近官方结构。
- 完成 Phase 166，继续提高语速滑杆 active 区域的垂直体量。
- 完成 Phase 167，仅在选中“自定义”人格时显示说明表单。
- 完成 Phase 168，统一语速滑杆 active 区域的高度边界，消除“蘑菇头”。
- 完成 Phase 169，将语速控件改成真实可拖动滑条，移除当前易错的半圆胶囊结构。

## 范围

- In:
  - 调整 `src/components/chat-sidebar.tsx` 底部设置入口的字号、图标和内边距。
  - 调整 `src/components/voice-settings-sheet.tsx` 语速滑杆的高亮轨道与 thumb 呈现。
  - 调整用户消息图片附件的预览生成、持久化与渲染回显链路。
  - 使用 `scripts/bump-version.sh` 升级版本到 `v0.1.5`，并推送分支与 tag。
  - 补齐 20260309 的 plan/memo、测试记录与提交记录。
- Out:
  - 不调整侧边栏默认展开逻辑。
  - 不改动其他语音/输入框/设置面板样式。

## Phase 计划

### Phase 161 - 收窄侧边栏设置入口

- 目标：在保留当前视觉方向的前提下，把底部“设置”按钮从过大状态收回到协调尺寸。
- 实施项：
  - 更新 `chat-sidebar.tsx` 中设置入口的 padding、gap、icon size 和 text size。
  - 保持 `chat-shell.tsx` 侧边栏默认展开逻辑不变。
- 验收项：
  - 底部“设置”入口视觉尺寸明显回落，不再压迫侧边栏。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 162 - 收紧语速滑杆高亮轨道

- 目标：让速度滑杆的深色高亮停在当前刻度，不再向右侵入下一个刻度点。
- 实施项：
  - 更新 `voice-settings-sheet.tsx` 的速度轨道填充宽度计算与 thumb 呈现。
  - 保持 `0.1x` 步进和全部刻度可点击逻辑不变。
- 验收项：
  - 语速滑杆在中高档位时不再出现高亮块压住右侧刻度。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 163 - 再收侧边栏设置入口

- 目标：在不改变结构的前提下，将底部“设置”入口的图标与文字再缩小一点。
- 实施项：
  - 微调 `chat-sidebar.tsx` 中设置入口的 `gap`、`icon size`、`text size`。
  - 保持侧边栏默认展开逻辑与整体布局不变。
- 验收项：
  - “设置”入口比 Phase 161 更克制，图标与文字不再显得偏大。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 164 - 再压设置入口体量

- 目标：进一步降低底部“设置”入口的视觉占比，使其接近普通列表入口而不是强调按钮。
- 实施项：
  - 继续下调 `chat-sidebar.tsx` 中设置入口的 `padding`、`gap`、`icon size`、`text size`。
  - 保持 hover、点击区域与侧边栏默认展开逻辑不变。
- 验收项：
  - “设置”入口不再显得偏大，观感更接近普通侧边栏项。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 165 - 修正语速滑杆样式结构

- 目标：让速度滑杆的高亮 fill 与 thumb 的连接关系更接近官方，不再出现一整段黑色胶囊感。
- 实施项：
  - 调整 `voice-settings-sheet.tsx` 的轨道高亮层为左侧填充。
  - 调整 active thumb 为自带右半圆深色底的结构，保留白色圆心。
- 验收项：
  - 1.0x 附近的滑杆样式不再显得突兀，fill 与 thumb 拼接自然。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 166 - 增加语速滑杆 active 高度

- 目标：让滑杆当前选中区域的垂直体量再厚一点，避免 active 区看上去偏瘦。
- 实施项：
  - 微调 `voice-settings-sheet.tsx` 中 fill 层的 `inset-y`。
  - 微调 active thumb 深色底的可见高度。
- 验收项：
  - active 区域高度较 Phase 165 更饱满，但不破坏轨道边框与圆角。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 167 - 仅在自定义人格显示说明表单

- 目标：让个性化里的“说明”编辑器只在选择“自定义”时出现，默认 `Assistant` 等人格不显示。
- 实施项：
  - 调整 `voice-settings-sheet.tsx` 中说明表单的显示条件。
  - 新增轻量辅助函数与单元测试，锁住显示条件。
- 验收项：
  - 默认 `Assistant` 人格不显示说明区域。
  - 选择“自定义”后说明区域出现且可保存。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 168 - 统一语速滑杆 active 高度边界

- 目标：让滑杆 active 深色底与外层按钮容器使用相同高度，消除当前的“蘑菇头”溢出感。
- 实施项：
  - 统一 `voice-settings-sheet.tsx` 中 active 按钮容器与深色底高度。
  - 保持现有步进、点击范围与横向结构不变。
- 验收项：
  - active 区域高度与 fill 对齐，不再出现上下突出的视觉错位。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 169 - 切换为真实可拖动滑条

- 目标：将语速控件从当前易错的伪滑条切换为真实 `range` 滑条，保留 `0.1x` 步进与当前视觉方向。
- 实施项：
  - 将 `voice-settings-sheet.tsx` 的速度控件改为真实 `input[type=range]` 拖动输入。
  - 简化视觉为轨道、左侧 fill、圆形 thumb 和点阵辅助刻度。
- 验收项：
  - 语速可以拖动调节。
  - 不再出现“蘑菇头”或右侧与灰点距离异常的问题。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 170 - 回退为普通双端点滑条

- 目标：放弃当前语速控件的胶囊化轨道，回退为普通双端点线性滑条，减少违和感。
- 实施项：
  - 调整 `voice-settings-sheet.tsx` 的速度控件视觉结构为细轨道、左侧进度、双端点与圆形 thumb。
  - 保留真实 `range` 拖动输入与 `0.1x` 步进逻辑。
- 验收项：
  - 速度控件不再出现大胶囊或伪官方样式。
  - 仍可拖动调节语速。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 171 - 放开语音态文本输入

- 目标：让语音面板顶部区域从静态标题恢复为可编辑输入框，支持直接打字。
- 实施项：
  - 调整 `chat-input.tsx` 中语音态 textarea 的 placeholder、只读状态和聚焦行为。
  - 放开语音态的文本输入、回车发送与粘贴附件逻辑。
- 验收项：
  - 语音面板显示文案“`不方便说话，你也可以打字`”。
  - 进入语音态后输入框出现光标并可输入。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 172 - 头部立即更新按钮

- 目标：启动后静默检查并后台下载更新包，下载完成后在主界面头部显示“立即更新”按钮。
- 实施项：
  - 扩展 Rust updater 命令，支持准备更新与安装已准备更新。
  - 新增头部更新 hook，在 `ChatShell` 启动时静默触发后台下载。
  - 在主界面头部挂载“立即更新”按钮，点击后安装并重启。
- 验收项：
  - 启动后自动静默检查更新。
  - 有更新且下载完成时，头部显示“立即更新”按钮。
  - 点击按钮后安装并重启。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 173 - 语音态预留附件空间

- 目标：将语音面板顶部文字区域下移，预留附件展示空间，并让语音态附件展示与普通聊天对齐。
- 实施项：
  - 调整 `chat-input.tsx` 中语音态附件托盘的渲染条件与留白。
  - 调整语音态 textarea 的起始内边距与高度，让文本区域整体下移。
- 验收项：
  - 语音面板顶部有稳定的预留空间。
  - 上传图片或文件后，展示与普通聊天输入区对齐。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 174 - 替换模型同步按钮图标

- 目标：将模型同步按钮的刷新图标替换为云下载箭头图标，保持现有交互和状态提示不变。
- 实施项：
  - 在 `model-selector.tsx` 中移除 `RotateCw` 图标依赖。
  - 新增内联 SVG 图标组件并替换同步按钮图标。
- 验收项：
  - 头部同步按钮显示为新的云下载箭头图标。
  - 同步按钮交互、禁用态和状态文案保持不变。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 175 - 语音态文字下压

- 目标：取消语音态默认附件占位高度，改为仅下压文字；只有上传附件时才额外增高。
- 实施项：
  - 调整 `chat-input.tsx` 中附件托盘渲染条件，仅在存在附件时显示。
  - 调整语音态 textarea 的上内边距：无附件时明显下压，有附件时收回为紧贴附件托盘。
- 验收项：
  - 无附件时文字明显下压。
  - 上传附件后顶部区域再增高。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 176 - 来源按钮不触发消息滚动

- 目标：修正消息区“来源”按钮的交互，点击后只打开来源抽屉，不再把对话滚动到该消息的思考面板位置。
- 实施项：
  - 调整 `src/components/chat-message.tsx` 中来源按钮点击处理，移除 `scrollIntoView` 行为。
  - 保留原有来源抽屉打开事件分发逻辑，不改动来源数量显示与抽屉内容。
- 验收项：
  - 点击“来源”后，对话列表当前位置不发生跳动。
  - 来源抽屉仍能正常打开。
  - `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 177 - 调整语音默认角色与个性化

- 目标：让打开语音面板时默认选中 `Ara` 声音和 `Assistant` 个性化，降低首次使用时的认知偏差。
- 实施项：
  - 在 `src/components/voice-settings-sheet.tsx` 中抽出语音默认常量，并将未知 voice id 的标签 fallback 调整为 `Ara`。
  - 在 `src/components/chat-input.tsx` 中改用新的默认常量初始化语音状态。
  - 新增 `src/components/voice-settings-defaults.test.ts` 锁住默认值与 fallback 行为。
- 验收项：
  - 打开语音面板时默认高亮 `Ara`。
  - 个性化默认高亮 `Assistant`。
  - `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 178 - 语音入口连接中状态

- 目标：为普通聊天输入区的语音入口按钮增加 `connecting` 中间态，避免从静止按钮直接跳到语音面板。
- 实施项：
  - 新增 `src/components/chat-input-voice-entry.ts`，抽出语音入口状态、连接中延时、aria 文案与波形常量。
  - 调整 `src/components/chat-input.tsx`，将语音入口改为 `idle | connecting | active` 状态机，并在组件卸载/禁用时清理连接定时器。
  - 新增 `src/components/chat-input-voice-entry.test.ts`，锁住连接中文案与波形常量。
- 验收项：
  - 普通输入区语音按钮点击后先进入灰底 `连接中` 态。
  - 随后切到现有语音面板与 `停止` 按钮。
  - `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 179 - 升级版本并触发 v0.1.4 发版

- 目标：将桌面应用版本升级到 `0.1.4`，并把当前 head 作为 `v0.1.4` 标签推送到远端以触发 GitHub Release 构建。
- 实施项：
  - 运行 `scripts/bump-version.sh v0.1.4`，同步更新 `package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`。
  - 运行发布前校验命令，确认当前 head 仍可通过测试、类型检查与前端构建。
  - 提交版本变更，创建 `v0.1.4` tag，并 push 分支与 tag。
- 验收项：
  - 三个版本文件均为 `0.1.4`。
  - `bun test`、`bun run typecheck`、`bun run build` 通过。
  - 本地存在 `v0.1.4` tag，且已 push 到远端。
- 状态：done

### Phase 180 - 修复附件重传与多附件回归

- 目标：修复附件在删除后无法立即再次上传的问题，并明确锁住多附件请求不会被压扁为单个 block。
- 实施项：
  - 调整 `src/components/chat-input.tsx`，在每次打开文件选择器前、发送后、删除附件后都清空底层 file input 的内部选择状态。
  - 保持 `attachments` 追加逻辑，确保连续多次选择文件时仍然追加到同一轮消息。
  - 在 `src/infrastructure/chat/gateway-turn-request-builder.test.ts` 新增多附件顺序测试，锁住同一条 user message 内多个 `image_url` / `file` block 的输出顺序。
- 验收项：
  - 附件上传后删除，可立即再次上传同一文件，无需切换对话。
  - 单轮消息中可连续加入多个附件。
  - `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 181 - 对齐用户消息附件气泡

- 目标：将用户消息中的文本与附件从同一个气泡中拆开，改为更接近官方的“文字气泡 + 独立附件卡片”右对齐布局。
- 实施项：
  - 调整 `src/components/chat-message-attachments.tsx`，将 user message 的文本块和附件块拆分渲染。
  - 保留现有附件名解析与图标映射逻辑，仅调整圆角、边框、间距和右对齐方式。
  - 让附件卡片拥有独立背景与边框，不再作为文字气泡的内嵌子块出现。
- 验收项：
  - 用户消息带附件时，文本气泡与附件卡片分离显示。
  - 附件卡片独立右对齐，视觉结构更接近官方截图。
  - `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 182 - 复刻附件文件底板图标

- 目标：将用户消息附件卡片左侧图标从当前 Lucide 图标替换为更接近官方的“文件底板 + 扩展名标签”样式。
- 实施项：
  - 新增本地资源 `src/assets/file-light.svg` 作为文件底板。
  - 调整 `src/components/chat-message-attachments.tsx`，去掉 Lucide 文件图标映射，改为扩展名 badge 渲染。
  - 保持现有附件名截断与卡片布局，仅替换左侧图标语言。
- 验收项：
  - PDF、Excel、Doc 等附件在消息卡片中显示文件底板样式。
  - 图标中心显示对应扩展名短标签。
  - `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 183 - 用户图片附件显示真实缩略图

- 目标：让用户消息中的图片附件显示真实缩略图，并把缩略图元数据随消息一起持久化，避免退化成 `IMG` 占位。
- 实施项：
  - 新增 `src/lib/chat-attachments.ts`，集中处理附件扩展名识别、图片预览生成与结构化元数据构建。
  - 扩展 `ChatMessage` / `SendChatTurnInput` 的附件类型，并在 `chat-shell.tsx`、`gateway-turn-bootstrap.ts`、`repository.ts` 中串起“发送前生成 -> optimistic 渲染 -> 本地持久化 -> reload 回显”的链路。
  - 调整 `src/components/chat-message-attachments.tsx`，让图片附件按官方式样显示真实缩略图，多图时保持右对齐换行；文件附件继续复用文件底板样式。
  - 新增 `src/lib/chat-attachments.test.ts`，锁住图片缩略图生成成功与失败回退逻辑。
- 验收项：
  - 用户发送图片后，消息区显示真实图片缩略图，不再退化成 `IMG` 占位。
  - 刷新/重载本地消息后，图片附件仍可按已持久化的缩略图元数据回显。
  - `bun test`、`bun run typecheck`、`bun run build` 通过。
### Phase 184 - 升级版本并触发 v0.1.5 发版

- 目标：将桌面应用版本从 `0.1.4` 升级到 `0.1.5`，并将版本提交、tag、push 到远端以触发新的 GitHub 打包流程。
- 实施项：
  - 执行 `scripts/bump-version.sh v0.1.5`，统一更新 `package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml` 与 `Cargo.lock` 版本号。
  - 执行 `bun run build` 与 `cargo check --manifest-path src-tauri/Cargo.toml`，验证版本元数据更新后构建链路正常。
  - 提交版本变更，创建并推送 `v0.1.5` tag 与当前工作分支。
- 验收项：
  - 前端与 Tauri 版本号都更新为 `0.1.5`。
  - 本地构建与 `cargo check` 通过。
  - `v0.1.5` tag 与当前分支成功 push 到远端。
- 状态：done

- 状态：done

## Phase 验收记录

### Phase 161

- 验收结果：已完成。底部“设置”入口已缩小，侧边栏默认展开逻辑保持不变。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 162

- 验收结果：已完成。速度滑杆改为在内轨中按百分比裁切高亮层，中高档位不再覆盖下一个刻度。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 163

- 验收结果：已完成。进一步收小了“设置”入口的间距、图标和字号，容器高度保持不变。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 164

- 验收结果：已完成。已将“设置”入口进一步压到常规侧边栏项的体量，收小了留白、图标与文字。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 165

- 验收结果：已完成。滑杆已改为左侧 fill 加 active thumb 右半圆底的结构，1.0x 附近不再是整段黑色胶囊。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 166

- 验收结果：已完成。已同步抬高 fill 层与 active thumb 的深色底高度，选中区域更饱满。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 167

- 验收结果：已完成。说明表单现在仅在 `custom` 人格下显示，默认 `Assistant` 等人格不再展示。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 168

- 验收结果：已完成。active 按钮容器与深色底现已使用同一高度边界，不再出现上下溢出的“蘑菇头”错位。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 169

- 验收结果：已完成。语速控件已切换为真实 `range` 滑条，保留点阵和左侧 fill，去除了半圆胶囊伪结构。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 170

- 验收结果：已完成。语速控件已回退为普通双端点滑条，移除了当前不协调的胶囊化视觉。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 171

- 验收结果：已完成。语音面板顶部区域已改为可编辑输入框，显示“`不方便说话，你也可以打字`”，进入语音态后会自动聚焦并可直接输入。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

- Phase 172 暂未新增单测：更新链路依赖 Tauri 原生 updater 运行时与安装行为，本轮以类型检查、现有单测和构建通过作为回归保障。

### Phase 172

- 验收结果：已完成。应用启动后会静默检查并后台下载更新包，下载完成后在主界面头部显示“立即更新”按钮，点击后安装并重启。
- 备注：已通过 `cargo check --manifest-path src-tauri/Cargo.toml`、`bun run typecheck`、`bun test`、`bun run build`。

### Phase 173

- 验收结果：已完成。语音态顶部现在会预留附件展示空间，文字输入整体下移；上传图片或文件后，展示留白与普通聊天输入区保持同一套对齐。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 174

- 验收结果：已完成。头部模型同步按钮已替换为云下载箭头 SVG，同步交互、禁用态与旁侧状态提示均保持不变。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 175

- 验收结果：已完成。语音态默认不再占附件高度，文字通过更大的顶部内边距下压；只有上传附件后，顶部区域才会额外增高并展示附件托盘。
- 备注：已通过 `bun run typecheck`、`bun test`、`bun run build`。

### Phase 176

- 验收结果：已完成。点击消息操作区“来源”后不再调用 `scrollIntoView`，页面位置保持不变，同时来源抽屉打开逻辑保持原样。
- 备注：本轮未新增单测，原因是改动仅为单点 UI 滚动副作用移除；已通过 `bun test`、`bun run typecheck`、`bun run build` 作为回归保障。

### Phase 177

- 验收结果：已完成。语音面板默认状态已切到 `Ara + Assistant`，顶部语音按钮标签 fallback 也同步改为 `Ara`，并新增默认值单测覆盖。
- 备注：已通过 `bun test`、`bun run typecheck`、`bun run build`。

### Phase 178

- 验收结果：已完成。语音入口现在会先显示 `连接中……` 的灰底波形按钮，再切到现有语音面板；`handleStopVoiceMode`、禁用语音和组件卸载时都会清理连接定时器。
- 备注：已通过 `bun test`、`bun run typecheck`、`bun run build`。

### Phase 179

- 验收结果：已完成。版本文件已统一升级到 `0.1.4`，发布前校验通过，并将 `v0.1.4` tag 推送到远端以触发发版流程。
- 备注：本轮为发布版本升级，沿用当前 head 的功能代码，仅更新版本元数据与 release 标签。

### Phase 180

- 验收结果：已完成。上传按钮现在会在重新打开选择器前清空原生 file input 的旧值，发送和删除时也会同步重置；同时新增回归测试锁住同一条 user message 内的多附件 block 顺序。
- 备注：已通过 `bun test`、`bun run typecheck`、`bun run build`。

### Phase 184

- 验收结果：已完成。版本文件已统一升级到 `0.1.5`，并通过 `bun run build` 与 `cargo check --manifest-path src-tauri/Cargo.toml` 校验；接下来提交、打 `v0.1.5` tag 并 push 到远端。
- 备注：本轮为发布版本升级，只调整版本元数据，不涉及业务逻辑改动。

### Phase 181

- 验收结果：已完成。用户消息中的文本气泡与附件卡片已拆分渲染，附件卡片改为独立右对齐的边框卡片，避免再包裹在同一个大气泡中。
- 备注：本轮为单点视觉结构调整，未新增单测；已通过 `bun test`、`bun run typecheck`、`bun run build` 作为回归保障。

### Phase 182

- 验收结果：已完成。用户消息附件卡片已切到本地文件底板图标，中心显示扩展名短标签，PDF/XLS/DOC 等文件的识别与展示更接近官方样式。
- 备注：本轮为本地资源与 UI 映射调整，未新增单测；已通过 `bun test`、`bun run typecheck`、`bun run build` 作为回归保障。

### Phase 183

- 验收结果：已完成。用户消息中的图片附件现在会优先生成真实缩略图并随消息持久化，消息区多图展示时不再回退成 `IMG` 占位；文件附件样式保持不变。
- 备注：新增 `chat-attachments` 单测，覆盖位图缩略图生成和 `FileReader` 回退链路；已通过 `bun test`、`bun run typecheck`、`bun run build`。

## 测试记录

- `scripts/bump-version.sh v0.1.5`：通过，已统一更新 `package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`。
- `bun run build`：通过（保留既有 Vite chunk warning 与 lottie eval warning）。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。

- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `bun test src/lib/chat-attachments.test.ts`：通过（5 pass）。
- `bun test`：通过（265 pass）。
- `scripts/bump-version.sh v0.1.4`：通过，已更新三个版本文件。
- `git tag v0.1.4`：待补充。
- `git push origin feat/tauri-migration`：待补充。
- `git push origin v0.1.4`：待补充。
- `bun run typecheck`：通过。
- `bun run build`（按需）：通过（保留既有 Vite chunk warning 与 lottie eval warning）。

## 提交记录

- `05f61da` - `phase(182): restyle attachment file icons`


- `4e349a2` - `phase(181): align user attachment bubbles`


- `eda79b6` - `phase(180): reset attachment picker between uploads`

- `f443639` - `phase(179): bump version to 0.1.4`

- `e81f22f` - `phase(178): add voice entry connecting state`

- `e3090b2` - `phase(177): default voice panel to ara assistant`

- `892d6d7` - `phase(176): keep source drawer from scrolling messages`

- `33381c7` - `phase(161): reduce sidebar settings entry size`
- `4bf1be1` - `phase(161): backfill plan and memo commit trace`
- `965d6af` - `phase(162): tighten voice speed slider fill`
- `d446a5e` - `phase(162): backfill plan and memo commit trace`
- `ae1b6ba` - `phase(163): reduce settings entry size again`
- `c656919` - `phase(163): backfill plan and memo commit trace`
- `cfc4228` - `phase(164): shrink sidebar settings entry`
- `651e04d` - `phase(164): backfill plan and memo commit trace`
- `f8e02c2` - `phase(165): refine voice speed slider styling`
- `f869557` - `phase(165): backfill plan and memo commit trace`
- `8ca562e` - `phase(166): increase voice speed active height`
- `f8b9860` - `phase(166): backfill plan and memo commit trace`
- `fcd33db` - `phase(167): gate custom prompt editor by personality`
- `c058ed0` - `phase(167): backfill plan and memo commit trace`
- `901bce1` - `phase(168): align voice speed active height`
- `d28ce39` - `phase(168): backfill plan and memo commit trace`
- `f24e98e` - `phase(169): switch voice speed control to draggable slider`
- `06e51e4` - `phase(170): simplify voice speed slider styling`
- `33a278c` - `phase(171): enable typed input in voice mode`
- `0a589e4` - `phase(172): add prepared app update flow`
- `2f986b9` - `phase(173): align voice input attachment spacing`
- `1fb8114` - `phase(174): replace model sync icon`
- `96dae9f` - `phase(175): push down voice placeholder text`
- `81decca` - `phase(183): render user image attachment previews`


## 例外记录

- 无

## 明日计划

- 若附件链路仍有问题，优先做真实多附件发送探针，核对网关收到的 `messages[].content[]` 结构。

- 关注 `v0.1.4` GitHub Release 构建结果；如有失败，优先修复打包链路与 updater 产物。

- 若用户继续收口语音面板，优先处理连接态与真实语音会话的对接，以及停止后的回收细节。

- 若用户继续收口语音面板，优先处理默认值、持久化与首开体验的一致性。

- 若用户继续收口消息区交互，优先处理来源抽屉、滚动定位和消息动作栏的细节一致性。
- 继续按用户反馈微调语音输入与侧边栏细节，如无新 UI 问题则回到更大块模块拆分工作。
