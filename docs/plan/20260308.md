# 今日目标

- 分阶段重构大文件，先后抽离 `chat-shell.tsx` 共享流式状态机、`grok-chat-service.ts` 恢复协议模块、媒体纯函数模块、媒体工作流模块，再继续切出发送主链的完成态组装器，持续降低回归概率。

## 范围

- In:
  - 为 `chat-shell.tsx` 抽取发送/重生成共享的流式处理辅助模块。
  - 为 `grok-chat-service.ts` 抽取会话恢复协议相关的查询与归一化辅助模块。
  - 为 `grok-chat-service.ts` 抽取媒体续签与 generated image 纯函数模块，并拆分对应纯函数测试。
  - 为 `grok-chat-service.ts` 抽取消息级媒体刷新与流结束卡片收口工作流模块。
  - 为 `grok-chat-service.ts` 抽取流结束后的完成态 turn 组装模块。
  - 保持现有发送、重生成、思考计时、卡片附件、错误处理、恢复协议、媒体续签与图片归并语义不变。
  - 为抽取模块补充或更新单元测试，锁住关键时序、状态归一化与媒体处理回退行为。
  - 执行 `bun run typecheck`、`bun test`、`bun run build` 回归验证。
- Out:
  - 不修改 `GrokChatService` 对外协议与恢复策略。
  - 不调整 UI 样式与交互文案。
  - 不在本 Phase 改动数据库、网络契约或上游接口。

## Phase 计划

### Phase 116 - 抽离 chat-shell 流式状态机辅助模块

- 目标：
  - 减少 `chat-shell.tsx` 中发送/重生成重复的流式事件处理逻辑，并用单测锁住行为。
- 实施项：
  - 从 `chat-shell.tsx` 提取共享的流式状态管理辅助模块，覆盖 delta/reasoning/card attachment/duration/cleanup 等公共行为。
  - 保留发送与重生成各自的差异动作在 `chat-shell.tsx` 本地，仅复用公共状态机与事件归并逻辑。
  - 新增单元测试，覆盖思考开始/结束、卡片附件延迟 flush、duration 计时、cleanup 等关键行为。
- 验收项：
  - `chat-shell.tsx` 发送/重生成逻辑中的重复状态机明显收敛。
  - 抽取模块有独立单测覆盖关键回归点。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 117 - 抽离 grok-chat-service 恢复协议模块

- 目标：
  - 将 `grok-chat-service.ts` 中与会话恢复接口强相关的配置归一化、状态解析、消息查询逻辑拆到独立模块，减少主服务文件体积并保持恢复链路语义不变。
- 实施项：
  - 提取恢复协议相关类型、配置归一化、JSON 归一化、重试等待与网关查询辅助逻辑。
  - 让 `GrokChatService` 通过显式依赖调用新模块，不改 `recoverPendingAssistant`、`streamTurn` 的对外行为。
  - 为新模块补充定向单元测试，覆盖状态/消息解析与 `invalid_after_response_id` 回退。
- 验收项：
  - `grok-chat-service.ts` 明显减重，恢复协议职责从主文件中收敛出去。
  - 新模块有独立测试，恢复相关既有测试继续通过。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 118 - 抽离媒体续签与 generated image 纯函数模块

- 目标：
  - 将 `grok-chat-service.ts` 中与媒体 URL 代理、续签、generated image 归并相关的纯函数拆到独立模块，并把纯函数测试从集成测试文件中拆出，继续压缩主文件体积并降低测试噪音。
- 实施项：
  - 提取媒体 URL 规范化、续签 payload 解析、generated image 资产推断与卡片归并辅助逻辑。
  - 让 `GrokChatService` 仅保留需要 `runtimeFetch` 的续签 I/O 封装和编排调用。
  - 新增独立纯函数测试文件，并从 `grok-chat-service.test.ts` 移出对应测试块。
- 验收项：
  - `grok-chat-service.ts` 与 `grok-chat-service.test.ts` 明显减重。
  - 新模块与新测试文件可独立回归，原有集成测试继续通过。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 119 - 抽离媒体刷新与流结束卡片工作流模块

- 目标：
  - 将 `grok-chat-service.ts` 中剩余的消息级媒体刷新与流结束 generated image 卡片收口逻辑下沉到独立工作流模块，进一步减少类内编排噪音，同时保留 renew API I/O 在类内。
- 实施项：
  - 提取消息级 `<tool-meta>` 卡片刷新工作流，改由外部 helper 接收 `hydrateGeneratedImageCards` 回调执行。
  - 提取流结束阶段的 generated image hydrate/dedupe/reasoning 归一化收口 helper。
  - 为新工作流模块补充定向单测，覆盖 markdown 重写与卡片去重行为。
- 验收项：
  - `grok-chat-service.ts` 继续减重，`listMessages` 与 `streamTurn` 的媒体编排明显收敛。
  - 新工作流模块有独立测试，既有集成测试继续通过。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 120 - 抽离流结束完成态组装模块

- 目标：
  - 将 `streamTurn` 在读流结束后负责 citation 合并、文案选择、reasoning 时长决策、`assistantMessage`/`anchors`/title 组装的收口逻辑拆到独立模块，继续缩小主发送链路体积。
- 实施项：
  - 新增完成态 turn 组装模块，封装 generated image 收口后的最终 content/research/duration/title 计算。
  - 将 `mergeResearchPayload` 与相关完成态纯辅助一并迁出 `grok-chat-service.ts`。
  - 为新模块补充定向单测，覆盖 citation 去重、最终文案选择、reasoning duration 优先级与 title 决议。
- 验收项：
  - `streamTurn` 的完成态收口明显收敛，主链路更聚焦于请求、reader 生命周期与恢复重试。
  - 新模块有独立测试，既有服务级回归继续通过。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 121 - 抽离流式分段累计器

- 目标：
  - 将 `streamTurn` 中针对每个 JSON segment 的状态累计、reasoning 事件归并、web search/card 收集与安全重试判定拆到独立模块，继续降低主链复杂度。
- 实施项：
  - 新增流式累计器模块，集中处理 response id/title/final message/reasoning/research/generated image card 的累计与去重。
  - 让 `streamTurn` 仅保留 reader、buffer 切片、request/retry 编排，分段处理统一交给累计器。
  - 为新模块补充定向单测，覆盖 synthetic tool_result、generated image 卡片去重、web search 去重与标题/response id 收集。
- 验收项：
  - `grok-chat-service.ts` 继续减重，流内 per-segment 状态机从主文件中明显收敛。
  - 新模块有独立测试，既有服务级回归继续通过。
  - `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 通过。
- 状态：done

### Phase 122 - 抽离网关 turn 请求构建器

- 目标：
  - 将 `grok-chat-service.ts` 中与附件序列化、用户消息文案拼装、responses 请求体组装相关的 helper 下沉到独立模块，继续缩小发送主链且不触碰 recovery/reader 语义。
- 实施项：
  - 新增请求构建器模块，集中承载 `buildUserMessageText()`、附件 data URI 序列化、`previous_response_id` 决策与 regenerate 请求体构造。
  - 让 `streamTurn` 改为直接调用请求构建器，移除主服务文件内联的附件 helper 和 content block 类型。
  - 为新模块补充定向单测，覆盖文本+附件、纯附件、oversize 校验、session anchor 与 regenerate payload 分支。
- 验收项：
  - `grok-chat-service.ts` 继续减重，请求体组装从主文件中明显收敛。
  - 新模块有独立测试，既有服务级回归继续通过。
  - `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 通过。
- 状态：done

### Phase 123 - 抽离 regenerate target 解析模块

- 目标：
  - 将 `resolveRegenerateTarget()` 中本地消息与 session 消息的 assistant 对齐算法抽到独立纯模块，保留 service 里的查询、消息回写与会话锚点更新。
- 实施项：
  - 新增 regenerate target resolver 模块，集中承载 role-shape、assistant ordinal 与 exact-content 三层匹配策略。
  - 让 `resolveRegenerateTarget()` 改为调用新模块，service 只保留 repository/recovery client 协调与结果落库。
  - 为新模块补充独立单测，覆盖空目标、空 session、role shape、assistant ordinal、唯一内容命中与内容歧义回退。
- 验收项：
  - `grok-chat-service.ts` 继续减重，regenerate target 匹配逻辑从主文件中明显收敛。
  - 新模块有独立测试，既有 regenerate 服务级回归继续通过。
  - `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 通过。
- 状态：done

### Phase 124 - 抽离 pending assistant recovery 工作流

- 目标：
  - 将 `recoverPendingAssistant()` 中的消息补拉、turn state 判定、completion 后补拉与 polling 编排下沉到独立模块，保留 service 里的本地消息读取、recovery client 装配与 completed turn 持久化。
- 实施项：
  - 新增 pending assistant recovery 模块，集中承载时间窗计算、anchor 前移判断、missing-message retrySend 判定、turn state 分支与 completion/poll 编排。
  - 让 `recoverPendingAssistant()` 改为薄封装，只负责读取本地消息、装配 recovery client 与注入 `persistRecoveredCompletedTurn()` callback。
  - 为新模块补充独立单测，覆盖立即恢复、streaming preview、active turn completed、anchor 前移 stale/non-stale 与 completion 延迟恢复。
- 验收项：
  - `grok-chat-service.ts` 继续减重，pending recovery 编排从主文件中明显收敛。
  - 新模块有独立测试，既有 recovery 服务级回归继续通过。
  - `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 通过。
- 状态：done

### Phase 125 - 抽离 completed-turn persistence 与 turn recovery 编排

- 目标：
  - 将 `persistRecoveredCompletedTurn()` 与 `attemptTurnRecovery()` 一并下沉到独立模块，保留 `GrokChatService` 中的 recovery client 装配、仓储调用注入与 `streamTurn()` 错误分支调度。
- 实施项：
  - 新增 completed-turn persistence 模块，集中承载 session messages 补拉、assistant 选择、已存在 assistant 更新/追加、anchor 刷新与会话标题回退逻辑。
  - 新增 turn recovery 模块，集中承载 turn state 查询、in-progress polling、completed/not_found/in_progress 分支决策与持久化 callback 编排。
  - 为两个模块补充独立单测，覆盖无 responseId、latest assistant fallback、existing assistant update、completed persistence、in-progress polling、active turn mismatch 与 failed 分支。
- 验收项：
  - `grok-chat-service.ts` 继续减重，completed-turn persistence 与 turn recovery 编排从主文件中明显收敛。
  - 新模块有独立测试，既有 service 集成回归继续通过。
  - `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 通过。
- 状态：done

### Phase 126 - 抽离 stream transport shell 与 generated-image renew I/O

- 目标：
  - 将 `streamTurn()` 中的 transport/retry shell 与 generated-image renew I/O 一并下沉到独立模块，保留 service 中的会话预写入、用户消息落库、完成态组装与最终持久化。
- 实施项：
  - 新增 stream transport 模块，集中承载请求发送、HTTP/idle timeout 重试、anchor conflict 去锚、turn recovery 编排以及 reader 读流/heartbeat/delta/reasoning 分发。
  - 新增 generated-image renew 模块，集中承载 renew API 调用、shared renew cache 与 generated image 卡片续签更新。
  - 让 `grok-chat-service.ts` 改为注入 recovery callback、event emitter 与 hydrator，不再内联 transport/retry/read-loop 与 renew I/O。
  - 为两个模块补充独立单测，覆盖 anchor conflict 去锚、transport failure recovery、idle timeout retry、turn_in_progress 失败、renew cache 复用与 non-renewable 卡片跳过。
- 验收项：
  - `grok-chat-service.ts` 继续减重，transport/retry shell 与 renew I/O 从主文件中明显收敛。
  - 新模块有独立测试，既有 service 集成回归继续通过。
  - `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 通过。
- 状态：done


### Phase 127 - 抽离 stream event/meta emitter 与 conversation preflight

- 目标：
  - 将 `streamTurn()` 中的流式事件发射器与会话 preflight/bootstrap 一并下沉到独立模块，保留 service 中的请求体构造、transport 调用、完成态组装与错误恢复主链。
- 实施项：
  - 新增 stream event emitter 模块，集中承载 started/delta/reasoning/heartbeat/completed/failed 事件发射与单调递增的 `meta.seq` / `responseId` 继承逻辑。
  - 新增 turn bootstrap 模块，集中承载 `conversationId/sessionId/clientTurnId` 解析、fallback title 决议、conversation 预写入与普通发送场景的 user message 追加。
  - 让 `grok-chat-service.ts` 改为注入 preflight/bootstrap 与 event emitter，不再内联 pre-upsert/user append 与 event/meta glue。
  - 为两个模块补充独立单测，覆盖显式 `responseId` 覆盖、seq 单调递增、regenerate/retryExistingUserMessage 跳过 append 与已有标题复用。
- 验收项：
  - `grok-chat-service.ts` 继续减重，event/meta glue 与 conversation preflight 从主文件中明显收敛。
  - 新模块有独立测试，既有 service 集成回归继续通过。
  - `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 通过。
- 状态：done


### Phase 128 - 抽离 completion persistence glue 与 regenerate sync

- 目标：
  - 将 `grok-chat-service.ts` 中剩余的 conversation persistence glue 与 regenerate target 同步逻辑下沉到独立模块，保留 service 中的接口实现、recovery 装配与 `streamTurn()` 主编排。
- 实施项：
  - 新增 conversation persistence 模块，集中承载 `createConversationRecord()`、fallback title、assistant turn 持久化与 anchors upsert。
  - 新增 regenerate target sync 模块，集中承载本地 assistant response id 回写、session 消息匹配与会话锚点刷新。
  - 让 `grok-chat-service.ts` 改为经 helper 调用完成持久化与 regenerate sync，不再内联会话标题/anchors/assistant id 回写细节。
  - 为两个模块补充独立单测，覆盖 regenerate 截枝持久化、anchors 刷新、空目标回退与 gateway session 对齐。
- 验收项：
  - `grok-chat-service.ts` 继续减重，completion persistence glue 与 regenerate sync 从主文件中明显收敛。
  - 新模块有独立测试，既有 service 集成回归继续通过。
  - `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 通过。
- 状态：done

### Phase 129 - 拆分 grok-chat-service 服务级测试

- 目标：
  - 将 `grok-chat-service.test.ts` 中媒体、恢复、重生成等高噪音用例拆到独立测试文件，保留基础流式/对话回归在主测试文件内，降低后续重构时的测试维护成本。
- 实施项：
  - 新增测试辅助模块，收敛 conversation/turn 夹具、流式事件工厂与常用断言准备逻辑。
  - 将媒体场景、恢复协议场景、重生成场景分别拆到独立测试文件；保留 `grok-chat-service.test.ts` 作为基础流式与普通对话回归入口。
  - 调整测试导入与共享桩，确保拆分后仍覆盖 service 主链关键行为。
- 验收项：
  - `grok-chat-service.test.ts` 明显减重，媒体/恢复/重生成测试迁移到独立文件。
  - 拆分后的测试文件与 helper 可独立维护，既有回归语义保持不变。
  - `bun run typecheck`、定向 `bun test` 通过。
- 状态：done

### Phase 130 - 拆分 chunk-parsers 聚合器与研究解析模块

- 目标：
  - 将 `chunk-parsers.ts` 拆成公共解析、card attachment、research 聚合等聚焦模块，避免继续在单文件里堆叠 deep research / image card / citation 解析细节。
- 实施项：
  - 新增公共解析模块，承载 JSON token 清洗、基础引用/消息辅助与共享小工具。
  - 新增 card attachment 与 research 模块，分别承接图片卡、引用卡、research steps / citations / inline citations 聚合逻辑。
  - 同步拆分 `chunk-parsers` 测试，将图片卡与 research 回归迁到独立测试文件，保留基础解析用例在主测试文件。
- 验收项：
  - `chunk-parsers.ts` 明显减重，deep research 与图片卡解析职责迁移到聚焦模块。
  - 新模块与新测试文件能独立维护，既有解析结果语义不变。
  - `bun run typecheck`、定向 `bun test` 通过。
- 状态：done

### Phase 131 - 抽离网关恢复 runtime 装配层

- 目标：
  - 将 `grok-chat-service.ts` 里剩余的恢复协议 runtime 装配与依赖拼装下沉到独立模块，保留 service 主体只负责对外接口与最薄编排。
- 实施项：
  - 新增 recovery runtime 模块，以闭包 + getter 方式接收 fetch/store/logger/配置读取，避免捕获构造时依赖导致测试替换失效。
  - 让 `grok-chat-service.ts` 改为委托 runtime 模块处理恢复链路装配与等待策略，不再直接维护相关细节。
  - 为新模块补充独立测试，覆盖 getter 取值、重试参数与依赖替换兼容性。
- 验收项：
  - `grok-chat-service.ts` 继续减重，恢复协议 runtime 装配从主文件中明显收敛。
  - 新模块有独立测试，且 service 构造后替换 `runtimeFetch` 的既有测试仍通过。
  - `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 通过。
- 状态：done

### Phase 132 - 拆分 recovery 服务级测试

- 目标：
  - 将 `grok-chat-service.recovery.test.ts` 按 pending assistant 恢复与 streamTurn transport/retry 两类场景拆分，降低单文件测试噪音并维持恢复协议回归覆盖。
- 实施项：
  - 将 `recoverPendingAssistant()` 相关场景拆到独立测试文件，保留 `streamTurn()` transport failure / turn recovery / in-progress retry 场景在主 recovery 测试文件。
  - 复用现有 `grok-chat-service-test-helpers.ts` 的 service/fetch 装配辅助，减少重复样板。
  - 保持现有测试断言与恢复协议语义不变。
- 验收项：
  - `grok-chat-service.recovery.test.ts` 明显减重，pending assistant 恢复测试迁移到独立文件。
  - recovery 相关定向测试继续通过。
  - `bun run typecheck`、定向 `bun test` 通过。
- 状态：done

### Phase 133 - 拆分 research 解析聚合模块

- 目标：
  - 将 `chunk-parsers-research.ts` 中 citation/XML 提取与 research step/detail/merge 聚合逻辑拆到聚焦模块，保持对外出口不变并继续压缩主聚合文件。
- 实施项：
  - 抽离 citation/inline citation/XML render 解析辅助模块。
  - 抽离 research step/detail/object/merge 聚合模块。
  - 让 `chunk-parsers-research.ts` 保留最薄聚合入口与对外导出，不改上层调用点。
- 验收项：
  - `chunk-parsers-research.ts` 明显减重，内部职责更清晰。
  - research 相关定向测试继续通过。
  - `bun run typecheck`、定向 `bun test` 通过。
- 状态：done

### Phase 134 - 继续拆分 pending recovery 服务级测试

- 目标：
  - 将 `grok-chat-service.pending-recovery.test.ts` 继续按“pending 可见性/状态恢复”和“anchor/after_response_id/completion lag”两类场景拆开，并收敛重复的 service 配置样板。
- 实施项：
  - 为测试 helper 增加带配置 overrides 的 service 创建辅助。
  - 将 pending assistant 的 in-progress/preview/turn state 场景与 anchor advanced/invalid after/completion lag 场景拆到不同测试文件。
  - 保持恢复协议断言与现有语义不变。
- 验收项：
  - `grok-chat-service.pending-recovery.test.ts` 明显减重，pending recovery 相关测试按语义分组。
  - recovery 相关定向测试继续通过。
  - `bun run typecheck`、定向 `bun test` 通过。
- 状态：done

### Phase 135 - 继续拆分 research fragment 聚合模块

- 目标：
  - 将 `chunk-parsers-research-fragments.ts` 中 tool usage/step 解析与 research object/detail/merge 聚合进一步拆分，继续降低单文件复杂度。
- 实施项：
  - 新增 tool usage/step 解析模块，承接 XML、toolUsageResults 与 step 归一化。
  - 新增 research object/merge 模块，承接 details、object 解析、messageTag step 构造与 fragment merge。
  - 保持 `chunk-parsers-research-fragments.ts` 为最薄重导出层，维持现有上层调用不变。
- 验收项：
  - `chunk-parsers-research-fragments.ts` 明显减重。
  - research 相关定向测试继续通过。
  - `bun run typecheck`、定向 `bun test` 通过。
- 状态：done


### Phase 136 - 拆分 gateway media asset helpers

- 目标：
  - 将 `gateway-media-assets.ts` 中 URL 代理、续签解析、card update 与 generated image asset 纯逻辑拆到聚焦模块，保留稳定导出面。
- 实施项：
  - 新增 [gateway-media-asset-meta.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-asset-meta.ts)、[gateway-media-url.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-url.ts)、[gateway-media-renewal.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-renewal.ts)、[gateway-media-card-updates.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-card-updates.ts) 与 [gateway-generated-image-assets.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-generated-image-assets.ts)。
  - 让 [gateway-media-assets.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-assets.ts) 收敛为最薄导出层，继续保持现有 import surface 稳定。
  - 复用现有媒体相关测试覆盖，不改外部调用协议。
- 验收项：
  - [gateway-media-assets.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-assets.ts) 从 1115 行收敛到 41 行。
  - generated image / renew / url / card update 逻辑分散到聚焦模块，既有媒体回归继续通过。
  - `bun run typecheck`、定向 `bun test`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 137 - 拆分 markdown normalize helpers

- 目标：
  - 将 `markdown-normalize.ts` 中卡片元数据、链接判断、render tag 与 `<tool-meta>` 清洗职责拆到聚焦 helper，降低 markdown 规范化文件的维护复杂度。
- 实施项：
  - 新增 [markdown-card-meta.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/markdown-card-meta.ts)、[markdown-link-utils.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/markdown-link-utils.ts)、[markdown-render-tags.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/markdown-render-tags.ts) 与 [markdown-tool-meta.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/markdown-tool-meta.ts)。
  - 让 [markdown-normalize.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/markdown-normalize.ts) 保留 markdown cleanup pipeline 与稳定重导出。
  - 复用现有 markdown 内容测试，锁住 XML/tag/tool-meta 清洗行为。
- 验收项：
  - [markdown-normalize.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/markdown-normalize.ts) 从 959 行收敛到 441 行。
  - markdown 规范化内部职责按 card meta、link utils、render tags、tool-meta 分开维护。
  - `bun run typecheck`、定向 `bun test`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 138 - 拆分 chat shell helpers 与 hooks

- 目标：
  - 先沿低风险切口拆分 `chat-shell.tsx` 的纯 helper、模型设置 state 与滚动/输入折叠 hook，在不触碰发送与重生成热路径的前提下降低文件复杂度。
- 实施项：
  - 新增 [chat-shell-helpers.ts](/Users/caolixiang/codey/chat-app/src/features/chat/chat-shell-helpers.ts)，承接会话视图模型、标题/时间等纯辅助逻辑。
  - 新增 [use-chat-shell-models.ts](/Users/caolixiang/codey/chat-app/src/features/chat/use-chat-shell-models.ts) 与 [use-chat-shell-scroll.ts](/Users/caolixiang/codey/chat-app/src/features/chat/use-chat-shell-scroll.ts)，分别承接模型设置状态与滚动/输入折叠状态。
  - 保留发送、重生成、恢复链路在 [chat-shell.tsx](/Users/caolixiang/codey/chat-app/src/features/chat/chat-shell.tsx) 内，避免本轮过度触碰高风险主链。
- 验收项：
  - [chat-shell.tsx](/Users/caolixiang/codey/chat-app/src/features/chat/chat-shell.tsx) 从 1755 行收敛到 1356 行。
  - 纯 helper、模型设置 state、滚动/折叠状态拆到独立文件，发送/重生成热路径语义保持不变。
  - `bun run typecheck`、定向 `bun test`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 139 - 拆分 structured reasoning 模块

- 目标：
  - 将 `structured-reasoning.ts` 中模型整理、tool usage、deepsearch 视图与多代理片段拆成独立模块，保留稳定导出面，降低 reasoning 展示层的维护成本。
- 实施项：
  - 新增 [structured-reasoning-models.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/structured-reasoning-models.ts)、[structured-reasoning-tools.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/structured-reasoning-tools.ts)、[structured-reasoning-deepsearch.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/structured-reasoning-deepsearch.ts) 与 [structured-reasoning-agents.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/structured-reasoning-agents.ts)。
  - 让 [structured-reasoning.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/structured-reasoning.ts) 收敛为最薄导出层，避免对外 import 面变化。
  - 复用现有 structured reasoning 测试覆盖，锁住 deepsearch / agents / tools 的渲染分流语义。
- 验收项：
  - [structured-reasoning.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/structured-reasoning.ts) 从 960 行收敛到 28 行。
  - models、tools、deepsearch、agents 逻辑拆成聚焦模块，既有 reasoning 视图语义不变。
  - `bun run typecheck`、定向 `bun test`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 140 - 拆分 chat message helpers

- 目标：
  - 将 `chat-message.tsx` 中 citation、附件块与按钮组拆到独立模块，保留消息布局与 PDF 导出编排在入口组件中，降低消息渲染层维护成本。
- 实施项：
  - 新增 [chat-message-citations.ts](/Users/caolixiang/codey/chat-app/src/components/chat-message-citations.ts)、[chat-message-attachments.tsx](/Users/caolixiang/codey/chat-app/src/components/chat-message-attachments.tsx) 与 [chat-message-actions.tsx](/Users/caolixiang/codey/chat-app/src/components/chat-message-actions.tsx)。
  - 让 [chat-message.tsx](/Users/caolixiang/codey/chat-app/src/components/chat-message.tsx) 保留布局、PDF 导出编排与稳定重导出，避免外部调用点回归。
  - 为 citation helper 保持现有测试覆盖，锁住引用提取与 generated 内容按钮显隐语义。
- 验收项：
  - [chat-message.tsx](/Users/caolixiang/codey/chat-app/src/components/chat-message.tsx) 从 785 行收敛到 233 行。
  - citations、attachments、actions 分别切到独立模块，消息布局与导出链路语义保持不变。
  - `bun run typecheck`、定向 `bun test`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 141 - 拆分 chunk-parsers-common 公共解析 helpers

- 目标：
  - 将 `chunk-parsers-common.ts` 中 records/JSON、response id、tooling/web search 等公共解析职责拆到内部模块，保留稳定对外导出面。
- 实施项：
  - 新增 [chunk-parsers-common-records.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-common-records.ts)、[chunk-parsers-common-response-id.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-common-response-id.ts) 与 [chunk-parsers-common-tooling.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-common-tooling.ts)。
  - 让 [chunk-parsers-common.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-common.ts) 收敛为最薄重导出层，继续兼容现有 research/card/parser 调用点。
  - 统一暴露 `readToolUsageCard()`，为后续 `chunk-parsers.ts` 去重重复实现做准备。
- 验收项：
  - [chunk-parsers-common.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-common.ts) 从 561 行收敛到 24 行。
  - common 层职责按 records、response-id、tooling/web-search 分开维护，既有解析语义不变。
  - `bun run typecheck`、定向 `bun test`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 142 - 拆分 chunk-parsers 主入口模块

- 目标：
  - 将 `chunk-parsers.ts` 中 reasoning events、title/meta、response text、message builders 拆到内部模块，保留主入口稳定导出面。
- 实施项：
  - 新增 [chunk-parsers-reasoning-events.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-reasoning-events.ts)、[chunk-parsers-title.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-title.ts)、[chunk-parsers-response.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-response.ts) 与 [chunk-parsers-message-builders.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-message-builders.ts)。
  - 让 [chunk-parsers.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers.ts) 收敛为薄导出层，继续统一对外暴露 common/card/research 与主解析能力。
  - 去掉 `chunk-parsers.ts` 内部重复的 `readToolUsageCard()`，改为复用 common 层统一实现。
- 验收项：
  - [chunk-parsers.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers.ts) 从 529 行收敛到 22 行。
  - 主入口职责按 reasoning、title/meta、response、message builders 分开维护，对外 API 与解析结果语义保持不变。
  - `bun run typecheck`、定向 `bun test`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 143 - 拆分 card attachment 解析模块

- 目标：
  - 将 `chunk-parsers-card-attachments.ts` 中 generated image 解析与 card payload 解析职责拆开，减少 parser 簇里最后一个 500+ 行纯解析文件的维护压力。
- 实施项：
  - 新增 [chunk-parsers-generated-image-attachments.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-generated-image-attachments.ts)，承接 generated image url、asset id、expiresAt、moderation 与 indexed-array 聚合逻辑。
  - 新增 [chunk-parsers-card-attachment-payload.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-card-attachment-payload.ts)，承接 cardAttachment/cardAttachmentsJson 的 payload 解析与 envelope 收集逻辑。
  - 让 [chunk-parsers-card-attachments.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-card-attachments.ts) 只保留 `extractGeneratedImageModeratedFromRawChunk()` 与 `extractCardAttachmentsFromRawChunk()` 的最薄编排。
- 验收项：
  - [chunk-parsers-card-attachments.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-card-attachments.ts) 从 506 行收敛到 71 行。
  - generated image 解析与 card payload 解析分开维护，`chunk-parsers-cards.test.ts`、全量 `bun test` 与 `bun run build` 通过。
  - `bun run typecheck`、定向 `bun test`、`bun test`、`bun run build` 通过。
- 状态：done

## Phase 验收记录





### Phase 116

- 验收结果：通过。
- 备注：
  - 新增 [chat-stream-runtime.ts](/Users/caolixiang/codey/chat-app/src/features/chat/chat-stream-runtime.ts) 承载 think tag 判断、reasoning 事件合并、流式计时器与完成后 reasoning 持久化辅助逻辑。
  - [chat-shell.tsx](/Users/caolixiang/codey/chat-app/src/features/chat/chat-shell.tsx) 发送/重生成两条链路已改为复用同一套 runtime；文件从 2211 行收敛到 1755 行。
  - 新增 [chat-stream-runtime.test.ts](/Users/caolixiang/codey/chat-app/src/features/chat/chat-stream-runtime.test.ts) 覆盖 delayed card flush、duration tick、finalize cleanup、reasoning persistence 等关键回归点。

### Phase 117

- 验收结果：通过。
- 备注：
  - 新增 [gateway-session-recovery.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-session-recovery.ts)，集中承载恢复协议状态类型、配置归一化、状态/消息解析、等待重试与 `sessions/*` 查询逻辑。
  - 新增 [gateway-session-recovery.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-session-recovery.test.ts)，覆盖 `normalizeTurnRecoverySetting`、状态/消息解析与 `invalid_after_response_id` 回退。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 3663 行收敛到 3310 行；恢复协议相关逻辑改为经 `getGatewayRecoveryClient()` 按需取用，保持测试里替换 `runtimeFetch` 的链路兼容。

### Phase 118

- 验收结果：通过。
- 备注：
  - 新增 [gateway-media-assets.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-assets.ts)，集中承载媒体 URL 代理、续签 payload 解析、generated image 资产推断、卡片归并与 markdown 修正纯函数。
  - 新增 [gateway-media-assets.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-assets.test.ts)，承接 `resolveGatewayMediaUrl`、`parseRenewedAssetUrlResponse`、`shouldRenewCardAssetUrl`、`inferGeneratedImageAssetId` 的独立纯函数回归。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 3310 行收敛到 2233 行；[grok-chat-service.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.test.ts) 从 2605 行收敛到 2412 行，仅保留服务级集成回归。

### Phase 119

- 验收结果：通过。
- 备注：
  - 新增 [gateway-media-workflows.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-workflows.ts)，集中承载消息级 generated image 刷新与流结束卡片收口工作流，继续通过回调注入 `hydrateGeneratedImageCards`，保留 renew API I/O 在 `GrokChatService` 内。
  - 新增 [gateway-media-workflows.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-workflows.test.ts)，覆盖消息 markdown 重写、reasoning 卡片归一化与 generated image 去重。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 2233 行收敛到 2086 行，`listMessages` 与流结束阶段的媒体编排已切到独立工作流模块。

### Phase 120

- 验收结果：通过。
- 备注：
  - 新增 [gateway-turn-completion.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-turn-completion.ts)，集中承载流结束后的最终 content/research/duration/title 收口，并暴露 `buildCompletedGatewayTurn()` 与 `mergeResearchPayload()`。
  - 新增 [gateway-turn-completion.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-turn-completion.test.ts)，覆盖 citation 去重、最终文案选择、reasoning duration 优先级与标题决议。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 2086 行收敛到 1836 行，`streamTurn` 的完成态组装已切到独立模块。

### Phase 121

- 验收结果：通过。
- 备注：
  - 新增 [gateway-stream-accumulator.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-stream-accumulator.ts)，集中承载 response id、title、final message、reasoning 事件、research 结构、web search meta 与 generated image card 的分段累计逻辑。
  - 新增 [gateway-stream-accumulator.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-stream-accumulator.test.ts)，覆盖 thinking 结束时 synthetic `tool_result`、generated image 卡片去重替换、web search 去重与标题/response id 汇聚。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 1836 行收敛到 1578 行，`streamTurn` 的 per-segment 状态机已切到独立累计器模块。

### Phase 122

- 验收结果：通过。
- 备注：
  - 新增 [gateway-turn-request-builder.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-turn-request-builder.ts)，集中承载附件 data URI 序列化、用户消息文案拼装、`previous_response_id` 决策与 regenerate 请求体构造。
  - 新增 [gateway-turn-request-builder.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-turn-request-builder.test.ts)，覆盖文本+附件、纯附件、session anchor、regenerate payload 与附件大小上限。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 1578 行收敛到 1455 行，`streamTurn` 的请求体组装已切到独立 builder 模块。

### Phase 123

- 验收结果：通过。
- 备注：
  - 新增 [gateway-regenerate-target-resolver.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-regenerate-target-resolver.ts)，集中承载 regenerate assistant 节点的 role-shape、assistant ordinal 与 exact-content 匹配策略。
  - 新增 [gateway-regenerate-target-resolver.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-regenerate-target-resolver.test.ts)，覆盖空目标、空 session、role shape、assistant ordinal、唯一内容命中与内容歧义回退。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 1455 行收敛到 1410 行，`resolveRegenerateTarget()` 已切到独立 resolver 模块。

### Phase 124

- 验收结果：通过。
- 备注：
  - 新增 [gateway-pending-assistant-recovery.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-pending-assistant-recovery.ts)，集中承载 pending assistant 的消息补拉、turn state 判定、completion 后补拉与 polling 编排。
  - 新增 [gateway-pending-assistant-recovery.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-pending-assistant-recovery.test.ts)，覆盖立即恢复、streaming preview、active turn completed、anchor 前移 stale/non-stale 与 completion 延迟恢复。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 1410 行收敛到 1097 行，`recoverPendingAssistant()` 已切到独立 recovery flow 模块。

### Phase 125

- 验收结果：通过。
- 备注：
  - 新增 [gateway-recovered-turn-persistence.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-recovered-turn-persistence.ts)，集中承载 recovered completed turn 的消息候选选择、assistant 更新/追加、anchor 刷新与标题回退。
  - 新增 [gateway-turn-recovery.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-turn-recovery.ts)，集中承载 turn state 查询、in-progress polling、completed/not_found/in_progress 分支决策。
  - 新增 [gateway-recovered-turn-persistence.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-recovered-turn-persistence.test.ts) 与 [gateway-turn-recovery.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-turn-recovery.test.ts)，覆盖 completed turn 持久化与 turn recovery 关键分支。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 1097 行收敛到 980 行，`persistRecoveredCompletedTurn()` 与 `attemptTurnRecovery()` 已切到独立 workflow 模块。

### Phase 126

- 验收结果：通过。
- 备注：
  - 新增 [gateway-stream-transport.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-stream-transport.ts)，集中承载请求发送、HTTP/idle timeout 重试、anchor conflict 去锚、turn recovery 编排与 reader 读流事件分发。
  - 新增 [gateway-generated-image-renewal.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-generated-image-renewal.ts)，集中承载 generated image 续签请求、shared renew cache 与卡片 URL 更新。
  - 新增 [gateway-stream-transport.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-stream-transport.test.ts) 与 [gateway-generated-image-renewal.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-generated-image-renewal.test.ts)，覆盖 transport/recovery 与 renew I/O 主链关键分支。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 980 行收敛到 656 行，`streamTurn()` transport shell 与 generated-image renew I/O 已切到独立模块。


### Phase 127

- 验收结果：通过。
- 备注：
  - 新增 [gateway-stream-event-emitter.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-stream-event-emitter.ts)，集中承载流式 started/delta/reasoning/heartbeat/completed/failed 事件发射与 `meta.seq` / `responseId` 继承。
  - 新增 [gateway-turn-bootstrap.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-turn-bootstrap.ts)，集中承载 `conversationId/sessionId/clientTurnId` 解析、conversation 预写入、fallback title 与 user message append preflight。
  - 新增 [gateway-stream-event-emitter.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-stream-event-emitter.test.ts) 与 [gateway-turn-bootstrap.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-turn-bootstrap.test.ts)，覆盖 seq 单调递增、显式 response id 覆盖、普通 turn append 与 regenerate/retry skip append。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 656 行收敛到 595 行，`streamTurn()` 的 event/meta glue 与 conversation preflight 已切到独立模块。


### Phase 128

- 验收结果：通过。
- 备注：
  - 新增 [gateway-conversation-persistence.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-conversation-persistence.ts)，集中承载会话记录构造、fallback title、assistant turn 持久化与 anchors upsert。
  - 新增 [gateway-regenerate-target-sync.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-regenerate-target-sync.ts)，集中承载本地 assistant response id 回写、session 消息匹配与会话锚点刷新。
  - 新增 [gateway-conversation-persistence.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-conversation-persistence.test.ts) 与 [gateway-regenerate-target-sync.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-regenerate-target-sync.test.ts)，覆盖 regenerate 截枝持久化、anchors 刷新、空目标回退与 gateway 对齐。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 从 595 行收敛到 501 行，completion persistence glue 与 regenerate sync 已切到独立模块。

### Phase 129

- 验收结果：通过。
- 备注：
  - 新增 [grok-chat-service-test-helpers.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service-test-helpers.ts)，集中承载 conversation/turn 夹具、流式 chunk 工厂与共享断言辅助。
  - 新增 [grok-chat-service.media.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.media.test.ts)、[grok-chat-service.recovery.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.recovery.test.ts)、[grok-chat-service.regenerate.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.regenerate.test.ts)，分别承接媒体、恢复协议与重生成回归。
  - [grok-chat-service.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.test.ts) 收敛为基础流式/普通对话用例，文件体积降到约 487 行，服务级测试入口更聚焦。

### Phase 130

- 验收结果：通过。
- 备注：
  - 新增 [chunk-parsers-common.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-common.ts)、[chunk-parsers-card-attachments.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-card-attachments.ts)、[chunk-parsers-research.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-research.ts)，分别承接公共解析、card attachment 与 research/citation 聚合。
  - 新增 [chunk-parsers-cards.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-cards.test.ts) 与 [chunk-parsers-research.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-research.test.ts)，原 [chunk-parsers.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers.test.ts) 收敛为基础解析回归。
  - [chunk-parsers.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers.ts) 收敛到约 529 行，聚合入口与剩余轻量逻辑更清晰。

### Phase 131

- 验收结果：通过。
- 备注：
  - 新增 [gateway-recovery-runtime.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-recovery-runtime.ts)，以闭包 + getter 方式集中承载恢复协议 runtime、等待参数与 query helper 装配，保持构造后可替换 `runtimeFetch`/store/logger 的测试语义。
  - 新增 [gateway-recovery-runtime.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-recovery-runtime.test.ts)，覆盖 getter 依赖读取、等待重试参数透传与 runtime 依赖替换兼容性。
  - [grok-chat-service.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.ts) 收敛到约 413 行，恢复协议相关 runtime glue 已切到独立模块。

### Phase 132

- 验收结果：通过。
- 备注：
  - 新增 [grok-chat-service.pending-recovery.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.pending-recovery.test.ts)，集中承载 `recoverPendingAssistant()` 的 pending assistant、anchor advanced、completion lag 与 invalid `after_response_id` 回归。
  - [grok-chat-service.recovery.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.recovery.test.ts) 收敛到约 425 行，仅保留 `streamTurn()` 的 transport failure、turn recovery 与 in-progress retry 场景。
  - recovery 服务级测试已按“pending assistant 恢复 / streamTurn recovery-retry”两类拆开，并复用了现有 test helper 的 service/fetch 装配。

### Phase 133

- 验收结果：通过。
- 备注：
  - 新增 [chunk-parsers-research-citations.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-research-citations.ts)，集中承载 citation card、inline citation 与 `grok:render` XML 提取。
  - 新增 [chunk-parsers-research-fragments.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-research-fragments.ts)，集中承载 research step/detail/object 解析、messageTag step 构造与 fragment merge。
  - [chunk-parsers-research.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-research.ts) 收敛到约 62 行，保留对外导出与 raw chunk 聚合入口。

### Phase 134

- 验收结果：通过。
- 备注：
  - 新增 [grok-chat-service.pending-recovery-anchor.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.pending-recovery-anchor.test.ts)，集中承载 anchor advanced、invalid `after_response_id`、retry send 与 completion lag 回归。
  - [grok-chat-service.pending-recovery.test.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service.pending-recovery.test.ts) 收敛到约 347 行，保留 pending 可见性、preview 与 active turn state 场景。
  - [grok-chat-service-test-helpers.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/grok-chat-service-test-helpers.ts) 新增可带 `AppConfig` overrides 的 service 构造辅助，减少 recovery 测试重复配置块。

### Phase 135

- 验收结果：通过。
- 备注：
  - 新增 [chunk-parsers-research-steps.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-research-steps.ts)，集中承载 tool usage XML、toolUsageResults、research step 与 messageTag step 解析。
  - 新增 [chunk-parsers-research-merge.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-research-merge.ts)，集中承载 details、research object 解析与 fragment merge。
  - [chunk-parsers-research-fragments.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-research-fragments.ts) 收敛到约 10 行，保留最薄重导出层。

### Phase 136

- 验收结果：通过。
- 备注：
  - [gateway-media-assets.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-media-assets.ts) 只保留对外导出面，纯逻辑拆到 5 个内部模块。
  - [gateway-generated-image-assets.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/gateway-generated-image-assets.ts) 承接 generated image asset 推断与聚合主逻辑，其余模块分别负责 meta、url、renewal 与 card update。
  - 媒体相关定向测试与全量回归通过。

### Phase 137

- 验收结果：通过。
- 备注：
  - [markdown-normalize.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/markdown-normalize.ts) 已收敛为清洗管线与重导出入口。
  - 新增的 4 个 helper 分别承接卡片元数据、链接判断、render tag 解析与 `<tool-meta>` 清洗，未改外部调用点。
  - markdown 内容定向测试与全量回归通过。

### Phase 138

- 验收结果：通过。
- 备注：
  - [chat-shell.tsx](/Users/caolixiang/codey/chat-app/src/features/chat/chat-shell.tsx) 只切出稳定 seam，保留 send/regenerate/recovery 主链在原文件。
  - [chat-shell-helpers.ts](/Users/caolixiang/codey/chat-app/src/features/chat/chat-shell-helpers.ts)、[use-chat-shell-models.ts](/Users/caolixiang/codey/chat-app/src/features/chat/use-chat-shell-models.ts)、[use-chat-shell-scroll.ts](/Users/caolixiang/codey/chat-app/src/features/chat/use-chat-shell-scroll.ts) 已分别承接 helper/view-model、模型状态与滚动状态。
  - chat shell 邻近测试与全量回归通过。

### Phase 139

- 验收结果：通过。
- 备注：
  - [structured-reasoning.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/structured-reasoning.ts) 已收敛为导出入口，复杂渲染逻辑拆到 4 个内部模块。
  - [structured-reasoning-deepsearch.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/structured-reasoning-deepsearch.ts) 承接 deepsearch 结果页主体，[structured-reasoning-agents.ts](/Users/caolixiang/codey/chat-app/src/components/markdown-content/structured-reasoning-agents.ts) 承接多代理片段，其余模块负责模型整理与工具块渲染。
  - structured reasoning 定向测试与全量回归通过。

### Phase 140

- 验收结果：通过。
- 备注：
  - [chat-message.tsx](/Users/caolixiang/codey/chat-app/src/components/chat-message.tsx) 已收敛为消息布局与 PDF 导出 orchestration 入口。
  - [chat-message-citations.ts](/Users/caolixiang/codey/chat-app/src/components/chat-message-citations.ts)、[chat-message-attachments.tsx](/Users/caolixiang/codey/chat-app/src/components/chat-message-attachments.tsx) 与 [chat-message-actions.tsx](/Users/caolixiang/codey/chat-app/src/components/chat-message-actions.tsx) 分别承接引用、附件与按钮组渲染。
  - chat message 定向测试与全量回归通过。

### Phase 141

- 验收结果：通过。
- 备注：
  - [chunk-parsers-common.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-common.ts) 已收敛为 24 行薄导出层。
  - [chunk-parsers-common-records.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-common-records.ts)、[chunk-parsers-common-response-id.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-common-response-id.ts) 与 [chunk-parsers-common-tooling.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-common-tooling.ts) 分别承接 records/JSON、response id、tooling/web search 逻辑。
  - `readToolUsageCard()` 已统一收口到 common tooling，便于主入口去掉重复实现。

### Phase 142

- 验收结果：通过。
- 备注：
  - [chunk-parsers.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers.ts) 已收敛为 22 行薄导出层。
  - [chunk-parsers-reasoning-events.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-reasoning-events.ts)、[chunk-parsers-title.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-title.ts)、[chunk-parsers-response.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-response.ts) 与 [chunk-parsers-message-builders.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-message-builders.ts) 已分别承接主入口解析职责。
  - 主入口的重复 `readToolUsageCard()` 已移除，统一改走 common tooling。

### Phase 143

- 验收结果：通过。
- 备注：
  - [chunk-parsers-generated-image-attachments.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-generated-image-attachments.ts) 已承接 generated image 附件解析主逻辑。
  - [chunk-parsers-card-attachment-payload.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-card-attachment-payload.ts) 已承接 card payload/envelope 解析与汇聚逻辑。
  - [chunk-parsers-card-attachments.ts](/Users/caolixiang/codey/chat-app/src/infrastructure/chat/chunk-parsers-card-attachments.ts) 收敛为 71 行的编排入口，测试与构建回归通过。

## 测试记录





- `bun run typecheck`：已执行，通过。
- `bun test src/features/chat/chat-stream-runtime.test.ts`：已执行，通过（4 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-session-recovery.test.ts`：已执行，通过（6 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-media-assets.test.ts`：已执行，通过（14 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-media-workflows.test.ts`：已执行，通过（2 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-turn-completion.test.ts`：已执行，通过（4 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-stream-accumulator.test.ts`：已执行，通过（4 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-turn-request-builder.test.ts`：已执行，通过（7 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-regenerate-target-resolver.test.ts`：已执行，通过（6 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-pending-assistant-recovery.test.ts`：已执行，通过（6 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-recovered-turn-persistence.test.ts`：已执行，通过（5 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-turn-recovery.test.ts`：已执行，通过（8 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-generated-image-renewal.test.ts`：已执行，通过（3 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-stream-transport.test.ts`：已执行，通过（4 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-stream-event-emitter.test.ts`：已执行，通过（2 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-turn-bootstrap.test.ts`：已执行，通过（2 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-conversation-persistence.test.ts`：已执行，通过（3 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-regenerate-target-sync.test.ts`：已执行，通过（2 pass / 0 fail）。
- `bun test src/infrastructure/chat/grok-chat-service.test.ts src/infrastructure/chat/grok-chat-service.media.test.ts src/infrastructure/chat/grok-chat-service.recovery.test.ts src/infrastructure/chat/grok-chat-service.regenerate.test.ts src/infrastructure/chat/chunk-parsers.test.ts src/infrastructure/chat/chunk-parsers-cards.test.ts src/infrastructure/chat/chunk-parsers-research.test.ts src/infrastructure/chat/gateway-recovery-runtime.test.ts`：已执行，通过（84 pass / 0 fail）。
- `bun test src/infrastructure/chat/grok-chat-service.recovery.test.ts src/infrastructure/chat/grok-chat-service.pending-recovery.test.ts`：已执行，通过（13 pass / 0 fail）。
- `bun test src/infrastructure/chat/chunk-parsers-research.test.ts`：已执行，通过（5 pass / 0 fail）。
- `bun test src/infrastructure/chat/grok-chat-service.test.ts src/infrastructure/chat/grok-chat-service.media.test.ts src/infrastructure/chat/grok-chat-service.recovery.test.ts src/infrastructure/chat/grok-chat-service.regenerate.test.ts`：已执行，通过。
- `bun test src/infrastructure/chat/chunk-parsers.test.ts src/infrastructure/chat/chunk-parsers-cards.test.ts src/infrastructure/chat/chunk-parsers-research.test.ts`：已执行，通过。
- `bun test src/infrastructure/chat/grok-chat-service.test.ts`：已执行，通过（26 pass / 0 fail）。
- `bun test src/infrastructure/chat/gateway-media-assets.test.ts src/infrastructure/chat/gateway-media-workflows.test.ts src/infrastructure/chat/gateway-generated-image-renewal.test.ts src/infrastructure/chat/grok-chat-service.media.test.ts`：已执行，通过。
- `bun test src/components/markdown-content.test.ts`：已执行，通过。
- `bun test src/features/chat/chat-stream-runtime.test.ts src/features/chat/chat-conversation-store.test.ts`：已执行，通过。
- `bun test src/components/markdown-content/structured-reasoning.test.ts`：已执行，通过。
- `bun test src/components/chat-message-citations.test.ts`：已执行，通过。
- `bun test src/infrastructure/chat/chunk-parsers.test.ts src/infrastructure/chat/chunk-parsers-cards.test.ts src/infrastructure/chat/chunk-parsers-research.test.ts`：已执行，通过（56 pass / 0 fail）。
- `bun test src/infrastructure/chat/chunk-parsers-cards.test.ts src/infrastructure/chat/chunk-parsers.test.ts src/infrastructure/chat/chunk-parsers-research.test.ts`：已执行，通过（56 pass / 0 fail）。
- `bun test`：已执行，通过（247 pass / 0 fail）。
- `bun run build`（按需）：已执行，通过（存在既有 Vite warning 与 chunk warning，无新增构建失败）。

## 提交记录

- `0090fac` `phase(116): extract chat stream runtime`
- `2057cbc` `phase(116): backfill plan and memo commit trace`
- `a9ea70c` `phase(117): extract gateway session recovery module`
- `41f75c5` `phase(117): backfill plan and memo commit trace`
- `af8ca9d` `phase(118): extract gateway media asset utilities`
- `c721071` `phase(118): backfill plan and memo commit trace`
- `446dc59` `phase(119): extract gateway media workflows`
- `6c3e9e2` `phase(119): backfill plan and memo commit trace`
- `f1f73fd` `phase(120): extract gateway turn completion module`
- `f5bd62f` `phase(120): backfill plan and memo commit trace`
- `49bc9fc` `phase(121): extract gateway stream accumulator`
- `7c66da7` `phase(121): backfill plan and memo commit trace`
- `0348e43` `phase(122): extract gateway turn request builder`
- `aea7274` `phase(122): backfill plan and memo commit trace`
- `4779fb9` `phase(123): extract regenerate target resolver`
- `44a9b41` `phase(123): backfill plan and memo commit trace`
- `f1f04b7` `phase(124): extract pending assistant recovery flow`
- `a4a6c46` `phase(124): backfill plan and memo commit trace`
- `bd56895` `phase(125): extract turn recovery workflows`
- `7cd9784` `phase(125): backfill plan and memo commit trace`
- `e4909c7` `phase(126): extract stream transport and media renewal`
- `e1362bf` `phase(126): backfill plan and memo commit trace`
- `36ae3a8` `phase(127): extract stream event emitter and turn bootstrap`
- `0c962f7` `phase(127): backfill plan and memo commit trace`
- `8a43cb7` `phase(128): extract conversation persistence and regenerate sync`
- `eef2269` `phase(128): backfill plan and memo commit trace`
- `44cedf1` `phase(129): split grok chat service tests`
- `b220852` `phase(129): backfill plan and memo commit trace`
- `7d658dc` `phase(130): split chunk parsers into focused modules`
- `bca41af` `phase(130): backfill plan and memo commit trace`
- `47bf546` `phase(131): extract gateway recovery runtime`
- `d672db0` `phase(131): backfill plan and memo commit trace`
- `407a263` `phase(132): split recovery service tests`
- `26f60a9` `phase(132): backfill plan and memo commit trace`
- `ca93f78` `phase(133): split research parser internals`
- `cb086b0` `phase(133): backfill plan and memo commit trace`
- `3822eb8` `phase(134): split pending recovery service tests`
- `0d253c6` `phase(134): backfill plan and memo commit trace`
- `595395d` `phase(135): split research fragment internals`

- `8d4f072` `phase(136): split gateway media asset helpers`
- `f524278` `phase(136): backfill plan and memo commit trace`

- `9fb7617` `phase(137): split markdown normalize helpers`
- `15a93b3` `phase(137): backfill plan and memo commit trace`

- `7f09973` `phase(138): split chat shell helpers and hooks`
- `efeb1d9` `phase(138): backfill plan and memo commit trace`

- `b8689fb` `phase(139): split structured reasoning modules`
- `6601719` `phase(139): backfill plan and memo commit trace`

- `4009dd3` `phase(140): split chat message helpers`

- `6fb62d9` `phase(141): split chunk parser common helpers`
- `0dbe885` `phase(141): backfill plan and memo commit trace`

- `abc038e` `phase(142): split chunk parser entry modules`

- `712dfd1` `phase(143): split card attachment parsers`

## 例外记录

- 无

## 明日计划

- 暂不继续机械拆分；后续仅在某个模块再次成为变更热点或明显阻碍维护时，再选择性拆分，例如 [structured-reasoning-panel.tsx](/Users/caolixiang/codey/chat-app/src/components/markdown-content/structured-reasoning-panel.tsx) 或 [settings-dialog.tsx](/Users/caolixiang/codey/chat-app/src/components/settings-dialog.tsx)。

