# 今日目标

- 对齐 DeepSearch 新协议：消费并展示 `x_grok.research` 相关字段，在 UI 中补齐正文注脚与思考细节展示。

## 范围

- In:
  - 解析并持久化 DeepSearch 研究结构（citation / inline_citation / steps / thinking time）。
  - 正文下方渲染注脚列表（可点击打开）。
  - 思考结果抽屉展示“深度挖掘细节”卡片。
- Out:
  - 本阶段不改网关请求参数（继续沿用既有 `x_grok.deep_search=true`）。
  - 本阶段不改 e2e 测试体系。

## Phase 计划

### Phase 45 - DeepSearch 研究结构消费与展示

- 目标：
  - 让 DeepSearch 返回的新研究字段在本地可见、可持久化、可渲染。
- 实施项：
  - 扩展域模型与存储：为 assistant message 增加 research 结构并落库。
  - 扩展流式解析：从 NDJSON 中提取 research（含 steps/citation）并合并到最终消息。
  - 渲染正文注脚：在正文下方显示 `Key Citations` 列表，支持点击跳转。
  - 渲染思考细节：在思考结果抽屉顶部新增“深度挖掘细节”区块。
  - 补充解析层单测与回归测试。
- 验收项：
  - DeepSearch 回复正文下方出现注脚列表（可点击）。
  - 思考结果抽屉出现深度挖掘细节卡片（标题+要点）。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 46 - DeepSearch 思考结果按 steps 顺序分组对齐

- 目标：
  - 修复思考结果抽屉信息归类错位，按 `research.steps` 的真实顺序展示 `header/summary/tool_usage_card`。
- 实施项：
  - 扩展 `ChatDeepSearchResearch`：新增 `steps` 结构并落库。
  - 扩展解析与合并：从 `x_grok.research.steps` / `modelResponse.steps` 提取并去重持久化。
  - 新增按 steps 分组构建逻辑：将步骤顺序映射到工具事件条目，形成有序分组。
  - 抽屉渲染优先使用 steps 分组视图；无 steps 时回退到既有 `details + toolChain + agent` 视图。
  - 补充 parser / service / structured reasoning 单测。
- 验收项：
  - DeepSearch 思考抽屉按步骤顺序展示，不再拆散分组。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

### Phase 47 - DeepSearch steps 工具条目纠偏（去除 XML 文本泄漏）

- 目标：
  - 修复思考抽屉中 `<xai:tool_usage_card>` 被当正文显示的问题，确保按“搜索/浏览”条目展示。
- 实施项：
  - 扩展 `research.steps`：保留 `toolUsages`（tool 名称、参数、结果列表）。
  - 解析 `toolUsageCards/toolUsageResults` 与 XML card 片段，统一沉淀到 `steps.toolUsages`。
  - 抽屉分组构建支持“无 reasoning events 时”从 `steps.toolUsages` 直接生成工具行。
  - 过滤 XML 原始行，避免回退到原始 `<xai:...>` 文本展示。
  - 补充相关单测与回归测试。
- 验收项：
  - 思考抽屉不再显示 `<xai:tool_usage_card>` 原文。
  - 即使缺失 `reasoningEvents`，仍可渲染搜索/浏览条目。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

## Phase 验收记录

### Phase 45

- 验收结果：
  - 已支持从流式 NDJSON 中提取 `research` 结构（`steps / citation_cards / inline_citations / thinking_start/end`）并持久化到消息。
  - 正文下方已新增 `Key Citations` 注脚列表，支持点击跳转。
  - 思考结果抽屉已新增“深度挖掘细节”卡片区块（标题 + 要点列表）。
  - 已按反馈修正注脚字段优先级：仅以 `research.inline_citations` 作为注脚入口，不再从 `reasoningEvents/webSearchResults` 兜底生成注脚。
- 备注：
  - 本阶段仍保持 `stream=true` NDJSON 协议消费，不新增 SSE 消费链路。

### Phase 46

- 验收结果：
  - 已新增 `research.steps` 的类型、解析、合并与持久化链路。
  - 思考抽屉新增“按 steps 顺序分组”渲染路径：优先展示 `header → summary → tool_usage_card` 的顺序信息。
  - 当存在 `steps` 时，抽屉不再将细节和工具事件拆为两条独立链路，从而避免归类错位。
  - 已补充 `buildDeepSearchGroupedSections` 单测与 research steps 相关回归断言。
- 备注：
  - 旧视图保留为兜底，仅在无 `steps` 时启用。

### Phase 47

- 验收结果：
  - `ChatDeepSearchResearchStep` 已新增 `toolUsages`，并在 parser/service 合并链路中稳定保留。
  - 已新增 steps 工具条目回退渲染逻辑：当无 `reasoningEvents` 时，按 `steps.toolUsages` 生成搜索/浏览条目。
  - 已过滤 tool_usage_card XML 原文，避免错误展示为摘要正文。
  - 已新增/更新单测覆盖该场景。
- 备注：
  - 本阶段只修正归类与展示，不调整抽屉视觉皮肤。

## 测试记录

- `bun run typecheck`：已执行，通过。
- `bun test src/infrastructure/chat/chunk-parsers.test.ts src/infrastructure/chat/grok-chat-service.test.ts`：已执行，通过（70 pass / 0 fail）。
- `bun test`：已执行，通过（126 pass / 0 fail）。
- `bun run build`（按需）：已执行，通过（保留既有 Vite warning，非阻断）。
- `bun run typecheck`：已执行，通过（Phase 46）。
- `bun test`：已执行，通过（127 pass / 0 fail，新增 DeepSearch steps 分组测试）。
- `bun run build`：已执行，通过（保留既有 Vite warning，非阻断）。
- `bun run typecheck`：已执行，通过（Phase 47）。
- `bun test src/components/markdown-content/structured-reasoning.test.ts src/infrastructure/chat/chunk-parsers.test.ts src/infrastructure/chat/grok-chat-service.test.ts`：已执行，通过（75 pass / 0 fail）。
- `bun test`：已执行，通过（129 pass / 0 fail）。
- `bun run build`：已执行，通过（保留既有 Vite warning，非阻断）。

## 提交记录

- `dd2d458`: phase(45): render deepsearch citations and research details
- `4d250bc`: phase(45): restrict citations to research inline fields
- `3e4710c`: phase(46): align deepsearch reasoning grouping by steps
- `798059d`: phase(47): render deepsearch tools from step metadata

## 例外记录

- 无

## 明日计划

- 根据本次用户体验反馈继续微调 DeepSearch 抽屉信息密度与信息排序。
