# 今日目标

- 为桌面端增加基于 Jina 的 Polymarket 订阅能力，支持本地持久化、启动补抓、10 分钟轮询与历史分页浏览。

## 范围

- In:
  - 新增本地订阅 schema 与 repository 接口
  - 新增 Jina 抓取、Markdown 解析、去重与轮询服务
  - 启动时自动补抓，运行中每 10 分钟轮询
  - 新增设置开关、订阅列表与“加载更多”
  - 支持将订阅条目一键带入新对话并自动发起“继续深入调研”
  - 补充单元测试与构建验证
- Out:
  - 不接入现有 Grok HTTP 网关
  - 不保证应用彻底退出期间的完整历史补齐
  - 不实现多账号、多来源订阅
  - 不在用户无操作时自动把订阅内容注入聊天上下文

## Phase 计划

### Phase 1 - 文档与方案收敛

- 目标：确认客户端直连 Jina、与现有 Grok 网关解耦的实现路径。
- 实施项：
  - 新增 `docs/plan/20260319.md` 与 `docs/memo/20260319.md`
  - 收敛“应用活着时轮询、真正退出后 best-effort 补抓”的产品边界
- 验收项：
  - 文档落地
  - 方案边界明确
- 状态：done

### Phase 2 - 存储、配置与订阅服务主链

- 目标：打通本地订阅 schema、Jina 抓取解析、轮询与 runtime 集成。
- 实施项：
  - 扩展 SQLite migrations、repository、memory repository
  - 新增 feed domain types、Jina client、Polymarket parser、feed service
  - 扩展 `config.toml` 与 runtime，接入订阅开关
- 验收项：
  - 本地库可自动迁移
  - 启动后可持久化订阅条目并在启用时自动轮询
- 状态：done

### Phase 3 - UI 与交互

- 目标：提供设置入口、订阅流视图和本地分页能力。
- 实施项：
  - 设置页新增“订阅”页签和保存开关
  - 侧边栏新增 `Polymarket` 入口
  - 主界面新增订阅流面板、立即同步和“加载更多”
- 验收项：
  - 用户可启停订阅并查看历史条目
  - “加载更多”可读取本地更多内容
- 状态：done

### Phase 4 - 测试与验证

- 目标：补齐 parser/service 测试并完成构建验证。
- 实施项：
  - 新增 Polymarket parser/service 测试
  - 更新 migration 测试断言
  - 执行 `bun test`、`bun run typecheck`、`bun run build`
- 验收项：
  - 核心测试通过
  - 计划、测试、提交记录更新
- 状态：done

### Phase 5 - OpenAI 翻译配置与翻译链路接入

- 目标：新增独立的 OpenAI-compatible 翻译配置，并将 Polymarket 入库前中文翻译接入 AI SDK。
- 实施项：
  - 扩展 `config.toml`、Tauri commands、`AppConfig` 与 runtime，新增独立 OpenAI 配置段
  - 引入 AI SDK + OpenAI-compatible provider，使用单独的 `base_url/api_key/model`
  - 扩展 `feed_items` schema 与 repository，持久化中文翻译及翻译状态
  - 在 Polymarket 同步链路中对新增条目做入库前翻译，失败时保留原文兜底
  - 设置页新增独立翻译配置区
- 验收项：
  - 不影响现有 Grok 聊天/语音网关配置
  - 新增 feed item 可持久化原文 + 中文翻译
  - 未配置 LLM key 时不会阻断订阅主链
- 状态：done

### Phase 6 - 回归验证

- 目标：验证配置、迁移与翻译主链可用，并覆盖关键回归。
- 实施项：
  - 补充/更新 migration、config、feed service 测试
  - 执行 `bun test`、`bun run typecheck`、`bun run build`
- 验收项：
  - 核心测试通过
  - 构建通过
  - 计划、测试、提交记录更新
- 状态：done

### Phase 7 - 翻译回填与 OpenAI 联通测试

- 目标：让翻译失败/跳过的 feed item 在后续同步时自动回填，并为独立 OpenAI 配置提供真实联通测试。
- 实施项：
  - 扩展 repository 与 feed service，支持查询/更新待补翻译条目
  - 在同步链路中补一轮旧条目翻译回填，避免失败后永久停留原文
  - 将设置页 `LLM 网关` 调整为独立 `OpenAI` 区
  - 默认翻译模型调整为 `gpt-5.4-mini`
  - 新增 `联通测试` 按钮，使用配置的 OpenAI-compatible 网关发送 `hello`
- 验收项：
  - 历史 `failed/skipped` 条目可在后续同步中自动补翻
  - 设置页可看到 OpenAI 独立区，并能执行一次 `hello` 联通测试
- 状态：done

### Phase 8 - 回归验证

- 目标：验证回填与联通测试主链稳定。
- 实施项：
  - 补充 repository/feed/config 测试
  - 执行 `bun test`、`bun run typecheck`、`bun run build`、`cargo check`
- 验收项：
  - 测试通过
  - 前端与 Tauri 编译通过
- 状态：done

### Phase 9 - OpenAI 内部命名收口

- 目标：将前端与运行时内部的 `llm` 命名统一为 `openai`，降低概念混淆。
- 实施项：
  - 重命名 `AppConfig`、runtime、settings dialog 与调用链中的 `llm*` 字段/回调
  - 将翻译配置读写 helper 与联通测试状态命名统一为 `openai*`
  - 将 feed translator 文件与类命名统一为 `openai`
  - 保持 OpenAI 翻译能力与现有 UI 行为不变
- 验收项：
  - 前端内部不再依赖新的 `llm*` 命名作为主字段
  - 设置页、runtime 与 feed 翻译链路保持可用
- 状态：done

### Phase 10 - Tauri 配置切换与回归验证

- 目标：将 Tauri 配置主字段统一为 `openai`，并完成回归验证。
- 实施项：
  - 将 `config.toml` 的主配置段调整为 `openai`
  - 执行 `bun test`、`bun run typecheck`、`bun run build`、`cargo check`
- 验收项：
  - 新版写入使用 `openai`
  - 测试与编译通过
- 状态：done

### Phase 11 - Polymarket 自动分页

- 目标：让订阅流滚动到底部时自动加载下一页，同时保留“加载更多”按钮兜底。
- 实施项：
  - 在 `PolymarketFeedPane` 增加底部 sentinel 与 `IntersectionObserver`
  - 增加自动加载的防重入控制，避免同一页重复触发
  - 保留现有“加载更多”按钮作为手动兜底入口
- 验收项：
  - 滚动到底部可自动触发 `onLoadMore`
  - 无 `IntersectionObserver` 时仍可手动点“加载更多”
  - 现有分页 cursor 行为不变
- 状态：done

### Phase 12 - 自动分页验证

- 目标：验证自动分页交互和构建链路稳定。
- 实施项：
  - 补充自动分页触发条件测试
  - 执行 `bun test`、`bun run typecheck`、`bun run build`
- 验收项：
  - 测试通过
  - 构建通过
- 状态：done

### Phase 13 - 订阅条目带入对话

- 目标：让 Polymarket 卡片支持一键带入新对话，并自动发送“继续深入调研”。
- 实施项：
  - 调整 `PolymarketFeedPane` 卡片操作区，移除孤立的 `原帖` 主按钮
  - 新增 feed 条目到聊天的 prompt builder，统一组织标题、正文、来源与媒体链接
  - 在 `chat-shell` 中接入“新建对话 + 自动发送”主链
- 验收项：
  - 用户可从订阅卡片直接发起新对话
  - 首条消息自动包含 feed 内容与“继续深入调研”
- 状态：done

### Phase 14 - 回归验证

- 目标：验证订阅带聊交互与现有订阅/聊天主链无回归。
- 实施项：
  - 补充 prompt builder 测试
  - 执行 `bun test`、`bun run typecheck`、`bun run build`
- 验收项：
  - 测试通过
  - 构建通过
- 状态：done

### Phase 15 - Feed 带聊模型与布局修正

- 目标：修正继续深入调研入口的模型选择与卡片布局问题。
- 实施项：
  - 将继续深入调研固定切到 Thinker/专家模型发送
  - 为发送链路补充 model override，避免隐藏的全局模型状态影响 feed 带聊
  - 规范化 feed 卡片正文，去掉与标题重复的内容和多余空行，收紧按钮布局
- 验收项：
  - 继续深入调研新开对话时使用 Thinker 模型
  - 卡片不再出现重复标题/正文和大块空白
- 状态：done

### Phase 16 - 回归验证

- 目标：验证 feed 带聊修正不引入新回归。
- 实施项：
  - 补充/更新 feed 卡片与模型相关测试
  - 执行 `bun test`、`bun run typecheck`、`bun run build`
- 验收项：
  - 测试通过
  - 构建通过
- 状态：done

## Phase 验收记录

### Phase 1

- 验收结果：已完成
- 备注：明确功能不走 Grok 网关，轮询固定 10 分钟，真正退出应用后仅做 best-effort 补抓。

### Phase 2

- 验收结果：已完成
- 备注：新增 feed subscription/items 表、repository 接口、Jina 抓取与 Polymarket parser，并在 runtime 初始化时启动订阅服务。

### Phase 3

- 验收结果：已完成
- 备注：设置页新增“订阅”页签；侧边栏新增 `Polymarket`；主界面支持立即同步与本地“加载更多”。

### Phase 4

- 验收结果：已完成
- 备注：新增 feed parser/service 测试并修正 migration 断言，`bun test` / `typecheck` / `build` 全通过。

### Phase 5

- 验收结果：已完成
- 备注：已新增独立 OpenAI-compatible 翻译配置、AI SDK 翻译服务、feed 翻译持久化字段，并在订阅流 UI 中优先展示中文译文。

### Phase 6

- 验收结果：已完成
- 备注：已完成 `bun test`、`bun run typecheck`、`bun run build` 与 `cargo check`，无新增失败。

### Phase 7

- 验收结果：已完成
- 备注：已支持同步时自动补翻 `failed/skipped` 的旧 feed item；设置页已改为独立 OpenAI 区，默认模型调整为 `gpt-5.4-mini`，并支持 `hello` 联通测试。

### Phase 9

- 验收结果：已完成
- 备注：已将前端/runtime/Tauri 的 OpenAI 翻译配置主字段统一切换为 `openai*`，并同步重命名 feed translator 文件与类；本轮不再保留旧 `llm` 命名兼容层。

### Phase 10

- 验收结果：已完成
- 备注：已完成 `bun test`、`bun run typecheck`、`bun run build` 与 `cargo check`；OpenAI 设置页、Polymarket 翻译链路与 Tauri 配置读写无新增失败。

### Phase 11

- 验收结果：已完成
- 备注：已新增订阅流底部 sentinel 与 `IntersectionObserver` 自动分页；保留“加载更多”按钮作为无 observer 或手动触发时的兜底入口。

### Phase 12

- 验收结果：已完成
- 备注：已完成自动分页相关测试以及 `bun test`、`bun run typecheck`、`bun run build` 全量回归，无新增失败。

### Phase 13

- 验收结果：已完成
- 备注：已将 Polymarket 卡片主操作改为“继续深入调研”；点击后会新建对话、发送 feed 原文，并将远程图片作为真实 `image_url` 多模态块带入请求。

### Phase 14

- 验收结果：已完成
- 备注：已完成 `bun test`、`bun run typecheck`、`bun run build` 回归，无新增失败。

### Phase 15

- 验收结果：已完成
- 备注：已将继续深入调研固定切到 Thinker/专家模型发送，并补了 model override；feed 卡片会压缩多余空行、隐藏与标题重复的正文，按钮布局已收紧。

### Phase 16

- 验收结果：已完成
- 备注：已完成局部测试、`bun run typecheck` 与 `bun run build`，无新增失败。

### Phase 8

- 验收结果：已完成
- 备注：已完成 `bun test`、`bun run typecheck`、`bun run build` 与 `cargo check`，OpenAI 独立设置页与翻译回填链路无新增失败。

## 测试记录

- `bun test`：通过（374 pass / 0 fail）
- `bun run typecheck`：通过
- `bun run build`：通过（存在既有 Vite chunk warning，无新增构建错误）
- `cargo check`（`src-tauri/`）：通过
- `bun test src/components/polymarket-feed-pane.test.ts src/features/chat/model-selection.test.ts`：通过（11 pass / 0 fail）

## 提交记录

- `39ae7f2` - 新增客户端直连 Jina 的 Polymarket 订阅主链，包含本地持久化、启动补抓、10 分钟轮询、设置开关与订阅流 UI。
- `ddc80f1` - 新增独立翻译配置与 AI SDK 翻译入库主链，Polymarket 条目可持久化中文翻译并在订阅流中展示。
- `1e8be97` - 将翻译配置拆为独立 `OpenAI` 设置页，新增 `hello` 联通测试，并在后续同步时自动回填旧的 `failed/skipped` 翻译。
- `1e6f236` - 将前端/runtime/Tauri 的翻译配置字段统一重命名为 `openai*`，并同步重命名 OpenAI feed translator 文件与调用链。
- `51e2331` - 为 Polymarket 订阅流增加滚动到底自动分页，并保留“加载更多”按钮兜底。
- `193f2b8` - 将 Polymarket 卡片接入“继续深入调研”主链，点击后新建对话并发送 feed 原文与远程图片块。
- `8bbe0f6` - 补充 feed 带聊相关测试与当日计划/Memo 记录，完成 `bun test`、`bun run typecheck`、`bun run build` 回归归档。
- `ee03b39` - 将继续深入调研固定切到 Thinker/专家模型发送，并收紧 Polymarket 卡片正文与按钮布局。
- `f4a9c6c` - 补充 feed 卡片规范化测试与本轮计划/Memo 记录，完成 `bun test`、`bun run typecheck`、`bun run build` 回归。

## 例外记录

- 本次功能跨越 schema、runtime service、设置页与主界面，拆分提交会产生不可运行的中间状态；因此合并为一次可运行提交交付。

## 明日计划

- 视使用反馈决定是否补充桌面通知、未读计数、更多订阅源，以及“继续深入调研”是否需要支持带入现有对话。
