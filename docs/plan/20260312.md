# 今日目标

- 完成 Phase 198，为 assistant 正文中的 `citation_card` 补回 inline citation pill 渲染，同时保留现有底部 `Key Citations` 列表与来源交互。
- 完成 Phase 199，修复底部 `Key Citations` 因多个 inline citation 指向同一 URL 而重复展示的问题。
- 完成 Phase 200，补齐语音控制层与 LiveKit 控制器能力，冻结 `session_id` 主链、静音策略和 session-event 上报契约。
- 完成 Phase 201，把语音控制器接入现有 `ChatInput` / `ChatShell`，支持语音模式连接、静音、停止、文本回显与最小持久化。
- 完成 Phase 202，使用真实网关做语音 token / 会话绑定 smoke test，验证 `session_id` 沿用和 session-event 契约。
- 完成 Phase 203，为语音 assistant 增加实时 `delta` 渲染，并避免 `audio_transcript` / `response.text` 双源重复叠加。
- 完成 Phase 204，修复桌面端语音入口无反馈问题，补连接中/失败可见状态与 macOS 麦克风权限声明。
- 完成 Phase 205，修复语音请求层 `Load failed` 并将连接错误落日志而非展示到 UI。
- 完成 Phase 206，补齐语音媒体链路：点击手势内麦克风预检、远端音频 attach/detach、媒体失败后强制清理假连接。
- 完成 Phase 207，修复语音 token 在陈旧 `session_id` 下的恢复链路，并过滤误用的本地 `conversation_id`。
- 完成 Phase 208，进入语音前校验 chat `session_id` 是否仍然存在；若本地锚点已失效则摘除旧 `session_id/last_response_id`，直接走新 voice session，避免入口闪退。
- 完成 Phase 209，收敛语音“连接中”视觉态，改为更接近官网的灰态动态声波按钮与中性等待文案。
- 完成 Phase 210，继续对齐语音连接过程交互：连接中回到单行输入框、去除用户语音回填、把右侧声波改成真实高低起伏动画。
- 完成 Phase 211，将已连接语音面板中的小柱状声波接到真实麦克风电平，讲话时动态抬升。
- 完成 Phase 212，将 idle 态“进入语音聊天”按钮按官网形态对齐，并补 hover 时的一次轻微波形起伏。
- 完成 Phase 213，修复语音中本地蓝色柱条的表现方式，改为“检测到说话时触发平滑固定动画”，并继续收口 idle 图标到用户指定的官方柱状样式。
- 完成 Phase 214，按用户给出的官方 DOM 细节修正 idle 语音入口图标：柱条改为垂直居中对齐，不再采用底部对齐的“柱状图”效果。
- 完成 Phase 215，排查最近语音连接失败的真实落点，验证 voice token / LiveKit 基础连通性，并修复 `session_closed.end_reason` 契约错误。
- 完成 Phase 216，修复同一语音轮次内用户转写被重复落成两条消息的问题，改为同 turn 覆盖更新。
- 完成 Phase 217，按真实 RTC 返回结构接入语音图片卡片，复用现有 chat/gallery 渲染与持久化链路。
- 完成 Phase 218，校准语音会话的 pending recovery、assistant canonical response anchor 与标题来源策略，修复“语音思考期误显示同步中 / 语音 final 锚点写偏 / 首句伪装系统标题”三条回归。
- 完成 Phase 219，修复文本 completed 后 assistant 尚未刷回消息列表时短暂误亮“同步中”的前端时序问题。
- 完成 Phase 220，基于真实网关标题事件收紧会话标题解析，只接受首轮明确标题事件，不再消费宽松的 conversation metadata。
- 完成 Phase 221，修复语音消息在 SQLite roundtrip 后丢失 `voiceEventKey`，避免 refined transcript 被重复落库并再次触发“同步中”。
- 完成 Phase 222，为语音会话恢复首条 user 的本地占位标题，并允许同轮 refined transcript 覆盖该占位标题。
- 完成 Phase 223，移除普通文本首轮对话把用户首句预写成标题的 fallback，只接受上游显式 title。
- 完成 Phase 224，收紧语音默认标题策略：旧对话重新进入语音不改标题；新建空对话直接进入语音时预置 `语音通话YYYYMMDDHHmm`。
- 完成 Phase 225，按真实 realtime 抓包收敛 voice/personality 传输值，避免 UI id 直接透传到 LiveKit `session.update` 与 voice token 请求。
- 完成 Phase 226，延后语音默认标题到首条 assistant 成功返回后再生成，并按 turn 开始时间修正语音插话时的消息顺序。
- 完成 Phase 227，修复 assistant 正文中的 legacy `render_searched_image image_id` 标签渲染，避免语音图片回复直接漏出原始占位标签。
- 完成 Phase 228，为语音 `response.grok.output.card_attachment` 增加“晚到卡片回填已落库 assistant 消息”的时序保护，避免图片卡晚于 assistant final 时被丢失。

## 范围

- In:
- 为 `grok:render card_type="citation_card"` 增加正文内联展开能力。
- 为正文内的域名型 citation 链接补 pill 样式，不影响普通链接与图片链接。
- 保留现有底部 `Key Citations` 列表与其去重逻辑。
- 新增回归测试，覆盖 citation tag 展开、重复 URL 收敛与正文 pill 渲染。
- 记录本轮测试、提交与验收结果。
- 基于现有原生 LiveKit 链路实现语音控制，不引入 `voice-gateway realtime/ws_pcm/gateway_tts`。
- 让已有文字对话进入语音时优先沿用已有 `session_id`，客户端不把 `conversation_id` 作为主传字段。
- 让语音模式下的输入框可承接 transcript / LiveKit 文本消息，不再误走普通 `/v1/responses`。
- 补最小回归测试，锁住 voice token 请求体、LiveKit 控制器辅助逻辑和语音输入 UI 行为。
- 使用本机真实网关配置做语音接口 smoke test，至少验证 token 下发、`session_id` 复用与 session-event 上报成功。
- 让语音 assistant 的 `delta` 复用现有 `streaming_assistant` 渲染，不新增第二套流式 UI。
- 让语音入口在连接中/失败时保持可见反馈，不再“点击后无反应”。
- 为 macOS bundle 补 `NSMicrophoneUsageDescription`，避免 WebRTC 抢麦被系统静默拒绝。
- 让语音 token / session-event 复用 Tauri HTTP 适配，避免桌面端 plain `fetch` 触发 `Load failed`。
- 将语音连接错误落到 app log，而不是挂在输入框 UI 上。
- 让语音点击时优先拉起麦克风权限，避免异步建房后才在后台触发设备失败。
- 让远端音频 track 被真实挂载与播放，不再出现“文字有回复但没有声音”。
- 让媒体失败时清理 LiveKit 房间与音频元素，避免 UI 已回退但房间仍悬挂。
- 让语音 token 续接在 `session_id` 已失效时，自动退化为 `conversation_id + strict_resume` 重试一次。
- 让前端只在已有上游会话锚点时才发送 `conversation_id`，避免把本地 `conv_*` id 误发给网关。
- 让语音入口在使用旧本地 conversation 时，先检查 chat `session_id` 是否还存在于网关。
- 若 `session_id` 已失效，则只在本地清掉旧 `session_id/last_response_id`，让语音新建会话，而不是继续拿死 session 去打 `/api/v1/voice/token`。
- 调整语音“连接中”按钮、等待文案与轻提示，去掉“请允许麦克风权限”这类误导性表述。
- 调整语音连接中布局，使其保持单行输入框展示，且不再把用户语音转写自动写回输入框。
- 让已连接语音面板中的本地小柱状声波由真实麦克风电平驱动，而不是固定动画。
- 收敛 idle 态语音入口按钮视觉，黑底白色柱状波形，并在 hover 时触发一次轻微波形动画。
- 收敛语音中本地蓝色柱条交互，不再按单次电平突刺式跳变，而是在检测到说话时启用平滑固定动画。
- 进一步按用户给出的官方示例修正 idle 入口图标的柱条排布与 hover 细节。
- 用真实网关复核最近语音失败是否发生在 token、signal 还是媒体阶段，并把本地确定错误的 `end_reason` 契约收紧到网关允许值。
- 用真实 RTC 会话验证语音图片返回结构，不依赖本地猜测字段名。
- 让同一语音 turn 的多次 user final transcript 只保留一条最终消息。
- 让语音 assistant 的 `response.grok.output.card_attachment` 被解析、持久化并复用现有图片卡片渲染。
- 让语音轮次不再误触发普通聊天的 pending recovery warmup / “同步中” 状态。
- 让语音 assistant final 只按 canonical assistant response id 落一次库，并用该锚点更新 `lastResponseId`。
- 基于真实 RTC / 网关证据确认语音标题来源；若 realtime 无 title，则改为保留空标题等待其他链路补齐，而不是使用首句假装“系统标题”。
- 让文本 completed 后到 `loadMessages()` 之间的 hydration 窗口不再被 pending recovery 误判为“上一轮未返回”。
- 基于真实 `/v1/responses` 流返回收紧标题来源，只接受显式 title 事件，不再从 `conversation.title` 之类宽松字段猜测会话标题。
- 让 `voiceEventKey` 在 SQLite 持久化往返后仍可被 `listMessages()` 返回，避免语音同轮 refined transcript 退化成两条 user 消息。
- 在当前未观测到 voice realtime title 事件的前提下，为语音首条 user 恢复本地占位标题，避免会话标题长期为空；若后续出现显式上游标题，仍以上游为准。
- 让普通文本新会话在首轮 completion 前后都不再把首条 user 文案当作标题；只有收到上游显式 title 事件时才更新标题，否则保持空标题/“新对话”。
- 让旧对话重新进入语音后继续保留原标题，不再被语音 transcript 改写；新建空对话若直接进入语音，则在建会话时即写入 `语音通话YYYYMMDDHHmm`。
- 让语音设置中的 UI 选项 id 与实际传输值解耦；本轮按真实 realtime 抓包把 `sal/rex/gork/kids_story/kids_trivia` 收敛到对应的严格 transport string，同时保持 `custom -> instructions` 现有链路不变。
- 让语音直开新对话在建会话/连房阶段继续显示默认“新对话”，只有首条 assistant 成功返回后才补 `语音通话YYYYMMDDHHmm`；同时让语音 streaming assistant 与最终 assistant 落库都使用 turn 开始时间参与排序，避免用户插话时新 user transcript 跑到未播完 assistant 的上方。
- 让语音图片卡在 `assistant final` 之后晚到时，也能按 `response_id` 回填到已落库 assistant 消息中，补齐 `tool-meta` 与 `reasoningEvents.card_attachment`。
- Out:
- 不改动 citation 结果页数据结构。
- 不改动 reasoning drawer / structured reasoning 面板。
- 不调整来源点击后的滚动与定位行为。
- 不改动上游 citation / tool-meta 协议。
- 不引入单独的语音页面。
- 不引入 ws pcm、gateway tts、memory/context 注入、多房间/多角色能力。
- 不在本轮引入浏览器自动化或 e2e 框架。

## Phase 计划

### Phase 198 - 恢复正文 Inline Citation Pill

- 目标：让 assistant 正文中的 citation tag 渲染为官网类似的域名 pill，同时保留现有底部 `Key Citations` 列表作为汇总入口。
- 实施项：
- 调整 `expandGrokRenderTags`，将 `citation_card` 展开为内联 markdown 链接，并对紧邻重复 URL 做最小去重。
- 调整 `MarkdownBody` 的 `a` 渲染器，仅对“域名文本 = 链接域名”的链接启用 pill 样式，保持图片链接和普通正文链接不变。
- 补充 markdown / citation 回归测试，锁住 inline pill 展开与底部 citations 共存行为。
- 验收项：
- 标题 `AI Memory & Vector DB Comparison` 的 assistant 正文中可出现域名 pill。
- 现有底部 `Key Citations` 仍可正常展示且无重复回归。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 199 - 去重底部重复 Citation

- 目标：让底部 `Key Citations` 对同 URL 的 inline citation 只展示一次，避免 `mem0.ai` / `lancedb.com` 这种重复来源在列表中出现两条。
- 实施项：
- 调整 `buildCitationItems`，对 inline citation 按规范化 URL 去重，不再仅按 `cardId + citationId` 去重。
- 补回归测试，锁住“不同 cardId 指向同一 URL 时底部只保留一条”。
- 验收项：
- `AI Memory & Vector DB Comparison` 的底部 `Key Citations` 中重复来源消失。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 200 - 语音控制层与 LiveKit 控制器

- 目标：保留现有原生 LiveKit 链路，补齐语音 token / session-event / 静音 / 文本事件消费所需的控制层能力。
- 实施项：
- 扩展 `VoiceService` 契约，明确 `session_id` 为客户端主键，并补 `instructions/isRawInstructions` 等必要字段。
- 扩展 `GatewayVoiceService`，让 `/api/v1/voice/token` 请求体符合新的 personality/custom 互斥规则与 `session_id` 主链约束。
- 重写 `LivekitSessionController` 的职责边界，支持连接、停止、麦克风静音、扬声器静音、发送文本、消费 `TranscriptionReceived/DataReceived/ChatMessage` 事件，并继续上报必要 session-events。
- 验收项：
- 语音 token 请求体可稳定表达 voice / personality / custom / speed / `session_id`。
- LiveKit 控制器具备连接态、静音态和文本事件消费能力，不再只有 `connect/disconnect` 空壳。
- 定向测试通过。
- 状态：done

### Phase 201 - ChatInput 与会话接线

- 目标：让现有输入框中的语音模式真正接入当前会话与 LiveKit 控制器，形成可用的交互主链路。
- 实施项：
- 在 `ChatShell` 中把当前会话 `session_id`、conversation 元信息和 voice service 控制器接入 `ChatInput`。
- 调整 `ChatInput` 语音模式行为：进入/停止语音、textarea transcript 回显、语音模式 Enter 发送 LiveKit 文本、普通文本模式保持原有发送。
- 记录并持久化最小 voice session 元信息，保证已有对话进入语音时优先沿用已有 `session_id`。
- 补 UI 与控制层回归测试。
- 验收项：
- 已有对话进入语音时不新造客户端 `session_id`。
- 麦克风/扬声器按钮分别映射 LiveKit 原生 mute / volume 能力。
- 语音模式 textarea 不再误走普通文本发送。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 202 - 真实网关语音 Smoke Test

- 目标：基于当前本机 `~/.nbsearch/config.toml` 的真实网关配置，验证语音 token / session-event 主链可用，并确认已有文本会话切入语音时 `session_id` 可沿用。
- 实施项：
- 读取本机真实网关配置并构造最小探针，请求 `/v1/responses` 建立文本会话，再请求 `/api/v1/voice/token` 复用同一 `session_id`。
- 校准 `GatewayVoiceService` 的 base URL 归一化，确保设置页保存 `/v1/responses` 时仍能正确命中 `/api/v1/voice/*`。
- 补发最小 `session-events` 探针，确认 `session_connected/session_closed` 链路可成功返回。
- 记录无法在 CLI 内完成的运行时边界，避免把“没有浏览器 WebRTC”误判成网关故障。
- 验收项：
- 真实网关返回 voice token，且 `session_id` 与文本主链一致。
- `session-events` 真机请求返回成功。
- 测试记录补充到文档。
- 状态：done

### Phase 203 - 语音 Assistant Delta 渲染

- 目标：让语音 assistant 的流式文本在消息区实时显示，并避免 `response.audio_transcript.delta` 与 `response.text.delta` 双重叠加。
- 实施项：
- 增加语音 assistant delta 源选择 helper，固定“同一轮首个 delta 源胜出”策略。
- 在 `ChatShell` 中把 assistant voice delta 接入现有 `streaming_assistant` 状态，并在 final 到达后清理流式态、落库最终消息。
- 补回归测试，锁住 delta 源选择行为。
- 验收项：
- 语音 assistant 输出过程中，消息区可实时看到文字增长。
- 不出现同一段文本重复叠加两次的问题。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 204 - 语音入口反馈与麦克风权限

- 目标：修复桌面端点击语音后“面板不展开、无反馈”的问题，让连接过程和失败原因对用户可见，并补齐 macOS 抢麦权限声明。
- 实施项：
- 调整 `ChatInput`，让语音面板在 `connecting` / `error` 阶段也可见，连接失败时保留错误提示与重试入口。
- 将常见 WebRTC 抢麦失败映射为可读文案，例如麦克风权限拒绝、未检测到设备、网关连接失败。
- 为 macOS bundle 新增 `src-tauri/Info.plist`，补 `NSMicrophoneUsageDescription`。
- 补回归测试，锁住语音面板可见性与错误提示行为。
- 验收项：
- 点击语音后立即出现连接中反馈，而不是静默无响应。
- 抢麦或连接失败时可见错误文案。
- macOS 打包后的 `Info.plist` 含麦克风权限声明。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 205 - 语音请求修复与日志落盘

- 目标：修复桌面端语音连接阶段的 `Load failed`，并把语音错误统一落到本地日志，移除输入框上的错误文案。
- 实施项：
- 为前端增加最小客户端日志命令，写入 app log 目录。
- 将语音连接/请求失败写入日志，保留控制台错误，去掉输入框下方的错误提示。
- 让 `GatewayVoiceService` 改用与聊天一致的 `runtimeFetch`。
- 补回归测试，锁住语音网关请求仍可在现有测试环境中通过。
- 验收项：
- 语音错误不再直接显示在输入框 UI。
- 语音请求改走 runtime fetch。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 206 - 语音媒体链路补全

- 目标：修复当前桌面端“无麦克风权限弹窗 / UI 自动回退 / 对方只回文字无声音”的组合问题，确保媒体链路要么完整建立，要么彻底失败并清理。
- 实施项：
- 在用户点击语音入口时先做麦克风访问预检，确保权限弹窗和设备错误发生在点击手势内。
- 在 `LivekitSessionController` 中为远端 audio track 执行 attach/detach，并补隐藏音频容器管理。
- 在开麦失败、媒体设备失败等场景强制断开房间并清理本地状态，避免假连接残留。
- 补回归测试，覆盖权限预检、远端音频挂载与失败清理。
- 验收项：
- 语音入口点击后，系统应优先触发麦克风权限申请或明确记录设备失败。
- 远端 assistant 音频 track 被 attach 后可进入浏览器/桌面播放链路。
- 媒体失败后不再保留悬挂 LiveKit 房间。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 207 - 语音 Token 续接恢复

- 目标：修复“点击语音闪一下就退出”的主因，让语音 token 在陈旧 `session_id` 下可以沿用上游 `conversation_id` 自动恢复，而不是把 400 直接吞成泛化失败。
- 实施项：
- 扩展语音 token 请求参数，显式透传上游 `conversation_id` 与 `strict_resume`。
- 调整 `GatewayVoiceService` 错误解析，兼容网关 legacy `{ status: "error", error: "..." }` 与纯文本错误体。
- 当网关返回 `Unknown session_id; omit session_id to create a new session` 时，仅在已有上游 `conversation_id` 的情况下自动去掉 `session_id` 重试一次。
- 为 `ChatShell` 补 resume helper，只在当前会话已经绑定上游锚点时才把 `conversation_id` 发往语音链路。
- 补回归测试，锁住 stale `session_id` 自动恢复与本地会话 id 过滤行为。
- 验收项：
- 已有文本会话切入语音时，若本地 `session_id` 已失效但仍有上游 `conversation_id`，语音 token 请求可自动恢复。
- 新建但尚未绑定上游的本地会话进入语音时，不会误发本地 `conv_*` id 作为 `conversation_id`。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 208 - 语音入口陈旧 Session 校验

- 目标：修复旧本地 conversation 上点击语音时因绑定了失效 chat `session_id` 而直接闪退的问题。
- 实施项：
- 为 `ChatService` 增加 chat session 存活检查，复用 `/v1/sessions/{session_id}/state` 判定 `available|missing|unknown`。
- 在 `ChatShell.handlePrepareVoiceSession()` 中，进入语音前先检查当前 conversation 的 `session_id`。
- 若网关确认 `session_id` 已失效，则本地仅清空该 conversation 的 `sessionId/lastResponseId`，保留本地 conversation id 与消息记录，再继续进入语音。
- 补回归测试，锁住 session 校验状态与 stale anchor 清理。
- 验收项：
- 旧 conversation 的死 `session_id` 不再让语音入口直接因为 `/api/v1/voice/token` 400 而退出。
- 新鲜 `session_id` 的 chat -> voice 主链保持不变。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 209 - 语音连接态视觉收敛

- 目标：让语音入口在“连接中”阶段的视觉状态更接近官网参考，避免当前文案和按钮形态误导用户。
- 实施项：
- 将语音入口连接态改为灰态动态声波按钮，并让声波在连接阶段具备持续旋转动效。
- 将连接中 textarea 文案改为中性等待提示，不再暗示一定会弹麦克风权限窗。
- 增加轻量连接中提示气泡，避免连接阶段完全没有状态反馈。
- 验收项：
- 点击语音后，连接中的入口按钮呈现灰态动态声波。
- 连接中不再展示“请允许麦克风权限”文案。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 210 - 语音连接中单行态与声波动画

- 目标：继续把语音连接中交互压向官网参考，移除当前仍然突兀的展开态和用户语音回填。
- 实施项：
- 让 `connecting` 阶段保持单行输入框布局，不展示已连接后的语音控制栏。
- 移除 user voice transcription 对输入框的自动回填，只保留手动键入文本。
- 将右侧连接中声波从旋转替换为逐条柱状条的高低起伏动画。
- 验收项：
- 连接中阶段显示浅色“连接中……”占位文案，布局保持单行。
- 用户说话内容不再自动注入输入框。
- 右侧声波具备动态高低变化。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 211 - 语音本地电平驱动

- 目标：让已连接语音面板内的小柱状声波反映真实麦克风电平，用户说话时可见动态变化。
- 实施项：
- 在 `LivekitSessionController` 中基于本地 microphone track 建立最小 audio analyser，周期采样电平并通过 lifecycle 向 UI 派发。
- 在 `ChatInput` 中接入 mic level 状态，将现有小柱状波改为真实电平驱动。
- 补最小回归测试，锁住 mic level 采样派发和 UI 高度映射。
- 验收项：
- 进入语音模式后，用户说话时小柱状波明显抬升。
- 麦克风静音或断开后，小柱状波回落。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 212 - Idle 语音入口按钮对齐

- 目标：让 idle 态“进入语音聊天”按钮更贴近官网参考，并在 hover 时给出轻微的波形反馈。
- 实施项：
- 调整 idle 态语音入口按钮的内层结构和颜色，使其呈现黑底白色柱状波形。
- 为 idle 态波形补一次性 hover 动画，而不是持续抖动或整按钮透明变化。
- 补最小回归测试，锁住 idle 波形 hover 配置。
- 验收项：
- idle 语音入口按钮视觉更接近官网黑底白波形。
- 鼠标 hover 到按钮上时，波形轻微波动一次。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 213 - 语音中本地波形收敛

- 目标：修复语音中蓝色柱条“毛毛虫式突刺”问题，让用户说话时显示平滑、固定节奏的动画；同时继续确保 idle 图标使用用户指定的官方柱状样式。
- 实施项：
- 调整语音中本地波形逻辑，仅用麦克风电平判定“是否在说话”，不再直接把每次采样值映射成高度突刺。
- 为说话中的蓝色柱条启用固定节奏动画，静音/停止说话后回落到静态低位。
- 补最小回归测试，锁住本地波形 speaking 判定与 idle 图标柱状配置。
- 验收项：
- 语音中用户说话时，蓝色柱条呈平滑固定动画，不再突刺式乱跳。
- idle 语音入口继续保持用户指定的官方柱状图标。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 214 - Idle 图标按官方 DOM 对齐

- 目标：修复 idle 语音入口图标“像底部柱状图而不是居中波形”的问题，按用户给出的官方 DOM 对齐。
- 实施项：
- 将 idle 态图标内部柱条从底部对齐改为垂直居中对齐。
- 保持 6 根柱条高度与 hover 波动配置，但 hover 动画围绕居中波形执行。
- 补最小回归测试，锁住 idle 图标柱条数量与高度配置。
- 验收项：
- idle 语音入口图标更接近用户提供的官方截图与 DOM。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 215 - 语音信令排查与关闭契约修复

- 目标：确认最近语音失败是否卡在 voice token、LiveKit signal 还是媒体阶段；同时修复客户端把非法 `end_reason` 发给 `/api/v1/voice/session-events` 的问题。
- 实施项：
- 检查前端日志与本地 `voice_sessions` 落库，按 `request_id` 对齐最近失败样本，判断失败阶段。
- 使用真实本机网关执行最小 voice smoke probe，验证 `/v1/responses`、`/api/v1/voice/token`、`session-events` 以及 `livekit.grok.com` 的基础连通性。
- 收紧 `VoiceSessionEndReason` 类型，并把 connect/media 失败后的 `session_closed.end_reason` 统一映射到网关允许的合法值。
- 增加回归测试，锁住“session_failed 之后仍然发送合法 session_closed.end_reason”的场景。
- 验收项：
- 最近失败样本可确认：最新故障点在 signaling 建连阶段，而不是 token 申请阶段。
- 真实探针可确认：网关 token 仍可下发，`livekit.grok.com` 基础 HTTPS 连通正常。
- 客户端不再发送非法 `end_reason`，相关回归测试通过。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 216 - 语音用户转写同轮合并

- 目标：修复语音输入一轮内“先短句、后修正版”被落成两条连续 user 消息的问题。
- 实施项：
- 为 `VoiceTextEvent` 增加稳定事件锚点字段，优先按 `response_id -> item_id -> event_id` 生成 `voiceEventKey`。
- 调整 `decodeLivekitTextEnvelope`，让 `conversation.item.input_audio_transcription.completed` 带出可持久化的 turn 锚点。
- 调整 `commitVoiceMessage`，同一 `voiceEventKey` 的 user final transcript 不再 append，而是 update 现有消息内容。
- 补回归测试，覆盖“同一语音 turn 二次 refined transcript 覆盖第一次文案”。
- 验收项：
- 同一语音轮次只保留一条最终 user 消息。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 217 - 语音图片卡片持久化与渲染

- 目标：基于真实 RTC 返回结构接入语音图片卡片，使语音 assistant 下发的图片与普通聊天复用同一套样式和持久化格式。
- 实施项：
- 按真实探针结果解析 `response.grok.output.card_attachment`，把字符串化 JSON 转成 `ChatCardAttachmentPayload`。
- 将 assistant transcript 中的 `<grok:render card_id="...">` 与实时 card attachment 一起持久化为现有消息内容 + `reasoningEvents` / `<tool-meta>`。
- 调整 `ChatShell` 语音 runtime 事件处理，使 assistant final 落库时携带图片卡片和 reasoning 元数据。
- 补回归测试，覆盖 `response.grok.output` 解析和语音 assistant 图片消息持久化。
- 验收项：
- 语音对话中的图片消息可按现有 chat/gallery 样式展示。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 218 - 语音恢复与 Anchor 校准

- 目标：统一修复语音主链最近暴露的三条问题：语音用户消息误触发普通聊天 pending recovery、`response.human_assist_turn.commit` 把 assistant final 锚点写成非 canonical response id、以及当前没有真实上游 title 时仍把语音首句伪装成系统标题。
- 实施项：
- 将 pending recovery 的“最后一条未完成 user 消息”判定限定为普通聊天轮次，不再把带 `voiceEventKey` 的语音 user final 当成待恢复消息。
- 调整 voice realtime 解析/控制层，跟踪本轮 canonical assistant response id，并优先用于 final assistant 落库与 `anchor_updated` 上报。
- 将 `response.human_assist_turn.commit` 退化为 final transcript fallback，不再单独制造第二条 assistant final。
- 用真实 RTC probe 与本地网关检索结果核对 voice realtime 是否存在 title 事件或可直接读取的 response title。
- 若无可用上游 title，则调整语音标题 fallback 策略：不再在 voice user 首句时生成伪系统标题。
- 补回归测试，覆盖“voice user final 不应触发 pending recovery candidate”“`audio_transcript.done` + `human_assist_turn.commit` 双 final 串联时，只保留一条 assistant final 且锚点保持 canonical response id”“voice conversation 在无上游 title 时保持空标题”三类场景。
- 验收项：
- 语音思考中不再显示“同步中”。
- 语音中断后的普通文字发送不再立即被 pending recovery 误接管。
- 同一语音 assistant 回答不再重复落两条 final。
- 语音回答结束后，后续文字/语音轮次沿用的 `lastResponseId` 为 canonical assistant response id。
- 标题行为与真实上游能力一致，不再把首句伪装成系统标题。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 219 - Completed 后 Pending Recovery 去抖

- 目标：修复文本图片/长回复在 `completed -> refresh/loadMessages` 窗口里短暂误亮“同步中”的问题，避免用户看到并不存在的恢复态。
- 实施项：
- 为文本 completed 后的会话增加短暂 hydration 保护状态，在 assistant 消息刷回当前消息列表前阻断 pending recovery 判定与 warmup UI。
- 收敛 `ChatShell` 中 pending recovery 的前置条件，让“正在 streaming”与“completed 后 hydration 中”共享同一套阻断逻辑。
- 补回归测试，锁住“completed hydration 中不应显示 pending recovery warmup / 不应再次尝试 pending recovery”的行为。
- 验收项：
- 文本首轮请求图片或长回复时，不再短暂闪现“同步中”。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 220 - 收紧标题事件解析

- 目标：基于真实网关标题流返回，收紧本地会话标题来源，只接受显式 title 事件，不再从 conversation metadata 宽松推断标题。
- 实施项：
- 调整标题解析器，只消费 `title.newTitle` / `newTitle` / 明确 title event 上的 `title` 字段，不再读取 `conversation.title`。
- 补回归测试，锁住“忽略 `conversation.title = New conversation` 占位值”“接受首轮末尾返回的明确标题事件”。
- 记录真实流测试结论到文档，明确当前策略为“首轮生成标题，后续轮次通常不再改标题”。
- 验收项：
- 首轮标题仍可正常落库，`New conversation` 占位不再进入标题解析链。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 221 - 修复 VoiceEventKey SQLite Roundtrip

- 目标：修复语音消息经 SQLite 持久化后 `voiceEventKey` 丢失的问题，避免同一轮 refined transcript 被当成新消息 append，并进一步触发 pending recovery。
- 实施项：
- 修正 SQLite repository 的读取映射，保留 `parseMessageContent()` 解析出的 `voiceEventKey`，并在读回阶段按同一 `voiceEventKey` 合并历史重复的 voice user final。
- 补回归测试，锁住“`voiceEventKey` 在 SQLite readback 后仍存在”“历史重复 voice user final 读回时只展示最新一条”。
- 验收项：
- 同一语音轮次的 refined transcript 不再出现两条 user 消息。
- 语音消息 reload 后不再被 pending recovery 误判为普通待恢复 user。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 222 - 语音首轮本地占位标题

- 目标：在当前 voice realtime 未提供稳定标题事件的前提下，为语音会话恢复本地占位标题，避免侧栏长期空标题。
- 实施项：
- 调整语音 user message 提交调用，为首条 user 启用 `allowDerivedTitle`，使用首条 user 文案生成本地占位标题。
- 保留同一 `voiceEventKey` refined transcript 对占位标题的覆盖能力；若未来 voice 上游提供显式标题，仍允许上游标题覆盖本地占位标题。
- 补回归测试，覆盖“首条 voice user 生成标题”“同轮 refined transcript 更新标题”。
- 验收项：
- 新语音会话在首轮 user 后可看到非空标题。
- 同一轮 refined transcript 会同步修正该标题。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 223 - 移除文本首轮 Prompt 标题 Fallback

- 目标：让普通文本新会话不再把首条 user 文案冒充成系统标题，只接受上游首轮显式 title。
- 实施项：
- 调整文本 turn bootstrap，不再在发送首轮前把 `fallbackConversationTitleFromPrompt(prompt)` 预写到 conversation title。
- 调整文本 turn completion，不再在首轮 completion 且缺少上游显式 title 时回退到 prompt-derived title。
- 补回归测试，锁住“新会话首轮 title 为空直到上游 title 到达”“缺少上游 title 时继续保持空标题”的行为。
- 验收项：
- 普通文本新会话首轮发送后，侧栏不再直接显示用户首句。
- 首轮如果缺少上游显式 title，则继续显示“新对话”而不是 prompt 标题。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 224 - 收紧语音默认标题策略

- 目标：避免旧对话重进语音时被重新命名，同时为“空白新对话直接开语音”提供稳定、可预期的默认标题。
- 实施项：
- 调整 `ChatShell.handlePrepareVoiceSession()`，仅在当前是空白新对话时才创建语音标题，格式固定为 `语音通话YYYYMMDDHHmm`。
- 调整语音 message commit，不再根据首条 voice user transcript 或 refined transcript 派生/覆盖 conversation title。
- 补回归测试，覆盖“语音直开标题格式”“非法 timezone 回退 Asia/Shanghai”“voice user turn 不改 existing title”。
- 验收项：
- 旧对话重新开启语音后，侧栏标题保持不变。
- 新建空对话直接进入语音时，标题形如 `语音通话202603121528`。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 226 - 延后语音标题并按开始时间排序

- 目标：避免语音直开新对话在尚未成功返回 assistant 前就被命名；同时修复 assistant 仍在语音输出时用户插话或新 response 开始后把旧流式内容覆盖掉的问题。
- 实施项：
- 调整 `ChatShell.handlePrepareVoiceSession()`，新语音空对话在 prepare/connect 阶段不再预写 `语音通话YYYYMMDDHHmm`，保持空标题/“新对话”。
- 调整 `commitVoiceMessage()`，允许 assistant 首次成功落库时再补默认语音标题，并支持显式传入 `messageCreatedAt` 作为 turn 开始时间。
- 调整语音 runtime 事件处理：当 user final 到达、或 assistant `delta` 切到新的 `responseId` 时，先把当前 streaming assistant 冻结落库，再开始后续 user / assistant 流。
- 调整 `buildVisibleMessages()` 与内存仓库消息读取顺序，让 streaming assistant 和后续 assistant final 都按开始时间排到插话 user turn 之前。
- 补回归测试，覆盖“首条 assistant 到达前不生成语音标题”“assistant 到达后再补默认标题”“active streaming assistant 排在后来的 user 语音 turn 上方”。
- 验收项：
- 新建空对话直接进入语音后，在 assistant 首条成功回复前侧栏继续显示“新对话”。
- assistant 首条成功回复落库后，标题才变为 `语音通话YYYYMMDDHHmm`。
- assistant 尚未播完时若用户插话，旧 partial assistant 不会被第二条流覆盖，而是先冻结为一条独立消息。
- assistant 正在语音输出时用户插话，UI 中 assistant 仍排在该 user turn 上方。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 227 - 修复 Legacy 搜图占位标签渲染

- 目标：修复 assistant 正文里直接出现 `<render_searched_image image_id="..."/>` 原始标签的问题，让语音图片回复继续复用现有图片卡渲染。
- 实施项：
- 调整 `expandGrokRenderTags()`，支持按 `image_id` 从现有图片卡列表中取对应图片并展开为 markdown image。
- 调整 `normalizeAssistantMarkdown()`，在没有可用卡片时清理残留 `render_searched_image` 标签，避免原始占位符直接展示给用户。
- 补回归测试，覆盖 legacy searched-image 标签展开与未命中卡片时的残留清理。
- 验收项：
- 类似 `Title: 语音通话202603121149` 的正文里不再出现 `<render_searched_image .../>` 原始标签。
- 语音图片回复中的 legacy searched-image 占位可正确渲染为现有图片样式。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

### Phase 228 - 回填晚到语音图片卡

- 目标：修复语音图片卡晚于 `assistant final` 到达时，消息已落库但未携带 `tool-meta/cards` 与 `reasoningEvents.card_attachment`、导致图片无法展示的问题。
- 实施项：
- 在 `voice-assistant-media` 中补纯函数，支持从已落库 assistant 消息提取现有 cards，并把晚到 cards 合并回消息内容与 `reasoningEvents`。
- 在 `ChatShell` 的 `response.grok.output` 事件分支中增加“按 `responseId` 回填已落库 assistant 消息”的兜底逻辑；若消息尚未落库，则继续沿用现有内存缓存路径。
- 补回归测试，覆盖“纯占位 assistant 消息被晚到 cards 回填”“已有 tool-meta 的 assistant 消息继续合并新 cards”。
- 验收项：
- 语音 assistant 若先落文本、后到图片卡，最终消息仍会补齐 `tool-meta` 与 `reasoningEvents.card_attachment`。
- 现有“卡先到、final 后到”的正常语音图片链路保持不变。
- `bun test`、`bun run typecheck`、`git diff --check` 通过。
- 状态：done

## Phase 验收记录

### Phase 198

- 验收结果：已完成。assistant 正文中的 `citation_card` 现已展开为 inline domain pill；底部 `Key Citations` 继续保留，未回归为“只在底部显示来源”。
- 备注：本轮对紧邻重复 URL 的 citation tag 做了最小去重，避免像 `mem0.ai` 这类相同来源在正文里连续重复两次。

### Phase 199

- 验收结果：已完成。底部 `Key Citations` 现在会按规范化 URL 去重，相同来源不再因不同 `cardId` 重复展示。
- 备注：正文 inline pill 不受影响，仍可保留多个引用标记；本轮只收敛底部汇总列表。

### Phase 200

- 验收结果：已完成。`VoiceService` / `GatewayVoiceService` / `LivekitSessionController` 已统一到 `session_id` 主链、`voice_gateway_session_id` 透传、LiveKit 原生静音和 `session-events` 上报语义。
- 备注：已补 `livekit-protocol`、`gateway-voice-service`、personality payload 回归测试，并通过定向 `bun test`、`bun run typecheck`。

### Phase 201

- 验收结果：已完成。`ChatInput` 已接入真实 `LivekitSessionController`，支持已有会话沿用 `session_id` 进入语音、LiveKit 原生 mic/speaker mute、语音模式 Enter 走 LiveKit 文本发送、user transcript 回填输入框，并由 `ChatShell` 负责语音消息/anchors/voice session 最小持久化。
- 备注：本轮仅持久化 final user/assistant 文本，未把语音 delta 接到主消息流渲染。

### Phase 202

- 验收结果：已完成。使用本机 `~/.nbsearch/config.toml` 的真实配置对 `http://localhost:8787/v1/responses` 做了文本建会话 -> `/api/v1/voice/token` -> `/api/v1/voice/session-events` 探针，复用同一 `session_id` 成功拿到 voice token，并成功写入 `session_connected` / `session_closed(manual_close)` 事件。
- 备注：真实探针确认了 `GatewayVoiceService` 需要把设置页保存的 `/v1/responses` 地址回收到网关根路径后再访问 `/api/v1/voice/*`；CLI 仍无 `RTCPeerConnection` / `navigator.mediaDevices`，因此本轮只验证网关契约与 session 绑定，不宣称已完成桌面端完整 WebRTC 媒体链路。

### Phase 203

- 验收结果：已完成。语音 assistant 的非 final 文本事件已接入现有 `streaming_assistant` 状态，assistant 开口后消息区会实时增长；final 到达前会清理旧 streaming 态，落最终 assistant 消息时不会留下重复的悬挂流式内容。
- 备注：同一 assistant turn 采用“首个 delta 源胜出”策略，`response.audio_transcript.delta` 与 `response.text.delta` 不再对同一轮文本做双重叠加。

### Phase 204

- 验收结果：已完成。`ChatInput` 现在会在 `connecting` 阶段立即展开语音面板，并在连接失败后展示可读错误文案；macOS debug bundle 的 `Info.plist` 已确认包含 `NSMicrophoneUsageDescription`。
- 备注：本轮不强行常驻错误态面板，失败后会回到普通输入框，并在输入框下方显示错误提示；这样不影响常规输入链路，同时能给出明确反馈。

### Phase 205

- 验收结果：已完成。`GatewayVoiceService` 已切到 `createRuntimeFetch`，桌面端语音 token / session-event 不再直接依赖浏览器 `fetch`；输入框 UI 已移除语音错误文案，失败细节改为前端日志写入 app log。
- 备注：新增 `append_client_log` Tauri 命令后，前端会把 `voice.prepare` / `voice.connect` / `voice.issue_token` / `voice.session_event` 相关错误写入 `frontend.log`，同时保留控制台错误，便于后续桌面端排障。

### Phase 206

- 验收结果：已完成。语音入口点击后会先执行麦克风访问预检，再进入会话准备与建房；远端 audio track 现在会 attach 到隐藏播放容器，并在 unsubscribe / disconnect 时清理；开麦失败或设备失败会强制断开房间，避免 UI 回退后仍残留 LiveKit 假连接。
- 备注：本轮同时明确了平台差异：macOS 仍依赖 `Info.plist + getUserMedia` 才会触发系统权限弹窗，Windows 不需要 `Info.plist`，但同样要在点击手势里触发 `getUserMedia`，否则只会拿到设备/权限错误而不是稳定弹窗。

### Phase 207

- 验收结果：已完成。app log 中暴露的真实失败点是 `/api/v1/voice/token` 返回 `400 Unknown session_id`，不是麦克风权限本身；当前前端已在携带上游 `conversation_id` 时自动降级为 strict resume 重试一次，并且只会对真正的上游 conversation anchor 启用该逻辑。
- 备注：本轮同时修正了错误消息解析，legacy JSON 与纯文本错误都会保留原始 message，便于后续继续从日志定位网关问题。

### Phase 208

- 验收结果：已完成。真实网关探针确认“新鲜 canonical `session_id` 的 chat -> voice”链路是通的，而出问题的旧 conversation 对应 `session_id` 在 `/v1/sessions/{session_id}/state` 已返回 `session_not_found`；当前客户端会先做该检查，命中失效锚点时本地摘除旧 `sessionId/lastResponseId` 后再进入语音。
- 备注：这轮没有把 `conversation_id` 提升为主锚点；正常主链仍然只依赖 `session_id`，只是对本地脏旧 session 增加了入口防护。

### Phase 209

- 验收结果：已完成。语音入口在连接阶段已切到更接近官网参考的灰态动态声波按钮，并增加轻量“连接中……”提示；等待文案已改成中性表述，不再把“是否会弹麦克风权限窗”误写进主提示里。
- 备注：本轮只收敛连接中的视觉反馈，没有改动语音 token、LiveKit 会话建立或媒体主链逻辑。

### Phase 210

- 验收结果：已完成。`connecting` 阶段已回到单行输入框布局，浅色“连接中……”文案直接展示在输入框内；用户实时语音转写不再回填 textarea；右侧声波已改为逐柱高低起伏动画，并复用了更接近现有麦克风按钮的紧凑柱状形态。
- 备注：本轮没有改动已连接后的语音控制栏，也没有动语音网关 / LiveKit 主链，仅调整连接中的渲染和 user transcript 回填策略。

### Phase 211

- 验收结果：已完成。已连接语音面板内的小柱状声波现在由真实本地麦克风电平驱动，用户说话时柱条会明显抬升，静音或断开后回落为静态低位。
- 备注：本轮使用 `livekit-client` 自带的 audio analyser 挂在本地 microphone track 上采样，未改动网关契约和已连接语音控制区布局。

### Phase 212

- 验收结果：已完成。idle 态“进入语音聊天”按钮已收敛为更接近官网的黑底白色柱状波形；hover 到按钮上时，波形会触发一次轻微起伏，不再整按钮发虚或持续乱动。
- 备注：本轮只调整 idle 态入口按钮的视觉和 hover 动效，没有改动连接中、已连接和语音主链逻辑。

### Phase 213

- 验收结果：已完成。语音中蓝色柱条不再按每次电平采样突刺式跳变，而是只用 mic level 判断“是否在说话”；一旦判定说话中，就播放固定节奏的平滑动画，停止说话、静音或断开后回落到静态低位。
- 备注：本轮同时保留了 idle 态入口的官方柱状图标，不再对该图标追加额外形变逻辑。

### Phase 214

- 验收结果：已完成。idle 态语音入口图标已从底部对齐的“柱状图”改为更接近官方 DOM 的垂直居中波形；hover 动画也改为围绕居中波形执行，不再出现底部抬升的错觉。
- 备注：本轮不改连接中、已连接波形和语音主链，只修正 idle 图标的排布细节。

### Phase 215

- 验收结果：已完成。前端日志与本地 `voice_sessions` 显示最近一组失败样本已越过 token 阶段，集中报在 `voice.connect` 的 signaling 建连阶段；真实探针确认 `/v1/responses`、`/api/v1/voice/token` 与 `/api/v1/voice/session-events` 正常，当前 voice token 返回 `wss://livekit.grok.com`，且该主机基础 HTTPS 连通正常。客户端已收紧 `session_closed.end_reason` 到网关允许的枚举，并修复 connect/media 失败后错误地上报 `session_failed`/`media_device_error` 作为关闭原因的问题。
- 备注：本机未找到网关/LiveKit 服务端落盘日志，因此对 `Abort handler called` 的更深层根因仍需结合服务端日志或上游状态继续追。

### Phase 216

- 验收结果：已完成。`conversation.item.input_audio_transcription.completed` 现在会带稳定 `voiceEventKey`，客户端按 `response_id -> item_id -> event_id` 生成 turn 锚点；同一语音轮次内的 refined transcript 不再 append 新 user 消息，而是 update 现有消息内容，并同步更新会话标题。
- 备注：本轮没有改 assistant 图片渲染，只收敛 user transcript 持久化与重复消息问题。

### Phase 217

- 验收结果：已完成。客户端现在会解析 RTC `response.grok.output.card_attachment`，把字符串化 image card JSON 收敛为现有 `ChatCardAttachmentPayload`；语音 assistant final 落库时会同时写入 `<tool-meta>` 与 `reasoningEvents.card_attachment`，因此图片渲染直接复用现有聊天/gallery 样式，不再丢图。
- 备注：本轮基于真实外部 RTC 探针确认上游事件结构，未沿用先前错误的 `response.image.card` 假设。

### Phase 218

- 验收结果：已完成。普通聊天 pending recovery 不再把带 `voiceEventKey` 的语音 user final 当成待恢复消息，因此语音思考期不会再误显示“同步中”；同时 voice realtime 现已维护 canonical assistant response id，并在 `response.human_assist_turn.commit` 到达时复用该 canonical id，从而避免把 `lastResponseId` 写成另一套非 canonical `response_id`，也避免同一语音回答重复落两条 assistant final。标题策略也已按真实能力收敛：当前 voice realtime 未提供可直接消费的 title 事件，因此客户端不再把语音首句伪装成系统标题。
- 备注：真实 RTC probe 明确显示 `response.audio_transcript.done.response_id` 与 `response.human_assist_turn.commit.response_id` 不一致；当前实现已以前者所在的 canonical response 链为准。另行尝试 `GET /v1/responses/{response_id}` 在本地网关上返回 `Not Found`，因此本轮未额外引入 response retrieve 补标题链路。

### Phase 219

- 验收结果：已完成。文本 completed 事件到达后，当前会话会进入一个短暂的 completed hydration 保护窗口；在 assistant 消息通过 `loadMessages()` 刷回当前消息列表之前，pending recovery 判定与“同步中” warmup 都会被阻断，因此不再把“流已完成但消息尚未回填”的窗口误判成恢复态。
- 备注：本轮根因已用真实图片流探针确认，不是“思考太久没包导致断流恢复”；真实网关图像轮次持续回包，最长包间隔约 3.1 秒，`同步中` 的来源是前端 `completed -> loadMessages()` 之间的时序窗口。

### Phase 220

- 验收结果：已完成。标题解析器现已只消费显式 title 事件位置，不再从 `conversation.title` 之类 conversation metadata 宽松猜测标题；真实文本流验证显示，当前上游策略是“首轮先回 `New conversation` 占位，末尾再回真实标题，后续轮次通常不再回标题”，本地现已按该真实行为收口。
- 备注：本轮保留了显式 `title.newTitle` / `newTitle` / title event 直出字符串三条真实标题入口，但移除了 `conversation.title` 回退，因此首轮占位不会再进入标题解析链。

### Phase 221

- 验收结果：已完成。SQLite 读回阶段现已保留语音消息的 `voiceEventKey`，并且会对同一 `voiceEventKey` 的历史 voice user final 只展示最新一条，因此 refined transcript 不会再因为 reload 后丢键而 append 第二条；pending recovery 也能重新识别这类消息是语音 final，而不是普通待恢复 user。
- 备注：这次根因不在网关，也不在 pending recovery 本身，而是在本地 SQLite `content_json -> ChatMessage` 映射把 `voiceEventKey` 漏掉了；本轮顺手对历史重复 voice user final 做了读时合并，避免旧数据继续在 UI 中重复出现。

### Phase 222

- 验收结果：已完成。语音 user final 提交路径现已启用本地标题派生，因此新的语音会话在首轮 user 文本落地后即可得到非空标题；如果同一 `voiceEventKey` 后续又收到 refined transcript，标题也会随该最终文案同步修正。
- 备注：当前仍未在 realtime 侧观测到稳定可消费的 voice title 事件，因此本轮恢复的是“本地占位标题”而不是放宽标题解析；若后续上游补出显式 voice title，现有 `upstreamTitle` 仍会优先覆盖本地占位标题。

### Phase 223

- 验收结果：已完成。普通文本主链现在不会再在 bootstrap 阶段把首条 user 文案写进标题，也不会在 completion 且未收到上游显式 title 时回退到 prompt-derived title；因此文本首轮若没有上游 title，侧栏会继续显示“新对话”，直到真实 title 事件到达。
- 备注：这次截图里出现的“标题 = 用户首句”并不是上游 title，而是文本链路里遗留的本地 fallback：`bootstrapGatewayTurn()` 先用 `fallbackConversationTitleFromPrompt(prompt)` 预写标题，`buildCompletedGatewayTurn()` 在缺少显式 title 时又继续保留它。本轮已把这两处 fallback 从普通文本主链移除。

### Phase 224

- 验收结果：已完成。语音标题策略已切换为“旧对话不改标题，新语音空对话在建会话时直接命名为 `语音通话YYYYMMDDHHmm`”；voice transcript 不再参与标题推导。
- 备注：标题时间戳按传入 timezone 格式化；若 timezone 非法，则回退到 `Asia/Shanghai`，避免本地配置异常把标题格式打坏。

### Phase 225

- 验收结果：已完成。`ChatInput` 现在不会再把语音设置 UI id 直接透传给 voice token 和 LiveKit `session.update`；本轮按真实 realtime 抓包把 `sal -> xai_sal`、`rex -> xai_rex`、`gork -> Gork`、`kids_story -> kids-stories`、`kids_trivia -> kids-trivia` 收敛到严格 transport string，并补回 canonical id 到 UI label 的回显兼容。
- 备注：本轮不再以 `voice-gateway` 对外 capabilities 枚举作为唯一依据，而以用户提供的 realtime 抓包为第一证据；`custom -> personality=null + instructions + is_raw_instructions=true` 现有链路保持不变。

### Phase 226

- 验收结果：已完成。语音直开新对话在 prepare/connect 阶段不再预写 `语音通话...` 标题，直到首条 assistant 成功返回并落库时，才会在当前 conversation 仍为空标题的前提下补默认语音标题；同时，当 user final 到达或 assistant `delta` 切换到新的 `responseId` 时，当前 streaming assistant 会先冻结落库，因此第二条流不再覆盖第一条 partial assistant。voice streaming assistant 与后续 assistant final 也都会使用 turn 开始时间参与排序，因此用户在 assistant 仍在语音输出时插话，assistant 会继续排在该 user turn 上方。
- 备注：本轮没有恢复“首条 user transcript 派生标题”，默认语音标题只在 assistant 首次成功返回后生成；另外为让测试仓库与 SQLite 保持一致，内存仓库 `listMessages()` 也已改为按 `createdAt` 升序返回。

### Phase 227

- 验收结果：已完成。markdown 渲染层现已支持 legacy `<render_searched_image image_id="..."/>` 标签，会按 `image_id` 从现有图片卡中展开成 markdown image；若未命中图片卡，残留标签也会在 normalize 阶段被清理，不再直接显示给用户。
- 备注：本轮只调整 assistant markdown 渲染入口，不改动语音 session、LiveKit 事件绑定或图片卡持久化结构。

### Phase 228

- 验收结果：已完成。语音 `response.grok.output.card_attachment` 现在即使晚于 `assistant final` 到达，也会按 `responseId` 回填到已落库 assistant 消息，补齐 `<tool-meta>{"cards":[...]}</tool-meta>` 与 `reasoningEvents.card_attachment`，不再出现“正文只剩 legacy/placeholder 标签但无图片卡可渲染”的状态。
- 备注：本轮只补 late card backfill 时序保护，不改变现有“卡片先缓存、assistant final 再落库”的主链；若消息尚未落库，仍继续走原有缓存路径。

## 测试记录

- `bun test`：通过。
- `bun run typecheck`：通过。
- `git diff --check`：通过。
- `bun test src/components/chat-message-citations.test.ts src/components/markdown-content.test.ts`：通过。
- `bun run build`（按需）：未执行，本轮未改动构建链路。
- `bun test src/infrastructure/voice/livekit-protocol.test.ts src/infrastructure/voice/gateway-voice-service.test.ts src/components/voice-settings-personality.test.ts`：通过。
- `bun test src/infrastructure/voice/gateway-voice-service.test.ts`：通过，新增 `/v1/responses` -> `/api/v1/voice/token` 根路径归一化回归。
- `bun test src/features/chat/voice-assistant-stream.test.ts`：通过，覆盖 assistant delta 源分类与“首源胜出”去重策略。
- `bun test src/components/chat-input-voice-entry.test.ts src/components/chat-input-voice-state.test.ts`：通过，覆盖语音面板可见性与连接失败文案映射。
- `bun run typecheck`：通过。
- `bun test`：通过。
- `git diff --check`：通过。
- `bun run build`：通过（仅保留现有 Vite chunk / eval warning，无新增构建错误）。
- `node /tmp/voice_smoke_20260312.mjs`：通过，真实网关返回 `session_id=sess_f80fa164-eddb-46c8-9ba2-e661a56938e4`、`response_id=be04f9e3-ac30-4f51-b078-f7f776e393c5`、`conversation_id=a9280fb0-cc29-44f7-a367-838f1d1fe94e`、`request_id=835a56e453d820d88f4445dfe4dd4f97`，`session_connected` 与 `session_closed(manual_close)` 均为 `status=success`。
- `bun run tauri:build:debug`：通过，生成 `src-tauri/target/debug/bundle/macos/NBSearch.app`。
- `plutil -p src-tauri/target/debug/bundle/macos/NBSearch.app/Contents/Info.plist`：通过，确认存在 `NSMicrophoneUsageDescription = "Request microphone access for voice conversations."`。
- `bun test src/infrastructure/voice/gateway-voice-service.test.ts src/components/chat-input-voice-state.test.ts src/components/chat-input-voice-entry.test.ts`：通过。
- `bun run typecheck`：通过。
- `git diff --check`：通过。
- `bun test`：通过。
- `bun run build`：通过。
- `bun run tauri:build:debug`：通过，生成 `src-tauri/target/debug/bundle/macos/NBSearch.app` 与 `src-tauri/target/debug/bundle/dmg/NBSearch_0.1.7_aarch64.dmg`。
- `bun test src/infrastructure/voice/livekit-session.test.ts src/infrastructure/voice/gateway-voice-service.test.ts src/components/chat-input-voice-entry.test.ts src/components/chat-input-voice-state.test.ts`：通过。
- `bun test`：通过。
- `bun run typecheck`：通过。
- `bun run build`：通过（仅保留既有 Vite chunk / eval warning）。
- `git diff --check`：通过。
- `bun test src/infrastructure/voice/gateway-voice-service.test.ts src/features/chat/chat-shell-helpers.test.ts`：通过，覆盖 stale `session_id` 自动重试与本地 conversation id 过滤。
- `bun test`：通过。
- `bun run typecheck`：通过。
- `bun run build`：通过（仅保留既有 Vite chunk / eval warning）。
- `git diff --check`：通过。
- `curl -sS 'http://localhost:8787/v1/sessions/sess_03ea4a80-dd8c-4748-8d0d-1fdc255bf699/state' -H 'Authorization: Bearer ***'`：返回 `404 session_not_found`，确认出问题的旧 conversation 绑定的是网关已不存在的 chat session。
- `curl -sS 'http://localhost:8787/v1/responses' ... session_id='sess_6129d446-8350-4123-8c51-6111c65013df'`：通过，真实新会话可正常完成一轮文本响应。
- `curl -sS 'http://localhost:8787/api/v1/voice/token' ... session_id='sess_6129d446-8350-4123-8c51-6111c65013df'`：通过，真实新会话可直接进入 voice token 下发。
- `bun test src/infrastructure/chat/grok-chat-service.voice-session.test.ts src/features/chat/chat-shell-helpers.test.ts`：通过，覆盖 session 存活检查与 stale anchor 清理。
- `bun test`：通过。
- `bun run typecheck`：通过。
- `bun run build`：通过（仅保留既有 Vite chunk / eval warning）。
- `bun test src/components/chat-input-voice-entry.test.ts src/components/chat-input-voice-state.test.ts`：通过，覆盖连接态声波配置与面板可见性逻辑。
- `bun run typecheck`：通过。
- `bun run build`：通过（仅保留既有 Vite chunk / eval warning）。
- `bun test src/components/chat-input-voice-entry.test.ts src/components/chat-input-voice-state.test.ts`：通过，覆盖连接态单行布局判定、user transcript 回填清理规则与柱状声波动画配置。
- `bun run typecheck`：通过。
- `bun run build`：通过（仅保留既有 Vite chunk / eval warning）。
- `bun test src/components/chat-input-voice-meter.test.ts src/components/chat-input-voice-entry.test.ts src/components/chat-input-voice-state.test.ts src/infrastructure/voice/livekit-session.test.ts`：通过，覆盖本地 mic level 映射、小柱状波高度计算和控制层 mic level 派发。
- `bun run typecheck`：通过。
- `bun run build`：通过（仅保留既有 Vite chunk / eval warning）。
- `bun test src/components/chat-input-voice-entry.test.ts src/components/chat-input-voice-meter.test.ts src/components/chat-input-voice-state.test.ts`：通过，覆盖 idle 波形 hover 配置、连接态波形配置和本地 mic level 映射。
- `bun run typecheck`：通过。
- `bun run build`：通过（仅保留既有 Vite chunk / eval warning）。
- `bun test src/components/chat-input-voice-meter.test.ts src/components/chat-input-voice-entry.test.ts src/components/chat-input-voice-state.test.ts`：通过，覆盖 speaking 判定、固定节奏蓝色柱条配置和 idle 柱状图标配置。
- `bun run typecheck`：通过。
- `bun run build`：通过（仅保留既有 Vite chunk / eval warning）。
- `bun test src/components/chat-input-voice-entry.test.ts src/components/chat-input-voice-meter.test.ts src/components/chat-input-voice-state.test.ts`：通过，覆盖 idle 图标柱条配置与居中波形相关 helper。
- `bun run typecheck`：通过。
- `bun run build`：通过（仅保留既有 Vite chunk / eval warning）。
- `node /tmp/voice_smoke_20260312.mjs`：通过，真实网关返回 `session_id=sess_45bff186-b2c4-4ff1-b61e-339b1fd80b3d`、`conversation_id=d96c17db-b644-4ad7-ba1c-617d14daa2e5`、`request_id=52144e72d6f7696aefd3ba0badb153e6`、`url=wss://livekit.grok.com`，`session_connected` 与 `session_closed(manual_close)` 均成功。
- `curl -sS -I https://livekit.grok.com`：通过，返回 `HTTP/2 200`，确认基础 HTTPS 连通正常。
- `bun -e '...'`（解码 voice token JWT）：通过，确认 token 中仍携带上游 `video.room` claim。
- `bun test src/infrastructure/voice/livekit-session.test.ts`：通过，覆盖 connect 失败后 `session_closed.end_reason=api_close` 的合法上报。
- `bun run typecheck`：通过。
- `bun test`：通过。
- `git diff --check`：通过。
- `open http://127.0.0.1:4319/voice-probe.html` + 外部 RTC probe：通过，真实返回 `response.grok.output.card_attachment`，并确认 assistant transcript 通过 `<grok:render card_id="...">` 引用 image card。
- `bun test src/infrastructure/voice/livekit-protocol.test.ts src/features/chat/voice-assistant-media.test.ts src/features/chat/voice-message-commit.test.ts`：通过。
- `bun run typecheck`：通过。
- `bun test`：通过。
- `git diff --check`：通过。
- `bun test src/features/chat/chat-shell-helpers.test.ts src/features/chat/voice-message-commit.test.ts src/infrastructure/voice/livekit-protocol.test.ts src/infrastructure/voice/livekit-session.test.ts`：通过，覆盖语音 pending recovery 隔离、voice title fallback、canonical response id 收敛与 duplicate final 抑制。
- `bun run typecheck`：通过。
- `bun test`：通过。
- `bun run build`：通过（仅保留既有 Vite `eval` / chunk size warning，无新增构建错误）。
- `curl -sS -H 'Authorization: Bearer ***' http://localhost:8787/v1/responses/efdff21e-2023-4fa1-a478-1c1842a7ae29`：返回 `Not Found`，本地网关未暴露可直接用于语音补标题的 response retrieve 路径。
- `bun test src/features/chat/chat-shell-helpers.test.ts src/infrastructure/chat/chunk-parsers.test.ts`：通过，覆盖 completed hydration 期间 pending recovery 去抖与标题解析收紧的定向回归。
- `bun run typecheck`：通过。
- `git diff --check`：通过。
- `bun test`：通过。
- `bun run build`：通过（仅保留既有 Vite `eval` / chunk size warning，无新增构建错误）。
- `bun --eval '...'` 文本图片流真实探针：通过，`durationMs=36637`、`chunkCount=81`、`maxGapMs=3144`，确认图像轮次持续回包，`同步中` 不是由长时间无包触发。
- `bun --eval '...'` 文本标题真实探针：通过，首轮观测到 `New conversation -> 南京问候与城市简介`；同一 `session_id` 第二轮未再返回 title，确认当前真实策略为“首轮生成标题，后续轮次通常不再改标题”。
- `bun test src/infrastructure/storage/sqlite/repository.test.ts src/features/chat/voice-message-commit.test.ts`：通过，覆盖 SQLite `voiceEventKey` roundtrip、历史重复语音消息读时合并，以及语音本地标题派生回归。
- `bun run typecheck`：通过。
- `git diff --check`：通过。
- `bun test`：通过。
- `bun run build`：通过（仅保留既有 Vite `eval` / chunk size warning，无新增构建错误）。
- `bun test src/infrastructure/chat/gateway-turn-bootstrap.test.ts src/infrastructure/chat/gateway-turn-completion.test.ts`：通过，覆盖文本首轮不再预写 prompt 标题，以及缺少上游显式 title 时保持空标题。
- `bun test src/features/chat/chat-shell-helpers.test.ts src/features/chat/voice-message-commit.test.ts`：通过，覆盖语音直开标题格式、非法 timezone 回退和 voice user turn 不改标题。
- `bun run typecheck`：通过。
- `bun test`：通过。
- `git diff --check`：通过。
- `bun test src/components/voice-settings-defaults.test.ts src/components/voice-settings-personality.test.ts`：通过，覆盖 voice/personality transport string 映射与 canonical label 回显。
- `bun run typecheck`：通过。
- `bun test`：通过。
- `bun test src/features/chat/chat-shell-helpers.test.ts src/features/chat/voice-message-commit.test.ts`：通过，覆盖语音标题延后生成与 streaming assistant / user 插话排序。
- `bun run typecheck`：通过。
- `bun test`：通过。
- `git diff --check`：通过。
- `bun test src/components/markdown-content.test.ts`：通过，新增覆盖 legacy `render_searched_image image_id` 标签展开与残留标签清理。
- `bun test`：通过。
- `bun run typecheck`：通过。
- `git diff --check`：通过。
- `bun test src/features/chat/voice-assistant-media.test.ts src/features/chat/voice-message-commit.test.ts`：通过，覆盖晚到语音图片卡回填与既有 assistant 图片消息合并。
- `bun run typecheck`：通过。
- `bun test`：通过。
- `git diff --check`：通过。

## 提交记录

- `d0d02db` - `phase(198): render inline citation pills`
- `7fc186b` - `phase(199): dedupe repeated key citations`
- `f0f62fb` - `phase(200): implement livekit voice controller`
- `01c836c` - `phase(201): wire voice sessions into chat shell`
- `5f6fdaf` - `phase(202): validate live voice gateway contract`
- `0e8f15a` - `phase(203): stream voice assistant deltas`
- `f528e4b` - `phase(204): surface voice connect feedback`
- `081d739` - `phase(205): route voice errors to app logs`
- `423e2ca` - `phase(206): repair livekit voice media flow`
- `cde32a2` - `phase(207): recover stale voice session anchors`
- `cdc7844` - `phase(208): guard voice entry against stale sessions`
- `4bf288f` - `phase(216): merge repeated voice user transcripts`
- `91404ef` - `phase(217): persist voice image cards`
- `9cb234b` - `phase(218): repair voice recovery anchors`
- `dea39e7` - `phase(219): suppress post-completion recovery warmup`
- `9750925` - `phase(219): record recovery warmup validation`
- `9a63381` - `phase(220): tighten title event parsing`
- `d4ffd9c` - `phase(221): preserve voice event keys after sqlite reload`
- `25a97fa` - `phase(222): derive voice titles from first user turn`
- `0d128ff` - `phase(223): remove prompt title fallback for new chats`
- `620ead1` - `phase(226): delay voice titles and preserve turn order`

## 例外记录

- Phase 224-226：相关语音标题、transport string 与插话排序调整已在用户确认后合并提交到 `620ead1`；本轮不再单独拆分补提交。
- 未提交探索项：本轮曾尝试实现“语音被打断后旧 assistant late final 继续回填”，后经用户核实 Grok 官方同样会在打断时丢弃旧回答，因此该探索改动已在提交前回滚，不纳入当前基线。

## 明日计划

- 若 inline citation pill 样式确认稳定，可继续评估是否需要让 citation 点击行为与官网更接近。
- 用真实语音会话复核 `Title: 你好呀！`，确认 user transcript 已合并、assistant 图片已按现有 chat/gallery 样式正常展示。
