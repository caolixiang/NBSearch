# 今日目标

- 修复侧边栏对话标题编辑按钮缺少明确 hover 反馈的问题。
- 增加会话星标能力，并确保老库升级后原有会话不丢失。
- 收敛会话行三枚操作按钮的留白，并让星标 hover 使用黄色提示。
- 去掉“星标”分组内会话默认常驻的尾部黄星，避免视觉噪音与标题错位。
- 将星标恢复为右侧操作轨；已星标常驻最右，未星标仅在 hover 时显示。
- 去掉普通输入框右侧独立的“语音输入”图标，只保留语音模式入口。
- 在输入框右侧增加模型快捷切换区域，并与页头模型选择保持同步。
- 修复输入框模型快捷切换菜单被裁剪的问题。

## 范围

- In:
- 优化侧边栏会话行中“重命名对话”按钮的鼠标悬停交互。
- 收紧会话行星标/重命名/删除三个操作按钮的横向留白。
- 将星标按钮 hover 与已星标状态调整为黄色语义。
- 去掉已归入 `星标` 分组的会话行默认常驻黄星，仅在 hover 时显示星标操作入口。
- 调整星标显示策略：已星标常驻，未星标仅在 hover 时显示；顺序为“重命名、删除、星标”，其中星标固定在最右侧。
- 去掉普通文本输入框右侧单独的麦克风按钮。
- 在输入框右侧加入 `Fast / Thinker / Max` 模型快捷切换，下拉选择后同步更新当前模型。
- 修复模型快捷切换上弹菜单被输入框容器裁剪的问题。
- 补充本次任务的计划、测试与提交记录。
- Out:
- 不改动侧边栏布局。
- 不调整删除按钮语义。
- 不做新的交互流程改版。

## Phase 计划

### Phase 1 - 修复标题编辑按钮 hover 反馈

- 目标：让侧边栏标题编辑按钮在鼠标移动上去时有清晰、稳定的视觉反馈。
- 实施项：
- 检查会话行操作按钮的显示与 hover 样式。
- 以最小改动增强编辑按钮 hover/active/focus-visible 反馈。
- 进行必要校验并记录结果。
- 验收项：
- 鼠标移入编辑按钮时有明显 hover 反馈。
- 不影响当前重命名与删除功能。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 3 - 会话星标与老库迁移兼容

- 目标：实现会话星标持久化、侧边栏 `星标` 分组置顶，并修复老数据库在新增字段后会话列表被查空的问题。
- 实施项：
- 在会话存储模型、内存仓储和 SQLite 仓储中新增 `starred` 字段与更新接口。
- 为 `conversations` 表增加 `starred` migration，并调整数据库初始化逻辑，让已打开旧连接也会补跑 migration。
- 在侧边栏加入星标按钮、`星标` 分组和适当的宽度调整。
- 补充星标排序、星标保留、老库迁移相关测试。
- 验收项：
- 支持星标与取消星标，取消后回到原本时间排序。
- `星标` 分组显示在 `今天`、`更早` 之前并置顶。
- 旧库升级后原有对话仍可显示。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 2 - 收敛标题编辑按钮 hover 视觉

- 目标：去掉编辑按钮 hover 时突兀的偏白底色，让交互反馈更贴近侧边栏整体色板。
- 实施项：
- 调整编辑按钮 hover 背景与边框颜色，保留清晰的交互反馈。
- 保持 focus-visible 与禁用态不变。
- 进行必要校验并记录结果。
- 验收项：
- 鼠标移入编辑按钮时仍有明确反馈，但不再出现突兀白底。
- 不影响当前重命名与删除功能。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 4 - 迁移事务与旧库升级夹具测试

- 目标：为 SQLite migration 增加事务保护，并用真实 v1 夹具验证旧库升级不会丢会话。
- 实施项：
- 将 migration 执行抽离为可测试的 runtime helper。
- 为每个 migration 增加 `BEGIN IMMEDIATE / COMMIT / ROLLBACK` 事务包裹。
- 新增 `legacy-v1.sql` 夹具，验证旧会话升级后仍能查询到且 `starred` 默认值正确。
- 新增失败 migration 回滚测试，验证不会留下半迁移状态。
- 验收项：
- 旧库夹具升级后保留原有会话数据。
- 失败 migration 不会留下部分 schema 变更，也不会误写 `schema_migrations`。
- `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 5 - 旧库升级 Smoke Test

- 目标：对真实文件级旧库升级流程做一次手工 smoke，确认夹具测试与实际文件行为一致。
- 实施项：
- 基于 `legacy-v1.sql` 生成临时 `chat-app.db` 文件。
- 使用当前 migration runtime 对该文件执行升级与重复检测。
- 校验 `conversations` schema、`schema_migrations` 版本记录以及旧会话查询结果。
- 验收项：
- `conversations` 表包含 `starred` 列。
- `schema_migrations` 为 `[1, 2]`。
- 旧会话升级后仍可查询到，且 `starred` 默认值为 `0`。
- 状态：done

### Phase 6 - 收紧侧边栏操作区并强化星标语义

- 目标：减少会话行三个操作按钮之间的留白，让操作区更紧凑，并让星标 hover 明确变为黄色。
- 实施项：
- 调整会话标题按钮右侧预留宽度，避免操作区显得稀疏。
- 收紧星标、重命名、删除三个按钮的尺寸与间距。
- 将星标按钮 hover 和已星标颜色统一到黄色语义。
- 进行必要校验并记录结果。
- 验收项：
- 三个操作按钮之间的视觉留白明显收紧。
- 星标 hover 变为黄色，已星标状态也保持明确可识别。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 7 - 去掉星标分组内的常驻尾部黄星

- 目标：移除 `星标` 分组会话行末尾默认显示的黄星，避免标题对齐被破坏，同时保留 hover 时的取消星标操作。
- 实施项：
- 调整星标按钮显示条件，不再因 `convo.starred` 而常驻显示。
- 保留 hover 展开的星标操作入口，确保取消星标路径不丢失。
- 进行必要校验并记录结果。
- 验收项：
- `星标` 分组中的会话默认不再显示尾部黄星。
- hover 时仍可看到星标按钮并执行取消星标。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 8 - 调整右侧星标显隐并固定到最右侧

- 目标：不再把星标插入标题流中；已星标会话在右侧最右位常驻显示，未星标会话仅在 hover 时显示星标入口，重命名和删除仍在其左侧展开。
- 实施项：
- 调整会话行 action 排列顺序为“重命名、删除、星标”。
- 调整星标显隐规则：已星标常驻，未星标 hover 才显示，并保持标题区预留宽度稳定。
- 保持 hover 展开时的按钮布局稳定，不让标题与图标错位。
- 进行必要校验并记录结果。
- 验收项：
- 已星标会话的星标常驻显示在最右侧；未星标会话默认不显示星标。
- hover 时重命名、删除在星标左边展开。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 9 - 去掉普通输入框的语音输入图标

- 目标：移除普通文本输入框右侧单独的“语音输入”图标，避免和语音模式入口重复。
- 实施项：
- 删除普通输入框中的语音输入按钮与对应本地状态。
- 保持语音模式入口、发送按钮和输入框布局不变。
- 进行必要校验并记录结果。
- 验收项：
- 普通输入框右侧不再显示独立的麦克风图标。
- 语音模式入口按钮仍可正常显示。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 10 - 增加输入框模型快捷切换

- 目标：在输入框右侧增加模型快捷切换区域，提供 `Fast / Thinker / Max` 三个快捷模式，并与页头模型选择同步。
- 实施项：
- 基于现有 `selectedModel` 和 `modelOptions` 生成输入框快捷模型预设，不新建独立状态源。
- 在输入框右侧加入快捷切换按钮和上弹菜单，选中后同步切换聊天模型。
- 当输入文本过长或已有附件时，将按钮从文字模式折叠为图标模式。
- 补充模型快捷选择与折叠规则测试。
- 验收项：
- 输入框右侧出现模型快捷切换按钮。
- 选择后页头模型选择同步更新。
- 输入过长时快捷切换退化为图标。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

### Phase 11 - 修复输入框模型菜单裁剪

- 目标：修复输入框右侧模型快捷切换菜单被裁剪或被覆盖的问题。
- 实施项：
- 调整输入框承载层的 overflow 策略，允许模型菜单向上溢出显示。
- 提高快捷菜单层级，避免被消息区或输入框邻近元素遮挡。
- 进行必要校验并记录结果。
- 验收项：
- 输入框模型菜单可完整向上展开。
- 不影响输入框、语音入口和发送按钮的现有布局。
- `bun test`、`bun run typecheck` 通过。
- 状态：done

## Phase 验收记录

### Phase 1

- 验收结果：已增强侧边栏编辑/删除按钮的 hover、border 与 focus-visible 反馈，激活会话行内也能明显识别悬停状态。
- 备注：仅调整按钮交互样式，未改动会话列表布局与行为逻辑。

### Phase 2

- 验收结果：已将编辑按钮 hover 底色从偏白卡片色收敛为更贴近侧边栏底色的暖色层次，并保留边框、focus 与按压反馈。
- 备注：本轮只收敛编辑按钮 hover 视觉，不扩散到侧边栏其它组件。

### Phase 3

- 验收结果：已完成会话星标持久化、侧边栏 `星标` 分组、侧边栏宽度调整，并修复旧库连接未补跑 migration 时会话查询失败的问题。
- 备注：星标切换不会改写 `updatedAt`，因此取消星标后仍按原本时间排序回退。

### Phase 4

- 验收结果：已为 migration 增加事务保护，并通过真实 v1 夹具测试验证旧会话升级后仍可读；同时新增失败 migration 回滚测试。
- 备注：运行时仍保持“每次取 DB 先做版本检测”，但已应用版本不会重复执行，只会做轻量比对。

### Phase 5

- 验收结果：文件级 smoke test 通过，临时旧库文件升级后 `schema_migrations=[1,2]`，两条旧会话仍可查询，且 `starred=0`。
- 备注：本轮未新增业务代码，只补充真实升级路径验证。

### Phase 6

- 验收结果：已收紧会话行三枚操作按钮的间距与占位宽度，悬停展开时不再显得松散；星标 hover 与已星标状态改为黄色语义。
- 备注：本轮仅调整侧边栏动作区样式，不改动星标分组、排序与持久化逻辑。

### Phase 7

- 验收结果：已移除 `星标` 分组内会话默认常驻的尾部黄星，标题对齐恢复正常；hover 时仍保留取消星标入口。
- 备注：本轮不新增替代标识，避免再次引入多余视觉元素。

### Phase 8

- 验收结果：已将星标固定到右侧最右位；已星标会话常驻显示，未星标会话 hover 时才显示，重命名和删除在其左侧展开，标题区不再出现尾部挂星。
- 备注：本轮只调整动作区排列与显隐策略，不改动星标持久化和排序逻辑。

### Phase 9

- 验收结果：已移除普通输入框右侧独立的麦克风图标，输入框仅保留语音模式入口和发送按钮。
- 备注：本轮不改动语音模式、麦克风静音和语音面板逻辑。

### Phase 10

- 验收结果：已在输入框右侧增加 `Fast / Thinker / Max` 模型快捷切换，并接入现有 `selectedModel`；页头模型选择与输入框快捷选择保持同步，长文本时按钮自动折叠为图标。
- 备注：本轮复用了现有模型状态链路，没有引入第二套模型持久化逻辑。

### Phase 11

- 验收结果：已移除输入框承载层对模型菜单的裁剪，并提高快捷菜单层级，模型菜单可完整向上展开。
- 备注：本轮只修正展示层级与裁剪，不改动模型选择逻辑。

## 测试记录

- `bun test`：通过（360 pass / 0 fail）
- `bun run typecheck`：通过
- `bun run build`（按需）：通过（Vite build 成功，保留现有 chunk warning）
- 文件级 smoke：
  - 临时旧库文件升级后 `conversations` 包含 `starred` 列
  - `schema_migrations` 为 `[1, 2]`
  - 旧会话 `conv_fixture_newer`、`conv_fixture_older` 均可查询
- `git diff --check`：通过

## 提交记录

- `9aa545a` - `phase(1): improve sidebar title edit hover feedback`
- `25b8780` - `phase(1): record sidebar hover fix docs`
- `b5fb88a` - `phase(2): soften sidebar title edit hover tone`
- `246ad40` - `phase(2): record hover tone refinement docs`
- `7a8b77a` - `phase(3): add persistent starred conversations`
- `3f85419` - `phase(3): record starred conversation rollout docs`
- `3c3ace2` - `phase(4): harden sqlite migrations`
- `852d16c` - `phase(4): record migration hardening docs`
- `67e4f44` - `phase(5): record sqlite migration smoke test`
- `4899225` - `phase(6): tighten sidebar action spacing`
- `42dda30` - `phase(6): record sidebar action polish docs`
- `a70d1cd` - `phase(7): hide starred tail icon by default`
- `c2768c3` - `phase(7): record starred icon cleanup docs`
- `12d2dda` - `phase(8): pin starred action to the right rail`
- `c9a03a5` - `phase(8): record starred action rail docs`
- `91d449d` - `phase(9): remove inline voice input button`
- `ffcb1b2` - `phase(9): record inline voice button removal docs`
- `44da4be` - `phase(10): add chat input quick model switch`
- `b2c6e9e` - `phase(10): record quick model switch docs`
- `f04c955` - `phase(10): finalize quick model switch docs`
- `fb23cc0` - `phase(11): fix quick model menu clipping`
- 待补充
- 待补充
- 待补充
- 待补充

## 例外记录

- 无

## 明日计划

- 继续观察星标分组与旧库迁移在真实用户环境中的回归情况。
