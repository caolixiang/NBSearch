# 今日目标

- 支持“对话 A 生成中时，用户可切换到对话 B 浏览/操作，再切回 A 继续看到实时进度”。
- 不中断后台已发起的流式请求，不丢失中间状态。

## 范围

- In:
  - 将单实例流式状态改为会话级状态管理。
  - 明确切换会话时的状态保留与并发规则。
  - 保持现有 repository + 本地 DB 持久化链路不变。
- Out:
  - 本阶段不引入 `react-query`。
  - 不做 e2e 测试体系建设。

## Phase 计划

### Phase 1 - 会话级流式状态改造方案确认

- 目标：
  - 完成可执行方案定义，覆盖状态模型、切换行为、并发约束、验收口径。
- 实施项：
  - 状态管理：引入会话级 Store（建议 `zustand`），按 `conversationId` 维护状态。
  - 会话状态字段：`isStreaming`、`assistantDelta`、`reasoningEvents`、`startedAt`、`abortController`、`lastError`。
  - 会话切换：仅变更 `activeConversationId`，不清理其他会话的流式状态。
  - 并发约束：同会话禁止并发两条流；不同会话允许并行流任务。
  - 数据链路：继续使用 repository + 本地 DB 持久化路径。
- 验收项：
  - A 会话生成中可切到 B，会话列表与消息可正常操作。
  - 切回 A 后继续看到 A 的实时流式进度。
  - A 完成后消息正确入库，刷新后内容一致。
  - B 不受 A 流状态污染（不出现串流、错位展示）。
- 状态：done

### Phase 2 - 会话切换不中断流式（第一批实现）

- 目标：
  - 先解除“生成中无法切换会话”的阻塞，并保证切回原会话时可继续看到实时流式内容。
- 实施项：
  - 将消息缓存改为按 `conversationId` 存储（`messagesByConversationId`）。
  - 将推理事件映射改为按 `conversationId` + `messageId/responseId` 双层存储。
  - 新增会话级流式状态映射 `streamingStateByConversationId`，流式渲染按当前会话读取，避免跨会话串流显示。
  - 切换会话时不再重置流式状态；若目标会话正在流式，保留本地流式视图，不强制 reload 覆盖。
  - 会话删除时清理对应会话缓存映射（消息、推理、错误）。
- 验收项：
  - 流式进行中可切到其他会话并浏览历史消息。
  - 切回原会话可继续看到正在流式输出的内容。
  - 历史会话与当前流式会话不再出现 UI 串流展示。
- 状态：done

### Phase 3 - 会话状态迁移至 Zustand

- 目标：
  - 将 `chat-shell` 中的会话级状态迁移到 `zustand`，降低组件复杂度并统一状态写入口。
- 实施项：
  - 新增 `chat-conversation-store.ts`，承载会话级核心状态与写操作（active/draft/messages/streaming/reasoning/error）。
  - `chat-shell` 切换为使用 store 状态与 action，不再直接持有上述会话级 `useState`。
  - 新增 `chat-conversation-store.test.ts` 覆盖 store 关键读写行为。
- 验收项：
  - 会话切换与并行流式行为与 Phase 2 保持一致。
  - `typecheck/build` 通过，新增 store 单测通过。
- 状态：done

### Phase 4 - Markdown 与底部滚动抖动修复

- 目标：
  - 修复年份区间 `2017-2021` 被错误拆成 markdown 列表的问题。
  - 修复到底部时输入框反复收起/弹出的乒乓抖动问题。
- 实施项：
  - 从本地 SQLite 读取 `嵇鹏飞：抖音电商转型核心操盘手` 原始消息复现渲染链路。
  - 移除 `normalizeAssistantMarkdown` 中“标题内遇 `-` 强制换行”的误伤规则，保留年份区间原样。
  - 新增 markdown 单测覆盖：跨行年份区间、标题年份区间不被拆分。
  - 调整消息区滚动收起策略：引入“底部锁定区 + 进出阈值滞回”，在底部附近保持输入框稳定，离开底部后恢复原有收起交互。
  - 保持消息滚动容器 `overscroll-y-none`，并保留较小的底部 spacer 上限。
- 验收项：
  - `字节跳动内部晋升路径（2017-2021）` 不再渲染为黑点列表。
  - 到达底部后不再出现输入框高频乒乓弹跳，特别是图片会话（如 `画一只老母鸡`）。
  - `bun test`、`bun run typecheck`、`bun run build` 通过。
- 状态：done

### Phase 5 - Markdown 标题层级增强

- 目标：
  - 让 `#` `##` `###` `####` 与正文形成清晰字号层级，提升长文浏览可读性。
- 实施项：
  - 调整 `MarkdownBody` 中 `h1~h4` 的渲染样式，按 `3xl/2xl/xl/lg` 分级并补齐 `h4` 映射。
  - 保持正文段落样式不变，避免破坏现有排版密度与图片混排逻辑。
- 验收项：
  - 标题层级在视觉上明显区分，且与正文字号有清晰差异。
  - `bun run typecheck` 与 markdown 相关单测通过。
- 状态：done

### Phase 6 - 输入框收起交互方向修正

- 目标：
  - 保留“上滑收起输入框”的交互，同时确保“下滑不收起”以减少阅读干扰。
- 实施项：
  - 调整 `handleMessagesScroll` 判定：仅当用户上滑时触发收起与回弹计时。
  - 非可滚动场景、底部锁定区、下滑场景统一保持输入框展开。
  - 保持底部锁定区与滞回阈值策略，避免到底部时抖动回归。
- 验收项：
  - 向下滑动时输入框不收起。
  - 向上滑动时输入框可收起并按既有时序回弹。
  - `bun run typecheck` 与 `bun test` 通过。
- 状态：done

### Phase 7 - Markdown 图片点击预览

- 目标：
  - 支持点击 markdown 图片进入原图预览，并在点击图片外区域后自动关闭。
- 实施项：
  - 在 `MarkdownImage` 增加预览弹层状态，点击图片时打开弹层并拦截默认链接跳转。
  - 使用现有 `Dialog` 组件渲染大图预览，点击遮罩关闭（保留 Esc 关闭）。
  - 保持图片加载失败、重试与下载逻辑不变，仅增强查看交互。
- 验收项：
  - 点击图片可打开原图预览。
  - 点击弹层外区域可自动关闭预览。
  - `bun run typecheck` 与 `bun test` 通过。
- 状态：done

### Phase 8 - 图片预览居中修正

- 目标：
  - 修复图片预览弹层中图片偏移，确保在视口内居中展示。
- 实施项：
  - 调整 `DialogContent` 预览容器布局为全屏 `flex` 居中，并覆盖默认 `top/left/translate` 定位样式。
  - 预览图增加 `mx-auto/my-auto` 与视口自适应最大尺寸，确保居中与完整显示。
- 验收项：
  - 点击图片后预览图视觉居中。
  - 点击遮罩关闭行为保持可用。
  - `bun run typecheck` 与 markdown 单测通过。
- 状态：done

### Phase 9 - 预览中心对齐聊天区与宽度上限

- 目标：
  - 让图片预览中心点对齐“聊天主区域”而不是整窗中心，并限制超大图宽度。
- 实施项：
  - 为聊天主区域添加 `data-chat-main-pane` 标识，供预览层定位使用。
  - 预览层按聊天主区域 `left/width` 计算居中容器，保持遮罩全屏但内容在聊天区中心。
  - 增加预览图 `max-width`（上限 960px 且不超过聊天区约 82% 宽度）并收紧 `max-height`，防止预览覆盖过大。
- 验收项：
  - 预览图在聊天区中心显示，不再整体偏左。
  - 超大原图不会无限放大，宽度受上限约束。
  - `bun run typecheck` 与 `bun test` 通过。
- 状态：done

### Phase 10 - 流事件元信息规范化（seq/ts/conversation）

- 目标：
  - 为前端消费事件补齐稳定元信息，支持后续心跳、超时与重放语义。
- 实施项：
  - 为 `ChatStreamEvent` 增加统一 `meta` 结构：`seq`、`ts`、`conversationId`、`responseId`（可选）。
  - `started/delta/reasoning/completed/failed` 全部通过统一发射器生成，保证 `seq` 单调递增。
  - 同步更新事件消费侧类型与处理逻辑，确保兼容既有渲染链路。
- 验收项：
  - 流式全链路事件都带 `meta`，且 `seq` 在单次请求内连续递增。
  - 不影响现有消息落库与 UI 渲染。
- 状态：done

### Phase 11 - 文本与图片事件解耦渲染

- 目标：
  - 避免图片事件高频更新阻塞正文 token 流式输出，提升“文字先行”体验。
- 实施项：
  - 在 `chat-shell` 将 `card_attachment` 更新改为节流合并（批量 flush），`delta` 仍保持实时更新。
  - 结束态（`completed/failed`）前强制 flush 缓冲，避免丢失最后一批图片卡片。
  - 保持推理状态与计时逻辑不回退。
- 验收项：
  - 带图长回答时文字流式更连续，不卡顿。
  - 最终图片与工具卡片仍完整显示。
- 状态：done

### Phase 12 - 仅展示最终图片（过滤中间态）

- 目标：
  - 进一步收紧图片抽取规则，确保中间进度图/审核图不进入最终渲染。
- 实施项：
  - 在 `cardAttachment` 解析路径增加与 `generatedImageUrls` 同级的过滤：`progress < 100`、`-part-*`、`moderated/r_rated` 直接丢弃。
  - 对 `response.output_item.added` 的生成图片卡片默认跳过，仅保留完成态来源。
  - 增补对应单元测试覆盖“added 中间态”和“cardAttachment 中 progress/moderated”场景。
- 验收项：
  - 生成图片会话不再出现中间态重复图。
  - 审核拒绝图不渲染，仅显示审核提示文案。
- 状态：done

### Phase 13 - 心跳/超时失败/基础续流语义

- 目标：
  - 提升长流稳定性与可观测性，补齐心跳与超时错误语义。
- 实施项：
  - 新增 `heartbeat` 事件（携带 `meta`），按固定间隔从流读取侧发出。
  - 为 `reader.read()` 增加空闲超时保护，超时时发出明确失败码（如 `stream_idle_timeout`）。
  - 在会话侧记录最近心跳时间，用于后续可视化与续流扩展（本阶段不改上游协议）。
- 验收项：
  - 网络卡住时不再无限等待，可收到明确失败事件。
  - 长流过程中可观测到心跳事件。
- 状态：done

### Phase 14 - 断线重连与基础续流恢复（meta/responseId）

- 目标：
  - 在流式空窗期触发超时时，进行一次无感重连，减少“刚开始就失败”的体验断点。
  - 请求链路显式传递 `previous_response_id`，增强续流上下文一致性。
- 实施项：
  - `streamTurn` 新增“空窗期超时一次重试”策略：仅在尚未收到可解析流事件时生效，避免中途重复内容。
  - 重试期间保留同一请求上下文，不重复入库用户消息。
  - 请求体新增 `previous_response_id`（来自 `anchors.lastResponseId`），并在重连时沿用。
  - 增补单测覆盖：首轮超时后重连成功、请求体包含 `previous_response_id`。
- 验收项：
  - 空窗期网络抖动时，可自动重连继续流式。
  - 非空窗期超时仍返回明确失败码，不进入重复输出风险路径。
- 状态：done

### Phase 15 - 连接健康状态可视化

- 目标：
  - 在流式期间提供连接健康提示，降低“界面卡住”误判。
- 实施项：
  - 基于 `lastHeartbeatAt` 计算连接状态（稳定/波动/等待数据）。
  - 在聊天页头部展示轻量状态标识，与错误提示并存。
  - 对 `stream_idle_timeout` 提示文案做用户可读化（避免暴露内部错误串）。
- 验收项：
  - 生成过程中可实时看到连接状态变化。
  - 超时失败时错误提示清晰可读。
- 状态：done

### Phase 16 - 新对话欢迎牛图标放大

- 目标：
  - 放大新对话空状态中心牛图标，提升视觉识别度。
- 实施项：
  - 调整 `WelcomeScreen` 中牛图标尺寸（仅影响新对话空状态，不改侧边栏与其他图标）。
- 验收项：
  - 新对话空状态中牛图标明显变大，标题文案与布局保持居中不变。
- 状态：done

### Phase 17 - 重新生成分支与数据库裁剪

- 目标：
  - 支持在历史 assistant 消息上触发“重新生成”，并将该节点之后的旧分支消息从数据库裁剪。
- 实施项：
  - 扩展 repository 增加“按消息截断后续记录”能力（memory/sqlite 一致）。
  - `chat-shell` 新增按目标 assistant 触发分支重生：本地与数据库先裁剪，再发起流式请求。
  - `streamTurn` 增加 regenerate 模式（`x_grok.regenerate + target_response_id`），不追加新的用户消息。
- 验收项：
  - 在第 N 条 assistant 触发重新生成后，第 N+1 及后续轮次从本地与数据库被移除。
  - 重新生成请求正确携带 `x_grok.target_response_id`，并能产出新 assistant 响应。
- 状态：done

### Phase 18 - 回复末尾重新生成按钮

- 目标：
  - 在每条系统回复末尾提供“重新生成”操作入口，位置对齐你给的 UI 参考。
- 实施项：
  - `ChatMessage` 为 assistant 消息新增底部按钮（刷新图标），点击触发 regenerate。
  - 流式进行中禁用按钮，避免同会话并发冲突。
- 验收项：
  - 每条 assistant 回复下方出现重新生成按钮。
  - 点击可进入分支重生流程，且不会打断既有滚动与流式体验。
- 状态：done

### Phase 19 - 重新生成按钮对齐官方样式

- 目标：
  - 将系统回复末尾的“重新生成”按钮样式对齐官方交互：官方刷新 SVG + 悬停文案气泡。
- 实施项：
  - 将按钮图标替换为官方 path 的 SVG，不再使用通用 icon 库图标。
  - 按钮尺寸、圆角与 hover/focus 反馈对齐官方动作按钮风格。
  - 增加悬停/聚焦提示文案“重新生成”，默认隐藏，交互触发后显示。
- 验收项：
  - 系统回复末尾显示官方风格刷新按钮。
  - 鼠标悬停或键盘聚焦按钮时，出现“重新生成”提示文案。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 20 - 系统回复导出 PDF 按钮与保存

- 目标：
  - 在系统回复末尾新增“导出 PDF”按钮（位于重新生成旁），并支持用户选择保存路径。
- 实施项：
  - 在 assistant 动作区新增 PDF 图标按钮，交互风格与重新生成按钮保持一致。
  - 生成导出 PDF 内容并弹出系统保存对话框，文件名格式：`title-N.pdf`（N 为当前对话轮次）。
  - 复用现有下载类反馈体验，至少覆盖导出中/已导出/失败状态。
- 验收项：
  - 系统回复末尾展示导出 PDF 按钮。
  - 点击后可弹出保存路径选择，并成功写入 PDF 文件。
  - 文件默认名符合 `title-N.pdf`。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 21 - 导出 PDF 支持样式与图片

- 目标：
  - 导出 PDF 时保留消息已渲染样式与图片，不再是纯文本版。
- 实施项：
  - 导出逻辑升级为 DOM 捕获分页：对当前回复渲染节点截图并分页写入 PDF。
  - 新增 `html2canvas` 依赖用于 DOM 渲染；保留文本导出链路作为兜底。
  - 导出时等待消息内图片加载完成，提升图片入 PDF 成功率。
- 验收项：
  - 导出文件可看到 Markdown 样式与图片。
  - DOM 捕获失败时仍可回退到文本导出，不阻塞功能。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 22 - 修复 PDF 导出仍回退纯文本

- 目标：
  - 修复“导出结果仍是纯文本、样式/图片未进入 PDF”的问题。
- 实施项：
  - 导出 DOM 捕获链路升级为双引擎：`html-to-image` 优先，失败回退 `html2canvas(foreignObjectRendering)`。
  - 文本兜底前先做 `normalizeAssistantMarkdown` 清洗，避免 `<grok:render>` 原样进入 PDF。
  - 新增 `html-to-image` 依赖，提升对当前样式体系（含复杂 CSS）的兼容性。
- 验收项：
  - 导出优先走 DOM 渲染路径，样式/图片可见。
  - 即便回退文本导出，内容也不再包含内部渲染标签噪声。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 23 - 修复 PDF 导出空白页

- 目标：
  - 修复导出文件出现整页空白的问题。
- 实施项：
  - 为 DOM 捕获结果增加“空白画布检测”（低色差判定），空白结果不再直接进入 PDF。
  - 新增第三捕获回退：`html2canvas(foreignObjectRendering=false)`。
  - 捕获时注入导出色彩变量，降低主题变量缺失导致的全白风险。
- 验收项：
  - 导出链路不再产出空白 PDF。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 24 - 增强空白页检测阈值

- 目标：
  - 解决“灰白噪点画布误判为有效页”导致仍导出空白的问题。
- 实施项：
  - 将空白检测从 RGB 微差改为亮度对比度（luma range）检测。
  - 低亮度对比画布强制视为空白，触发后续回退链路。
- 验收项：
  - 灰白噪点页不再被判定为有效导出内容。
  - `bun run typecheck` 与相关单测通过。
- 状态：done

### Phase 25 - 以暗像素占比阻断伪有效白页

- 目标：
  - 解决“非纯色白页（压缩噪点）仍被误判为有效页”的残留问题。
- 实施项：
  - 增加暗像素占比检测（dark pixel ratio），要求捕获画布存在可见墨迹后才视为有效。
  - 统一导出有效性判断为：非均匀 + 有可见墨迹。
- 验收项：
  - 白页/灰噪点页不会进入 PDF 页面输出。
  - `bun run typecheck` 与 `bun test src/features/chat/export-message-pdf.test.ts` 通过。
- 状态：done

### Phase 26 - 导出捕获强制浅色主题变量

- 目标：
  - 修复深色主题变量导致“白字白底近似空白”的导出问题。
- 实施项：
  - 导出捕获统一注入浅色主题变量（黑字白底）。
  - 在 html2canvas `onclone` 阶段移除 `dark` 影响并覆盖根变量。
- 验收项：
  - 导出页文本在白底上具备可见对比度，不再出现主题导致的近似空白。
  - `bun run typecheck` 与 `bun test src/features/chat/export-message-pdf.test.ts` 通过。
- 状态：done

### Phase 27 - 离屏导出容器隔离页面状态

- 目标：
  - 解决在当前页面节点直接捕获时受主题/布局/动画影响导致导出异常的问题。
- 实施项：
  - 构建离屏导出容器：克隆消息内容到固定白底黑字容器，再进行截图分页。
  - 捕获对象从原页面节点切换为离屏容器，避免实时 UI 状态干扰。
  - 保留两段 html2canvas 回退（foreignObject true/false）和有效页判定。
- 验收项：
  - 导出不再依赖当前页面可视状态，结果稳定可重复。
  - `bun run typecheck` 与 `bun test src/features/chat/export-message-pdf.test.ts` 通过。
- 状态：done

### Phase 28 - 修复 PDF 富样式导出误判回退文本

- 目标：
  - 修复导出链路将有效 DOM 截图误判为空白后回退文本的问题，优先输出富样式 PDF。
- 实施项：
  - 将“整图墨迹阈值”判定调整为“分页级内容判定”，降低长文/稀疏排版被误杀概率。
  - 恢复并前置 `html-to-image` 捕获路径，`html2canvas` 作为回退，统一复用离屏容器。
  - 导出有效页判定失败时继续尝试回退链路，只有全部失败才走文本兜底。
- 验收项：
  - 针对 `嵇鹏飞：抖音电商转型核心操盘手` 导出不再优先落到纯文本样式。
  - `bun run typecheck`、`bun test src/features/chat/export-message-pdf.test.ts` 通过。
- 状态：done

### Phase 29 - PDF 失败兜底改为渲染态文本提取

- 目标：
  - 在 DOM 捕获仍失败的场景下，避免导出结果出现原始 markdown 语法噪声（`###`、`**` 等）。
- 实施项：
  - 文本兜底优先从已渲染消息节点提取 `innerText`，而非直接使用原始 markdown。
  - 兜底路径附带图片 URL 列表（若有），保证内容可追溯。
  - 保留 markdown 文本路径作为次级兜底，并增加 markdown 语法清洗。
- 验收项：
  - 同一消息导出即使走文本兜底，也不再出现明显 markdown 语法符号噪声。
  - `bun run typecheck`、`bun test src/features/chat/export-message-pdf.test.ts`、`bun test` 通过。
- 状态：done

### Phase 30 - 独立 Markdown 容器富样式导出兜底

- 目标：
  - 在“当前消息节点捕获失败”时仍尽量输出富样式 PDF，而不是直接降级到文本。
- 实施项：
  - 新增独立 Markdown 容器渲染路径：运行时挂载 `MarkdownBody` 到隔离容器，再走截图导出。
  - 该路径作为主捕获失败后的第二富样式兜底，最后才是文本导出。
  - 保留现有命名规则与保存路径交互不变。
- 验收项：
  - `bun run typecheck`、`bun test src/features/chat/export-message-pdf.test.ts`、`bun test` 通过。
  - 导出链路具备“主节点捕获 -> 独立容器捕获 -> 文本兜底”三级回退。
- 状态：done

### Phase 31 - 以 v11 为准精简 PDF 导出链路

- 目标：
  - 以 v11 结果为基线，移除历史 canvas 判定与文本兜底分支，只保留单一导出链路。
- 实施项：
  - `buildMessagePdfBytes` 改为仅走“独立 Markdown 容器渲染 + html-to-image 捕获 + PDF 分页写入”。
  - 删除历史空白检测、主节点/离屏节点多段捕获、文本渲染兜底逻辑。
  - 导出失败直接抛错，由现有按钮状态显示“导出失败”。
- 验收项：
  - 导出链路无兜底分支，代码显著收敛。
  - `bun run typecheck`、`bun test src/features/chat/export-message-pdf.test.ts`、`bun test` 通过。
- 状态：done

### Phase 32 - 对话动作栏末轮常显

- 目标：
  - 调整“重新生成/导出 PDF”动作栏显示规则：最后一轮 assistant 始终显示，历史轮次仅 hover/focus 显示。
- 实施项：
  - 在 `chat-shell` 计算当前可见消息中的最后一条非流式 assistant 消息 ID。
  - `ChatMessage` 新增 `actionsAlwaysVisible` 控制位；当命中最后一轮时动作栏常显。
  - 保持历史轮次现有 hover/focus 触发逻辑不变。
- 验收项：
  - 只有一轮 assistant 时动作栏始终显示。
  - 多轮 assistant 时仅最后一轮常显，前面轮次默认隐藏，移动到对应消息区域后显示。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 33 - 动作栏新增来源统计并跳转思考结果

- 目标：
  - 在“重新生成/导出 PDF”后追加来源数量标签，点击可跳转并打开对应消息的思考结果页。
- 实施项：
  - 在 `ChatMessage` 动作栏新增来源统计标签（文案格式 `xxx 个来源`），样式对齐官方胶囊按钮风格。
  - 基于 `reasoningEvents.tool_result` 聚合来源总数，优先使用 `webSearchResults` 长度并兼容 `webSearchResultsCount`。
  - 通过自定义事件将点击动作路由到对应 `StructuredReasoningPanel`，自动滚动到消息思考区并打开右侧“思考结果”抽屉。
- 验收项：
  - 有来源时显示 `xxx 个来源`，无来源时不显示。
  - 点击来源标签可定位并打开该条消息的思考结果页。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 34 - 非生图 Gallery 尺寸与布局对齐官方

- 目标：
  - 调整 markdown 非生图图片布局，解决“双图过高、三图方块化”导致的观感问题，贴近官方图库样式。
- 实施项：
  - 非生图多图（2/3/4+）统一改为 `grid + gap-2`，移除双图不等宽和三图 `aspect-square` 规则。
  - 非生图多图卡片统一为 `h-52 + object-cover + rounded-lg`，降低视觉高度。
  - 非生图单图改为 `w-full + object-contain + max-h-[448px] + rounded-lg`，不再使用原有最小宽度拉伸逻辑。
- 验收项：
  - 双图高度明显降低，且不再使用不等宽布局。
  - 三图不再强制方块，整体高度与官方截图接近。
  - 单图按官方风格走 contain 且有高度上限。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 35 - 非生图双图宽度收敛对齐官方

- 目标：
  - 修复 markdown 非生图双图占满整行的问题，使两图分组宽度收敛并居中展示，贴近官方 `w-fit + mx-auto` 视觉。
- 实施项：
  - 双图分组容器改为 `w-fit + mx-auto`，避免继承整行宽度。
  - 双图卡片增加 `max-h-56 + max-w-80` 约束，控制单卡视觉尺寸，避免横向无限拉伸。
  - 保持三图/四图与生图路径不变，仅影响“非生图双图”分支。
- 验收项：
  - 非生图双图不再占满消息可用宽度，整体居中且尺寸更接近官方。
  - 三图和单图现有效果不回退。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 36 - 图片悬停光标改为手型

- 目标：
  - 图片可点击预览时，悬停光标使用手型（pointer），不再显示放大镜样式。
- 实施项：
  - 将 markdown 图片组件类名中的 `cursor-zoom-in` 调整为 `cursor-pointer`。
  - 保持点击预览、遮罩关闭与图片加载逻辑不变。
- 验收项：
  - 鼠标移动到图片上显示手型光标。
  - 点击图片预览功能正常。
  - `bun run typecheck` 通过。
- 状态：done

### Phase 38 - 收紧最后一条消息到底部输入框间距

- 目标：
  - 将最后一条输出内容到底部输入框的视觉距离再收紧一点，减少大空白感。
- 实施项：
  - 在 `chat-shell` 下调 `messageBottomSpacerPx` 计算系数与上限（仅微调，不改滚动机制）。
  - 保持“内容可拖拽超过输入框区域”的既有能力。
  - 不改输入框收起/回弹逻辑，不改图片与 markdown 渲染逻辑。
- 验收项：
  - 最后一条消息到底部输入框距离较上一版更近。
  - 不出现末尾内容被输入框遮挡的回归。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 37 - 非生图双图容器再加宽一档

- 目标：
  - 在保持双图居中与不铺满整行的前提下，将双图容器宽度再放大一点，提升浏览观感。
- 实施项：
  - 将非生图双图卡片宽度上限从 `max-w-80` 调整为 `max-w-88`。
  - 保持双图 `max-h-56` 高度约束与 `w-fit + mx-auto` 居中规则不变。
  - 不改动单图、三图、四图与生图路径。
- 验收项：
  - 双图整体宽度较上一版略增，但仍不占满整行。
  - 三图/单图/生图表现不回退。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 39 - 输入框文字字号微调增大

- 目标：
  - 将对话输入框内文字和占位符字号稍微调大，提升可读性。
- 实施项：
  - 将 `ChatInput` 文本域字号从 `text-sm` 调整为 `text-base`。
  - 保持输入框布局、行高策略、按钮尺寸与交互逻辑不变。
- 验收项：
  - 输入框占位符与输入文本视觉上略大于原先。
  - 不影响输入框高度自适应和附件/语音按钮布局。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 40 - 移除侧边栏顶部品牌区与 Logo

- 目标：
  - 移除侧边栏顶部左侧品牌区（`NBSearch` 文案与 logo），简化头部视觉。
- 实施项：
  - 删除 `chat-sidebar` 头部中的 `GrokAvatar + NBSearch` 区块。
  - 顶部仅保留“新建对话”和“收起侧边栏”按钮。
  - 移除不再使用的 `GrokAvatar` 引入。
- 验收项：
  - 侧边栏顶部不再显示 logo 与品牌文案。
  - 头部操作按钮可正常使用。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 41 - 右上角新增“新建对话”按钮

- 目标：
  - 在“已有对话”场景下，于主聊天区右上角增加官方风格“新建对话”按钮。
- 实施项：
  - 在 `chat-shell` header 右侧新增圆形按钮，使用你提供的官方 SVG 图标样式。
  - 按钮显示条件为 `conversations.length > 0`，仅在已有会话时展示。
  - 点击按钮复用现有 `handleNewConversation` 流程，不新增状态分支。
- 验收项：
  - 已有对话时，右上角显示新建按钮；无对话时不显示。
  - 点击按钮可进入“新对话”草稿状态。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 42 - 右上角新建按钮悬浮提示文案

- 目标：
  - 为右上角“新建对话”按钮补充悬浮提示文案，内容为“新建聊天”。
- 实施项：
  - 在 header 新建按钮外层增加 `group` 包裹与提示气泡元素。
  - 默认隐藏提示，hover/focus 时显示，位置调整为按钮下方避免顶部裁切。
  - 不改按钮点击逻辑与显示条件。
- 验收项：
  - 鼠标悬浮或键盘聚焦按钮时显示“新建聊天”。
  - 离开按钮后提示自动隐藏。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 43 - DeepSearch 联动 expert 模型与请求参数

- 目标：
  - 点击 DeepSearch 后自动切换到 expert 模型；用户手动切换模型时取消 DeepSearch 选中。
  - 发送请求时按网关文档传递 `x_grok.deep_search` 参数。
- 实施项：
  - 将 `ChatInput` DeepSearch 改为受控状态（由 `chat-shell` 管理）。
  - 新增 expert 模型解析逻辑：优先 `expert` 关键字模型，其次 `reasoning` 模型。
  - DeepSearch 开启时自动切 expert；`ModelSelector` 手动切模型时强制取消 DeepSearch。
  - 扩展 `SendChatTurnInput` 与 `streamTurn` 请求体，非重生成场景写入 `x_grok: { deep_search: true }`。
  - 新增服务层单测，断言 deep search 请求 payload。
- 验收项：
  - 点击 DeepSearch 后模型自动切到 expert 类模型。
  - 手动切换模型后 DeepSearch 自动取消选中。
  - `/v1/responses` 请求体正确包含 `x_grok.deep_search`。
  - `bun run typecheck`、`bun test` 通过。
- 状态：done

### Phase 44 - 思考结果展示工具链明细

- 目标：
  - 在“思考结果”中直观展示本轮实际命中的工具链，便于确认是否触发 DeepSearch 工具调用。
- 实施项：
  - `buildStructuredReasoningSummary` 增加 `toolChain` 聚合：按 `toolName` 统计 `usage/completed/running`。
  - 排除 `chatroom_send` 这类 agent 内部通信工具，避免污染用户可见工具链。
  - 在思考抽屉顶部新增“工具链”区块，展示工具名与执行状态（如 `进行中 1/2`、`已完成 2`）。
  - 在“思考了 Ns”文案后追加 `· N个工具` 提示，提高发现性。
  - 新增单测 `structured-reasoning.test.ts` 覆盖工具链聚合与过滤逻辑。
- 验收项：
  - 有 `tool_usage/tool_result` 时，思考抽屉可看到工具链统计。
  - `chatroom_send` 不进入工具链计数。
  - `bun run typecheck`、`bun test`、`bun run build` 通过。
- 状态：done

## Phase 验收记录

### Phase 1

- 验收结果：
  - 方案层验收通过，技术边界和验收标准已明确，可进入实现阶段。
- 备注：
  - 当前聊天页历史问题：单实例流式状态（`isStreaming`、`abortRef`、`streamingAssistantText`）导致“生成中不可切会话”。

### Phase 2

- 验收结果：
  - 已完成会话级流式状态改造，核心链路可用（切换不中断、切回可见流式、会话级缓存生效）。
  - 已放开并发多会话发送（A 流式时可切到 B 发起新流式；输入框 loading 仅绑定当前会话）。
- 备注：
  - 侧边栏操作不再被全局流式状态统一禁用；删除正在流式的会话仍被保护性拦截。

### Phase 3

- 验收结果：
  - 已完成会话级状态向 `zustand` 迁移，`chat-shell` 中对应状态改由 store 提供。
  - 新增 store 单元测试并通过，迁移后行为保持一致。
- 备注：
  - 当前仍有 `chat-shell` 内部 UI 状态（如 sidebar、input 动画）使用局部 `useState`，符合“会话级进 store、局部 UI 保持本地”的划分。

### Phase 4

- 验收结果：
  - 已在数据库原始记录上定位并复现根因，修复后归一化结果不再出现 `\n- 2021`。
  - 已调整滚动收起策略，底部区域不再触发输入框往返收起/展开。
- 备注：
  - 该问题根因是旧规则 `/(^|\n)(#{1,6}\s[^\n-]+)-\s?/` 误把标题中的年份区间 `-` 当作列表起始符拆行。

### Phase 5

- 验收结果：
  - 已完成 `h1~h4` 层级样式增强，标题与正文字号区分明显。
- 备注：
  - 仅调整标题组件样式，不改动正文与图片段落逻辑。

### Phase 6

- 验收结果：
  - 已完成“仅上滑收起”交互修正，下滑与底部锁定区保持展开。
- 备注：
  - 在不移除底部几何锁定策略的前提下，避免了下滑误收起导致的阅读干扰。

### Phase 7

- 验收结果：
  - 图片点击预览已接入，遮罩点击关闭行为正常。
- 备注：
  - 预览不改动原有图片抓取/失败兜底链路。

### Phase 8

- 验收结果：
  - 已修复预览弹层图片未居中问题，当前为全屏居中展示。
- 备注：
  - 仅调整预览弹层布局，不改动图片加载与交互关闭逻辑。

### Phase 9

- 验收结果：
  - 已实现预览内容按聊天区居中，宽图有最大宽度限制。
- 备注：
  - 继续保持“点击遮罩关闭”和 Esc 关闭；在用户反馈后进一步收窄预览上限，减少覆盖操作区域。

### Phase 10

- 验收结果：
  - 已为 `started/delta/reasoning/completed/failed` 全部补齐统一 `meta`（`seq/ts/conversationId/responseId?`）。
  - 事件序号在单次流式过程中单调递增，渲染与落库链路保持兼容。
- 备注：
  - `responseId` 在流中后段自动带入，早期事件允许为空。

### Phase 11

- 验收结果：
  - 已将 `card_attachment` 事件改为节流批量 flush，`delta` 维持实时更新。
  - `completed/failed` 前会清空并处理缓冲卡片，避免丢失最后一批图片。
- 备注：
  - 解耦在消费层完成，不改变上游协议字段。

### Phase 12

- 验收结果：
  - `cardAttachment` 解析链路已补齐中间态过滤：`progress < 100`、`-part-*`、`moderated/r_rated` 直接丢弃。
  - 对 `response.output_item.added` 的生成图已默认跳过，仅保留完成态来源。
  - 新增对应单测覆盖并通过。
- 备注：
  - 非生成图卡片（如普通检索图片卡）不受影响。

### Phase 13

- 验收结果：
  - 已增加 `heartbeat` 事件并纳入流事件 `meta` 序列，正常流式可观测到心跳。
  - 已为流读取加入空闲超时（20s），超时返回 `failed(stream_idle_timeout)`。
  - 已补充 `grok-chat-service` 单测覆盖正常流心跳与超时失败路径。
- 备注：
  - 本阶段先完成“基础续流语义”的心跳与失败可观测，不改变上游协议与重连策略。

### Phase 14

- 验收结果：
  - 已实现“空窗期超时一次重连”，仅在无可见输出时触发，避免重复文本风险。
  - 请求体已补充 `previous_response_id`，并在重连请求中保持一致。
  - 新增 `grok-chat-service` 单测覆盖“首轮超时后二次成功”与 `previous_response_id` 传递。
- 备注：
  - 中途已有输出时仍直接失败，不做重放拼接，避免双写/重复输出。

### Phase 15

- 验收结果：
  - 已基于 `lastHeartbeatAt` 在聊天头部展示连接状态（稳定/波动/等待数据）。
  - 已将 `stream_idle_timeout_*` 错误归一为用户可读文案，避免暴露内部错误串。
  - 连接状态可与模型错误/会话错误并存展示，不影响现有流式渲染链路。
- 备注：
  - 状态刷新依赖现有 1s 计时更新，不新增额外轮询。

### Phase 16

- 验收结果：
  - 已将新对话欢迎页牛图标尺寸由默认 `lg(size-14)` 放大到 `size-20`。
  - 仅影响 Welcome 空状态，侧边栏/其他图标尺寸不受影响。
- 备注：
  - 通过给 `GrokAvatar` 增加可选 `className` 覆盖实现，保持原有 `size` 枚举兼容。

### Phase 17

- 验收结果：
  - 已新增 repository 截断能力，支持按目标消息删除其后续记录（memory/sqlite 均支持）。
  - 已接入 `x_grok.regenerate + target_response_id` 调用路径，regenerate 请求不再附带新的用户输入块。
  - 触发重生时会先裁剪本地与数据库消息，再进入新的流式响应链路。
- 备注：
  - 当前策略为“替换目标 assistant 并裁剪其后续分支”，保持单分支会话历史。

### Phase 18

- 验收结果：
  - 每条 assistant 消息底部已新增重新生成按钮（流式中禁用）。
  - 点击按钮可从对应节点触发重生，不影响既有思考流与图片流式事件处理。
- 备注：
  - 位置与交互参考系统回复末尾动作区，使用轻量图标按钮呈现。

### Phase 19

- 验收结果：
  - 已替换为官方刷新 SVG 路径，按钮交互样式更新为圆形 ghost 风格。
  - 已新增“重新生成”悬停提示文案，默认隐藏，hover/focus 可见。
- 备注：
  - 文案提示采用消息内浮层样式，不引入额外 tooltip 依赖。

### Phase 20

- 验收结果：
  - 已在系统回复末尾新增导出 PDF 按钮，并与重新生成按钮并排展示。
  - 点击导出后会弹出系统保存路径选择，默认文件名为 `title-N.pdf`（N 为系统回复轮次）。
  - 导出状态支持“导出中/已导出/导出失败”反馈。
- 备注：
  - PDF 由前端按页面栅格生成，内容按当前回复文本导出；Tauri 端新增 `save_pdf_document` 负责二进制落盘。

### Phase 21

- 验收结果：
  - 导出链路已切换为“优先抓取当前回复 DOM 渲染并分页写入 PDF”，样式与图片会进入导出内容。
  - 已在导出前等待消息内图片加载；导出失败时自动回退文本导出兜底。
- 备注：
  - 当前使用 `html2canvas + pdf-lib` 组合，避免 `@react-pdf/renderer` 额外重建 Markdown 渲染树。

### Phase 22

- 验收结果：
  - 已确认旧导出文件命中纯文本兜底路径，现已补齐 `html-to-image` 优先捕获与二级回退策略。
  - 文本兜底输出已增加 Markdown 归一化清洗，不再出现内部 `grok:render` 标签原文。
- 备注：
  - 本次不改按钮交互，仅修复导出产物一致性。

### Phase 23

- 验收结果：
  - 已增加空白画布检测并串联三段捕获回退，避免“捕获成功但内容全白”进入 PDF。
  - 主题变量在导出捕获时显式注入，降低颜色解析异常导致空白的概率。
- 备注：
  - 若三段捕获均判定无效，仍回退文本导出，保证导出可用性。

### Phase 24

- 验收结果：
  - 已将空白检测更新为亮度范围判定（容忍压缩噪点），可识别“看似非纯色但视觉空白”的页面。
- 备注：
  - 该修复仅调整检测阈值，不改变导出按钮与命名规则。

### Phase 25

- 验收结果：
  - 已新增暗像素占比判定，进一步过滤“有噪点但无内容”的伪有效画布。
  - 导出页有效性判断已统一收敛，减少空白页漏检。
- 备注：
  - 该阶段不调整导出交互，仅加强内容有效性校验。

### Phase 26

- 验收结果：
  - 导出捕获已强制浅色变量并移除 clone 中的深色主题影响，确保文本对比度可见。
- 备注：
  - 此阶段主要修复主题变量引起的可视化问题，不改导出链路结构。

### Phase 27

- 验收结果：
  - 已改为离屏导出容器捕获，导出过程与当前页面状态隔离。
  - 继续保留捕获回退与内容有效性检测，避免空白页回归。
- 备注：
  - 该阶段核心是“捕获对象切换”，不是阈值调整。

### Phase 28

- 验收结果：
  - 已将导出有效性判定由“整图阈值”调整为“分页级内容判定”，避免长文截图被误判空白。
  - 导出链路恢复 `html-to-image` 先尝试，失败后回退 `html2canvas`（含 `foreignObject` 两种模式）。
  - 捕获顺序调整为“原始消息节点优先，离屏克隆节点兜底”，规避离屏节点在部分场景下导致空白捕获。
  - 仅在三段 DOM 捕获均无有效页时才回退文本兜底。
- 备注：
  - 本阶段重点为“减少误判兜底”，未改动按钮交互与命名规则。

### Phase 29

- 验收结果：
  - 兜底文本来源已切换为渲染态 DOM 文本提取，避免原始 markdown 语法直接出现在 PDF 中。
  - 已在兜底内容中附带去重图片 URL 列表（若存在）。
- 备注：
  - 该阶段不改变主捕获链路，仅增强“主链路失败后的可用性与可读性”。

### Phase 30

- 验收结果：
  - 已新增“独立 Markdown 容器渲染并捕获”路径，作为主节点捕获失败后的富样式兜底。
  - 导出链路顺序已扩展为：主节点捕获 -> 独立容器捕获 -> 文本兜底。
- 备注：
  - 该阶段保持按钮交互、文件命名和存储流程不变，仅增强导出链路鲁棒性。

### Phase 31

- 验收结果：
  - 已按 v11 基线精简为单链路导出：独立 Markdown 容器渲染并捕获后直接写入 PDF。
  - 已移除多段捕获、空白判定和文本兜底，失败直接返回错误状态。
- 备注：
  - 该阶段目标是“简化与可维护性”，不再保留历史兼容路径。

### Phase 32

- 验收结果：
  - 动作栏已支持末轮常显：最新 assistant 消息固定显示“重新生成/导出 PDF”，历史消息保持 hover/focus 显示。
  - 仅调整显示策略，不影响按钮行为、导出逻辑与重新生成逻辑。
- 备注：
  - 对流式中的 `streaming_assistant` 不启用常显逻辑，保持当前禁用行为。

### Phase 33

- 验收结果：
  - 动作栏已新增 `xxx 个来源` 来源统计标签，来源数来自 `reasoningEvents` 聚合结果。
  - 点击来源标签会平滑定位到对应消息的思考区域，并自动打开该消息的“思考结果”抽屉。
- 备注：
  - 本阶段按要求未接入站点 favicon 堆叠，仅保留数量标签与跳转交互。

### Phase 34

- 验收结果：
  - 非生图多图已统一为官方风格网格（`gap-2`），双图不再超高，三图不再方块化。
  - 非生图单图已切换为 `object-contain + max-h-[448px]` 方案，布局与官方单图样式一致。
- 备注：
  - 本阶段仅调整 markdown 图片样式映射，不改生图路径与图片加载逻辑。

### Phase 35

- 验收结果：
  - 非生图双图已改为内容宽度收敛并居中（`w-fit + mx-auto`），不再横向铺满整行。
  - 双图卡片已补充 `max-h-56` 和 `max-w-80`，整体视觉尺寸收敛。
- 备注：
  - 本阶段仅调整双图样式分支，未改动三图、四图和生图样式逻辑。

### Phase 36

- 验收结果：
  - markdown 图片悬停光标已从 `zoom-in` 改为 `pointer`，与点击行为一致。
- 备注：
  - 本阶段仅调整鼠标样式，不影响预览与加载逻辑。

### Phase 38

- 验收结果：
  - 底部 spacer 已从 `0.72 * inputHeight + 20` 下调为 `0.62 * inputHeight + 14`，上限从 `180` 下调至 `164`。
  - 最后一条消息到底部输入框的视觉间距已收紧，且滚动链路保持不变。
- 备注：
  - 本阶段仅改消息区底部留白公式，不涉及输入框动画与列表布局结构变更。

### Phase 37

- 验收结果：
  - 非生图双图卡片宽度上限已由 `max-w-80` 提升至 `max-w-88`，容器视觉更宽但仍保持居中收敛。
- 备注：
  - 本阶段仅做双图宽度微调，不改动其他图片分支。

### Phase 39

- 验收结果：
  - 输入框文本与占位符字号已从 `text-sm` 提升为 `text-base`，视觉更清晰。
- 备注：
  - 本阶段仅调整字号，不改输入框布局和交互逻辑。

### Phase 40

- 验收结果：
  - 侧边栏顶部品牌区已移除，当前仅保留新建与收起按钮。
- 备注：
  - 本阶段仅调整侧边栏顶部视觉结构，不改会话列表与设置区逻辑。

### Phase 41

- 验收结果：
  - 主聊天区 header 右上角已新增新建对话按钮（官方风格圆形描边 + 铅笔图标）。
  - 仅在已有会话时显示，点击可触发现有新建对话流程。
- 备注：
  - 本阶段复用既有 `handleNewConversation`，不改动会话持久化与流式状态逻辑。

### Phase 42

- 验收结果：
  - 右上角新建按钮已增加悬浮提示，文案为“新建聊天”。
  - 提示支持 hover 与 focus-within 显示，且位于按钮下方避免被窗口顶部遮挡。
- 备注：
  - 本阶段仅补充提示层样式与交互，不改按钮业务行为。

### Phase 43

- 验收结果：
  - DeepSearch 已切换为受控状态，开启后自动切 expert，手动改模型会取消选中。
  - 请求体在非重生成场景已补齐 `x_grok.deep_search=true`，并通过单测覆盖。
- 备注：
  - 重生成路径保持 `x_grok.regenerate` 原逻辑，不与 deep search 混用。

### Phase 44

- 验收结果：
  - 思考抽屉已新增“工具链”区块，可直接查看 `webSearch/xSearch/browsePage/imageSearch` 等工具命中情况与完成度。
  - “思考了 Ns”后追加 `· N个工具` 提示，用户无需进入抽屉即可感知是否发生工具调用。
  - 新增 `structured-reasoning` 单测覆盖 toolChain 聚合与 `chatroom_send` 过滤。
- 备注：
  - 本阶段仅增强展示层与聚合逻辑，不改流式协议与数据库 schema。

## 测试记录

- `bun test src/components/markdown-content.test.ts`：已执行，通过（38 pass / 0 fail）。
- `bun test`：已执行，通过（110 pass / 0 fail）。
- `bun run typecheck`：已执行，通过。
- `bun run build`（按需）：已执行，通过（含 Vite 体积与动态导入 warning，非阻断）。
- `bun test src/components/markdown-content.test.ts`：已执行，通过（38 pass / 0 fail，Phase 5 回归）。
- `bun run typecheck`：已执行，通过（Phase 5 回归）。
- `bun test`：已执行，通过（110 pass / 0 fail，Phase 6 回归）。
- `bun run typecheck`：已执行，通过（Phase 6 回归）。
- `bun test`：已执行，通过（110 pass / 0 fail，Phase 7 回归）。
- `bun run typecheck`：已执行，通过（Phase 7 回归）。
- `bun test src/components/markdown-content.test.ts`：已执行，通过（38 pass / 0 fail，Phase 8 回归）。
- `bun run typecheck`：已执行，通过（Phase 8 回归）。
- `bun test`：已执行，通过（110 pass / 0 fail，Phase 9 回归）。
- `bun run typecheck`：已执行，通过（Phase 9 回归）。
- `bun run typecheck`：已执行，通过（Phase 10 事件元信息改造）。
- `bun test`：已执行，通过（110 pass / 0 fail，Phase 10 回归）。
- `bun run typecheck`：已执行，通过（Phase 11 图片事件节流）。
- `bun test`：已执行，通过（110 pass / 0 fail，Phase 11 回归）。
- `bun run typecheck`：已执行，通过（Phase 12 最终图过滤）。
- `bun test src/infrastructure/chat/chunk-parsers.test.ts`：已执行，通过（45 pass / 0 fail）。
- `bun test`：已执行，通过（113 pass / 0 fail，Phase 12 回归）。
- `bun test src/infrastructure/chat/grok-chat-service.test.ts`：已执行，通过（19 pass / 0 fail，Phase 13 新增心跳/超时单测）。
- `bun run typecheck`：已执行，通过（Phase 13 心跳与超时语义）。
- `bun test`：已执行，通过（115 pass / 0 fail，Phase 13 回归）。
- `bun run typecheck`：已执行，通过（Phase 14 断线重连与 previous_response_id）。
- `bun test`：已执行，通过（116 pass / 0 fail，Phase 14 回归）。
- `bun run typecheck`：已执行，通过（Phase 15 连接健康状态提示）。
- `bun test`：已执行，通过（116 pass / 0 fail，Phase 15 回归）。
- `bun run typecheck`：已执行，通过（Phase 16 Welcome 图标放大）。
- `bun test`：已执行，通过（116 pass / 0 fail，Phase 16 回归）。
- `bun run typecheck`：已执行，通过（Phase 17/18 重新生成分支与按钮）。
- `bun test`：已执行，通过（117 pass / 0 fail，Phase 17 回归）。
- `bun run typecheck`：已执行，通过（Phase 18 回复末尾重新生成按钮）。
- `bun test`：已执行，通过（117 pass / 0 fail，Phase 18 回归）。
- `bun run typecheck`：已执行，通过（Phase 19 重新生成按钮官方样式对齐）。
- `bun test`：已执行，通过（117 pass / 0 fail，Phase 19 回归）。
- `bun run typecheck`：已执行，通过（Phase 20 导出 PDF 按钮与保存）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 20 回归，含新增文件名规则单测）。
- `cargo check`（`src-tauri`）：已执行，通过（Phase 20 新增 `save_pdf_document` 命令编译校验）。
- `bun run typecheck`：已执行，通过（Phase 21 富样式 PDF 导出）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 21 回归）。
- `bun run typecheck`：已执行，通过（Phase 22 导出链路双引擎修复）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 22 回归）。
- `bun run typecheck`：已执行，通过（Phase 23 空白页修复）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 23 回归）。
- `bun run typecheck`：已执行，通过（Phase 24 空白页检测阈值增强）。
- `bun test src/features/chat/export-message-pdf.test.ts`：已执行，通过（3 pass / 0 fail）。
- `bun run typecheck`：已执行，通过（Phase 25 暗像素占比判定）。
- `bun test src/features/chat/export-message-pdf.test.ts`：已执行，通过（3 pass / 0 fail，Phase 25 回归）。
- `bun run typecheck`：已执行，通过（Phase 26 浅色变量强制注入）。
- `bun test src/features/chat/export-message-pdf.test.ts`：已执行，通过（3 pass / 0 fail，Phase 26 回归）。
- `bun run typecheck`：已执行，通过（Phase 27 离屏容器捕获）。
- `bun test src/features/chat/export-message-pdf.test.ts`：已执行，通过（3 pass / 0 fail，Phase 27 回归）。
- `bun run typecheck`：已执行，通过（Phase 28 分页级内容判定 + 多捕获回退）。
- `bun test src/features/chat/export-message-pdf.test.ts`：已执行，通过（3 pass / 0 fail，Phase 28 回归）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 28 全量回归）。
- `bun run typecheck`：已执行，通过（Phase 29 渲染态文本兜底）。
- `bun test src/features/chat/export-message-pdf.test.ts`：已执行，通过（3 pass / 0 fail，Phase 29 回归）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 29 全量回归）。
- `bun run typecheck`：已执行，通过（Phase 30 独立容器富样式兜底）。
- `bun test src/features/chat/export-message-pdf.test.ts`：已执行，通过（3 pass / 0 fail，Phase 30 回归）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 30 全量回归）。
- `bun run typecheck`：已执行，通过（Phase 31 v11 单链路精简）。
- `bun test src/features/chat/export-message-pdf.test.ts`：已执行，通过（3 pass / 0 fail，Phase 31 回归）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 31 全量回归）。
- `bun run typecheck`：已执行，通过（Phase 32 动作栏末轮常显）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 32 回归）。
- `bun run typecheck`：已执行，通过（Phase 33 来源统计与跳转交互）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 33 回归）。
- `bun run typecheck`：已执行，通过（Phase 34 非生图 Gallery 对齐官方）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 34 回归）。
- `bun run typecheck`：已执行，通过（Phase 35 非生图双图宽度收敛）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 35 回归）。
- `bun run typecheck`：已执行，通过（Phase 36 图片光标手型）。
- `bun run typecheck`：已执行，通过（Phase 37 非生图双图宽度微调）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 37 回归）。
- `bun run typecheck`：已执行，通过（Phase 38 收紧消息底部间距）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 38 回归）。
- `bun run typecheck`：已执行，通过（Phase 39 输入框字号增大）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 39 回归）。
- `bun run typecheck`：已执行，通过（Phase 40 移除侧边栏品牌区）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 40 回归）。
- `bun run typecheck`：已执行，通过（Phase 41 右上角新建对话按钮）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 41 回归）。
- `bun run typecheck`：已执行，通过（Phase 42 新建按钮悬浮提示）。
- `bun test`：已执行，通过（120 pass / 0 fail，Phase 42 回归）。
- `bun run typecheck`：已执行，通过（Phase 43 DeepSearch 模型与请求联动）。
- `bun test`：已执行，通过（121 pass / 0 fail，Phase 43 回归，含新增 deep_search payload 单测）。
- `bun run typecheck`：已执行，通过（Phase 44 工具链展示）。
- `bun test src/components/markdown-content/structured-reasoning.test.ts src/components/markdown-content.test.ts`：已执行，通过（40 pass / 0 fail，Phase 44 定向回归）。
- `bun test`：已执行，通过（123 pass / 0 fail，Phase 44 全量回归）。
- `bun run build`：已执行，通过（Phase 44 构建校验；保留现有 Vite warning，非阻断）。

## 提交记录

- `d11685f`: phase(2): support concurrent streaming across conversations
- `c4e5d63`: phase(2): add streaming state helpers and unit tests
- `e0408d9`: phase(3): migrate conversation state to zustand
- `840ce9f`: phase(4): fix markdown hyphen parsing and bottom scroll jitter
- `70a664b`: phase(5): enhance markdown heading hierarchy
- `6d8b597`: phase(6): adjust composer collapse to upward scroll only
- `887c36e`: phase(7): add markdown image preview with outside click close
- `e935f20`: phase(8): center markdown image preview modal
- `5b911cf`: phase(9): align preview to chat pane and cap modal size
- `2e69ff0`: phase(10): add stream event meta envelope
- `d24858d`: phase(11): throttle card attachment updates during streaming
- `d0cc35f`: phase(12): filter intermediate generated image cards
- `d3f6a41`: phase(13): add stream heartbeat and idle-timeout semantics
- `43c07e6`: phase(14): add idle-timeout reconnect and previous_response_id
- `7e3edaf`: phase(15): add streaming connection health indicator
- `0b2ac6b`: phase(16): enlarge welcome cow logo in new chat screen
- `dab49f6`: phase(17): add regenerate branching flow and message truncation
- `aa5d421`: phase(18): add assistant regenerate action button
- `9aba4a4`: phase(20): add assistant pdf export action with save dialog
- `42dc5c6`: phase(21): export assistant pdf with rendered markdown and images
- `606ebe1`: phase(21): finalize plan and memo commit records
- `16098b1`: phase(22): harden pdf export rendering with dual capture engines
- `ce43908`: phase(23): prevent blank pdf capture with layered fallbacks
- `271ca40`: phase(24): tighten blank-page detection for pdf export
- `09779cd`: phase(25): require visible ink for pdf page capture
- `4e1bfb1`: phase(26): force light theme variables for pdf capture
- `021c8a4`: phase(27): capture pdf from offscreen cloned message container
- `13fb1c6`: phase(35): tighten non-generated two-image gallery width
- `79ead47`: phase(36): use pointer cursor for markdown images
- `9ea59f9`: phase(37): slightly widen two-image markdown gallery
- `7d639b1`: phase(38): tighten message-to-composer bottom spacing
- `86ae172`: phase(39): increase chat input text size
- `f126abb`: phase(40): remove sidebar brand block and logo
- `24449db`: phase(41): add header new-chat button for existing conversations
- `9243d4d`: phase(42): show header new-chat tooltip below button
- `eb21e0d`: phase(43): wire deepsearch with expert model and x_grok payload

## 例外记录

- 无

## 明日计划

- 补充更细粒度回归验证（会话切换、并行流式、删除清理）。
- 评估是否继续拆分 `chat-shell`（将流事件处理逻辑拆为 hooks/模块）。
