# 今日目标

- 将 `polymarket` / `kalshi` 订阅抓取从 `Jina + timeline parser` 切换为基于网关 `grok fast` 的固定 JSON schema 拉取，并改为基于帖子 `id` 去重、每 30 分钟同步一次。
- 在现有 feed rail 基础上收口来源模型：`Polymarket` / `Kalshi` 继续内置品牌化，额外 X 账号改为可由用户任意添加，不再继续硬编码新 source。

## 范围

- In:
  - 新增 feed gateway fetch client，使用固定 prompt/schema 拉取 `x.com/Polymarket` 与 `x.com/Kalshi`
  - 调整 feed source config、sync service 与存储去重逻辑，改为基于帖子 `id`
  - 将订阅轮询周期从 10 分钟调整为 30 分钟
  - 更新/替换相关单元测试并完成必要验证
- Out:
  - 不改聊天/语音主链协议
  - 不新增新的 feed UI 字段展示
  - 不在本轮引入新的数据库表

## Phase 计划

### Phase 1 - 文档与方案收敛

- 目标：明确新的 feed 拉取主链、JSON schema 约束与去重策略。
- 实施项：
  - 新增 `docs/plan/20260321.md` 与 `docs/memo/20260321.md`
  - 收敛 `grok fast -> JSON -> normalize -> 入库` 路径
  - 确认用帖子 `id` 作为 feed item 去重主键，并将轮询周期调整为 30 分钟
- 验收项：
  - 文档落地
  - 实现边界明确
- 状态：done

### Phase 2 - Feed 主链与调度改造

- 目标：以网关 JSON 拉取替换 Jina/timeline parser，并同时完成基于帖子 `id` 的去重和 30 分钟调度调整。
- 实施项：
  - 新增 feed gateway fetch client 与固定 schema/prompt
  - 调整 source config、sync service 与旧 reader/parser 依赖，统一改走 `grok fast`
  - 调整 feed item id/content hash 生成与 repository 去重逻辑
  - 将订阅默认 poll interval 与前端文案改为 30 分钟
- 验收项：
  - `polymarket` / `kalshi` 都能通过同一 JSON schema 获取帖子
  - 相同帖子不会重复入库
  - 默认轮询周期为 30 分钟
- 状态：done

### Phase 3 - 测试与归档

- 目标：完成回归测试、构建验证与文档归档。
- 实施项：
  - 更新/新增 feed gateway client、sync service、repository 去重测试
  - 执行 `bun test`、`bun run typecheck`、`bun run build`（按需）
  - 回填当日计划与 Memo 的测试/提交记录
- 验收项：
  - 测试通过
  - 文档与提交记录完整
- 状态：done

### Phase 4 - Feed Gateway Session Hotfix

- 目标：修复 feed gateway 请求缺少 `session_id` 导致的 `missing_session_id` 错误。
- 实施项：
  - 为 feed fetch 请求补充独立的 `session_id` 与 `client_turn_id`
  - 补充对应 client 回归测试，覆盖请求体必填字段
- 验收项：
  - feed 同步请求不再返回 `missing_session_id`
  - 热修复测试通过
- 状态：done

### Phase 5 - Hotfix 验证与归档

- 目标：完成 hotfix 验证并回填计划/Memo/提交记录。
- 实施项：
  - 执行必要测试与类型检查
  - 回填 hotfix 的测试结果与提交记录
- 验收项：
  - 测试通过
  - 文档归档完成
- 状态：done

### Phase 6 - Feed 保留期与升级清库

- 目标：将 feed 保留策略收敛为“按本地入库时间保留 48 小时”，并在本次升级时默认清空旧版本遗留 feed 数据。
- 实施项：
  - 为 repository 增加按 `discoveredAt` 删除过期 feed 的能力，并在 feed sync service 启动/定时同步时执行清理
  - 新增 SQLite migration，将旧版本 `feed_items` 全量清空，同时把 `feed_subscriptions` 轮询周期和轮询状态重置到新策略
  - 收口 feed fetch prompt，明确优先获取最近 24 小时帖子，不足时再回补到 5 条
- 验收项：
  - 入库超过 48 小时的 feed 会被自动清理，判断基准为本地 `discoveredAt`
  - 升级到本版本时会默认清空旧 feed，并把轮询周期统一到 30 分钟
  - prompt 明确要求“最近 24 小时优先，不足再补旧帖”
- 状态：done

### Phase 7 - Retention 验证与归档

- 目标：完成保留期/升级清库改动的验证、文档归档与提交记录补录。
- 实施项：
  - 执行 feed retention、migration、typecheck 相关测试
  - 回填计划与 Memo 的测试结果、提交记录、风险说明
  - 补录此前遗漏的 `phase(37)` 归档记录
- 验收项：
  - `bun test` 与 `bun run typecheck` 通过
  - 计划与 Memo 的记录完整
- 状态：done

### Phase 8 - 订阅入口 UI 收口

- 目标：将侧边栏订阅入口收敛为单个 `X / Twitter` 入口，同时保留 `Polymarket` / `Kalshi` 的切换能力。
- 实施项：
  - 将侧边栏 feed 区从双入口改为单入口，统一使用 X/Twitter 图标
  - 在 `chat shell` 中收口 feed sidebar 选中态与描述文案，改为单个 feed sidebar id
  - 在 feed 主视图头部增加来源切换，支持在 `Polymarket` / `Kalshi` 间切换
- 验收项：
  - 左侧“订阅”区只保留一个入口
  - 仍可在 feed 主视图切换 `Polymarket` / `Kalshi`
  - 侧边栏订阅入口图标替换为 X/Twitter
- 状态：done

### Phase 9 - Feed UI 验证与归档

- 目标：完成本轮 feed 入口 UI 变更的测试、文档回填与归档提交。
- 实施项：
  - 补充单入口 sidebar 逻辑测试并执行 feed pane 受影响测试
  - 执行 `bun test`、`bun run typecheck`
  - 回填计划与 Memo 的测试结果、提交记录与例外信息
- 验收项：
  - 受影响测试、全量测试与类型检查通过
  - 文档归档完整
- 状态：done

### Phase 10 - Feed 面板交互重构

- 目标：将 feed 视图从聊天头部中拆出，改成独立面板交互，并继续收口消息源切换与无效按钮。
- 实施项：
  - 移除 feed 视图复用的聊天头部，去掉顶部新建聊天按钮与重复标题区
  - 将消息源切换提升到 feed 面板最上方，并删除侧边栏订阅副标题
  - 删除 feed 面板内的“订阅设置”按钮，保留更直接的同步操作
- 验收项：
  - feed 视图顶部不再出现聊天模式的 header/new conversation 入口
  - source selector 位于面板最上方
  - 侧边栏 `X / Twitter` 入口不再显示 `Polymarket / Kalshi` 副标题
- 状态：done

### Phase 11 - Feed Panel 验证与归档

- 目标：完成本轮 feed panel 交互重构的验证、文档回填与归档提交。
- 实施项：
  - 执行受影响测试、全量 `bun test` 与 `bun run typecheck`
  - 回填计划与 Memo 的测试结果、提交记录与例外说明
  - 补录 `phase(41)` 的文档归档 hash
- 验收项：
  - 测试通过
  - 文档归档完整
- 状态：done

### Phase 12 - Sidebar 居中微调

- 目标：修正统一后的 `X / Twitter` 侧边栏入口在单行状态下的垂直对齐。
- 实施项：
  - 将 feed sidebar item 从顶对齐改为垂直居中
  - 收口图标容器的上边距，避免单行文本视觉偏上
- 验收项：
  - `X / Twitter` 入口在侧边栏内居中显示
  - 不影响现有 sidebar 激活态与 hover 样式
- 状态：done

### Phase 13 - Sidebar 微调验证与归档

- 目标：完成 sidebar 居中微调的验证、文档回填与归档提交。
- 实施项：
  - 执行 `bun run typecheck` 与最终全量 `bun test`
  - 回填计划与 Memo 的测试结果、提交记录与例外说明
- 验收项：
  - 验证通过
  - 文档归档完整
- 状态：done

### Phase 14 - Feed 来源品牌化按钮

- 目标：将 feed 面板顶部的 `Polymarket` / `Kalshi` 文本切换按钮替换为品牌 SVG，收口来源切换的视觉层级。
- 实施项：
  - 在 `FeedPane` 顶部 selector 中接入 `Polymarket` / `Kalshi` brand SVG
  - 调整 selector 与当前来源区样式，并在用户反馈后移除顶部冗余的 `X / Twitter` 说明卡
  - 将同步按钮下移为当前消息源的 icon-only 操作，并删除冗余说明文案，仅保留同步状态时间
  - 视情况同步收口当前 source 标题区域，避免新旧两套来源视觉冲突
- 验收项：
  - 顶部来源切换改为品牌 SVG 按钮
  - 激活态/非激活态清晰
  - 不影响现有 feed 切换、同步与空态交互
- 状态：done

### Phase 15 - 品牌化 UI 验证与归档

- 目标：完成品牌 SVG selector 的验证、文档回填与归档提交。
- 实施项：
  - 执行受影响测试、全量 `bun test` 与 `bun run typecheck`
  - 回填计划与 Memo 的测试结果、提交记录与例外说明
  - 补录此前遗漏的 `phase(45)` 文档归档 hash
- 验收项：
  - 验证通过
  - 文档归档完整
- 状态：done

### Phase 16 - Feed Source Rail

- 目标：将顶部来源切换改造成可扩展的 source rail，避免未来订阅源增多后平铺占满整行。
- 实施项：
  - 将 selector 改为单行横向滚动的 rail，不再使用 `wrap + flex-1` 均分布局
  - 保留当前选中项的强调态，并把未选中项收口为紧凑 chip
  - 将来源展示逻辑收口为“有品牌 SVG 就显示品牌，没有品牌 SVG 才回退 `@handle`”
- 验收项：
  - 顶部来源切换不会因为来源增多而换行平铺
  - 当前选中项与未选中项层级清晰
  - 未来新增无品牌来源时可以直接回退为 `@handle`
- 状态：done

### Phase 17 - Source Rail 验证与归档

- 目标：完成 source rail 改造的验证、文档回填与归档提交。
- 实施项：
  - 执行全量 `bun test` 与 `bun run typecheck`
  - 回填 plan/memo 的测试结果、提交记录与例外说明
  - 归档 `phase(49)` / `phase(50)` / `phase(51)` 的 UI 收口提交
- 验收项：
  - 验证通过
  - 文档归档完整
- 状态：done

### Phase 18 - Feed 自定义 X 账号模型

- 目标：保留 `Polymarket` / `Kalshi` 的内置品牌来源，同时支持通过配置任意追加 X 账号来源，不再为每个账号扩展 enum/布尔字段。
- 实施项：
  - 将 feed source 从固定 union 收口为“内置来源 + `x:<handle>` 自定义来源”模型，并新增 handle 规范化/URL 解析 helper
  - 扩展 `AppConfig` / runtime / Tauri subscriptions 配置，保留内置来源开关，并新增自定义 X 账号列表持久化
  - 调整 feed service、grok feed client、source rail 与 settings dialog，使其基于动态来源列表运行；设置页可新增/删除自定义 X 账号
- 验收项：
  - `Polymarket` / `Kalshi` 仍保留现有品牌 SVG 与开关逻辑
  - 可添加如 `@elonmusk` 或 `https://x.com/elonmusk` 这类自定义账号，新增后能出现在 source rail 并参与同步
  - 不再需要为新账号改 `FeedSource` enum 或新增单独布尔配置
- 状态：done

### Phase 19 - 自定义账号验证与归档

- 目标：完成自定义 X 账号模型的测试、文档回填与归档提交。
- 实施项：
  - 更新/新增 source config、feed client、feed service、settings dialog、chat shell 相关测试
  - 执行 `bun test`、`bun run typecheck`，并视需要追加 `cargo check`
  - 回填 plan/memo 的测试结果、提交记录与例外说明
- 验收项：
  - 验证通过
  - 文档归档完整
- 状态：done

### Phase 20 - X 订阅 UI 收口

- 目标：将设置页“订阅”语义收口为 `X 订阅`，并让 feed 视图只展示当前启用中的来源。
- 实施项：
  - 将设置页 tab 文案、图标与页内标题改为 `X 订阅`
  - 调整 feed source 列表解析，只返回当前启用中的内置源 + 自定义账号
  - 当当前激活来源被关闭时，实时切换到下一个可用来源；若没有可用来源，则展示空态而不是继续显示已关闭来源
- 验收项：
  - 设置页不再显示泛化的“订阅”语义，而是明确为 `X 订阅`
  - 关闭 `Polymarket` / `Kalshi` 后，feed rail 与当前 feed 视图立即隐藏对应来源
  - 全部来源关闭时，feed 视图显示无来源空态
- 状态：done

### Phase 21 - X 订阅 UI 验证与归档

- 目标：完成本轮 `X 订阅` UI 收口的验证、文档回填与归档提交。
- 实施项：
  - 更新/新增 feed source helper、chat shell、settings dialog 相关测试
  - 执行 `bun test` 与 `bun run typecheck`
  - 回填 plan/memo 的测试结果、提交记录与例外说明
- 验收项：
  - 验证通过
  - 文档归档完整
- 状态：done

### Phase 22 - X 订阅文案收口

- 目标：去掉设置页里对普通用户无意义的说明文字，并将自定义账号说明改成更自然的用户语言。
- 实施项：
  - 移除 `Polymarket` / `Kalshi` 开关卡片下方的冗余说明文字
  - 将“自定义 X 账号”说明改成普通用户可直接理解的描述
  - 保持现有开关、保存与同步逻辑不变
- 验收项：
  - 两个内置来源卡片不再显示说明段落
  - 自定义账号说明不再出现 `feed source rail` 这类内部术语
  - 设置页交互与保存逻辑不受影响
- 状态：done

### Phase 23 - X 订阅文案归档

- 目标：完成本轮文案收口的验证、文档回填与归档提交。
- 实施项：
  - 执行必要校验并记录结果
  - 回填 plan/memo 的实施、验证、提交与例外说明
- 验收项：
  - 验证通过
  - 文档归档完整
- 状态：done

### Phase 24 - Feed 视频渲染过滤

- 目标：避免 feed 卡片把视频直链当图片渲染，同时保留视频链接在“继续深入调研”的上下文中。
- 实施项：
  - 新增 feed 媒体 helper，识别视频链接并区分“可渲染图片”与“仅保留上下文”的媒体 URL
  - `FeedPane` 仅渲染可展示的图片链接，不再为视频直链输出缩略图按钮
  - “继续深入调研”仍保留全部媒体链接文本上下文，但只把图片链接作为图片附件传给模型
- 验收项：
  - 视频链接不再在 feed 卡片里渲染成坏图
  - 混合图片/视频媒体时，图片仍可正常展示
  - 深入调研时，视频链接仍保留在文本上下文中
- 状态：done

### Phase 25 - Feed 视频过滤验证与归档

- 目标：完成本轮媒体过滤改动的验证、文档回填与归档提交。
- 实施项：
  - 更新/新增 feed pane 与 chat shell helper 相关测试
  - 执行 `bun test` 与 `bun run typecheck`
  - 回填 plan/memo 的测试结果、提交记录与例外说明
- 验收项：
  - 验证通过
  - 文档归档完整
- 状态：done

## Phase 验收记录

### Phase 1

- 验收结果：已确认本轮只替换 feed 抓取入口与去重主键，不改现有 feed 表结构、翻译链路与 UI 信息密度。
- 备注：为了避免扩大改动面，`content` 继续保留原帖原文，中文展示仍走现有 OpenAI 翻译回填链路。

### Phase 2

- 验收结果：已新增基于网关 `grok fast` 的 feed fetch client，使用固定 prompt/schema 拉取 `x.com/Polymarket` 与 `x.com/Kalshi`；旧 `Jina + timeline parser` 运行时依赖已移除。
- 备注：feed item 改为基于帖子 `id` 生成稳定主键与 dedupe key，同时将默认轮询周期和前端文案统一调整为 30 分钟。

### Phase 3

- 验收结果：`bun test`、`bun run typecheck`、`bun run build` 全部通过，计划与 Memo 已回填本轮实现、验证与提交记录。
- 备注：`vite build` 仍保留既有动态导入 warning 与 chunk size warning，本轮未处理。

### Phase 4

- 验收结果：已将 feed fetch 请求的 `session_id` / `client_turn_id` 改成与现有聊天主链一致的网关兼容格式，避免 `missing_session_id` 后继续触发 `invalid_session_id`。
- 备注：请求现在统一使用 `sess_<uuid>`、`turn_<uuid>` 与 `Idempotency-Key`，不再使用自定义前缀。

### Phase 5

- 验收结果：hotfix 已通过 `bun test` 与 `bun run typecheck`，计划与 Memo 已补录 `phase(35)` / `phase(36)` 提交记录。
- 备注：本轮 hotfix 未改构建链路，因此不重复执行 `bun run build`。

### Phase 6

- 验收结果：feed 现已按本地 `discoveredAt` 保留 48 小时；清理会在服务启动时执行一次，并在后续同步周期内按 30 分钟节流复用。
- 备注：升级时通过 SQLite `migration 6` 清空旧 `feed_items`，同时把 `feed_subscriptions` 的轮询周期和轮询状态统一重置到新策略。

### Phase 7

- 验收结果：受影响用例、全量 `bun test` 与 `bun run typecheck` 均已通过；计划与 Memo 已补录 retention/migration 改动及 `phase(37)` 历史遗漏。
- 备注：本轮未改打包入口与构建链路，因此不重复执行 `bun run build`。

### Phase 8

- 验收结果：侧边栏 feed 区已收口为单个 `X / Twitter` 入口；原来的 `Polymarket` / `Kalshi` 双入口已移除。
- 备注：为避免丢失 source 切换能力，feed 主视图头部新增了 `Polymarket` / `Kalshi` 轻量切换按钮。

### Phase 9

- 验收结果：helper 测试、feed pane 测试、全量 `bun test` 与 `bun run typecheck` 均已通过；计划与 Memo 已回填本轮 UI 变更。
- 备注：本轮只改前端交互结构，未改后端协议、数据库或构建链路，因此不重复执行 `bun run build`。

### Phase 10

- 验收结果：feed 视图已不再复用聊天 header；顶部新建聊天按钮与重复标题区域已从 feed 模式移除。
- 备注：消息源切换被提升到面板最上层，`X / Twitter` 入口只保留单行标题，不再显示来源副标题。

### Phase 11

- 验收结果：受影响测试、全量 `bun test` 与 `bun run typecheck` 均已通过；计划与 Memo 已回填本轮 feed panel 交互重构。
- 备注：本轮继续只改前端视图结构，未影响构建链路，因此不重复执行 `bun run build`。

### Phase 12

- 验收结果：统一后的 `X / Twitter` 侧边栏入口已改为垂直居中，不再因为单行布局出现视觉上偏上的问题。
- 备注：这是纯样式级微调，只触碰了 sidebar feed item 的布局类名。

### Phase 13

- 验收结果：`bun run typecheck` 与最终全量 `bun test` 均已通过；计划与 Memo 已补录这次 sidebar 居中修正。
- 备注：本轮仍未影响构建链路，因此不重复执行 `bun run build`。

### Phase 14

- 验收结果：feed 面板顶部来源切换已改为 `Polymarket` / `Kalshi` 品牌 SVG 按钮，并同步将当前来源标题区收口为同一套 wordmark 视觉。
- 备注：初版保留的 `X / Twitter` 说明卡已按用户反馈移除；同步按钮也已下移到当前来源区并改为 icon-only，顶部不再出现会误解成“全局同步”的操作。

### Phase 15

- 验收结果：品牌化 selector 调整后再次执行全量 `bun test` 与 `bun run typecheck`，结果继续通过；`phase(45)` 遗漏记录已在本轮补齐。
- 备注：本轮仅改 feed panel 头部视觉层级，未改后端协议、持久化与构建链路，因此不重复执行 `bun run build`。

### Phase 16

- 验收结果：顶部来源切换已改成单行 source rail，当前选中项保留强调态，未选中项收口为紧凑 chip；未来来源增多时可直接横向扩展，不需要继续平铺。
- 备注：品牌来源已不再重复显示 `@handle`；只有未来新增且没有 SVG 的来源，才会回退为 `@handle` 文本。

### Phase 17

- 验收结果：source rail 改造后再次执行全量 `bun test` 与 `bun run typecheck`，结果继续通过；新增 helper 测试后全量测试更新为 397 通过。
- 备注：本轮只改 feed panel 头部交互与显示策略，未改后端协议、持久化与构建链路，因此不重复执行 `bun run build`。

### Phase 18

- 验收结果：feed 来源模型已收口为“内置品牌源 + 自定义 X 账号列表”；`Polymarket` / `Kalshi` 继续使用品牌 SVG，自定义账号改为 `x:<handle>` 动态 source id，并支持 `@handle` / `x.com/handle` 规范化。
- 备注：`AppConfig`、runtime、Tauri `subscriptions` 命令、feed service、grok prompt/schema 校验、source rail 与设置页均已切到新模型；新增自定义账号不会再触发新的 enum/布尔字段扩展。

### Phase 19

- 验收结果：全量 `bun test`、`bun run typecheck` 与 `cargo check` 均已通过；新增自定义账号回归后，全量测试更新为 404 通过。
- 备注：本轮未改构建入口与打包资源，因此不重复执行 `bun run build`；`phase(19)` 文档归档提交的 hash 已在本轮补录。

### Phase 20

- 验收结果：设置页“订阅”已收口为 `X 订阅`，对应 tab 图标也已切到 X；feed source 解析改为只返回当前启用中的内置源加自定义账号。
- 备注：当 `Polymarket` / `Kalshi` 被关闭时，`ChatShell` 会立即把当前来源切换到下一个可用来源；若没有可用来源，则直接展示 feed 空态，不再继续显示已关闭来源。

### Phase 21

- 验收结果：全量 `bun run typecheck` 与 `bun test` 已再次通过；本轮收口后全量测试更新为 406 通过。
- 备注：本轮继续只改前端 UI 与来源解析逻辑，未改构建入口与打包资源，因此不重复执行 `bun run build`。

### Phase 22

- 验收结果：`X 订阅` 页中 `Polymarket` / `Kalshi` 卡片的说明文字已移除；自定义账号说明已改成“添加后可直接查看账号最新动态”的用户语言。
- 备注：本轮只收口文案与显示条件，不改来源解析、同步、存储与保存链路。

### Phase 23

- 验收结果：`bun run typecheck` 已通过，计划与 Memo 已补录本轮文案收口与提交记录。
- 备注：本轮属于纯文案/展示收口，按例外流程未重复执行 `bun test` 与 `bun run build`。

### Phase 24

- 验收结果：feed 卡片现在只渲染可展示的图片链接；`video.twimg.com` 这类视频直链不会再被当作图片输出，从而避免坏图占位。
- 备注：深入调研时，所有媒体链接仍会保留在文本上下文里；只有图片链接会继续作为图片附件传给模型。

### Phase 25

- 验收结果：`bun run typecheck` 与全量 `bun test` 已再次通过；本轮收口后全量测试更新为 408 通过。
- 备注：本轮涉及前端显示和提问附件链路，因此补跑了全量测试；未改构建入口与打包资源，不重复执行 `bun run build`。

## 测试记录

- `bun test`：Phase 3 曾通过，391 个测试全部通过；Phase 7 回归后全量 394 个测试通过；Phase 9 UI 收口后全量 396 个测试通过；Phase 11、Phase 13、Phase 15 与本轮 Phase 17 结束前均再次执行；新增 source rail helper 测试后，全量更新为 397 个测试全部通过；本轮新增自定义账号模型回归后，全量更新为 404 个测试全部通过；本轮 `X 订阅` 收口后再次执行，全量更新为 406 个测试全部通过；本轮视频渲染过滤后再次执行，全量更新为 408 个测试全部通过。
- `bun test src/infrastructure/feed/grok-feed-client.test.ts src/infrastructure/feed/polymarket-feed-service.test.ts src/infrastructure/storage/memory/repository.test.ts src/infrastructure/storage/sqlite/migration-runtime.test.ts`：通过，19 个受影响测试全部通过。
- `bun test src/features/chat/chat-shell-helpers.test.ts src/components/polymarket-feed-pane.test.ts`：通过；source rail helper 测试补充后，受影响测试计数随全量测试一起更新。
- `bun test src/domain/feed/source-config.test.ts src/infrastructure/feed/grok-feed-client.test.ts src/infrastructure/feed/polymarket-feed-service.test.ts src/features/chat/chat-shell-helpers.test.ts src/components/polymarket-feed-pane.test.ts`：通过，38 个受影响测试全部通过。
- `bun test src/infrastructure/feed/grok-feed-client.test.ts src/infrastructure/feed/polymarket-feed-service.test.ts src/domain/feed/source-config.test.ts`：通过，18 个核心自定义账号回归测试全部通过。
- `bun run typecheck`：通过；本轮已在 Phase 13、Phase 15、Phase 17、自定义账号改造收口后、`X 订阅` UI 收口后、本轮文案收口后，以及视频渲染过滤后复验。
- `cargo check`：通过；Tauri `subscriptions` 配置命令与桌面端编译均通过校验。
- `bun run build`（按需）：Phase 3 已通过；本轮未影响构建链路，因此未重复执行。既有 `vite` 动态导入 warning 与 chunk size warning 保持不变。

## 提交记录

- `519c27e` - `phase(33): plan gateway feed schema switch`
- `357a4cb` - `phase(34): switch feed sync to gateway json schema`
- `48c9873` - `phase(35): archive gateway feed schema migration`
- `88bb339` - `phase(36): fix feed gateway session id format`
- `8cc77cf` - `phase(37): archive feed gateway session hotfix`
- `346ad39` - `phase(38): add feed retention and migration reset`
- `070e17b` - `phase(39): archive feed retention migration reset`
- `cf3b762` - `phase(40): unify feed sidebar entry`
- `1fbaffa` - `phase(41): archive unified feed sidebar ui`
- `aea0cbf` - `phase(42): refine feed panel interaction`
- `067740c` - `phase(44): center unified feed sidebar item`
- `1bc7758` - `phase(45): archive feed panel sidebar polish`
- `8b1e2ec` - `phase(46): brand feed source selector`
- `40ead96` - `phase(47): simplify feed source header`
- `36748f6` - `phase(48): refine feed source sync affordance`
- `db2ef43` - `phase(49): tighten feed source sync layout`
- `0485654` - `phase(50): hide branded source handles`
- `f15c0c6` - `phase(51): add feed source rail`
- `53be8cd` - `phase(52): archive feed source rail`
- `1cad132` - `phase(18): support custom x feed accounts`
- `37dc050` - `phase(19): archive custom x feed accounts`
- `7f25802` - `phase(20): refine x subscription visibility`
- `288f947` - `phase(21): archive x subscription visibility`
- `b3c5d4c` - `phase(22): simplify x subscription copy`
- `7b215d0` - `phase(23): archive x subscription copy`
- `73c5182` - `phase(24): filter feed video previews`

## 例外记录

- `phase(52)` 与 `phase(19)` 的归档 commit hash 已在本轮补录。
- 本轮 `Phase 22-23` 为纯文案/展示收口，按例外流程仅执行 `bun run typecheck`，未重复执行 `bun test` 与 `bun run build`。
- `phase(23)` 的归档 commit hash 已在本轮补录。
- `phase(25)` 为本轮文档归档提交，commit hash 同样存在最后一笔归档提交的自引用限制，将在下一轮补录。

## 明日计划

- 继续观察 `X 订阅` 在真实使用场景下的文案理解成本、启停切换、空态表现、自定义账号列表管理体验，以及混合图片/视频媒体时的展示效果；如有必要再补来源排序、滚动提示或更强的列表编辑交互。
