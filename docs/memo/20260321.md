# 20260321 Memo

## Item

- [00:03] Item: 启动 feed 抓取方案切换，准备将 `polymarket` / `kalshi` 从 `Jina + parser` 改为网关 `grok fast` 固定 JSON schema 拉取。
- [00:04] Item: 新增 20260321 计划与 Memo 文档，先按“方案收敛、主链改造、ID 去重与调度、测试归档”四个 Phase 推进；新增要求包括“用帖子 `id` 去重”和“每 30 分钟更新一次”。
- [00:18] Item: 完成 feed 主链切换：新增 `grok feed client`、共享 gateway URL helper 与固定 prompt/schema，移除 `Jina + timeline parser` 运行时依赖，并保留原帖原文 + OpenAI 翻译回填链路。
- [00:19] Item: 完成帖子 `id` 去重与 30 分钟调度收口：feed item 主键/去重键改为基于帖子 `id` 生成，默认轮询周期和设置页/FeedPane/chat shell 文案统一改为 30 分钟。
- [00:22] Item: 完成验证，`bun test`（390 通过）、`bun run typecheck` 与 `bun run build` 全部通过；构建仅保留既有 `vite` 动态导入 warning 与 chunk size warning。
- [00:24] Item: 提交 `phase(33)`，归档今天的 feed JSON schema 切换计划与执行边界。
- [00:25] Item: 提交 `phase(34)`，归档 gateway feed client、旧 Jina/parser 删除、post-id 去重、30 分钟调度与相关测试。
- [00:28] Item: 提交 `phase(35)`，归档首轮 gateway feed schema 迁移文档。
- [00:34] Item: 用户反馈 feed 页面仍报 `missing_session_id` / `invalid_session_id`；已定位为新 feed client 使用了网关不接受的自定义 session id 前缀。
- [00:38] Item: 完成 hotfix：feed fetch 请求改为使用与聊天主链一致的 `sess_<uuid>` / `turn_<uuid>`，并保留 `Idempotency-Key`；新增回归测试覆盖请求体字段格式。
- [00:41] Item: 完成 hotfix 验证，`bun test`（391 通过）与 `bun run typecheck` 全部通过。
- [00:42] Item: 提交 `phase(36)`，归档 feed gateway session id 格式修复。
- [16:00] Item: 收口本轮 retention 策略：feed 改为按本地 `discoveredAt` 保留 48 小时；服务启动时先清理一次，后续按 30 分钟节流复用清理逻辑；升级通过 SQLite `migration 6` 默认清空旧版本遗留 `feed_items` 并重置订阅轮询状态。
- [16:03] Item: 将 feed fetch prompt 收口为“优先最近 24 小时帖子，不足再补到 5 条”，并移除未被调用的 `clearFeedItems()` 仓储接口，保持改动最小。
- [16:05] Item: 完成本轮验证，受影响测试 19 项通过；全量 `bun test` 更新为 394 通过，`bun run typecheck` 通过。
- [16:08] Item: 提交 `phase(38)`，归档 feed retention、升级清库 migration 与 prompt 收口实现。
- [16:10] Item: 提交 `phase(39)`，回填 feed retention / migration reset 的文档归档记录。
- [16:12] Item: 启动 feed 入口 UI 收口：将侧边栏“订阅”区从双入口改为单个 `X / Twitter` 入口，并规划在 feed 主视图内补 `Polymarket` / `Kalshi` 切换，避免丢失 source 访问能力。
- [16:15] Item: 完成 UI 实现：`ChatSidebar` 改用 X/Twitter 图标与单入口，`ChatShell` 收口为统一 feed sidebar id，`FeedPane` 头部新增 source 切换按钮。
- [16:17] Item: 完成验证，`bun test src/features/chat/chat-shell-helpers.test.ts src/components/polymarket-feed-pane.test.ts` 22 项通过；全量 `bun test` 更新为 396 通过，`bun run typecheck` 通过。
- [16:18] Item: 提交 `phase(40)`，归档 unified feed sidebar 与 feed 主视图 source 切换实现。
- [16:24] Item: 启动第二轮 feed UI 收口：去掉 feed 视图对聊天 header 的复用，移除顶部新建聊天按钮与重复标题区，并把 source selector 提升到面板最上方。
- [16:27] Item: 完成 feed panel 重构：侧边栏 `X / Twitter` 入口去掉副标题，feed 面板顶部改为 `X / Twitter + source selector + sync` 的独立区域，同时删除“订阅设置”按钮。
- [16:29] Item: 完成验证，`bun test src/features/chat/chat-shell-helpers.test.ts src/components/polymarket-feed-pane.test.ts` 22 项通过；全量 `bun test` 继续为 396 通过，`bun run typecheck` 通过。
- [16:30] Item: 提交 `phase(42)`，归档 feed panel 去 chat 化后的交互与顶部结构重构。
- [16:31] Item: 用户继续要求将侧边栏 `X / Twitter` 单入口在单行状态下改为真正居中；已收口为最小样式修正。
- [16:32] Item: 完成 sidebar 居中修正验证，`bun run typecheck` 通过；随后再次执行全量 `bun test`，结果保持 396 通过。
- [16:33] Item: 提交 `phase(44)`，归档 unified feed sidebar item 的居中修正。
- [16:36] Item: 启动第三轮 feed panel 收口：将顶部 `Polymarket` / `Kalshi` 文本按钮替换为用户提供的品牌 SVG，并同步收口当前来源标题区。
- [16:39] Item: 完成品牌 selector 初版实现：顶部来源切换改为 wordmark button，当前来源标题区也统一使用品牌 SVG；`bun test` 与 `bun run typecheck` 均通过。
- [16:40] Item: 提交 `phase(46)`，归档 feed 来源品牌化 selector 首版实现。
- [16:41] Item: 用户反馈顶部 `X / Twitter` / `消息源` 说明卡不需要；已将其移除，只保留品牌来源切换与同步按钮，并再次执行 `bun test` / `bun run typecheck`，结果继续保持 396 通过。
- [16:42] Item: 提交 `phase(47)`，收口 feed panel 顶部来源区，移除冗余 `X / Twitter` 说明卡。
- [16:44] Item: 用户继续要求同步按钮下移到当前消息源区，改成 icon-only，并删除“同步 X 上的最新动态...”说明文案；已完成实现并再次执行 `bun test` / `bun run typecheck`，结果继续保持 396 通过。
- [16:45] Item: 提交 `phase(48)`，收口 feed panel 当前消息源区的同步交互与状态展示。
- [16:46] Item: 继续收口 feed panel 当前来源区：将同步状态与刷新按钮贴近展示，并把品牌 source 的 `@handle` 改成“仅在无 SVG 时回退”。
- [16:47] Item: 用户提出未来消息源增多时不应全平铺；已将顶部 selector 改成单行 source rail，当前选中项保留强调态，未选中项收口为紧凑 chip，可继续横向扩展。
- [16:47] Item: 完成 source rail 与 fallback helper 验证，新增 helper 测试后全量 `bun test` 更新为 397 通过，`bun run typecheck` 通过。
- [16:47] Item: 提交 `phase(49)`、`phase(50)` 与 `phase(51)`，分别归档同步状态布局收口、品牌 source handle 回退逻辑，以及 source rail 主链实现。
- [17:19] Item: 收口 feed 来源模型为“内置 `Polymarket` / `Kalshi` + 任意自定义 X 账号列表”；source id 改为支持 `x:<handle>`，并新增 `@handle` / `x.com/handle` 规范化、Tauri `subscriptions.custom_accounts` 持久化，以及 feed rail/settings dialog 动态来源管理。
- [17:19] Item: 完成本轮验证：`bun run typecheck`、`cargo check` 通过；全量 `bun test` 更新为 404 通过，并补齐自定义账号的 source config / grok feed client / feed service 回归测试。
- [17:19] Item: 提交 `phase(18)`，归档自定义 X 账号来源模型、动态 source rail 与设置页列表化管理实现。
- [17:29] Item: 收口设置页命名与 feed 来源可见性：设置 tab / 页内标题改为 `X 订阅`，内置来源解析改为“只返回当前启用项”，并让 feed 视图在来源被关闭后立即切换或回退到无来源空态。
- [17:35] Item: 完成本轮验证，`bun run typecheck` 通过；全量 `bun test` 更新为 406 通过，并补齐“无启用来源返回 null”“禁用内置源不再出现在来源列表”回归测试。
- [17:36] Item: 回填 `phase(52)` / `phase(19)` 的归档 hash，并提交 `phase(20)`，归档 `X 订阅` 命名、启用来源过滤与 feed 无来源空态实现。
- [17:42] Item: 继续收口 `X 订阅` 文案：移除 `Polymarket` / `Kalshi` 卡片下方的说明句，并把“自定义 X 账号”说明改成普通用户能直接理解的表述。
- [17:43] Item: 完成本轮必要校验，`bun run typecheck` 通过；本轮属于纯文案/展示收口，不重复执行全量 `bun test`。
- [17:46] Item: 收口 feed 媒体展示链路：新增媒体 helper，把视频直链从 feed 卡片渲染和“继续深入调研”的图片附件中过滤掉，但继续保留在调研 prompt 的文本上下文里。
- [17:49] Item: 完成本轮验证，`bun run typecheck` 与全量 `bun test` 均通过；全量测试更新为 408 通过，并补齐视频过滤的 feed pane / deep research helper 回归测试。
- [17:49] Item: 回填 `phase(23)` 的归档 hash，并提交 `phase(24)`，归档 feed 视频预览过滤实现。
- [17:55] Item: 启动发版准备，按仓库规则执行 `./scripts/bump-version.sh v0.2.4`，将前端与 Tauri 版本统一提升到 `0.2.4`。
- [17:56] Item: 完成发版前校验，`bun run release:preflight`、`cargo check` 与 `git diff --check` 全部通过；构建仅保留既有 Vite 动态导入 warning 与 chunk size warning。
- [17:56] Item: 提交 `phase(26)`，归档 `0.2.4` 版本提升；`package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 与 `src-tauri/Cargo.lock` 已同步到新版本。
- [18:01] Item: 用户在推送前要求暂停发版；已确认远端尚未收到任何新 tag，本地此前创建的 `v0.2.4` tag 已删除，GitHub release workflow 未触发。
- [18:03] Item: 按新要求将内置 `Polymarket` / `Kalshi` 调整为默认开放订阅：前端 env fallback 改为默认开启，Tauri `subscriptions` TOML 缺省值也改为默认开启，同时保持显式关闭的用户配置不被覆盖。
- [18:04] Item: 完成本轮验证，默认开放订阅改动已通过全量 `bun test`（408 通过）、`bun run typecheck` 与 `cargo check`。
- [18:04] Item: 提交 `phase(28)`，归档内置消息源默认开放订阅实现；发布仍保持暂停状态，待确认后再重新创建/推送 `v0.2.4` tag。
- [18:08] Item: 已重新创建本地 `v0.2.4` tag，并执行 `git push origin feat/tauri-migration v0.2.4`；远端分支现已更新到 `124fd06`，`v0.2.4` tag 也已推送，GitHub release workflow 应开始运行。
- [18:08] Item: 开始回填本轮真实发版结果，将当天 plan/memo 从“暂停发版”状态修正为“已完成推送并触发 release 条件”。

## Commits

- `519c27e`: 新增 20260321 计划与 Memo，确定 feed 抓取改走 `grok fast` 固定 JSON schema，并把去重与调度目标收敛到帖子 `id` + 30 分钟。
- `357a4cb`: 将 feed 同步从 `Jina + parser` 切换到 gateway JSON client，删除旧抓取实现，改为 post-id 去重并补齐新 client/service/repository 测试。
- `48c9873`: 回填首轮 gateway feed schema 迁移的计划、验证与提交记录。
- `88bb339`: 将 feed gateway 请求改为网关兼容的 `sess_<uuid>` / `turn_<uuid>` 格式，修复 `invalid_session_id` 并补充回归测试。
- `8cc77cf`: 回填 feed gateway session hotfix 的计划、验证与提交记录。
- `346ad39`: 新增 feed 48 小时本地保留与升级清库 migration，统一旧订阅到 30 分钟轮询，并将 prompt 收口为“最近 24 小时优先，不足回补”。
- `070e17b`: 回填 feed retention / migration reset 的计划、验证与提交记录。
- `cf3b762`: 将侧边栏订阅入口收口为单个 `X / Twitter`，并在 feed 主视图补充 `Polymarket` / `Kalshi` 来源切换。
- `1fbaffa`: 回填 unified feed sidebar UI 的计划、验证与提交记录。
- `aea0cbf`: 将 feed 面板从聊天 header 中拆出，移除顶部新建聊天按钮/订阅设置按钮，并把 source selector 提升到面板最上方。
- `067740c`: 将单行 `X / Twitter` 侧边栏入口改为垂直居中，修正统一入口在 sidebar 中的视觉偏移。
- `1bc7758`: 回填 feed panel / sidebar polish 的计划、验证与提交记录。
- `8b1e2ec`: 将 feed 面板顶部 `Polymarket` / `Kalshi` 来源切换替换为品牌 SVG，并统一当前来源标题区样式。
- `40ead96`: 移除顶部冗余的 `X / Twitter` 说明卡，保留更干净的品牌来源切换与同步按钮。
- `36748f6`: 将同步按钮下移到当前消息源区，改为 icon-only，并删除冗余说明文案，仅保留同步状态时间。
- `db2ef43`: 将同步状态与刷新按钮进一步收紧到同一行，避免 feed 当前来源区过度拉伸。
- `0485654`: 将品牌 source 的 `@handle` 改为仅在无 SVG 时回退显示，避免重复信息。
- `f15c0c6`: 将顶部来源切换改造成单行 source rail，并新增 helper 测试覆盖品牌/handle fallback 逻辑。
- `53be8cd`: 回填 source rail 文档归档提交，补录 `phase(52)` hash。
- `1cad132`: 支持内置品牌源 + 自定义 X 账号列表模型，新增 `x:<handle>` 动态来源、设置页账号列表管理、Tauri 自定义账号持久化及相关回归测试。
- `37dc050`: 回填自定义 X 账号模型的文档归档记录，补录 `phase(19)` hash。
- `7f25802`: 将设置页收口为 `X 订阅`，并让 feed 只展示当前启用来源；禁用内置源后会立即隐藏并在无来源时展示空态。
- `288f947`: 回填 `X 订阅` 可见性收口的文档归档记录，补录 `phase(21)` hash。
- `b3c5d4c`: 精简 `X 订阅` 设置页文案，移除内置来源冗余说明，并把自定义账号说明改成更直白的用户语言。
- `7b215d0`: 回填 `X 订阅` 文案收口的文档归档记录，补录 `phase(23)` hash。
- `73c5182`: 为 feed 增加视频媒体过滤 helper；视频链接不再渲染成预览图，但仍保留在深入调研的文本上下文中。
- `2119fbd`: 回填 feed 视频预览过滤的文档归档记录，补录 `phase(25)` hash。
- `915a750`: 将应用版本统一升级到 `0.2.4`，并同步 `Cargo.lock` 中的 `nbsearch` 包版本。
- `b06e62c`: 归档 `v0.2.4` 发版准备文档，但随后按用户要求暂停发版；本地 `v0.2.4` tag 已删除，未向远端推送。
- `33d8513`: 将内置 `Polymarket` / `Kalshi` 订阅默认值改为开启，并同步更新前端配置测试与 Tauri `subscriptions` 缺省值。
- `124fd06`: 回填内置消息源默认开放订阅的计划、测试与发布暂停状态，作为重新发版前的最终归档基线。

## Notes

- 计划保持现有 feed 存储/UI 结构不变，只替换抓取入口与去重主键，避免扩大改动面。
- `content` 继续保留原帖原文，避免影响现有翻译存储、继续深入调研 prompt 与 UI 展示逻辑。
- `v0.2.4` 已重新推送到远端：当前 tag 指向 `124fd06`，远端分支也已同步到该提交；后续重点转为观察 GitHub release workflow 与产物结果。
- `phase(27)` 完成态归档提交的 hash 会在下一轮补录，沿用最后一笔归档提交自引用例外处理。
