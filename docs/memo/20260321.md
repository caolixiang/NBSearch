# 20260321 Memo

## Item

- [00:03] Item: 启动 feed 抓取方案切换，准备将 `polymarket` / `kalshi` 从 `Jina + parser` 改为网关 `grok fast` 固定 JSON schema 拉取。
- [00:04] Item: 新增 20260321 计划与 Memo 文档，先按“方案收敛、主链改造、ID 去重与调度、测试归档”四个 Phase 推进；新增要求包括“用帖子 `id` 去重”和“每 30 分钟更新一次”。
- [00:18] Item: 完成 feed 主链切换：新增 `grok feed client`、共享 gateway URL helper 与固定 prompt/schema，移除 `Jina + timeline parser` 运行时依赖，并保留原帖原文 + OpenAI 翻译回填链路。
- [00:19] Item: 完成帖子 `id` 去重与 30 分钟调度收口：feed item 主键/去重键改为基于帖子 `id` 生成，默认轮询周期和设置页/FeedPane/chat shell 文案统一改为 30 分钟。
- [00:22] Item: 完成验证，`bun test`（390 通过）、`bun run typecheck` 与 `bun run build` 全部通过；构建仅保留既有 `vite` 动态导入 warning 与 chunk size warning。
- [00:24] Item: 提交 `phase(33)`，归档今天的 feed JSON schema 切换计划与执行边界。
- [00:25] Item: 提交 `phase(34)`，归档 gateway feed client、旧 Jina/parser 删除、post-id 去重、30 分钟调度与相关测试。
- [00:28] Item: 提交 `phase(35)`，归档首轮 gateway feed schema 迁移文档。
- [00:34] Item: 用户反馈 feed 页面仍报 `missing_session_id` / `invalid_session_id`；已定位为新 feed client 使用了网关不接受的自定义 session id 前缀。
- [00:38] Item: 完成 hotfix：feed fetch 请求改为使用与聊天主链一致的 `sess_<uuid>` / `turn_<uuid>`，并保留 `Idempotency-Key`；新增回归测试覆盖请求体字段格式。
- [00:41] Item: 完成 hotfix 验证，`bun test`（391 通过）与 `bun run typecheck` 全部通过。
- [00:42] Item: 提交 `phase(36)`，归档 feed gateway session id 格式修复。
- [16:00] Item: 收口本轮 retention 策略：feed 改为按本地 `discoveredAt` 保留 48 小时；服务启动时先清理一次，后续按 30 分钟节流复用清理逻辑；升级通过 SQLite `migration 6` 默认清空旧版本遗留 `feed_items` 并重置订阅轮询状态。
- [16:03] Item: 将 feed fetch prompt 收口为“优先最近 24 小时帖子，不足再补到 5 条”，并移除未被调用的 `clearFeedItems()` 仓储接口，保持改动最小。
- [16:05] Item: 完成本轮验证，受影响测试 19 项通过；全量 `bun test` 更新为 394 通过，`bun run typecheck` 通过。
- [16:08] Item: 提交 `phase(38)`，归档 feed retention、升级清库 migration 与 prompt 收口实现。
- [16:10] Item: 提交 `phase(39)`，回填 feed retention / migration reset 的文档归档记录。
- [16:12] Item: 启动 feed 入口 UI 收口：将侧边栏“订阅”区从双入口改为单个 `X / Twitter` 入口，并规划在 feed 主视图内补 `Polymarket` / `Kalshi` 切换，避免丢失 source 访问能力。
- [16:15] Item: 完成 UI 实现：`ChatSidebar` 改用 X/Twitter 图标与单入口，`ChatShell` 收口为统一 feed sidebar id，`FeedPane` 头部新增 source 切换按钮。
- [16:17] Item: 完成验证，`bun test src/features/chat/chat-shell-helpers.test.ts src/components/polymarket-feed-pane.test.ts` 22 项通过；全量 `bun test` 更新为 396 通过，`bun run typecheck` 通过。
- [16:18] Item: 提交 `phase(40)`，归档 unified feed sidebar 与 feed 主视图 source 切换实现。
- [16:24] Item: 启动第二轮 feed UI 收口：去掉 feed 视图对聊天 header 的复用，移除顶部新建聊天按钮与重复标题区，并把 source selector 提升到面板最上方。
- [16:27] Item: 完成 feed panel 重构：侧边栏 `X / Twitter` 入口去掉副标题，feed 面板顶部改为 `X / Twitter + source selector + sync` 的独立区域，同时删除“订阅设置”按钮。
- [16:29] Item: 完成验证，`bun test src/features/chat/chat-shell-helpers.test.ts src/components/polymarket-feed-pane.test.ts` 22 项通过；全量 `bun test` 继续为 396 通过，`bun run typecheck` 通过。
- [16:30] Item: 提交 `phase(42)`，归档 feed panel 去 chat 化后的交互与顶部结构重构。
- [16:31] Item: 用户继续要求将侧边栏 `X / Twitter` 单入口在单行状态下改为真正居中；已收口为最小样式修正。
- [16:32] Item: 完成 sidebar 居中修正验证，`bun run typecheck` 通过；随后再次执行全量 `bun test`，结果保持 396 通过。
- [16:33] Item: 提交 `phase(44)`，归档 unified feed sidebar item 的居中修正。

## Commits

- `519c27e`: 新增 20260321 计划与 Memo，确定 feed 抓取改走 `grok fast` 固定 JSON schema，并把去重与调度目标收敛到帖子 `id` + 30 分钟。
- `357a4cb`: 将 feed 同步从 `Jina + parser` 切换到 gateway JSON client，删除旧抓取实现，改为 post-id 去重并补齐新 client/service/repository 测试。
- `48c9873`: 回填首轮 gateway feed schema 迁移的计划、验证与提交记录。
- `88bb339`: 将 feed gateway 请求改为网关兼容的 `sess_<uuid>` / `turn_<uuid>` 格式，修复 `invalid_session_id` 并补充回归测试。
- `8cc77cf`: 回填 feed gateway session hotfix 的计划、验证与提交记录。
- `346ad39`: 新增 feed 48 小时本地保留与升级清库 migration，统一旧订阅到 30 分钟轮询，并将 prompt 收口为“最近 24 小时优先，不足回补”。
- `070e17b`: 回填 feed retention / migration reset 的计划、验证与提交记录。
- `cf3b762`: 将侧边栏订阅入口收口为单个 `X / Twitter`，并在 feed 主视图补充 `Polymarket` / `Kalshi` 来源切换。
- `1fbaffa`: 回填 unified feed sidebar UI 的计划、验证与提交记录。
- `aea0cbf`: 将 feed 面板从聊天 header 中拆出，移除顶部新建聊天按钮/订阅设置按钮，并把 source selector 提升到面板最上方。
- `067740c`: 将单行 `X / Twitter` 侧边栏入口改为垂直居中，修正统一入口在 sidebar 中的视觉偏移。

## Notes

- 计划保持现有 feed 存储/UI 结构不变，只替换抓取入口与去重主键，避免扩大改动面。
- `content` 继续保留原帖原文，避免影响现有翻译存储、继续深入调研 prompt 与 UI 展示逻辑。
- `phase(45)` 为本轮 feed panel / sidebar 文档归档提交，其 commit hash 需在下一轮补录；这是文档归档提交天然存在的自引用限制。
