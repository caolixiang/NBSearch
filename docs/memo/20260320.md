# 20260320 Memo

## Item

- [20:33] Item: 启动 Kalshi 订阅扩展与 tray 同步修复，确认现状为单一 Polymarket 硬编码，且轮询完全依赖前端 `setTimeout`，窗口隐藏到 tray 后会失去稳定调度。
- [20:33] Item: 新增 20260320 计划与 Memo 文档，拆分为多源订阅主链、UI 交互优化、tray 原生调度、测试验证四个 Phase。
- [20:46] Item: 完成 feed domain/config/runtime/service 主链改造，新增 `kalshi` 来源、双来源订阅配置、通用 Jina reader / X timeline parser，以及按来源同步/回填翻译的 feed service。
- [20:52] Item: 完成订阅 UI 收口：侧边栏支持多个 feed 入口，主内容面板改为通用 FeedPane，设置页新增 Kalshi 开关，并将 OpenAI 文案改成订阅翻译通用描述。
- [20:57] Item: 完成 tray 同步修复：Tauri 原生侧增加 `nbsearch://feed-sync-tick` 周期事件，窗口恢复时补发一次 tick，前端 feed service 改为监听原生 tick 并按 due-check 执行同步。
- [21:03] Item: 完成回归验证，`bun test`、`bun run typecheck`、`bun run build` 与 `cargo check` 全部通过；额外补了 Kalshi parser/service、feed helper 与 config 默认值测试。
- [22:05] Item: 复跑 `bun test`（388 通过）、`bun run typecheck`、`bun run build` 与 `cargo check`，确认当前工作树仍可通过；`vite build` 仅保留既有动态导入与 chunk size warning。
- [22:10] Item: 提交 `phase(28)`，归档多源 feed 主链改造，包括 `kalshi` 来源配置、通用 timeline parser、双来源 sync service 与前端 due-check/native tick listener 接口。
- [22:11] Item: 提交 `phase(29)`，归档多来源订阅 UI 改造，包括侧边栏多入口、通用 FeedPane、设置页双开关与 chat shell 的来源切换/带聊状态管理。
- [22:11] Item: 提交 `phase(30)`，归档 Tauri tray 原生调度修复，新增 `nbsearch://feed-sync-tick` 周期事件与窗口恢复补发逻辑。
- [22:12] Item: 提交 `phase(31)`，归档 Kalshi/parser/config/feed helper 回归测试补充，并确认多源订阅与 tray 同步改动已通过验证。

## Commits

- `18424de`: 新增多源 feed 主链，支持 `polymarket` / `kalshi` 共用抓取、解析、翻译与按来源调度同步。
- `b743e76`: 将订阅 UI 泛化为多来源入口，设置页、侧边栏、FeedPane 与 chat shell 均支持按来源切换和带聊。
- `e20a541`: 在 Tauri 原生侧增加 feed sync tick 周期事件，并在窗口恢复时补发一次 catch-up tick。
- `c40965b`: 补充多源 feed/config/parser/helper 回归测试，完成本轮验证归档。

## Notes

- 采用“Rust 只负责调度，前端继续负责抓取/翻译/入库”的最小改动方案，避免重写现有 feed 主链。
- 现有存储 schema 已支持 `source` 维度，因此本轮无需新增数据库迁移。
- [21:10] Item: 定位 Kalshi 无内容的根因为 Jina 当前返回的是 X 登录页式时间线而非旧版 `## ... posts` 结构；已补 parser 兼容 `http://x.com/.../status/...` 登录页布局，并清洗账号链接、`Show more`、视频时长与互动数字噪音。实际抓取复测可解析出 17 条 Kalshi 帖子。
