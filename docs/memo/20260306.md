# 20260306 Memo

## Item

- [00:01] Item: 启动 Phase 85，目标为补齐 `turn in_progress` 长尾恢复策略，减少会话在恢复窗口内的误失败。
- [00:02] Item: 服务层新增 `TurnRecoveryOutcome.in_progress`，并在 `attemptTurnRecovery` 中接入 `/state` 活跃 turn 一致性校验。
- [00:02] Item: `streamTurn` 已在 transport/http/no_body/stream 异常分支支持 `in_progress` 等待重试；超限返回 `turn_in_progress`。
- [00:03] Item: 新增两条单测：`in_progress -> retry -> completed`、`in_progress` 超限返回 `turn_in_progress`。
- [00:03] Item: 执行回归验证：`bun run typecheck`、`bun test` 全部通过（161 pass）。
- [00:02] Item: 提交 `53e059c`（phase(85)），完成 in-progress 恢复策略与失败码收口。
- [00:04] Item: 提交 `0830ab6`（phase(85)），补录 plan/memo 提交追踪。
- [00:06] Item: 启动 Phase 86，目标为将 `turn in_progress` 重试预算与等待时长改为 `config.toml` 可配置（不新增 UI）。
- [00:08] Item: 完成配置链路：Tauri `read_gateway_config` 增加 `turnInProgressRetryMaxAttempts/turnInProgressRetryDelayMs`，前端 `AppConfig` 与服务层已接入。
- [00:09] Item: 更新测试并验证配置覆盖：`turnInProgressRetryMaxAttempts=0` 时立即返回 `turn_in_progress`，全量 `bun test` 通过（161 pass）。
- [00:11] Item: 提交 `7d4c0f9`（phase(86)），完成 in-progress 重试参数配置化（仅读 config.toml，无 UI）。
- [00:11] Item: 提交 `80a2f13`（phase(86)），补录 plan/memo 提交追踪。
- [00:12] Item: 启动 Phase 87，目标为首次保存网关时同步写入 recovery 默认参数到 `config.toml`。
- [00:13] Item: 完成实现并验证：`save_gateway_config` 会写入 recovery 默认值；`bun run typecheck`、`bun test`、`cargo check` 全部通过。
- [00:14] Item: 提交 `ad744ea`（phase(87)），完成首次保存网关即写入 recovery 默认参数。
- [00:14] Item: 提交 `b91d65e`（phase(87)），补录 plan/memo 提交追踪。
- [00:16] Item: 启动 Phase 88，按“所有重试契约”要求扩展 recovery 配置，覆盖 stream idle / turn recovery / in-progress 全参数。
- [00:22] Item: 完成全量参数接入与服务层替换，新增测试 `does not retry idle timeout when configured retry budget is 0`。
- [00:23] Item: 回归验证通过：`bun run typecheck`、`bun test`（162 pass）、`cargo check`。
- [00:24] Item: 提交 `31425de`（phase(88)），完成所有重试契约配置化与落盘联动。
- [00:24] Item: 提交 `e00eafc`（phase(88)），补录 plan/memo 提交追踪。
- [00:26] Item: 启动 Phase 89，目标为处理“仅用户消息无助手结果”场景：自动恢复 + 单按钮重试。
- [00:31] Item: 服务层新增 `recoverPendingAssistant` 与 `retryExistingUserMessage`，支持补拉恢复和无重复 user 重试。
- [00:33] Item: chat-shell 接入 pending 自动恢复与头部 `重试` 按钮；点击先补拉，失败再重发原用户文本（附件提示重传）。
- [00:35] Item: 新增并通过回归测试：`recoverPendingAssistant` 补拉路径；全量 `bun test` 通过（163 pass）。
- [01:16] Item: 提交 `0b5b244`（phase(89)），完成 pending 轮次恢复与单按钮重试链路。
- [03:02] Item: 启动 Phase 90，复盘“手动终止后点击重试卡在 turn_is_still_in_progress”的链路并确认本地会话仅有 user 消息。
- [03:05] Item: 服务层 `recoverPendingAssistant` 增加 `inProgress` 语义，并在 in-progress 下按配置做短轮询补拉。
- [03:07] Item: chat-shell `重试` 流程改为：pending 且会话仍处理中时不重发，只提示“上一轮仍在处理中，请稍后点击重试”。
- [03:09] Item: 统一 `turn_in_progress` 错误文案为中文提示，避免直接暴露网关内部字符串。
- [03:10] Item: 新增单测 `reports pending in_progress when pending assistant has not landed yet`，并通过 `bun run typecheck`、`bun test`（164 pass）。
- [03:12] Item: 提交 `c2e3d1e`（phase(90)），完成 pending 重试防误重发与 in-progress 语义修正。
- [03:26] Item: 启动 Phase 91，处理“上轮一直处理中导致重试长期无效”问题。
- [03:28] Item: 新增 pending 超时兜底：`in_progress` 持续超过 30 秒后，`重试` 自动脱离旧 `session_id` 发起自愈重试（仍复用原 user 轮次）。
- [03:30] Item: 回归验证通过：`bun run typecheck`、`bun test`（164 pass）。
- [03:31] Item: 提交 `8336a56`（phase(91)），完成 pending 卡死会话的脱离 session 自愈重试。
- [03:36] Item: 启动 Phase 92，按反馈移除顶部状态噪音：重试按钮下移到消息区，状态仅在对话区展示。
- [03:40] Item: 恢复链路新增 preview 渲染：`recoverPendingAssistant` 支持返回 in-progress 文本片段，chat-shell 自动轮询并实时展示。
- [03:42] Item: `TypingIndicator` 新增自定义 label，pending 恢复阶段显示“同步中”。
- [03:43] Item: 新增单测 `returns preview content while pending assistant is still streaming` 并通过回归（`bun run typecheck`、`bun test` 165 pass）。
- [03:45] Item: 提交 `c967971`（phase(92)），完成 pending 恢复可见化与重试交互降噪。
- [03:48] Item: 真实网关探针（提权）验证：`sess_0e29a418-b468-4269-b797-8261f80a274f` 在 30s 内持续 `in_progress=true` 且 `/messages` 始终为空。
- [03:50] Item: 启动 Phase 93，补齐 completion 后入库延迟补拉窗口（state 完成后追加 3 次 messages 重试）。
- [03:52] Item: 新增单测 `recovers after completion when session messages become visible with delay`，回归通过（`bun run typecheck`、`bun test` 166 pass）。
- [03:53] Item: 真实探针复现“同步中长期无内容”问题：`abort -> recover` 后持续 `in_progress=true` 且 `preview/messages` 为空，单次 detached 不足以恢复。
- [03:54] Item: 启动 Phase 94，将自动 detached 从单次触发升级为“限次 + 冷却”的连续自愈（18s 起、15s 间隔、最多 12 次）。
- [03:55] Item: 抽离 `pending-recovery-auto-detach.ts` 判定逻辑，并在 chat-shell 接入，避免 effect 内分支漂移。
- [03:56] Item: 新增 `pending-recovery-auto-detach.test.ts`（6 用例）覆盖阈值、冷却、次数上限、message 切换重置。
- [03:57] Item: 回归验证通过：`bun run typecheck`、`bun test`（172 pass）。
- [03:58] Item: 提权真实探针通过：`bun /tmp/pending_recovery_real_probe_v2.ts` 在第 6 次自动 detached 后成功恢复 assistant 消息。
- [03:59] Item: 提交 `2904238`（phase(94)），完成 pending 自动 detached 连续自愈与判定单测。
- [04:07] Item: 定位 `南京当前及近期天气预报` 会话漂移：本地 conversation 锚点已切到 `sess_b7dc...`，符合 pending detached 触发后重置 session 的链路特征。
- [04:10] Item: 启动 Phase 95，新增动态阈值：已有 assistant 锚点会话自动 detached 阈值提升到 90s，首轮无锚点仍维持 18s。
- [04:11] Item: 手动 `重试` 在 `in_progress` 且未超阈值时改为“继续同步”，不再立即 detached，避免早切 session。
- [04:12] Item: 回归验证通过：`bun run typecheck`、`bun test`（172 pass）。
- [04:13] Item: 提交 `e66cf29`（phase(95)），完成 anchored 会话延迟 detached 与手动重试防漂移。
- [04:14] Item: 基于网关证据复盘：旧会话 `sess_c8d8...` 的 `turn_76168092...` 长期 in_progress；新会话 `sess_b7dc...` 的 `turn_03149ea7...` 完成，确认漂移由 detached 新会话触发。
- [04:16] Item: 启动 Phase 96，移除 detached 恢复路径：删除 `forceDetachedRetry`、自动 detached effect、detached 判定文件及其单测。
- [04:18] Item: `recoverPendingAssistant` 改为 turn-first：先看 `state.active_client_turn_id`，再查 `turns` 决定“补拉/继续同步/可重发”。
- [04:20] Item: 新增服务层单测：active turn completed 可恢复、active turn not_found 返回可重发态。
- [04:21] Item: 回归验证通过：`bun run typecheck`、`bun test`（168 pass）。
- [04:22] Item: 提交 `df6d587`（phase(96)），完成同 session 恢复主链路与 turn-first 判定。
- [04:43] Item: 启动 Phase 97，修复“重启后 pending 仅显示重试按钮、不自动同步”的回归。
- [04:45] Item: 新增 `pending-recovery-bootstrap` 预算跟踪，并在 chat-shell 启动恢复/轮询恢复链路接入 `none` 结果有限重试。
- [04:46] Item: 新增单测覆盖预算消耗、消息切换重置、会话清理；回归验证通过（`bun run typecheck`、`bun test` 171 pass）。
- [04:47] Item: 提交 `44efc3d`（phase(97)），完成启动恢复 `none` 结果的自动同步窗口修复。
- [05:23] Item: 启动 Phase 98，修复“上游已生成但 messages 延迟可见时，点击重试误触发第三轮”问题。
- [05:25] Item: `recoverPendingAssistant` 增加锚点前进保护：`state.last_response_id` 领先本地锚点且消息暂未可见时，返回 `inProgress=true` 持续同步。
- [05:26] Item: 新增单测 `keeps syncing when session anchor advanced but messages are not visible yet`；回归通过（`bun run typecheck`、`bun test` 172 pass）。
- [05:27] Item: 提交 `00a514a`（phase(98)），完成 pending 误重发防护。
- [05:32] Item: 启动 Phase 99，定位 `Friendly/友好调情开场` 会话恢复失败链路：网关 `messages?after_response_id` 返回 `invalid_after_response_id`。
- [05:34] Item: `queryGatewaySessionMessages` 增加 `invalid_after_response_id` 自动回退全量查询；`recoverPendingAssistant` 增加 pending user 时间阈值过滤，避免误选旧 assistant。
- [05:35] Item: 新增单测 `recovers pending assistant when after_response_id is invalid by falling back to full session messages`；回归通过（`bun run typecheck`、`bun test` 173 pass）。
- [05:36] Item: 提交 `03a09af`（phase(99)），完成 invalid_after_response_id 恢复链路修复。
- [05:45] Item: 启动 Phase 100，按产品交互要求移除 pending 手动“重试”按钮，改为仅系统自动恢复。
- [05:46] Item: 删除 `handleRetryPendingTurn` 与 `parseRetryableUserMessageContent`，并将终止文案改为“已终止，正在自动同步结果。”。
- [05:47] Item: 回归验证通过（`bun run typecheck`、`bun test` 173 pass）；提交 `19f9c71`（phase(100)）。
- [06:12] Item: 启动 Phase 101，目标为处理“锚点已前进但消息长期缺失”极端场景，改为系统自动同轮重发。
- [06:14] Item: 服务层 `recoverPendingAssistant` 新增 `retrySend` 语义，stale 缺失超过 30s 时返回自动重发信号。
- [06:16] Item: chat-shell 接入 `retrySend` 自动重发：从 pending user 内容提取文本并调用 `retryExistingUserMessageId`，且每条 pending user 仅自动重发一次。
- [06:18] Item: 新增单测 `requests retry send when anchor advanced but missing message stays stale`，并更新 `keeps syncing...` 用例时间戳分支。
- [06:19] Item: 回归验证通过（`bun run typecheck`、`bun test` 174 pass）；提交 `898233c`（phase(101)）。
- [07:26] Item: 启动 Phase 102，目标为彻底移除 mini 模型依赖（前台不展示、后台模型目录不消费）。
- [07:28] Item: `catalog.ts` 删除 `openai/gpt-5-mini` 默认项，并在 remote/stored normalize 阶段统一过滤 mini（`type=mini` 或 id 含 mini）。
- [07:29] Item: `model-selector.tsx` 移除 mini 特判与回退副作用，改为直接渲染已过滤模型集合。
- [07:31] Item: 更新 `catalog` 单测，验证 payload 中 mini 会被过滤；回归通过（`bun run typecheck`、`bun test` 174 pass）。
- [07:33] Item: 上游模型列表探测：按 `~/.nbsearch/config.toml` 请求 8787/8788 的 `/v1/models/tauri_chat_models`，当前均连接失败（网关未启动）。
- [07:35] Item: 提交 `46190f5`（phase(102)），完成 mini 模型目录与选择器依赖移除。
- [07:48] Item: 启动 Phase 103，按上游已移除 mini 的现状清理本地 mini 专属冗余分支。
- [07:49] Item: 删除 `catalog.ts` 中 `isMiniModelCandidate` 与 normalize 分支调用，模型解析回归通用逻辑。
- [07:50] Item: 更新 `catalog` 单测文案与输入数据，移除 mini 特判语义；回归通过（`bun run typecheck`、`bun test` 174 pass）。
- [07:52] Item: 提交 `ceb639e`（phase(103)），完成 mini 专属 normalize 冗余分支清理。
- [08:18] Item: 启动 Phase 104，目标为下线 DeepSearch 开关/请求触发与 `has_deep_search` 会话标记，同时保留深度思考专用视图与 citations。
- [08:24] Item: 已移除输入区 DeepSearch 按钮与发送参数透传；`chat-shell` 删除 deepSearch 状态与 expert 自动切换耦合。
- [08:29] Item: 已移除服务层 `x_grok.deep_search` 发送逻辑，并删除 `SendChatTurnInput.deepSearch`。
- [08:33] Item: 已删除会话 `has_deep_search` 链路（domain/sqlite/migration/sidebar 图标）。
- [08:36] Item: 按反馈保留深度思考专用视图，避免回退旧领导者样式；并将面板内部组件命名改为中性 `Research*`。
- [08:38] Item: 回归验证通过：`bun run typecheck`、`bun test`（171 pass）。
- [08:40] Item: 提交 `5a6c0f4`（phase(104)），完成 DeepSearch 触发与会话标记下线并保留专用视图样式。
- [08:44] Item: 提交 `4adde2a`（phase(104)），补录 Phase 104 的 plan/memo 记录。
- [08:47] Item: 启动 Phase 105，按反馈将输入框适当上移（DeepSearch icon 下线后视觉补偿）。
- [08:48] Item: 输入区微调完成：`chat-input` 外层 `pb-4 -> pb-5`。
- [08:50] Item: 本地库结构收口：已在 `~/.nbsearch/data/chat-app.db` 执行 `ALTER TABLE conversations DROP COLUMN has_deep_search` 并通过 `PRAGMA table_info` 校验。
- [08:57] Item: 回归验证通过：`bun run typecheck`、`bun test`（171 pass）。
- [08:58] Item: 提交 `2878fea`（phase(105)），完成输入框上移微调。
- [09:12] Item: 启动 Phase 106，目标为“有多 Agent 时强制走多代理样式”，避免 research timeline 覆盖多代理分组视图。
- [09:14] Item: 新增 `mergeReasoningRolloutIds`，合并 `summary.rolloutIds` 与 `research.uiLayout.rolloutIds` 后进行 agent 判定。
- [09:15] Item: `StructuredReasoningPanel` 已改为仅在单代理时展示 research timeline，多代理时始终回退到 AgentSection 分组样式。
- [09:16] Item: 新增并通过单测 `multi-agent rollout merge`（2 条），并完成回归：`bun run typecheck`、`bun test`（173 pass）。
- [09:17] Item: 提交 `7209444`（phase(106)），完成多 Agent 优先样式切换。
- [09:31] Item: 启动 Phase 107，目标为修复多代理样式与 research details 杂糅问题，多 Agent 下不再展示 details 卡片。
- [09:33] Item: 新增 `shouldShowResearchDetails` 并接入 `StructuredReasoningPanel/ReasoningDrawer`，仅单代理且无 timeline 时显示 details。
- [09:34] Item: 新增单测 `hides research details when rendering multi-agent view`，并完成回归：`bun run typecheck`、`bun test`（174 pass）。
- [09:35] Item: 提交 `69132b7`（phase(107)），完成多代理视图 details 隔离。
- [09:48] Item: 启动 Phase 108，目标为彻底移除“工具链”模块（UI + 类型 + 统计逻辑），不做隐藏保留。
- [09:50] Item: 已删除 `ToolChainSection`、`StructuredReasoningToolChainItem`、`StructuredReasoningSummary.toolChain` 与 `buildStructuredReasoningSummary` 的工具链计数逻辑。
- [09:51] Item: 已清理 `structured-reasoning.test.ts` 中 toolChain 相关用例；回归通过 `bun run typecheck`、`bun test`（172 pass）。
- [09:52] Item: 提交 `8c85d00`（phase(108)），完成工具链全链路移除。

- [10:06] Item: 启动 Phase 109，新增版本号批量更新脚本，支持输入 `0.1.2` 或 `v0.1.2`。
- [10:08] Item: 新增 `scripts/bump-version.sh`，统一改写 `package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`，并支持 `NBSEARCH_ROOT` 临时根目录覆盖。
- [10:10] Item: 已完成验证：`sh -n scripts/bump-version.sh` 通过；临时目录执行 `NBSEARCH_ROOT=... sh scripts/bump-version.sh 0.1.2` 后，三处版本均更新为 `0.1.2`。
- [10:11] Item: 提交 `1ec9222`（phase(109)），新增版本号批量更新脚本。

## Commits

- `53e059c`: phase(85): harden in-progress turn recovery and failure code
- `0830ab6`: phase(85): backfill plan and memo commit trace
- `7d4c0f9`: phase(86): make in-progress retry configurable via config toml
- `80a2f13`: phase(86): backfill plan and memo commit trace
- `ad744ea`: phase(87): persist recovery defaults on first gateway save
- `b91d65e`: phase(87): backfill plan and memo commit trace
- `31425de`: phase(88): make all retry contracts configurable via recovery settings
- `e00eafc`: phase(88): backfill plan and memo commit trace
- `0b5b244`: phase(89): add pending turn recovery and single retry flow
- `c2e3d1e`: phase(90): harden pending retry when session turn is in progress
- `d79d82a`: phase(90): backfill plan and memo commit trace
- `8336a56`: phase(91): add detached retry fallback for stalled pending turns
- `3ce1a5f`: phase(91): backfill plan and memo commit trace
- `c967971`: phase(92): show pending recovery output inline and simplify retry ui
- `cecae90`: phase(92): backfill plan and memo commit trace
- `ff3f159`: phase(93): recover pending turns after completion visibility lag
- `b4eb8b6`: phase(93): backfill plan and memo commit trace
- `2904238`: phase(94): repeat detached pending recovery retries with cooldown
- `0b620f7`: phase(94): backfill plan and memo commit trace
- `e66cf29`: phase(95): delay detached recovery for anchored sessions
- `fb7c62b`: phase(95): backfill plan and memo commit trace
- `df6d587`: phase(96): enforce same-session pending recovery flow
- `44efc3d`: phase(97): keep startup pending recovery syncing after none result
- `00a514a`: phase(98): prevent pending resend when session anchor already advanced
- `03a09af`: phase(99): recover pending turn after invalid_after_response_id
- `19f9c71`: phase(100): remove manual pending retry button and rely on auto sync
- `898233c`: phase(101): auto resend stale pending turns without manual retry
- `46190f5`: phase(102): remove mini model dependency from catalog and selector
- `ceb639e`: phase(103): remove mini-only normalization branches
- `5a6c0f4`: phase(104): remove deepsearch trigger and conversation flag
- `4adde2a`: phase(104): backfill plan and memo records
- `2878fea`: phase(105): nudge composer upward after toggle removal
- `7209444`: phase(106): prioritize multi-agent reasoning view
- `69132b7`: phase(107): hide research details in multi-agent view
- `8c85d00`: phase(108): remove reasoning toolchain UI and data path
- `1ec9222`: phase(109): add version bump script
- `5e39c11`: phase(110): validate release tag version before build
- `6dc6613`: phase(111): bump desktop version to 0.1.1

## Notes

- 无
