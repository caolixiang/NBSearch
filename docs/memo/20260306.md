# 20260306 Memo

## Item

- [00:01] Item: 启动 Phase 85，目标为补齐 `turn in_progress` 长尾恢复策略，减少会话在恢复窗口内的误失败。
- [00:02] Item: 服务层新增 `TurnRecoveryOutcome.in_progress`，并在 `attemptTurnRecovery` 中接入 `/state` 活跃 turn 一致性校验。
- [00:02] Item: `streamTurn` 已在 transport/http/no_body/stream 异常分支支持 `in_progress` 等待重试；超限返回 `turn_in_progress`。
- [00:03] Item: 新增两条单测：`in_progress -> retry -> completed`、`in_progress` 超限返回 `turn_in_progress`。
- [00:03] Item: 执行回归验证：`bun run typecheck`、`bun test` 全部通过（161 pass）。
- [00:02] Item: 提交 `53e059c`（phase(85)），完成 in-progress 恢复策略与失败码收口。
- [00:04] Item: 提交 `0830ab6`（phase(85)），补录 plan/memo 提交追踪。
- [00:06] Item: 启动 Phase 86，目标为将 `turn in_progress` 重试预算与等待时长改为 `config.toml` 可配置（不新增 UI）。
- [00:08] Item: 完成配置链路：Tauri `read_gateway_config` 增加 `turnInProgressRetryMaxAttempts/turnInProgressRetryDelayMs`，前端 `AppConfig` 与服务层已接入。
- [00:09] Item: 更新测试并验证配置覆盖：`turnInProgressRetryMaxAttempts=0` 时立即返回 `turn_in_progress`，全量 `bun test` 通过（161 pass）。
- [00:11] Item: 提交 `7d4c0f9`（phase(86)），完成 in-progress 重试参数配置化（仅读 config.toml，无 UI）。

## Commits

- `53e059c`: phase(85): harden in-progress turn recovery and failure code
- `0830ab6`: phase(85): backfill plan and memo commit trace
- `7d4c0f9`: phase(86): make in-progress retry configurable via config toml

## Notes

- 无
