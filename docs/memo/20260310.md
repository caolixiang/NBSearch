# 20260310 Memo

## Item

- [08:00] Item: 启动 Phase 186，排查 `50个高质量国内外AI X博主推荐` 中孤立数字 `1` 的来源；已确认原始消息正常，问题在 markdown 归一化把两位数列表拆坏。
- [08:08] Item: 完成 Phase 186，已将列表换行补全规则收紧为“不匹配编号内部数字”，并新增 `10.`、`11.`、`12.` 连续列表回归测试；`bun test` 与 `bun run typecheck` 全部通过。
- [08:20] Item: 启动 Phase 187，继续排查同一标题下中文段落 `36-50` 被显示为 `1-15` 的问题；已确认上游原始返回为 `36.` 开始，问题在 `MarkdownBody` 丢失 `ol[start]`。
- [08:32] Item: 完成 Phase 187，已让 `MarkdownBody` 的列表渲染器透传 parser props，并新增 `MarkdownBody` 静态渲染测试，锁住 `<ol start="36">` 输出；`bun test` 与 `bun run typecheck` 通过。
- [08:40] Item: 启动 Phase 188，补充仓库协作规则；要求发版前必须先调用 `scripts/bump-version.sh`，避免手工改多个版本文件。
- [08:44] Item: 完成 Phase 188，已在 `AGENTS.md` 增加发版规则，并同步更新当日 `plan` 与 `memo`。
- [08:46] Item: 记录 Phase 188 提交 `e8aaacc`，对应规则变更与当日文档已一并落盘。
- [08:55] Item: 启动 Phase 189，调整设置页网关保存按钮状态；目标为保存成功后显示“已保存”并禁用，待输入变更后恢复可点。
- [09:05] Item: 完成 Phase 189，已为网关配置增加已保存快照、抽出按钮状态纯函数并新增单测；`bun test` 与 `bun run typecheck` 通过。
- [09:08] Item: 记录 Phase 189 提交 `e6b54ec`，对应设置页按钮状态与测试已落盘。
- [09:20] Item: 启动 Phase 190，在设置页新增“个性化配置”Tab；本轮将时区配置接入 `config.toml` 并增加显式保存按钮。
- [09:33] Item: 完成 Phase 190，已新增时区 helper、Tauri `save_personalization_config` 命令和设置页时区保存交互；`bun test`、`bun run typecheck`、`cargo check` 通过。
- [09:44] Item: 记录 Phase 190 提交 `d162a18`，对应个性化配置时区保存与配置落盘已提交。
- [10:05] Item: 启动 Phase 191，围绕“首轮时区上下文 + 每轮当前时间注入”做真实网关探针；已确认单纯每轮 `instructions` 会被旧时间污染，需改为首轮 XML instruction + 每轮隐藏时间后缀。
- [10:18] Item: 完成 Phase 191，已新增 `gateway-turn-context` helper，并在请求构造层接入首轮 locale instruction 与每轮 authoritative time suffix；`bun test`、`bun run typecheck` 与真实本地网关探针通过。
- [10:47] Item: 记录 Phase 191 提交 `ce7fc7e`，对应首轮 locale instruction 与每轮隐藏时间上下文已提交。
- [10:58] Item: 启动 Phase 192，优化个性化配置时区提示；移除 `UTC` 预置项，并将“IANA 时区”提示改成普通用户可理解的文案。
- [11:02] Item: 完成 Phase 192，已移除 `UTC` 默认选项并补充单测锁住历史自定义值回显；`bun test` 与 `bun run typecheck` 通过。
- [11:07] Item: 记录 Phase 192 提交 `36ad7a2`，对应时区提示优化与 `UTC` 预置项移除已提交。

## Commits

- `69f0db6`: `phase(186): preserve two-digit ordered markdown lists`
- `6119ad9`: `phase(187): preserve ordered list start in markdown rendering`
- `e8aaacc`: `phase(188): require bump-version script before release`
- `e6b54ec`: `phase(189): persist gateway save button state`
- `d162a18`: `phase(190): add personalization timezone settings`
- `ce7fc7e`: `phase(191): inject locale and turn time context`
- `36ad7a2`: `phase(192): simplify timezone settings copy`

## Notes

- 无
