# 20260307 Memo

## Item

- [00:02] Item: 启动 Phase 114，将 regenerate 的旧分支剪裁从 `chat-shell` 下沉到服务层，避免 completed 事件后再二次补交流程造成竞态。
- [00:05] Item: `GrokChatService` 已在 regenerate 完成时按目标 assistant `responseId` 截断旧消息分支，并直接落新 assistant 与 anchors。
- [00:07] Item: `chat-shell` regenerate 完成分支已改为只刷新列表；`visibleMessages` 在流式期间会临时隐藏 anchor 后旧分支，并将流式 assistant 插入到 anchor 后。
- [00:10] Item: 新增服务层回归测试：两轮正常发送后重生成第一轮，仓储最终仅保留第一轮 user 与新 assistant；回归通过 `bun run typecheck`、定向 `bun test`、全量 `bun test`（177 pass）。
- [02:11] Item: 启动 Phase 115，使用 `scripts/bump-version.sh v0.1.2` 将 `package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml` 统一升级到 `0.1.2`。
- [02:11] Item: 完成版本校验与构建确认：`sh -n scripts/bump-version.sh`、`./scripts/bump-version.sh v0.1.2`、`bun run build` 全部通过；构建仅有既有 Vite warning，无新增失败。
- [02:12] Item: 提交 `c4a7bf1`（phase(115)），完成 0.1.2 版本提升与发版前文档收口。

## Commits

- `9cc197e`: phase(114): move regenerate truncation into service layer
- `c4a7bf1`: phase(115): bump version to 0.1.2

## Notes

- 无
