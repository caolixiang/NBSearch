# 20260426 Memo

## Item

- [16:31] Item: 启动 Phase 1，目标为修复同步网关模型后 `Expert` 档误显示为 `NBSearch Max` 的问题；当前定位为本地模型选择器按 `grok-4.20` 版本号粗粒度映射导致。
- [16:34] Item: 完成 Phase 1 实现与验证：模型选择器已按专家/最新档区分 `grok-4.20-0309-reasoning` 与 `grok-4.20-expert`；`bun test src/components/model-selector.test.ts`、全量 `bun test` 与 `bun run typecheck` 均通过。
- [16:44] Item: 启动 Phase 2，目标为 Expert / Max 完成后补齐缺失标题；优先保留上游标题，缺失时走 OpenAI 兼容网关总结，未配置或失败时回退用户首句前 10 个字。
- [16:50] Item: 完成 Phase 2 实现与验证：新增 `gateway-title-generation` helper 并在 `GrokChatService` 完成持久化前接入；定向测试、全量 `bun test`、`bun run typecheck` 与 `git diff --check` 均通过。
- [18:10] Item: 启动 Phase 3，目标为按用户要求完成 `v0.2.5` 本地发版打包；已停止本地 Vite dev server，工作区启动前干净。
- [18:14] Item: 完成 Phase 3 发版打包验证：版本已统一升至 `0.2.5`，`bun run release:preflight` 与 `bun run tauri:build` 均通过，产物为 `NBSearch.app` 和 `NBSearch_0.2.5_aarch64.dmg`。
- [18:20] Item: 启动 Phase 4，按用户确认创建并推送 `v0.2.5` tag 触发线上 release workflow；已补录 Phase 3 commit hash。
- [19:13] Item: 完成 Phase 4：已创建并推送 `v0.2.5` tag，远端 tag 指向 `5513cd8`，GitHub release workflow `24954315043` 已进入 `in_progress`。

## Commits

- `4390f3b`: 修复同步后 NBSearch Expert / Max 展示名误合并问题，并补充模型选择器回归测试。
- `63416a5`: 回填 Phase 1 计划与 Memo 记录，归档模型展示名修复验证结果。
- `2a07ffa`: 为 Expert / Max 缺失标题增加 OpenAI 兼容网关总结与用户首句十字回退，并补充 helper / 服务集成测试。
- `4873a6b`: 回填 Phase 2 计划与 Memo 记录，归档 Expert / Max 标题补齐验证结果。
- `e2cb1c6`: 将版本统一升到 `0.2.5`，完成 `release:preflight` 与 Tauri release 打包，并归档产物路径。
- `5513cd8`: 补录 Phase 4 tag 触发计划并创建 `v0.2.5` release tag。
- `待提交`: `phase(4): archive v0.2.5 release trigger status`

## Notes

- Phase 4 状态归档提交为本轮最后一笔提交，hash 无法在同一提交内自引用写入；实际 hash 将在最终回复给出，并留待下一轮补录。
