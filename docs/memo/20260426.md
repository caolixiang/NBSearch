# 20260426 Memo

## Item

- [16:31] Item: 启动 Phase 1，目标为修复同步网关模型后 `Expert` 档误显示为 `NBSearch Max` 的问题；当前定位为本地模型选择器按 `grok-4.20` 版本号粗粒度映射导致。
- [16:34] Item: 完成 Phase 1 实现与验证：模型选择器已按专家/最新档区分 `grok-4.20-0309-reasoning` 与 `grok-4.20-expert`；`bun test src/components/model-selector.test.ts`、全量 `bun test` 与 `bun run typecheck` 均通过。
- [16:44] Item: 启动 Phase 2，目标为 Expert / Max 完成后补齐缺失标题；优先保留上游标题，缺失时走 OpenAI 兼容网关总结，未配置或失败时回退用户首句前 10 个字。
- [16:50] Item: 完成 Phase 2 实现与验证：新增 `gateway-title-generation` helper 并在 `GrokChatService` 完成持久化前接入；定向测试、全量 `bun test`、`bun run typecheck` 与 `git diff --check` 均通过。

## Commits

- `4390f3b`: 修复同步后 NBSearch Expert / Max 展示名误合并问题，并补充模型选择器回归测试。
- `63416a5`: 回填 Phase 1 计划与 Memo 记录，归档模型展示名修复验证结果。
- `待提交`: `phase(2): add expert max title fallback`

## Notes

- 无
