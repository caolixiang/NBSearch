# 20260303 Memo

## Item

- [02:24] Item: 按 `docs/plan/20260303.md` 启动执行，先完成“生成中可切换会话且切回可见实时进度”的第一批实现。
- [02:24] Item: `chat-shell` 改为按 `conversationId` 缓存消息与推理映射；新增 `streamingConversationId`，隔离流式渲染作用域，避免跨会话串流显示。
- [02:24] Item: 更新当日计划文档，补充 Phase 2 执行记录与测试记录（`bun run typecheck` 通过）。
- [02:26] Item: 执行 `bun run typecheck` 与 `bun run build`，均通过；记录并保留构建 warning（非阻断）。
- [02:48] Item: 完成会话级并行流式重构：流式状态改为 `streamingStateByConversationId`，每个会话独立维护文本/推理/计时/停止控制器。
- [02:48] Item: 放开 A 流式时切到 B 继续发送，`ChatInput` loading 改为只绑定当前会话；停止按钮只中止当前会话流。
- [02:48] Item: 再次执行 `bun run typecheck` 与 `bun run build`，均通过；更新 `docs/plan/20260303.md` 将 Phase 2 标记为 done。
- [03:05] Item: 新增 `conversation-streaming-state` 工具模块，收敛会话流式状态读写逻辑（get/is/upsert/patch/remove）。
- [03:05] Item: `chat-shell` 接入该工具模块，降低并行流式相关状态变更的重复逻辑和回归风险。
- [03:05] Item: 新增并通过单元测试 `src/features/chat/conversation-streaming-state.test.ts`（5/5），并更新计划文档测试记录。
- [03:16] Item: 引入 `zustand` 并新增 `chat-conversation-store.ts`，将会话级状态（active/draft/messages/streaming/reasoning/error）迁移到 store。
- [03:16] Item: `chat-shell` 改为通过 store action 读写会话状态，局部 UI 状态继续保留在组件内。
- [03:16] Item: 新增并通过 `chat-conversation-store.test.ts`；执行 `bun test`（两文件 9/9）、`bun run typecheck`、`bun run build` 全通过。
- [03:19] Item: 回归执行全量 `bun test`（108/108 通过）并复验 `bun run typecheck`、`bun run build`，确认 Zustand 迁移可提交。
- [03:49] Item: 通过本地 `~/.nbsearch/data/chat-app.db` 复现 `嵇鹏飞：抖音电商转型核心操盘手` 渲染问题，确认为标题内 `-` 被误拆成列表。
- [03:49] Item: 移除 markdown 标题误拆规则并新增两条单测（跨行年份区间、标题年份区间）；执行 `bun test`（110/110）与 `bun run typecheck`、`bun run build` 全通过。
- [03:49] Item: 调整聊天区滚动收起策略（仅上滚收起；向下/近底保持展开），修复“画一只老母鸡”场景底部输入框乒乓弹跳。
- [04:00] Item: 在滚动收起逻辑中改为“底部锁定区 + 滞回阈值”几何方案：底部附近稳定展开，离开底部后恢复收起交互，兼顾体验与交互特性。
- [04:15] Item: 增强 Markdown 标题层级渲染：`h1~h4` 调整为 `3xl/2xl/xl/lg` 并补齐 `h4` 组件，拉开与正文字号差异以提升浏览体验。
- [04:15] Item: 执行 `bun run typecheck` 与 `bun test src/components/markdown-content.test.ts` 回归，均通过。
- [04:43] Item: 根据交互反馈将输入框收起改为“仅上滑触发”，下滑与底部锁定区保持展开，避免阅读过程中误收起。
- [04:43] Item: 执行 `bun run typecheck` 与全量 `bun test` 回归，均通过。
- [04:49] Item: 新增 Markdown 图片预览：点击图片打开原图弹层，点击图片外区域自动关闭，并拦截图片点击默认跳转。
- [04:49] Item: 执行 `bun run typecheck` 与全量 `bun test` 回归，均通过（110/110）。
- [04:53] Item: 修复图片预览未居中问题：弹层改为全屏 flex 居中布局，并调整预览图容器为视口内自适应居中显示。
- [04:53] Item: 执行 `bun run typecheck` 与 `bun test src/components/markdown-content.test.ts` 回归，均通过。
- [04:57] Item: 进一步修正预览定位：以聊天主面板为中心区域进行居中布局，并增加最大宽度限制（<=1200px 且不超过聊天区宽度）。
- [04:57] Item: 执行 `bun run typecheck` 与全量 `bun test` 回归，均通过（110/110）。
- [05:01] Item: 按反馈继续收窄预览上限：最大宽度调整为 `min(960px, 聊天区约82%)`，并收紧最大高度留白，减少覆盖操作区域。
- [05:01] Item: 再次执行 `bun run typecheck` 与全量 `bun test` 回归，均通过（110/110）。
- [06:01] Item: 新一轮按优先级启动 2/3/4/5 协议与渲染优化，先落计划文档后执行；拆分为 Phase 10~13，要求分 Phase 提交与逐次 Memo 记录。
- [06:04] Item: 完成 Phase 10：流事件统一补齐 `meta(seq/ts/conversationId/responseId?)`，服务端发射改为统一元信息封装。
- [06:04] Item: 执行 `bun run typecheck` 与 `bun test` 回归，均通过（110/110）。
- [06:05] Item: 完成 Phase 11：`chat-shell` 对 `card_attachment` 事件改为节流合并，减少图片事件对文本流式渲染的阻塞。
- [06:05] Item: 在 `completed/failed` 前强制 flush 图片卡片缓冲，避免尾批丢失；执行 `bun run typecheck` 与 `bun test` 回归通过。
- [06:08] Item: 完成 Phase 12：在 `cardAttachment` 路径补齐中间态过滤（`progress < 100`、`-part-*`、`moderated/r_rated`），并跳过 `response.output_item.added` 的生成图。
- [06:08] Item: 新增 parser 单测覆盖上述场景；执行 `bun run typecheck`、`bun test src/infrastructure/chat/chunk-parsers.test.ts`、`bun test` 全通过。
- [06:22] Item: 完成 Phase 13：在 `streamTurn` 中新增 `heartbeat` 事件与空闲超时保护（20s），超时返回 `failed(stream_idle_timeout)`。
- [06:22] Item: 会话流式状态新增 `lastHeartbeatAt`，消费层记录最新心跳时间，为后续续流可视化打基础。
- [06:22] Item: 新增 `grok-chat-service` 测试覆盖正常流心跳与超时失败路径；执行 `bun test src/infrastructure/chat/grok-chat-service.test.ts`、`bun run typecheck`、`bun test` 全通过。
- [06:44] Item: 完成 Phase 14：`streamTurn` 增加空窗期超时一次重连（仅无可见输出时触发），并新增重连退避等待逻辑。
- [06:44] Item: 请求体补充 `previous_response_id`（来自 `anchors.lastResponseId`），重连场景保持同值传递，增强上下文一致性。
- [06:44] Item: 新增单测覆盖“首轮超时后重连成功 + previous_response_id 透传”；执行 `bun run typecheck` 与 `bun test` 全通过（116/116）。
- [07:02] Item: 完成 Phase 15：聊天页头部新增连接健康状态提示（连接稳定/连接波动/等待数据），基于 `lastHeartbeatAt` 动态计算。
- [07:02] Item: `stream_idle_timeout` 错误改为用户可读文案“连接超时，流式已中断，请重试。”，并与模型错误共存展示。
- [07:02] Item: 执行 `bun run typecheck` 与 `bun test` 全量回归通过（116/116）。
- [07:18] Item: 完成 Phase 16：新对话欢迎页牛图标放大，`WelcomeScreen` 改为通过 `GrokAvatar className` 覆盖尺寸到 `size-20`。
- [07:18] Item: `GrokAvatar` 新增可选 `className` 参数，保证默认 `sm/md/lg` 行为不变，仅为局部覆盖提供扩展点。
- [07:18] Item: 执行 `bun run typecheck` 与 `bun test` 全量回归通过（116/116）。
- [08:02] Item: 启动 Phase 17/18：实现按历史 assistant 节点重生成，触发后裁剪该节点后的旧分支消息（数据库与本地状态同步）。
- [08:02] Item: `streamTurn` 增加 regenerate 模式（`x_grok.regenerate + target_response_id`），并在该模式下跳过追加新用户消息。
- [08:02] Item: 执行 `bun run typecheck` 与 `bun test` 全量回归通过（117/117，Phase 17）。
- [08:19] Item: 完成 Phase 18：在 assistant 消息末尾新增重新生成按钮，并接入分支重生处理函数。
- [08:19] Item: 生成中状态下禁用重新生成按钮，避免 session 并发冲突。
- [08:19] Item: 执行 `bun run typecheck` 与 `bun test` 全量回归通过（117/117，Phase 18）。
- [16:03] Item: 完成 Phase 19：将系统回复末尾重新生成按钮替换为官方 SVG，更新为圆形 ghost 动作按钮风格。
- [16:03] Item: 新增悬停/聚焦文案“重新生成”（默认隐藏），并执行 `bun run typecheck` 与 `bun test` 全量回归通过（117/117）。
- [16:14] Item: 完成 Phase 20：在系统回复动作区新增“导出 PDF”按钮（位于重新生成旁），支持导出状态提示（导出中/已导出/导出失败）。
- [16:14] Item: 新增 `export-message-pdf` 工具，按 `title-N.pdf` 规则生成文件名并将回复内容渲染为 PDF 页面；Tauri 新增 `save_pdf_document` 命令落盘。
- [16:14] Item: 执行 `bun run typecheck`、`bun test`（120/120，含新增文件名规则单测）与 `cargo check`（src-tauri）回归，均通过。
- [16:15] Item: 完成 Phase 21：导出 PDF 升级为 DOM 富样式导出，支持带 Markdown 样式与图片分页写入，保留文本兜底。
- [16:15] Item: 新增 `html2canvas` 依赖，并在导出前等待消息图片加载，提升图片进入 PDF 的稳定性。
- [16:15] Item: 执行 `bun run typecheck` 与 `bun test` 全量回归（120/120）通过。
- [16:29] Item: 完成 Phase 22：针对“导出仍是纯文本”修复为双引擎捕获（`html-to-image` 优先，`html2canvas` 回退）。
- [16:29] Item: 文本兜底前增加 `normalizeAssistantMarkdown` 清洗，移除内部 `grok:render` 标签噪声。
- [16:29] Item: 执行 `bun run typecheck` 与 `bun test` 回归（120/120）通过。
- [16:36] Item: 完成 Phase 23：修复 PDF 空白页，新增“空白画布检测”并补充第三段回退捕获（html2canvas 非 foreignObject）。
- [16:36] Item: 导出捕获时注入前景/背景主题变量，降低样式解析异常导致的空白风险。
- [16:36] Item: 执行 `bun run typecheck` 与 `bun test` 回归（120/120）通过。
- [16:41] Item: 完成 Phase 24：将空白检测改为亮度范围（luma range）判定，防止灰白噪点页误判为有效内容。
- [16:41] Item: 执行 `bun run typecheck` 与 `bun test src/features/chat/export-message-pdf.test.ts` 回归通过。
- [16:45] Item: 完成 Phase 25：新增暗像素占比（dark pixel ratio）检测，进一步阻断伪有效白页进入 PDF。
- [16:45] Item: 执行 `bun run typecheck` 与 `bun test src/features/chat/export-message-pdf.test.ts` 回归通过。
- [16:49] Item: 完成 Phase 26：导出捕获强制浅色主题变量，在 html2canvas clone 阶段移除 dark 并覆盖根变量。
- [16:49] Item: 执行 `bun run typecheck` 与 `bun test src/features/chat/export-message-pdf.test.ts` 回归通过。
- [16:57] Item: 完成 Phase 27：导出改为离屏容器捕获（克隆消息内容后截图），隔离页面主题/动画/布局状态干扰。
- [16:57] Item: 执行 `bun run typecheck` 与 `bun test src/features/chat/export-message-pdf.test.ts` 回归通过。
- [20:52] Item: 启动并完成 Phase 28：将 PDF 导出有效性判定从“整图墨迹阈值”改为“分页级内容判定”，降低误判触发文本兜底概率。
- [20:52] Item: 导出捕获链路改为 `html-to-image` 优先，失败后回退 `html2canvas(foreignObject true/false)`，仅三段均失败才走文本兜底。
- [20:52] Item: 执行 `bun run typecheck`、`bun test src/features/chat/export-message-pdf.test.ts`、`bun test` 全通过（120/120）。
- [20:55] Item: 根据 `-v8.pdf` 复核结果（仍是文本兜底）继续修复：导出链路改为“先捕获原始消息节点，再回退离屏克隆节点”，避免离屏节点导致白图误判。
- [20:55] Item: 新增 `capturePageImagesFromTarget` 统一三段捕获逻辑（html-to-image + html2canvas 两模式），复用到 direct/offscreen 两条路径。
- [20:55] Item: 复验 `bun run typecheck`、`bun test src/features/chat/export-message-pdf.test.ts`、`bun test` 全通过（120/120）。
- [21:00] Item: 针对 `-v9.pdf` 仍回退文本的问题，增强兜底路径：优先从渲染态 DOM 提取 `innerText`，不再直接导出原始 markdown 语法。
- [21:00] Item: 兜底内容新增图片 URL 列表（去重）并保留 markdown 清洗次级兜底，提升导出可读性与可追溯性。
- [21:00] Item: 执行 `bun run typecheck`、`bun test src/features/chat/export-message-pdf.test.ts`、`bun test` 全通过（120/120）。
- [21:04] Item: 新增 Phase 30：实现“独立 Markdown 容器渲染并捕获”路径，作为主消息节点捕获失败后的富样式兜底。
- [21:04] Item: 导出链路升级为三级回退：主节点捕获 -> 独立容器捕获 -> 文本兜底。
- [21:04] Item: 修复导出模块类型问题并复验 `bun run typecheck`、`bun test src/features/chat/export-message-pdf.test.ts`、`bun test` 全通过（120/120）。
- [21:09] Item: 按“以 v11 为准”完成 Phase 31：精简 `export-message-pdf.ts` 为单一导出链路，移除历史空白判定与文本兜底。
- [21:09] Item: `buildMessagePdfBytes` 仅保留“独立 Markdown 容器渲染 + html-to-image 捕获 + PDF 分页”路径，导出失败直接抛错。
- [21:09] Item: 复验 `bun run typecheck`、`bun test src/features/chat/export-message-pdf.test.ts`、`bun test` 全通过（120/120）。
- [21:12] Item: 完成 Phase 32：调整消息动作栏显示规则，新增末轮 assistant 常显能力（历史轮次保持 hover/focus 显示）。
- [21:12] Item: `chat-shell` 新增最后一条非流式 assistant 识别；`ChatMessage` 增加 `actionsAlwaysVisible` 控制动作栏固定显示。
- [21:12] Item: 执行 `bun run typecheck` 与 `bun test` 全通过（120/120）。
- [21:17] Item: 完成 Phase 33：动作栏新增来源统计标签（`xxx_来源`），来源数由 `reasoningEvents.tool_result` 聚合计算。
- [21:17] Item: 点击来源标签可平滑定位到对应消息思考区域，并通过自定义事件打开该消息“思考结果”抽屉。
- [21:17] Item: 执行 `bun run typecheck` 与 `bun test` 全通过（120/120）。
- [21:24] Item: 完成 Phase 34：对齐官方图库样式，非生图多图统一为 `grid + gap-2 + h-52 + object-cover + rounded-lg`，移除双图不等宽和三图方块规则。
- [21:24] Item: 非生图单图改为官方风格 `w-full + object-contain + max-h-[448px] + rounded-lg`，不再使用最小宽度拉伸。
- [21:24] Item: 同步将来源标签文案修正为 `xxx 个来源`，并执行 `bun run typecheck` 与 `bun test` 全通过（120/120）。
- [21:38] Item: 完成 Phase 35：按官方两图样式继续收敛非生图双图布局，容器改为 `w-fit + mx-auto`，避免双图占满整行。
- [21:38] Item: 双图卡片补充 `max-h-56 + max-w-80` 约束，保持双图视觉高度一致并限制横向扩张；三图/生图路径保持不变。
- [21:38] Item: 执行 `bun run typecheck` 与 `bun test` 回归，均通过（120/120）。
- [21:39] Item: 完成 Phase 36：markdown 图片悬停光标从 `cursor-zoom-in` 调整为 `cursor-pointer`，改为手型提示可点击预览。
- [21:39] Item: 本次仅改样式类名，不改预览弹层、下载按钮和图片加载链路。
- [21:39] Item: 执行 `bun run typecheck` 回归，通过。
- [21:41] Item: 完成 Phase 37：按反馈将非生图双图容器再加宽一档，卡片宽度上限由 `max-w-80` 调整为 `max-w-88`。
- [21:41] Item: 保持双图 `w-fit + mx-auto` 与 `max-h-56` 规则不变，仅做宽度微调；三图/单图/生图路径不改。
- [21:41] Item: 执行 `bun run typecheck` 与 `bun test` 回归，均通过（120/120）。
- [21:44] Item: 完成 Phase 38：收紧最后一条消息到底部输入框间距，调整 `messageBottomSpacerPx` 公式为更小缓冲。
- [21:44] Item: spacer 计算由 `0.72 * inputHeight + 20` 改为 `0.62 * inputHeight + 14`，上限由 `180` 下调到 `164`。
- [21:44] Item: 执行 `bun run typecheck` 与 `bun test` 回归，均通过（120/120）。
- [21:45] Item: 补齐 `docs/plan` 与 `docs/memo` 的提交记录，追加 Phase 35~38 的 commit hash，确保文档可追溯。

## Commits

- `d11685f`: phase(2): support concurrent streaming across conversations
- `c4e5d63`: phase(2): add streaming state helpers and unit tests
- `e0408d9`: phase(3): migrate conversation state to zustand
- `840ce9f`: phase(4): fix markdown hyphen parsing and bottom scroll jitter
- `70a664b`: phase(5): enhance markdown heading hierarchy
- `6d8b597`: phase(6): adjust composer collapse to upward scroll only
- `887c36e`: phase(7): add markdown image preview with outside click close
- `e935f20`: phase(8): center markdown image preview modal
- `5b911cf`: phase(9): align preview to chat pane and cap modal size
- `2e69ff0`: phase(10): add stream event meta envelope
- `d24858d`: phase(11): throttle card attachment updates during streaming
- `d0cc35f`: phase(12): filter intermediate generated image cards
- `d3f6a41`: phase(13): add stream heartbeat and idle-timeout semantics
- `43c07e6`: phase(14): add idle-timeout reconnect and previous_response_id
- `7e3edaf`: phase(15): add streaming connection health indicator
- `0b2ac6b`: phase(16): enlarge welcome cow logo in new chat screen
- `dab49f6`: phase(17): add regenerate branching flow and message truncation
- `aa5d421`: phase(18): add assistant regenerate action button
- `9aba4a4`: phase(20): add assistant pdf export action with save dialog
- `42dc5c6`: phase(21): export assistant pdf with rendered markdown and images
- `606ebe1`: phase(21): finalize plan and memo commit records
- `16098b1`: phase(22): harden pdf export rendering with dual capture engines
- `ce43908`: phase(23): prevent blank pdf capture with layered fallbacks
- `271ca40`: phase(24): tighten blank-page detection for pdf export
- `09779cd`: phase(25): require visible ink for pdf page capture
- `4e1bfb1`: phase(26): force light theme variables for pdf capture
- `021c8a4`: phase(27): capture pdf from offscreen cloned message container
- `13fb1c6`: phase(35): tighten non-generated two-image gallery width
- `79ead47`: phase(36): use pointer cursor for markdown images
- `9ea59f9`: phase(37): slightly widen two-image markdown gallery
- `7d639b1`: phase(38): tighten message-to-composer bottom spacing

## Notes

- Phase 10~13 已进入收尾，下一步可按需要继续做“断线重连/续流”协议扩展。
