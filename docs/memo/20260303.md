# 20260303 Memo

## Item

- [02:24] Item: 按 `docs/plan/20260303.md` 启动执行，先完成“生成中可切换会话且切回可见实时进度”的第一批实现。
- [02:24] Item: `chat-shell` 改为按 `conversationId` 缓存消息与推理映射；新增 `streamingConversationId`，隔离流式渲染作用域，避免跨会话串流显示。
- [02:24] Item: 更新当日计划文档，补充 Phase 2 执行记录与测试记录（`bun run typecheck` 通过）。
- [02:26] Item: 执行 `bun run typecheck` 与 `bun run build`，均通过；记录并保留构建 warning（非阻断）。
- [02:48] Item: 完成会话级并行流式重构：流式状态改为 `streamingStateByConversationId`，每个会话独立维护文本/推理/计时/停止控制器。
- [02:48] Item: 放开 A 流式时切到 B 继续发送，`ChatInput` loading 改为只绑定当前会话；停止按钮只中止当前会话流。
- [02:48] Item: 再次执行 `bun run typecheck` 与 `bun run build`，均通过；更新 `docs/plan/20260303.md` 将 Phase 2 标记为 done。
- [03:05] Item: 新增 `conversation-streaming-state` 工具模块，收敛会话流式状态读写逻辑（get/is/upsert/patch/remove）。
- [03:05] Item: `chat-shell` 接入该工具模块，降低并行流式相关状态变更的重复逻辑和回归风险。
- [03:05] Item: 新增并通过单元测试 `src/features/chat/conversation-streaming-state.test.ts`（5/5），并更新计划文档测试记录。
- [03:16] Item: 引入 `zustand` 并新增 `chat-conversation-store.ts`，将会话级状态（active/draft/messages/streaming/reasoning/error）迁移到 store。
- [03:16] Item: `chat-shell` 改为通过 store action 读写会话状态，局部 UI 状态继续保留在组件内。
- [03:16] Item: 新增并通过 `chat-conversation-store.test.ts`；执行 `bun test`（两文件 9/9）、`bun run typecheck`、`bun run build` 全通过。
- [03:19] Item: 回归执行全量 `bun test`（108/108 通过）并复验 `bun run typecheck`、`bun run build`，确认 Zustand 迁移可提交。
- [03:49] Item: 通过本地 `~/.nbsearch/data/chat-app.db` 复现 `嵇鹏飞：抖音电商转型核心操盘手` 渲染问题，确认为标题内 `-` 被误拆成列表。
- [03:49] Item: 移除 markdown 标题误拆规则并新增两条单测（跨行年份区间、标题年份区间）；执行 `bun test`（110/110）与 `bun run typecheck`、`bun run build` 全通过。
- [03:49] Item: 调整聊天区滚动收起策略（仅上滚收起；向下/近底保持展开），修复“画一只老母鸡”场景底部输入框乒乓弹跳。
- [04:00] Item: 在滚动收起逻辑中改为“底部锁定区 + 滞回阈值”几何方案：底部附近稳定展开，离开底部后恢复收起交互，兼顾体验与交互特性。
- [04:15] Item: 增强 Markdown 标题层级渲染：`h1~h4` 调整为 `3xl/2xl/xl/lg` 并补齐 `h4` 组件，拉开与正文字号差异以提升浏览体验。
- [04:15] Item: 执行 `bun run typecheck` 与 `bun test src/components/markdown-content.test.ts` 回归，均通过。
- [04:43] Item: 根据交互反馈将输入框收起改为“仅上滑触发”，下滑与底部锁定区保持展开，避免阅读过程中误收起。
- [04:43] Item: 执行 `bun run typecheck` 与全量 `bun test` 回归，均通过。
- [04:49] Item: 新增 Markdown 图片预览：点击图片打开原图弹层，点击图片外区域自动关闭，并拦截图片点击默认跳转。
- [04:49] Item: 执行 `bun run typecheck` 与全量 `bun test` 回归，均通过（110/110）。
- [04:53] Item: 修复图片预览未居中问题：弹层改为全屏 flex 居中布局，并调整预览图容器为视口内自适应居中显示。
- [04:53] Item: 执行 `bun run typecheck` 与 `bun test src/components/markdown-content.test.ts` 回归，均通过。
- [04:57] Item: 进一步修正预览定位：以聊天主面板为中心区域进行居中布局，并增加最大宽度限制（<=1200px 且不超过聊天区宽度）。
- [04:57] Item: 执行 `bun run typecheck` 与全量 `bun test` 回归，均通过（110/110）。
- [05:01] Item: 按反馈继续收窄预览上限：最大宽度调整为 `min(960px, 聊天区约82%)`，并收紧最大高度留白，减少覆盖操作区域。
- [05:01] Item: 再次执行 `bun run typecheck` 与全量 `bun test` 回归，均通过（110/110）。
- [06:01] Item: 新一轮按优先级启动 2/3/4/5 协议与渲染优化，先落计划文档后执行；拆分为 Phase 10~13，要求分 Phase 提交与逐次 Memo 记录。
- [06:04] Item: 完成 Phase 10：流事件统一补齐 `meta(seq/ts/conversationId/responseId?)`，服务端发射改为统一元信息封装。
- [06:04] Item: 执行 `bun run typecheck` 与 `bun test` 回归，均通过（110/110）。
- [06:05] Item: 完成 Phase 11：`chat-shell` 对 `card_attachment` 事件改为节流合并，减少图片事件对文本流式渲染的阻塞。
- [06:05] Item: 在 `completed/failed` 前强制 flush 图片卡片缓冲，避免尾批丢失；执行 `bun run typecheck` 与 `bun test` 回归通过。
- [06:08] Item: 完成 Phase 12：在 `cardAttachment` 路径补齐中间态过滤（`progress < 100`、`-part-*`、`moderated/r_rated`），并跳过 `response.output_item.added` 的生成图。
- [06:08] Item: 新增 parser 单测覆盖上述场景；执行 `bun run typecheck`、`bun test src/infrastructure/chat/chunk-parsers.test.ts`、`bun test` 全通过。

## Commits

- `d11685f`: phase(2): support concurrent streaming across conversations
- `c4e5d63`: phase(2): add streaming state helpers and unit tests
- `e0408d9`: phase(3): migrate conversation state to zustand
- `840ce9f`: phase(4): fix markdown hyphen parsing and bottom scroll jitter
- `70a664b`: phase(5): enhance markdown heading hierarchy
- `6d8b597`: phase(6): adjust composer collapse to upward scroll only
- `887c36e`: phase(7): add markdown image preview with outside click close
- `e935f20`: phase(8): center markdown image preview modal
- 待本次提交：phase(9): align preview center to chat pane and cap width

## Notes

- Phase 2 已完成，后续重点为补测试与必要的 store 抽离。
