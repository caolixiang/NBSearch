# 20260319 Memo

## Item

- [17:00] Item: 启动 Polymarket 订阅功能开发，确认采用客户端直连 Jina、与现有 Grok 网关完全解耦的方案。
- [17:08] Item: 新增 20260319 计划与 Memo 文档，拆分为存储、抓取轮询、runtime 集成、UI、验证六个 Phase。
- [17:25] Item: 扩展 SQLite schema、repository 与 `config.toml`，新增 feed subscriptions/items 持久化和订阅配置读写。
- [17:42] Item: 新增 Jina client、Polymarket timeline parser 与 feed service，接入启动补抓和 10 分钟轮询。
- [17:58] Item: 设置页新增“订阅”页签，侧边栏新增 `Polymarket` 入口，主界面增加订阅流、立即同步与“加载更多”。
- [18:10] Item: 补充 parser/service 测试并修正 migration 断言，完成 `bun test`、`bun run typecheck`、`bun run build` 验证。
- [17:55] Item: 启动 Polymarket 中文翻译接入，确认新增独立 `LLM 网关` 配置，不复用现有 Grok 聊天/语音网关。
- [18:18] Item: 已接入 AI SDK + OpenAI-compatible provider，新增独立 `LLM 网关` 配置、feed 翻译入库字段、翻译主链与设置页保存入口。
- [18:31] Item: 完成 `bun test`、`bun run typecheck`、`bun run build` 与 `cargo check` 回归验证，当前改动可通过前端与 Tauri 编译检查。
- [18:42] Item: 用户收敛需求，不做模型同步，改为 OpenAI 独立区 + `hello` 联通测试，并补后续同步的翻译自动回填。
- [19:05] Item: 已将翻译配置从通用 `LLM 网关` 交互中拆出为独立 `OpenAI` 设置页；后续同步会自动回填旧的 `failed/skipped` 翻译，并将默认模型切到 `gpt-5.4-mini`。
- [19:16] Item: 完成最终回归，`bun test`、`bun run typecheck`、`bun run build` 与 `cargo check` 全通过，准备归档 Phase 7/8 提交记录。
- [19:28] Item: 启动内部命名收口，准备将前端/runtime/Tauri 的翻译配置主字段由 `llm` 统一切换为 `openai`，不再保留旧命名兼容层。
- [19:41] Item: 已完成 `openai*` 命名切换，覆盖前端/runtime/Tauri 配置字段、设置页状态、OpenAI feed translator 文件与调用链。
- [19:44] Item: 完成本轮回归，`bun test`、`bun run typecheck`、`bun run build` 与 `cargo check` 全通过，准备归档 Phase 9/10 提交记录。
- [19:56] Item: 启动 Polymarket 自动分页改造，准备在滚动到底部时自动触发下一页加载，同时保留“加载更多”按钮兜底。
- [20:02] Item: 已完成 Polymarket 自动分页主链，新增底部 sentinel + `IntersectionObserver` 自动触发下一页加载，并保留“加载更多”按钮手动兜底。
- [20:05] Item: 完成自动分页全量回归，`bun test`、`bun run typecheck`、`bun run build` 全通过，准备归档 Phase 11/12 提交记录。
- [20:11] Item: 已完成 Polymarket 条目一键“继续深入调研”主链，点击卡片会新建对话并发送 feed 原文；远程图片会作为真实 `image_url` 多模态块进入聊天请求，同时在本地消息气泡中展示预览。
- [20:11] Item: 本轮回归已完成，`bun test`、`bun run typecheck`、`bun run build` 全通过；新增用例覆盖原文 prompt、远程图片附件和请求体构造。
- [20:24] Item: 修正“继续深入调研”默认模型为 Thinker/专家，避免复用 Polymarket 页面下隐藏的全局选中模型；发送链路新增 model override，定位为代码侧问题而非网关普遍故障。
- [20:24] Item: 收紧 Polymarket 卡片布局，去掉与标题重复的正文、压缩多余空行，并缩短按钮占位；已补充卡片规范化测试并完成 `bun test`、`bun run typecheck`、`bun run build` 回归。

## Commits

- `39ae7f2`: 新增 Polymarket 订阅能力，接入 Jina 抓取、SQLite 持久化、启动补抓/定时轮询，以及设置页与订阅流界面。
- `ddc80f1`: 新增独立 `LLM 网关` 配置与 AI SDK 翻译入库链路，Polymarket 条目可持久化中文翻译并在订阅流展示。
- `1e8be97`: 将翻译配置拆为独立 `OpenAI` 设置页，新增 `hello` 联通测试，并在同步时自动回填旧的失败/跳过翻译。
- `1e6f236`: 将前端/runtime/Tauri 的翻译配置字段统一重命名为 `openai*`，并同步重命名 OpenAI feed translator 文件与调用链。
- `51e2331`: 为 Polymarket 订阅流增加滚动到底自动分页，并保留“加载更多”按钮兜底。
- `193f2b8`: 将 Polymarket 卡片接入“继续深入调研”主链，点击后新建对话并发送 feed 原文与远程图片块。
- `8bbe0f6`: 补充 feed 带聊相关测试与当日计划/Memo 记录，完成 `bun test`、`bun run typecheck`、`bun run build` 回归归档。
- `ee03b39`: 将继续深入调研固定切到 Thinker/专家模型发送，并收紧 Polymarket 卡片正文与按钮布局。
- `f4a9c6c`: 补充 feed 卡片规范化测试与本轮计划/Memo 记录，完成 `bun test`、`bun run typecheck`、`bun run build` 回归。

## Notes

- 当前方案只保证应用存活期间的持续轮询；应用彻底退出后，重新打开时仅做 best-effort 补抓。
- 继续深入调研目前默认新建对话，不会把订阅条目插入已有会话。
