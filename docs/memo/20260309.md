# 20260309 Memo

## Item

- [00:02] Item: 启动 Phase 161，只收侧边栏底部“设置”入口尺寸；先补 20260309 的 plan/memo，再在 `chat-sidebar.tsx` 做单点视觉回调，保持侧边栏默认展开不动。
- [00:05] Item: 完成 Phase 161，已将侧边栏底部“设置”入口的字号、图标和内边距收回一档，并通过 `bun run typecheck`、`bun test`、`bun run build`。
- [00:08] Item: 启动 Phase 162，针对语音设置抽屉中的速度滑杆继续收口，重点修正高亮轨道右侧过长、压住下一个刻度的问题。
- [00:14] Item: 完成 Phase 162，已将速度滑杆改成内轨裁切高亮层，去掉右侧多余延伸，并保持 `0.1x` 步进与刻度点击逻辑不变；全量通过 `bun run typecheck`、`bun test`、`bun run build`。
- [00:18] Item: 启动 Phase 163，只把侧边栏“设置”入口再缩一小档，继续做单点视觉收口，不动默认展开和其他布局。
- [00:21] Item: 完成 Phase 163，已将“设置”入口的 icon、文字与间距各缩一小格，并保持点击容器高度不变；全量通过 `bun run typecheck`、`bun test`、`bun run build`。
- [00:25] Item: 启动 Phase 164，继续压缩侧边栏“设置”入口的整体体量，这次同时收左右留白、图标和字号，目标是让它更接近普通列表项。
- [00:28] Item: 完成 Phase 164，已将“设置”入口降到常规侧边栏项体量：`size-4` 图标、约 `1.05rem` 字号、缩窄水平留白；全量通过 `bun run typecheck`、`bun test`、`bun run build`。
- [00:33] Item: 启动 Phase 165，修正语音速度滑杆的视觉结构，重点改 fill 与 active thumb 的连接方式，避免当前黑色胶囊感。
- [00:37] Item: 完成 Phase 165，已将滑杆改为左侧 fill 加 active thumb 右半圆底的结构，白色圆心保持不变；全量通过 `bun run typecheck`、`bun test`、`bun run build`。
- [00:40] Item: 启动 Phase 166，只增加语速滑杆 active 区域的垂直体量；这轮不改横向结构、不动步进和点击逻辑。
- [00:44] Item: 完成 Phase 166，已将 fill 层与 active thumb 深色底同步抬高，选中区域更饱满；全量通过 `bun run typecheck`、`bun test`、`bun run build`。
- [00:49] Item: 启动 Phase 167，个性化里的“说明”编辑器改为仅在选择“自定义”时显示，并补轻量单测锁住该行为。
- [00:53] Item: 完成 Phase 167，已将“说明”表单收口到 `custom` 人格下，并新增 `voice-settings-personality.test.ts` 锁住显示条件；全量通过 `bun run typecheck`、`bun test`、`bun run build`。
- [00:58] Item: 启动 Phase 168，定位到语速滑杆 active 深色底高度超过按钮容器，导致“蘑菇头”溢出；这轮只统一高度边界，不改横向结构。
- [01:02] Item: 完成 Phase 168，已将 active 按钮容器与深色底统一到相同 `34px` 高度边界，滑杆不再出现上下突出的“蘑菇头”；全量通过 `bun run typecheck`、`bun test`、`bun run build`。
- [01:08] Item: 启动 Phase 169，放弃当前半圆胶囊伪滑条，改为真实可拖动的 `range` 滑条，避免继续在样式细节上反复修补。
- [01:14] Item: 完成 Phase 169，语速控件已切到真实 `range` 拖动输入，视觉简化为轨道、左侧 fill、圆形 thumb 和点阵辅助刻度；全量通过 `bun run typecheck`、`bun test`、`bun run build`。

- [00:44] Item: 启动 Phase 170，放弃当前语速控件的大胶囊样式，回退为普通双端点滑条，只保留真实拖动、进度线和圆形 thumb。

- [00:48] Item: 完成 Phase 170，语速控件已回退为普通双端点滑条，保留真实拖动与 `0.1x` 步进；`bun run typecheck`、`bun test`、`bun run build` 全部通过。

- [00:51] Item: 启动 Phase 171，修正语音面板顶部区域：改成可编辑输入框，替换提示文案，并放开语音态的输入与发送链路。

- [00:53] Item: 完成 Phase 171，语音面板顶部已支持直接打字：placeholder 改为 `不方便说话，你也可以打字`，进入语音态自动聚焦，并放开回车发送与粘贴附件；`bun run typecheck`、`bun test`、`bun run build` 全部通过。

- [01:00] Item: 启动 Phase 172，准备把自动更新改成“启动静默下载、下载完成后头部显示立即更新按钮”的两段式交互；Rust 侧增加已准备更新缓存，前端头部接入按钮。

- [01:08] Item: 完成 Phase 172，自动更新已改为“启动静默下载、下载完成后头部显示立即更新按钮”的两段式链路；Rust 侧新增 prepared update 缓存，前端头部可直接安装并重启。

- [01:12] Item: 启动 Phase 173，调整语音面板顶部预留空间与附件托盘对齐；这轮只动 `chat-input.tsx`，让语音态也预留附件区并把文字输入整体下移。

- [01:13] Item: 完成 Phase 173，语音态顶部已增加常驻附件预留位，并将文字输入整体下移；图片/文件展示与普通聊天输入区对齐，`bun run typecheck`、`bun test`、`bun run build` 全部通过。

- [01:15] Item: 启动 Phase 174，只替换模型同步按钮图标；这轮不改交互、不改文案，仅将 `RotateCw` 换成云下载箭头 SVG。

- [01:17] Item: 完成 Phase 174，模型同步按钮图标已替换为云下载箭头 SVG；同步交互、禁用态和状态文案保持不变，`bun run typecheck`、`bun test`、`bun run build` 全部通过。

- [01:18] Item: 启动 Phase 175，回收语音态默认附件占位高度，改成只下压文字；只有真的上传附件时，顶部区域才增高。

- [01:20] Item: 完成 Phase 175，语音态默认不再预占附件高度，而是直接把文字下压；上传附件后顶部区域才增高，`bun run typecheck`、`bun test`、`bun run build` 全部通过。


- [01:36] Item: 启动 Phase 176，修正消息操作栏“来源”按钮的滚动副作用；目标是点击后只打开来源抽屉，不再把对话位置拉回到思考面板。
- [01:38] Item: 完成 Phase 176，已移除 `chat-message.tsx` 中来源按钮点击时的 `scrollIntoView`，保留来源抽屉事件分发；`bun test`、`bun run typecheck`、`bun run build` 全部通过。


- [01:40] Item: 启动 Phase 177，调整语音面板默认值；目标是首开时直接高亮 `Ara` 和 `Assistant`，不再落在 `Leo + 自定义`。
- [01:42] Item: 完成 Phase 177，已抽出语音默认常量并改写 `chat-input.tsx` 初始化，同时新增 `voice-settings-defaults.test.ts` 锁住默认值与标签 fallback；`bun test`、`bun run typecheck`、`bun run build` 全部通过。

- [01:44] Item: 启动 Phase 178，只给普通聊天输入区的语音入口增加 `连接中` 中间态，不改语音面板内部布局。
- [01:47] Item: 完成 Phase 178，已为语音入口补上 `idle/connecting/active` 状态机，新增 `chat-input-voice-entry` 辅助模块与测试，并在卸载/禁用时清理连接定时器；`bun test`、`bun run typecheck`、`bun run build` 全部通过。

## Commits

- `e81f22f`: `phase(178): add voice entry connecting state`

- `e3090b2`: `phase(177): default voice panel to ara assistant`

- `892d6d7`: `phase(176): keep source drawer from scrolling messages`

- `33381c7`: `phase(161): reduce sidebar settings entry size`
- `4bf1be1`: `phase(161): backfill plan and memo commit trace`
- `965d6af`: `phase(162): tighten voice speed slider fill`
- `d446a5e`: `phase(162): backfill plan and memo commit trace`
- `ae1b6ba`: `phase(163): reduce settings entry size again`
- `c656919`: `phase(163): backfill plan and memo commit trace`
- `cfc4228`: `phase(164): shrink sidebar settings entry`
- `651e04d`: `phase(164): backfill plan and memo commit trace`
- `f8e02c2`: `phase(165): refine voice speed slider styling`
- `f869557`: `phase(165): backfill plan and memo commit trace`
- `8ca562e`: `phase(166): increase voice speed active height`
- `f8b9860`: `phase(166): backfill plan and memo commit trace`
- `fcd33db`: `phase(167): gate custom prompt editor by personality`
- `c058ed0`: `phase(167): backfill plan and memo commit trace`
- `901bce1`: `phase(168): align voice speed active height`
- `d28ce39`: `phase(168): backfill plan and memo commit trace`
- `f24e98e`: `phase(169): switch voice speed control to draggable slider`

- `06e51e4`: `phase(170): simplify voice speed slider styling`

- `33a278c`: `phase(171): enable typed input in voice mode`

- `0a589e4`: `phase(172): add prepared app update flow`

- `2f986b9`: `phase(173): align voice input attachment spacing`

- `1fb8114`: `phase(174): replace model sync icon`

- `96dae9f`: `phase(175): push down voice placeholder text`

## Notes

- `bun run build` 仍有既有 Vite chunk warning 与 `lottie.js` eval warning，本轮未新增。
