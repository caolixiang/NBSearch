# 20260316 Memo

## Item

- [11:20] Item: 排查侧边栏会话标题编辑按钮缺少 hover 反馈的问题，准备做最小样式修复。
- [11:34] Item: 调整侧边栏会话行编辑/删除按钮的 hover 与 focus-visible 样式，解决编辑标题按钮悬停无反馈的问题。
- [11:40] Item: 收敛编辑标题按钮 hover 的视觉对比，去掉偏白底色，改为更贴近侧边栏色板的交互态。
- [11:55] Item: 新增会话星标功能，加入 `星标` 分组置顶展示，并将侧边栏宽度从 `w-64` 调整为 `w-72`。
- [12:02] Item: 修复老数据库缺少 `starred` 列时会话列表查空的问题；确认本地库中原有 10 条会话仍在，并完成本地 schema 修复。
- [12:07] Item: 为 SQLite migration 增加事务保护，并新增真实 `legacy-v1.sql` 夹具测试与失败 migration 回滚测试。
- [12:11] Item: 对临时旧版 `chat-app.db` 文件执行真实升级 smoke，确认 `schema_migrations=[1,2]` 且旧会话仍能读出。
- [12:24] Item: 收紧侧边栏会话行星标/重命名/删除三个操作按钮的间距与预留宽度，并将星标 hover 与已星标状态统一为黄色语义。
- [12:38] Item: 去掉 `星标` 分组会话默认常驻的尾部黄星，只在 hover 时保留星标操作入口，修正标题末尾错位与视觉噪音。
- [12:47] Item: 将星标移到右侧最右位；已星标会话常驻显示，未星标会话仅在 hover 时显示，重命名和删除在其左边展开，避免标题区再次挂星。
- [13:02] Item: 去掉普通输入框右侧单独的“语音输入”麦克风按钮，仅保留语音模式入口和发送按钮。
- [13:16] Item: 在输入框右侧增加模型快捷切换，提供 `Fast / Thinker / Max` 三个快捷模式，并在文本过长时折叠为图标；选择后与页头模型选择同步。
- [13:24] Item: 修复输入框模型快捷切换菜单被裁剪的问题，放开输入框容器 overflow 并提高菜单层级。
- [13:31] Item: 修复快捷模型回显误判问题，选择 `Max` 后优先按预设绑定的 `modelId` 回显，而不是先按关键词推断回 `Thinker`。
- [13:40] Item: 将输入框快捷模型切换的预设文案翻译为中文，按钮和下拉菜单统一显示为 `极速 / 专家 / 实验性`。
- [13:49] Item: 将快捷模型 `实验性` 的副文案从“最佳效果”收敛为“多代理”，避免错误暗示。
- [13:50] Item: 执行 `v0.2.1` 发版流程，使用脚本更新版本号并准备 release notes、tag 与远端打包。

## Commits

- `9aa545a`: 修复侧边栏标题编辑按钮 hover 反馈，增强会话行操作按钮的边框、底色与 focus-visible 状态。
- `25b8780`: 补充 Phase 1 的计划与 memo 提交记录。
- `b5fb88a`: 将编辑按钮 hover 由偏白底色收敛为更贴近侧边栏的暖色交互态。
- `246ad40`: 补充 Phase 2 的计划与 memo 提交记录。
- `7a8b77a`: 增加持久化星标、侧边栏 `星标` 分组、宽度调整，并补齐旧库 migration 兼容。
- `3f85419`: 补充 Phase 3 的计划与 memo 提交记录。
- `3c3ace2`: 为 SQLite migration 增加事务保护，并新增真实旧库夹具升级测试与失败回滚测试。
- `852d16c`: 补充 Phase 4 的计划与 memo 提交记录。
- `67e4f44`: 补充 Phase 5 的计划与 memo 提交记录。
- `4899225`: 收紧侧边栏会话行三枚操作按钮的间距，并将星标 hover/激活状态调整为黄色语义。
- `42dda30`: 补充 Phase 6 的计划与 memo 提交记录。
- `a70d1cd`: 去掉 `星标` 分组会话默认常驻的尾部黄星，只在 hover 时显示星标操作入口。
- `c2768c3`: 补充 Phase 7 的计划与 memo 提交记录。
- `12d2dda`: 调整右侧操作轨显示策略，已星标会话常驻最右黄星，未星标会话仅在 hover 时显示星标入口。
- `c9a03a5`: 补充 Phase 8 的计划与 memo 提交记录。
- `91d449d`: 去掉普通输入框右侧独立的语音输入按钮，仅保留语音模式入口与发送按钮。
- `ffcb1b2`: 补充 Phase 9 的计划与 memo 提交记录。
- `44da4be`: 在输入框右侧增加 `Fast / Thinker / Max` 模型快捷切换，并接入现有 `selectedModel` 同步链路。
- `b2c6e9e`: 补充 Phase 10 的计划与 memo 提交记录。
- `f04c955`: 补充 Phase 10 的最终文档记录。
- `fb23cc0`: 修复输入框模型快捷切换菜单被输入框容器裁剪的问题，并提高菜单层级。
- `78013cd`: 补充 Phase 11 的计划与 memo 提交记录。
- `7d13fca`: 修复快捷模型选中态误判，选择 `Max` 后输入框快捷模型按钮与菜单勾选均优先按预设绑定模型回显。
- `dddd3dd`: 补充 Phase 12 的计划与 memo 提交记录。
- `0e633ed`: 将输入框快捷模型预设文案中文化，统一为 `极速 / 专家 / 实验性`。
- `2be31e5`: 补充 Phase 13 的计划与 memo 提交记录。
- `8420a74`: 将 `实验性` 副文案从“最佳效果”调整为“多代理”。
- `1a8134b`: 补充 Phase 14 的计划与 memo 提交记录。
- `1c35b4e`: 使用 `scripts/bump-version.sh` 将仓库版本提升到 `0.2.1`，准备发布提交与 tag。
- 待补充（Phase 15 docs）

## Notes

- 已直接修复本地 `~/.nbsearch/data/chat-app.db` 的 `conversations.starred` 列缺失问题，并补写 migration 记录；原会话数据未丢失。
