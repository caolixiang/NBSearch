# 20260304 Memo

## Item

- [00:06] Item: 启动 Phase 45，目标为对齐 DeepSearch 新 research 字段并补齐正文注脚与思考细节展示。
- [08:10] Item: 完成 Domain/Storage 扩展：`ChatMessage` 增加 `research`，SQLite `content_json` 增加 research 持久化与反序列化。
- [08:10] Item: 完成流式解析扩展：新增 `extractDeepSearchResearchFromRawChunk` 与 `extractInlineCitationsFromText`，在 `streamTurn` 聚合并写入 assistant message。
- [08:10] Item: 完成 UI 渲染：正文下方新增 `Key Citations` 注脚列表；思考抽屉新增“深度挖掘细节”卡片区块。
- [08:10] Item: 完成回归测试：`bun run typecheck`、定向 parser/service 测试、全量 `bun test`、`bun run build` 均通过。
- [08:11] Item: 提交 `dd2d458`（phase(45)），完成 DeepSearch 研究结构解析、落库与渲染联动。
- [08:16] Item: 按反馈修正注脚来源：移除 `reasoningEvents/webSearchResults` 与正文解析兜底，注脚仅由 `research.inline_citations` 驱动，并按 `card_id + citation_id` 去重。
- [08:16] Item: 修正后再次执行 `bun run typecheck`、定向测试与全量 `bun test`，均通过。
- [08:16] Item: 提交 `4d250bc`（phase(45)），完成注脚字段收紧修复。
- [16:35] Item: 启动 Phase 46，修复 DeepSearch 思考抽屉信息归类错位问题，改为优先按 `research.steps` 顺序分组渲染。
- [16:45] Item: 完成 DeepSearch steps 数据链路扩展：`domain` 新增 `steps` 类型；parser/service/repository 支持提取、合并、落库。
- [16:55] Item: 完成 UI 分组重构：新增 `buildDeepSearchGroupedSections`，按 `header/summary/tool_usage_card` 与工具事件映射顺序展示；无 steps 时回退旧视图。
- [17:03] Item: 完成回归验证：`bun run typecheck`、`bun test`、`bun run build` 全部通过。
- [17:08] Item: 提交 `3e4710c`（phase(46)），完成 DeepSearch 抽屉按 steps 顺序归类修复。
- [17:22] Item: 启动 Phase 47，修复思考抽屉将 `<xai:tool_usage_card>` 原文错误展示为正文的问题。
- [17:31] Item: 完成 parser/service 扩展：`steps` 新增 `toolUsages`，统一提取 `toolUsageCards/toolUsageResults/XML` 工具调用与结果。
- [17:40] Item: 完成 UI 回退渲染改造：缺失 `reasoningEvents` 时从 `steps.toolUsages` 生成工具条目；过滤 XML 原始行。
- [17:47] Item: 完成回归验证：`bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 全部通过。
- [17:49] Item: 提交 `798059d`（phase(47)），完成 DeepSearch steps 工具条目结构化渲染修复。
- [18:08] Item: 启动 Phase 48，按 Grok deepsearch 结果页重构为独立时间线视图，移除领导者语义。
- [18:16] Item: 完成时间线构建与渲染：新增 `buildDeepSearchTimeline`，抽屉按 steps 顺序展示“思考段 + 工具条目”。
- [18:22] Item: 完成 XML 清洗增强：过滤所有 `<xai:...>` 行，避免摘要出现 XML 残留。
- [18:27] Item: 完成回归验证：`bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 全部通过。
- [18:31] Item: 提交 `0031a9a`（phase(48)），完成 DeepSearch 结果页时间线视图切换。
- [18:42] Item: 启动 Phase 49，修复旧 DeepSearch 数据（仅 details，无 steps）仍走旧领导者视图的问题。
- [18:48] Item: 完成兼容路径：新增 `buildDeepSearchLegacyTimeline`，在 `steps` 缺失时按 details 构建时间线并交错工具条目。
- [18:53] Item: 完成定位验证：`~/.nbsearch/data/chat-app.db` 中目标会话 `conv_283f2208-...` 最新消息为 `details=17, steps=0`，已命中兼容修复场景。
- [18:55] Item: 完成回归验证：`bun run typecheck`、`bun test`、`bun run build` 全部通过。
- [18:58] Item: 提交 `8e44e95`（phase(49)），完成 DeepSearch 旧数据时间线兼容。
- [09:18] Item: 启动 Phase 50，修复旧 DeepSearch `details.title` 中 XML 片段未清洗导致的空白卡片渲染问题。
- [09:20] Item: 完成修复：`buildDeepSearchLegacyTimeline` 对 title 执行 XML 清洗，并在 title/bullets 同时为空时直接丢弃卡片。
- [09:21] Item: 新增 `buildDeepSearchLegacyTimeline` 单测，覆盖纯 `<xai:...>` 标题卡片应被忽略的场景。
- [09:24] Item: 完成回归验证：`bun run typecheck`、`bun test`、`bun run build` 全部通过。
- [09:34] Item: 启动 Phase 51，目标为“已搜索 X”图标对齐官方样式并支持可展开结果列表。
- [09:40] Item: 完成解析层扩展：支持 `raw_function_result.xSearchResults` 与 `steps.toolUsageResults.xSearchResults`，并补齐 X 帖子的 author/handle/time/postId 字段。
- [09:43] Item: 完成 UI 改造：`已搜索 X` 替换官方 SVG，支持展开卡片列表；列表头像统一占位 avatar，不使用真实头像 URL。
- [09:47] Item: 新增/更新测试：`chunk-parsers` 增加 xSearchResults 解析与 DeepSearch steps 落库断言；结构化推理测试回归通过。
- [09:50] Item: 完成回归验证：`bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 全部通过。
- [10:02] Item: 启动 Phase 52，修复旧会话 X 搜索“无数量、不可展开/不可点击”问题。
- [10:08] Item: 完成 legacy 兜底：从 `research.details` 的 XML 工具卡片提取 `tool_args.limit`，回填 X 条目 `resultsCount`。
- [10:10] Item: 完成交互兜底：X 条目即使无结果数组也可展开，并支持跳转到 X 搜索页面。
- [10:12] Item: 新增结构化推理单测，覆盖 legacy XML `limit` 数量回填场景。
- [10:15] Item: 完成回归验证：`bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build` 全部通过。
- [10:24] Item: 启动 Phase 53，优化 X 推文 cell 样式与间距，对齐官方观感。
- [10:26] Item: 完成样式调整：X 列表改为 `divide-y` 零间距，cell 默认透明、悬停轻背景，去除刺眼底色。
- [10:27] Item: 完成内容展示调整：推文正文移除 `line-clamp`，改为完整展示。
- [10:28] Item: 完成回归验证：`bun run typecheck` 与 `bun test src/components/markdown-content/structured-reasoning.test.ts` 通过。
- [10:42] Item: 启动 Phase 54，修复正文尾部 `Key Citations` 原文列表渲染污染问题。
- [10:45] Item: 完成清洗逻辑：`normalizeAssistantMarkdown` 增加尾部 `Key Citations` 区块识别与剔除（仅 URL 引用尾段触发）。
- [10:47] Item: 新增测试：覆盖“剔除尾部 citations 块”与“保留普通 prose 中 key citations 词组”两类场景。
- [10:50] Item: 完成回归验证：`bun run typecheck`、`bun test src/components/markdown-content.test.ts`、全量 `bun test`、`bun run build` 通过。
- [10:00] Item: 启动 Phase 55，目标为“正文继续清洗 Key Citations，但引用通过 Key Citations 区块完整回填”。
- [10:02] Item: 完成引用渲染链路修复：`buildCitationItems` 增加尾部 Key Citations 解析回填，合并 inline + tail 并按 URL 去重。
- [10:03] Item: 新增 `chat-message-citations` 单测，覆盖 markdown 链接/括号 URL/裸 URL 解析与 inline+tail 去重场景。
- [10:04] Item: 完成回归验证：`bun run typecheck`、定向 `bun test`、全量 `bun test` 通过。
- [10:05] Item: 提交 `6ce0b12`（phase(55)），完成 Key Citations 尾部回填渲染修复。
- [10:12] Item: 启动 Phase 56，修复 Key Citations 将 `<tool-meta>...</tool-meta>` 内部 URL 误解析为引用的问题。
- [10:14] Item: 完成修复：尾部 citation 解析前剔除整段 `tool-meta` 元数据，并新增对应回归测试。
- [10:15] Item: 提交 `ce1f0eb`（phase(56)），完成 tool-meta 噪声过滤修复。
- [10:18] Item: 启动 Phase 57，修复普通推理模型误进入 DeepSearch 视图的问题，恢复非 DeepSearch 的 agent 思考视图。
- [10:20] Item: 完成分流修复：新增 `hasDeepSearchContent` 判定，`StructuredReasoningPanel` 仅在 research 存在 steps/details 时启用 DeepSearch timeline。
- [10:21] Item: 完成回归验证：`bun run typecheck`、`bun test src/components/markdown-content/structured-reasoning.test.ts`、全量 `bun test` 通过。
- [10:22] Item: 提交 `856a2f3`（phase(57)），完成思考视图分流修复。
- [10:25] Item: 启动 Phase 58，修复思考入口文案与时长口径：文案去除工具数量，时长改为真实 reasoning 窗口。
- [10:28] Item: 完成实现：前端发送/重新生成链路改为 reasoning 起止计时；服务端持久化优先使用 research thinking 时间窗。
- [10:29] Item: 完成回归验证：`bun run typecheck`、全量 `bun test` 通过。
- [10:30] Item: 提交 `2e487c7`（phase(58)），完成思考时长口径修正与文案精简。
- [10:34] Item: 启动 Phase 59，目标为“新开对话默认切 fast + DeepSearch 改为单轮生效自动熄灭”。
- [10:36] Item: 完成实现：抽取 `model-selection` 工具函数；新建/切换 draft 时统一重置为 fast 并关闭 DeepSearch；发送链路加入 DeepSearch 单轮复位。
- [10:37] Item: 完成回归验证：`bun run typecheck`、`bun test src/features/chat/model-selection.test.ts`、全量 `bun test` 通过（147 pass）。
- [10:39] Item: 提交 `751fbef`（phase(59)），完成新对话模型默认值与 DeepSearch 单轮复位行为。
- [10:41] Item: 启动 Phase 60，目标为“在对话列表给 DeepSearch 类型会话增加显微镜图标”。
- [10:43] Item: 完成实现：会话记录新增 `hasDeepSearch` 并在 SQLite `listConversations` 聚合返回；Sidebar 标题前按标记展示显微镜图标。
- [10:44] Item: 完成回归验证：`bun run typecheck`、全量 `bun test` 通过（147 pass）。
- [10:45] Item: 提交 `8584ce1`（phase(60)），完成对话列表 DeepSearch 类型标记展示。
- [10:47] Item: 启动 Phase 61，目标为“已在新对话窗口时再次点击新建，不重置当前模型选择”。
- [10:48] Item: 完成实现：`handleNewConversation` 先判断 draft；若已在 draft，仅清空草稿消息并返回，不执行 composer reset。
- [10:48] Item: 完成回归验证：`bun run typecheck`、全量 `bun test` 通过（147 pass）。
- [10:48] Item: 提交 `f96b281`（phase(61)），完成草稿内重复新建不重置模型行为。
- [10:50] Item: 启动 Phase 62，目标为“模型同步按钮改为旁侧状态提示：已同步/同步失败，请重试”。
- [10:51] Item: 完成实现：刷新按钮 tip 改为“同步网关模型”；手动同步成功/失败在按钮旁显示短文本并自动消失；移除顶部模型同步错误文案。
- [10:52] Item: 完成回归验证：`bun run typecheck`、全量 `bun test` 通过（147 pass）。
- [10:52] Item: 提交 `7679c2a`（phase(62)），完成模型同步提示交互优化。
- [10:58] Item: 启动 Phase 63，目标为“hasDeepSearch 改为请求驱动，历史数据不再结构推断”。
- [11:03] Item: 完成实现：新增 SQLite migration v2（`conversations.has_deep_search`）；会话列表改为读持久字段；`streamTurn` 完成时按 `existing.hasDeepSearch || isDeepSearch` 置位。
- [11:04] Item: 完成回归验证：`bun run typecheck`、全量 `bun test` 通过（147 pass）。
- [11:06] Item: 提交 `272c4e7`（phase(63)），完成 DeepSearch 标记持久化与请求驱动改造。
- [11:12] Item: 启动 Phase 64，处理“单轮图片重生成后同条消息出现 4 张图（proxy + signed 重复）”问题，并按用户更正将 DB 路径统一为 `~/.nbsearch`。
- [11:16] Item: 完成实现：`AGENTS.md` 增加数据库路径说明；`grok-chat-service` 增加 generated image 同源去重与 markdown 图片行清洗，并在流式落库前去重。
- [11:18] Item: 新增回归测试 `deduplicates upstream+signed image cards for the same generated outputs`，覆盖同源 proxy+signed 卡片合并场景。
- [11:19] Item: 完成回归验证：`bun run typecheck`、`bun test src/infrastructure/chat/grok-chat-service.test.ts`、`bun test` 全部通过（148 pass）。
- [11:21] Item: 提交 `1834313`（phase(64)），完成生成图片同源去重与 markdown 清洗修复。
- [11:25] Item: 补录 Phase 65：开发者手动提交 `a1a15e0` 删除 `draw.txt`，本轮补齐 plan/memo 与例外记录（先提交后补文档）。
- [11:26] Item: 启动 Phase 66，目标为“生成图片结构块隐藏 PDF 导出 + 重新生成按钮右对齐”。
- [11:27] Item: 完成实现：`ChatMessage` 新增 generated-image 结构块判定；纯 generated-image 轮次不显示 PDF 按钮；消息操作栏改为 `w-full justify-end`。
- [11:28] Item: 新增单测：`shouldHidePdfExportForGeneratedContent` 覆盖纯 generated/non-generated/no tool-meta 三类判定。
- [11:29] Item: 完成回归验证：`bun run typecheck`、`bun test src/components/chat-message-citations.test.ts`、`bun test` 全部通过（151 pass）。
- [11:31] Item: 提交 `e06f3a8`（phase(66)），完成生成图片结构块隐藏 PDF 与操作栏右对齐。
- [11:28] Item: 启动 Phase 67，目标为“设置界面宽度适当增加”。
- [11:28] Item: 完成实现：`SettingsDialog` 弹窗宽度从 `max-w-2xl` 调整为 `max-w-3xl`，其余布局逻辑保持不变。
- [11:29] Item: 完成回归验证：`bun run typecheck`、`bun test` 全部通过（151 pass）。
- [11:30] Item: 提交 `feef985`（phase(67)），完成设置弹窗宽度微调。
- [11:34] Item: 启动 Phase 68，按反馈继续调整设置弹窗宽度，并修正 `sm` 断点覆盖。
- [11:35] Item: 先将宽度调至 `sm:max-w-4xl` 验证后偏宽，随后按反馈回调为 `sm:max-w-3xl`，保留断点覆盖能力。
- [11:36] Item: 完成回归验证：`bun run typecheck`、`bun test` 全部通过（151 pass）。
- [11:37] Item: 提交 `b637dc5`（phase(68)），完成设置弹窗宽度回调至更平衡的 `sm:max-w-3xl`。
- [11:40] Item: 启动 Phase 69，目标为“网关配置真实落地到配置目录 config.toml，且读取优先级高于 .env”。
- [11:44] Item: 完成后端落地：Tauri 新增 `read_gateway_config/save_gateway_config` 命令，读写 `app_config_dir/config.toml` 的 `[gateway]` 段。
- [11:46] Item: 完成前端链路：`loadAppConfig` 改为异步并优先读取 config.toml；`App` 改为等待运行时初始化后再渲染；设置页接入真实保存与即时生效。
- [11:47] Item: 完成交互细节：API Key 占位符改为 `gw-...`，保存后即时重建 runtime chat/voice 服务并刷新模型列表。
- [11:48] Item: 完成回归验证：`bun run typecheck`、`bun test`、`cargo check` 全部通过。
- [12:06] Item: 启动 Phase 70，目标为“外观配置落地：主题切换（浅色/深色/跟随系统）+ 字体大小（小/默认/大）+ 移除语言项”。
- [12:12] Item: 完成配置落地：`config.toml` 新增 `[appearance]`（`theme/font_size`），新增 Tauri 命令 `save_appearance_config` 并扩展 `read_gateway_config` 返回外观字段。
- [12:18] Item: 完成前端接入：新增外观应用器（深色切换、跟随系统监听、根字号应用），设置页外观改为真实保存并即时应用，移除语言项。
- [12:20] Item: 完成兼容修正：补齐受影响单测中的 `AppConfig` 新字段（`themeMode/fontSizeMode`）。
- [12:22] Item: 完成回归验证：`bun run typecheck`、`bun test`、`cargo check` 全部通过。
- [12:39] Item: 启动 Phase 71，目标为“配置文件路径统一到 `~/.nbsearch/config.toml`，并修复网关保存后设置框自动关闭”。
- [12:41] Item: 完成路径调整：`resolve_app_config_toml_path` 改为 `~/.nbsearch/config.toml`，并增加旧 `app_config_dir/config.toml` 自动迁移拷贝。
- [12:42] Item: 完成交互修复：网关保存成功后不再关闭设置弹窗，保留保存成功提示。
- [12:44] Item: 完成回归验证：`bun run typecheck`、`bun test`、`cargo check` 全部通过。
- [12:56] Item: 启动 Phase 72，目标为“删除历史迁移兼容代码（旧配置文件迁移 + 旧数据库迁移）”。
- [12:58] Item: 完成清理：移除旧配置迁移函数 `resolve_legacy_app_config_toml_path` 与拷贝逻辑，`config.toml` 仅使用 `~/.nbsearch/config.toml`。
- [13:00] Item: 完成清理：移除旧数据库迁移函数 `migrate_legacy_db_if_needed` 及其辅助函数调用，数据库仅走 `~/.nbsearch/data/chat-app.db`。
- [13:02] Item: 完成回归验证：`bun run typecheck`、`bun test`、`cargo check` 全部通过。
- [13:20] Item: 启动 Phase 73，目标为“外观面板自动保存 + 字体大小视觉重构 + 网关保存成功静默”。
- [13:24] Item: 完成实现：外观面板移除保存按钮，主题/字体切换改为即点即生效并自动持久化；字体大小由下拉改为三段卡片按钮。
- [13:25] Item: 完成实现：网关保存成功后不再显示“网关配置已保存”，仅在失败时显示错误提示。
- [13:27] Item: 完成回归验证：`bun run typecheck`、`bun test` 全部通过。
- [13:36] Item: 启动 Phase 74，目标为“收敛外观字体大小控件宽度，避免占满整行”。
- [13:38] Item: 完成实现：字体大小容器改为 `flex flex-wrap`，卡片改为固定宽度 `w-40`，整体布局更紧凑。
- [13:39] Item: 完成回归验证：`bun run typecheck` 通过。
- [13:40] Item: 提交 `6d652e5`（phase(74)），完成字体大小控件宽度收敛。
- [12:46] Item: 启动 Phase 75，处理 DeepSearch 新对话回归：标题丢失、思考页回退普通视图、`invalid_previous_response_id` 接续报错。
- [12:52] Item: 完成 parser 扩展：补充 `conversation/modelResponse` 候选路径、UUID responseId 提取、标题清洗、`header/summary` token 到 research steps/details 提取。
- [12:57] Item: 完成服务与前端锚点修复：无 `sessionId` 时不发送 `previous_response_id`；发送前 anchors 兜底并在 completed 先本地回写 anchors。
- [13:04] Item: 完成回归验证：`bun run typecheck`、`bun test` 全量通过（156 pass）。
- [13:06] Item: 提交 `28ef23a`（phase(75)），完成 DeepSearch 新对话回归修复（标题提取、专用视图数据链路、接续锚点稳定化）。
- [13:18] Item: 启动 Phase 76，修复消息操作栏对齐回归：仅纯生成图片轮次右对齐，普通/图文轮次恢复左对齐。
- [13:20] Item: 完成实现：`chat-message` 操作栏由固定 `justify-end` 改为条件对齐（复用 `hidePdfExport` 判定纯生图）。
- [13:22] Item: 完成回归验证：`bun run typecheck`、`bun test src/components/chat-message-citations.test.ts` 通过（8 pass）。
- [13:23] Item: 提交 `56d86a2`（phase(76)），完成按钮组按轮次类型对齐修复。
- [13:28] Item: 启动 Phase 77，目标为隐藏 Mini 并将 Grok 三档外显名改为 NBSearch Fast/Thinker/Max（内部 ID 不变）。
- [13:30] Item: 完成实现：`ModelSelector` 增加 Mini 过滤与别名展示映射，并补充“历史选中 Mini 自动切到可见模型”的防呆。
- [13:31] Item: 完成回归验证：`bun run typecheck`、`bun test src/features/chat/model-selection.test.ts` 通过（4 pass）。
- [13:32] Item: 提交 `9287d35`（phase(77)），完成模型下拉认知简化与 NBSearch 外显命名。
- [13:33] Item: 启动 Phase 78，目标为“生产打包不内置网关默认值与 API Key + 设置页 API Key 增加小眼睛显示/隐藏”。
- [13:34] Item: 完成实现：`readEnvAppConfig` 改为仅开发环境读取 `.env` 网关默认值；生产环境网关地址/API Key 默认留空避免泄露。
- [13:34] Item: 完成实现：`SettingsDialog` API Key 输入框新增 `Eye/EyeOff` 切换按钮，支持明文查看并在打开设置时重置为隐藏态。
- [13:35] Item: 完成回归验证：`bun run typecheck`、`bun test` 全部通过（156 pass）。
- [13:36] Item: 提交 `90d2a60`（phase(78)），完成生产包网关默认安全化与 API Key 可见性切换。
- [20:39] Item: 启动 Phase 79，目标为“参考 Mac 保持主配置不动，新增 Windows 专用打包配置并收敛明文 HTTP 权限”。
- [20:40] Item: 完成实现：新增 `src-tauri/tauri.windows.conf.json`，补齐 NSIS/MSI 目标、WebView2 安装策略、降级阻止与固定 WiX UpgradeCode。
- [20:40] Item: 完成实现：`src-tauri/capabilities/default.json` 移除 `http://*` / `http://*:*`，仅保留 localhost/127 明文 HTTP，外网仅 HTTPS。
- [20:42] Item: 完成回归验证：`bun run typecheck`、`bun test`、`cargo check`、`bunx tauri build --debug --no-bundle --config src-tauri/tauri.windows.conf.json` 全部通过。

## Commits

- `dd2d458`: phase(45): render deepsearch citations and research details
- `4d250bc`: phase(45): restrict citations to research inline fields
- `3e4710c`: phase(46): align deepsearch reasoning grouping by steps
- `798059d`: phase(47): render deepsearch tools from step metadata
- `0031a9a`: phase(48): switch deepsearch drawer to timeline view
- `8e44e95`: phase(49): add deepsearch legacy timeline fallback
- `6ce0b12`: phase(55): restore key citations via tail fallback parser
- `ce1f0eb`: phase(56): ignore tool-meta urls in key citations fallback
- `856a2f3`: phase(57): scope deepsearch timeline to deepsearch payload
- `2e487c7`: phase(58): fix reasoning duration window and simplify label
- `751fbef`: phase(59): reset fast model and deepsearch per turn
- `8584ce1`: phase(60): mark deepsearch conversations in sidebar
- `f96b281`: phase(61): keep model when recreating draft chat
- `7679c2a`: phase(62): show model sync notice beside refresh
- `272c4e7`: phase(63): persist deepsearch flag from request
- `1834313`: phase(64): dedupe regenerated image cards
- `a1a15e0`: phase(65): remove deprecated draw.txt file
- `e06f3a8`: phase(66): hide pdf for generated image blocks
- `feef985`: phase(67): widen settings dialog layout
- `b637dc5`: phase(68): tune settings dialog width to sm max-w-3xl
- `6d652e5`: phase(74): reduce font size control width in appearance panel
- `28ef23a`: phase(75): fix deepsearch parsing and continuation anchors
- `56d86a2`: phase(76): align action bar right only for generated-image turns
- `9287d35`: phase(77): hide mini model and relabel grok options
- `90d2a60`: phase(78): secure packaged gateway defaults and add api key visibility toggle

## Notes

- 无
