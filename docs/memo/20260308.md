# 20260308 Memo

## Item

- [02:20] Item: 启动 Phase 116，准备先从 `chat-shell.tsx` 下手，抽取发送/重生成共享的流式状态机辅助模块，优先降低回归风险。
- [02:21] Item: 新增 `chat-stream-runtime.ts`，集中承载 think tag 判断、reasoning 事件合并、duration 计时、delayed card flush 与完成后 reasoning 持久化辅助逻辑。
- [02:22] Item: `chat-shell.tsx` 的发送与重生成链路已切到共享 runtime，仅保留前置参数解析、完成后 UI 刷新和异常分支差异逻辑。
- [02:23] Item: 新增 `chat-stream-runtime.test.ts`，覆盖 delayed card flush、duration tick、finalize cleanup、reasoning persistence；全量回归通过 `bun run typecheck`、定向 `bun test`、`bun test`、`bun run build`。
- [02:24] Item: 提交 `0090fac`（phase(116)），完成 `chat-shell.tsx` 第一阶段流式状态机抽离。
- [02:29] Item: 启动 Phase 117，切到 `grok-chat-service.ts` 的恢复协议模块，目标先拆出状态/消息查询、JSON 归一化与等待重试辅助逻辑。
- [02:39] Item: 新增 `gateway-session-recovery.ts` 与对应测试，把恢复协议状态解析、`invalid_after_response_id` 回退和等待逻辑从 `grok-chat-service.ts` 中抽离；`grok-chat-service.test.ts`、全量 `bun test`、`bun run build` 全部通过。
- [02:40] Item: 提交 `a9ea70c`（phase(117)），完成 `grok-chat-service.ts` 恢复协议模块抽离。
- [02:43] Item: 启动 Phase 118，准备继续拆媒体 URL 代理、续签 payload 解析与 generated image 纯函数簇，并同步拆分对应纯函数测试。
- [02:51] Item: 新增 `gateway-media-assets.ts` 与 `gateway-media-assets.test.ts`，将媒体 URL 代理、续签 payload 解析、generated image 资产推断与 markdown/card 归并纯函数从 `grok-chat-service.ts` 中抽离；全量 `bun test` 与 `bun run build` 通过。
- [02:52] Item: 提交 `af8ca9d`（phase(118)），完成媒体 URL 与 generated image 纯函数模块抽离。
- [02:57] Item: 启动 Phase 119，继续拆消息级媒体刷新与流结束 generated image 卡片收口工作流，目标是让 `grok-chat-service.ts` 只保留 renew I/O 与最薄编排。
- [03:02] Item: 新增 `gateway-media-workflows.ts` 与 `gateway-media-workflows.test.ts`，把消息级 generated image 刷新、reasoning 卡片归一化与流结束卡片收口从 `grok-chat-service.ts` 中抽离；`bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build` 全部通过。
- [03:02] Item: 提交 `446dc59`（phase(119)），完成媒体工作流编排模块抽离。
- [03:03] Item: 提交 `6c3e9e2`（phase(119)），补齐当日 plan/memo 追踪记录。
- [03:19] Item: 启动 Phase 120，原计划继续拆 `streamTurn` 的分段状态机；代码审查后改为先抽离更低风险的完成态 turn 组装器，避免触碰 transport/recovery 主链。
- [03:26] Item: 新增 `gateway-turn-completion.ts` 与 `gateway-turn-completion.test.ts`，把 citation 合并、最终文案选择、reasoning duration 决策与 title 决议从 `streamTurn` 收口段抽离；`bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build` 全部通过。
- [03:26] Item: 提交 `f1f73fd`（phase(120)），完成流结束完成态组装模块抽离。
- [03:35] Item: 启动 Phase 121，继续拆 `grok-chat-service.ts` 的流式分段累计逻辑，目标先抽离每个 JSON segment 的状态机与去重收集器，不触碰 reader/recovery 主链。
- [03:43] Item: 新增 `gateway-stream-accumulator.ts` 与 `gateway-stream-accumulator.test.ts`，把 response id/title/final message/research/web search/generate image card 的分段累计、synthetic `tool_result` 与 idle timeout 安全重试判定从 `grok-chat-service.ts` 中抽离；`bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build` 全部通过。
- [03:45] Item: 提交 `49bc9fc`（phase(121)），完成流式分段累计器模块抽离。

- [04:10] Item: 启动 Phase 122，改按更稳的切口先抽 `grok-chat-service.ts` 的 turn 请求构建器，目标是下沉附件 data URI、用户文案拼装与 request payload 组装，不触碰 recovery/reader 热路径。
- [04:24] Item: 新增 `gateway-turn-request-builder.ts` 与 `gateway-turn-request-builder.test.ts`，把 `buildUserMessageText()`、附件 content block 序列化、`previous_response_id` 与 regenerate payload 组装从 `grok-chat-service.ts` 中抽离；`bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build` 全部通过。
- [04:27] Item: 提交 `0348e43`（phase(122)），完成网关 turn 请求构建器抽离。

- [04:33] Item: 启动 Phase 123，继续抽 `resolveRegenerateTarget()` 的 assistant 对齐算法，目标是把本地/网关消息匹配纯逻辑从 service 中移出，不触碰落库与 session 锚点更新。
- [04:46] Item: 新增 `gateway-regenerate-target-resolver.ts` 与 `gateway-regenerate-target-resolver.test.ts`，把 role-shape、assistant ordinal、exact-content 三层匹配从 `grok-chat-service.ts` 中抽离；`bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build` 全部通过。
- [04:46] Item: 提交 `4779fb9`（phase(123)），完成 regenerate target 解析模块抽离。
- [04:47] Item: 提交 `44a9b41`（phase(123)），补齐当日 plan/memo 追踪记录。
- [04:52] Item: 启动 Phase 124，继续抽 pending assistant 恢复编排，目标把消息补拉、turn state 轮询与 completion 后补拉等待从 `grok-chat-service.ts` 中整体移出。
- [05:04] Item: 新增 `gateway-pending-assistant-recovery.ts` 与 `gateway-pending-assistant-recovery.test.ts`，把 pending assistant 的消息补拉、turn state 判定、completion 后补拉与 polling 编排从 `grok-chat-service.ts` 中抽离；`bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build` 全部通过。
- [05:05] Item: 提交 `f1f04b7`（phase(124)），完成 pending assistant recovery flow 抽离。
- [05:06] Item: 提交 `a4a6c46`（phase(124)），补齐当日 plan/memo 追踪记录。
- [05:18] Item: 启动 Phase 125，将 `persistRecoveredCompletedTurn()` 与 `attemptTurnRecovery()` 合并成一轮更大的拆分，避免继续做碎片化切口。
- [05:32] Item: 新增 `gateway-recovered-turn-persistence.ts` / `gateway-turn-recovery.ts` 及对应测试，把 completed turn 持久化与 turn recovery polling/分支决策整体从 `grok-chat-service.ts` 中抽离；修复了中途发现的 `createConversationRecord` 参数接错回归，随后通过 `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build`。
- [05:34] Item: 提交 `bd56895`（phase(125)），完成 turn recovery workflows 抽离。
- [05:35] Item: 提交 `7cd9784`（phase(125)），补齐当日 plan/memo 追踪记录。
- [05:42] Item: 启动 Phase 126，按“1 & 2”一起推进，将 `streamTurn()` transport shell 与 generated-image renew I/O 合并成一轮更大的拆分。
- [06:06] Item: 新增 `gateway-stream-transport.ts` / `gateway-generated-image-renewal.ts` 及对应测试，把请求发送、HTTP/idle timeout 重试、anchor conflict 去锚、reader 读流与 generated image renew I/O 从 `grok-chat-service.ts` 中抽离；中途修正了 renew 用例数据不符合真实 `rawUrl + local asset_id` 前提的问题，随后通过 `bun run typecheck`、定向 `bun test`、全量 `bun test`、`bun run build`。
- [06:08] Item: 提交 `e4909c7`（phase(126)），完成 stream transport 与 media renewal 抽离。

- [06:09] Item: 提交 `e1362bf`（phase(126)），补齐上一轮 stream transport / media renewal 的 plan 与 memo 追踪记录。
- [06:16] Item: 启动 Phase 127，按“1 & 2”一起推进，把 `streamTurn()` 里剩余的 event/meta 发射器与 conversation preflight/bootstrap 合并成一轮更大的拆分。
- [06:22] Item: 新增 `gateway-stream-event-emitter.ts` / `gateway-turn-bootstrap.ts` 及对应测试，把 started/delta/reasoning/heartbeat/completed/failed 事件发射、单调 `meta.seq`、conversation 预写入、fallback title 与 user message append 从 `grok-chat-service.ts` 中抽离；随后通过 `bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build`。
- [06:24] Item: 提交 `36ae3a8`（phase(127)），完成 stream event emitter 与 conversation preflight 抽离。

- [06:26] Item: 提交 `0c962f7`（phase(127)），补齐 stream event emitter / turn bootstrap 的 plan 与 memo 追踪记录。
- [06:31] Item: 启动 Phase 128，改按更合适的切口继续下沉 completion persistence glue 与 regenerate target sync，避免回到 `streamTurn()` 热路径。
- [06:39] Item: 新增 `gateway-conversation-persistence.ts` / `gateway-regenerate-target-sync.ts` 及对应测试，把会话记录构造、fallback title、assistant turn 持久化、anchors upsert、本地 assistant response id 回写与 session 对齐从 `grok-chat-service.ts` 中抽离；随后通过 `bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build`。
- [06:41] Item: 提交 `8a43cb7`（phase(128)），完成 completion persistence glue 与 regenerate sync 抽离。
- [06:42] Item: 提交 `eef2269`（phase(128)），补齐 completion persistence glue / regenerate sync 的 plan 与 memo 追踪记录。
- [06:47] Item: 启动 Phase 129，优先拆 `grok-chat-service.test.ts`，把媒体、恢复协议与重生成场景拆到独立测试文件，降低后续 service 重构的测试噪音。
- [08:59] Item: 新增 `grok-chat-service-test-helpers.ts`、`grok-chat-service.media.test.ts`、`grok-chat-service.recovery.test.ts`、`grok-chat-service.regenerate.test.ts`，`grok-chat-service.test.ts` 收敛为基础流式/普通对话回归；定向测试通过。
- [09:00] Item: 提交 `44cedf1`（phase(129)），完成 `grok-chat-service.test.ts` 拆分。
- [07:00] Item: 提交 `b220852`（phase(129)），补齐服务级测试拆分的 plan 与 memo 追踪记录。
- [07:05] Item: 启动 Phase 130，继续拆 `chunk-parsers.ts`，把公共解析、card attachment 与 research/citation 聚合切到聚焦模块。
- [07:19] Item: 新增 `chunk-parsers-common.ts`、`chunk-parsers-card-attachments.ts`、`chunk-parsers-research.ts` 与对应测试拆分文件，原 `chunk-parsers.ts` / `chunk-parsers.test.ts` 收敛为聚合入口与基础解析回归；定向测试通过。
- [07:20] Item: 提交 `7d658dc`（phase(130)），完成 chunk parser 模块化拆分。
- [07:21] Item: 提交 `bca41af`（phase(130)），补齐 chunk parser 拆分的 plan 与 memo 追踪记录。
- [07:26] Item: 启动 Phase 131，继续抽 `grok-chat-service.ts` 剩余的恢复协议 runtime 装配层，重点保证 service 构造后替换 `runtimeFetch` 的测试语义不回归。
- [07:39] Item: 新增 `gateway-recovery-runtime.ts` 与 `gateway-recovery-runtime.test.ts`，通过 getter 读取 fetch/store/logger/runtime 配置，避免闭包提前捕获旧依赖；随后通过 `bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build`。
- [07:40] Item: 提交 `47bf546`（phase(131)），完成 recovery runtime 抽离。
- [07:41] Item: 提交 `d672db0`（phase(131)），补齐 recovery runtime 的 plan 与 memo 追踪记录。
- [07:48] Item: 启动 Phase 132，将 `grok-chat-service.recovery.test.ts` 按 pending assistant 恢复与 stream/retry 两类场景拆开，优先降低 service 级恢复测试噪音。
- [07:56] Item: 新增 `grok-chat-service.pending-recovery.test.ts`，`grok-chat-service.recovery.test.ts` 收敛为 `streamTurn()` recovery/retry 用例，并改为复用 test helper 的 service/fetch 装配；定向 `bun test` 通过。
- [07:57] Item: 提交 `407a263`（phase(132)），完成 recovery 服务级测试拆分。
- [07:58] Item: 提交 `26f60a9`（phase(132)），补齐 recovery 服务级测试拆分的 plan 与 memo 追踪记录。
- [08:02] Item: 启动 Phase 133，继续拆 `chunk-parsers-research.ts`，把 citation/XML 提取与 research fragment merge 切到聚焦模块，保持对外导出不变。
- [08:12] Item: 新增 `chunk-parsers-research-citations.ts` 与 `chunk-parsers-research-fragments.ts`，原 `chunk-parsers-research.ts` 收敛为 raw chunk 聚合入口；随后通过 `bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build`。
- [08:13] Item: 提交 `ca93f78`（phase(133)），完成 research parser 内部拆分。
- [08:14] Item: 提交 `cb086b0`（phase(133)），补齐 research parser 内部拆分的 plan 与 memo 追踪记录。
- [08:20] Item: 启动 Phase 134，继续拆 `grok-chat-service.pending-recovery.test.ts`，按 pending state 与 anchor fallback 两组回归切开，并收敛 recovery 测试里的配置样板。
- [08:28] Item: `grok-chat-service-test-helpers.ts` 已支持 `AppConfig` overrides；新增 `grok-chat-service.pending-recovery-anchor.test.ts`，原 pending recovery 测试收敛为 pending state 场景；随后通过 `bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build`。
- [08:29] Item: 提交 `3822eb8`（phase(134)），完成 pending recovery 服务级测试拆分。
- [08:34] Item: 启动 Phase 135，继续拆 `chunk-parsers-research-fragments.ts`，把 steps/tool usage 与 object/merge 聚合切到两个聚焦模块。
- [08:43] Item: 新增 `chunk-parsers-research-steps.ts` 与 `chunk-parsers-research-merge.ts`，`chunk-parsers-research-fragments.ts` 收敛为最薄重导出层；随后通过 `bun run typecheck`、定向 `bun test`、全量 `bun test` 与 `bun run build`。
- [08:44] Item: 提交 `595395d`（phase(135)），完成 research fragment 内部拆分。
- [08:51] Item: 提交 `0d253c6`（phase(134)），补齐 pending recovery 服务级测试拆分的 plan 与 memo 追踪记录。

- [08:58] Item: 启动 Phase 136，先从 `gateway-media-assets.ts` 开刀，把 generated image asset、renew 解析、URL 代理与 card update 纯逻辑拆成聚焦模块，同时保持旧导出面不动。
- [08:59] Item: 新增 `gateway-media-asset-meta.ts`、`gateway-media-url.ts`、`gateway-media-renewal.ts`、`gateway-media-card-updates.ts` 与 `gateway-generated-image-assets.ts`，`gateway-media-assets.ts` 已收敛为薄导出层；媒体定向测试、全量 `bun test` 与 `bun run build` 通过。
- [09:00] Item: 提交 `8d4f072`（phase(136)），完成 gateway media asset helpers 拆分。

- [09:02] Item: 启动 Phase 137，继续拆 `markdown-normalize.ts`，把 card meta、链接规则、render tag 与 `<tool-meta>` 清洗切到聚焦 helper，避免 markdown 规范化继续堆成单文件。
- [09:03] Item: 新增 `markdown-card-meta.ts`、`markdown-link-utils.ts`、`markdown-render-tags.ts` 与 `markdown-tool-meta.ts`，`markdown-normalize.ts` 已收敛为 cleanup pipeline；`src/components/markdown-content.test.ts`、全量 `bun test` 与 `bun run build` 通过。
- [09:04] Item: 提交 `9fb7617`（phase(137)），完成 markdown normalize helpers 拆分。

- [09:06] Item: 启动 Phase 138，改按稳定 seam 拆 `chat-shell.tsx`，先切纯 helper、模型 state 与滚动/输入折叠 hook，避免直接动 send/regenerate/recovery 热路径。
- [09:07] Item: 新增 `chat-shell-helpers.ts`、`use-chat-shell-models.ts` 与 `use-chat-shell-scroll.ts`，`chat-shell.tsx` 已收敛到 1356 行；`src/features/chat/chat-stream-runtime.test.ts`、`src/features/chat/chat-conversation-store.test.ts`、全量 `bun test` 与 `bun run build` 通过。
- [09:08] Item: 提交 `7f09973`（phase(138)），完成 chat shell helpers 与 hooks 拆分。

- [09:10] Item: 启动 Phase 139，继续拆 `structured-reasoning.ts`，把 models、tools、deepsearch 与 agents 渲染分流到独立模块，避免结果页视图逻辑继续堆成一个文件。
- [09:11] Item: 新增 `structured-reasoning-models.ts`、`structured-reasoning-tools.ts`、`structured-reasoning-deepsearch.ts` 与 `structured-reasoning-agents.ts`，`structured-reasoning.ts` 已收敛为薄导出层；`src/components/markdown-content/structured-reasoning.test.ts`、全量 `bun test` 与 `bun run build` 通过。
- [09:12] Item: 提交 `b8689fb`（phase(139)），完成 structured reasoning 模块拆分。

- [09:14] Item: 启动 Phase 140，继续拆 `chat-message.tsx`，把 citations、attachments 与 actions 分开，保留消息布局与 PDF 导出 orchestration 在入口组件。
- [09:15] Item: 新增 `chat-message-citations.ts`、`chat-message-attachments.tsx` 与 `chat-message-actions.tsx`，`chat-message.tsx` 已收敛到 233 行；`src/components/chat-message-citations.test.ts`、全量 `bun test` 与 `bun run build` 通过。
- [09:16] Item: 提交 `4009dd3`（phase(140)），完成 chat message helpers 拆分。

- [09:18] Item: 启动 Phase 141，先拆 `chunk-parsers-common.ts`，把 records/JSON、response id 与 tooling/web search 公共解析切到内部模块，保持 `chunk-parsers-common.ts` 的对外导出不变。
- [09:19] Item: 新增 `chunk-parsers-common-records.ts`、`chunk-parsers-common-response-id.ts` 与 `chunk-parsers-common-tooling.ts`，`chunk-parsers-common.ts` 已收敛到 24 行；`readToolUsageCard()` 也已统一收口到 common 层。
- [09:20] Item: 提交 `6fb62d9`（phase(141)），完成 chunk parser common helpers 拆分。

- [09:22] Item: 启动 Phase 142，继续拆 `chunk-parsers.ts`，把 reasoning events、title/meta、response 与 message builders 四组职责切到内部模块，保持主入口只做重导出。
- [09:23] Item: 新增 `chunk-parsers-reasoning-events.ts`、`chunk-parsers-title.ts`、`chunk-parsers-response.ts` 与 `chunk-parsers-message-builders.ts`，`chunk-parsers.ts` 已收敛到 22 行，同时去掉了主入口里重复的 `readToolUsageCard()` 实现。
- [09:24] Item: 提交 `abc038e`（phase(142)），完成 chunk parser entry modules 拆分。

- [09:26] Item: 启动 Phase 143，只补一刀必要拆分：把 `chunk-parsers-card-attachments.ts` 的 generated image 解析与 card payload 解析拆开，不继续动高风险热路径。
- [09:27] Item: 新增 `chunk-parsers-generated-image-attachments.ts` 与 `chunk-parsers-card-attachment-payload.ts`，`chunk-parsers-card-attachments.ts` 已收敛到 71 行；`chunk-parsers-cards` 定向测试、全量 `bun test` 与 `bun run build` 通过。
- [09:28] Item: 提交 `712dfd1`（phase(143)），完成 card attachment 解析模块拆分。

## Commits

- `0090fac` `phase(116): extract chat stream runtime`
- `2057cbc` `phase(116): backfill plan and memo commit trace`
- `a9ea70c` `phase(117): extract gateway session recovery module`
- `41f75c5` `phase(117): backfill plan and memo commit trace`
- `af8ca9d` `phase(118): extract gateway media asset utilities`
- `c721071` `phase(118): backfill plan and memo commit trace`
- `446dc59` `phase(119): extract gateway media workflows`
- `6c3e9e2` `phase(119): backfill plan and memo commit trace`
- `f1f73fd` `phase(120): extract gateway turn completion module`
- `f5bd62f` `phase(120): backfill plan and memo commit trace`
- `49bc9fc` `phase(121): extract gateway stream accumulator`
- `7c66da7` `phase(121): backfill plan and memo commit trace`
- `0348e43` `phase(122): extract gateway turn request builder`
- `aea7274` `phase(122): backfill plan and memo commit trace`
- `4779fb9` `phase(123): extract regenerate target resolver`
- `44a9b41` `phase(123): backfill plan and memo commit trace`
- `f1f04b7` `phase(124): extract pending assistant recovery flow`
- `a4a6c46` `phase(124): backfill plan and memo commit trace`
- `bd56895` `phase(125): extract turn recovery workflows`
- `7cd9784` `phase(125): backfill plan and memo commit trace`
- `e4909c7` `phase(126): extract stream transport and media renewal`
- `e1362bf` `phase(126): backfill plan and memo commit trace`
- `36ae3a8` `phase(127): extract stream event emitter and turn bootstrap`
- `0c962f7` `phase(127): backfill plan and memo commit trace`
- `8a43cb7` `phase(128): extract conversation persistence and regenerate sync`
- `eef2269` `phase(128): backfill plan and memo commit trace`
- `44cedf1` `phase(129): split grok chat service tests`
- `b220852` `phase(129): backfill plan and memo commit trace`
- `7d658dc` `phase(130): split chunk parsers into focused modules`
- `bca41af` `phase(130): backfill plan and memo commit trace`
- `47bf546` `phase(131): extract gateway recovery runtime`
- `d672db0` `phase(131): backfill plan and memo commit trace`
- `407a263` `phase(132): split recovery service tests`
- `26f60a9` `phase(132): backfill plan and memo commit trace`
- `ca93f78` `phase(133): split research parser internals`
- `cb086b0` `phase(133): backfill plan and memo commit trace`
- `3822eb8` `phase(134): split pending recovery service tests`
- `0d253c6` `phase(134): backfill plan and memo commit trace`
- `595395d` `phase(135): split research fragment internals`

- `8d4f072` `phase(136): split gateway media asset helpers`
- `f524278` `phase(136): backfill plan and memo commit trace`

- `9fb7617` `phase(137): split markdown normalize helpers`
- `15a93b3` `phase(137): backfill plan and memo commit trace`

- `7f09973` `phase(138): split chat shell helpers and hooks`
- `efeb1d9` `phase(138): backfill plan and memo commit trace`

- `b8689fb` `phase(139): split structured reasoning modules`
- `6601719` `phase(139): backfill plan and memo commit trace`

- `4009dd3` `phase(140): split chat message helpers`

- `6fb62d9` `phase(141): split chunk parser common helpers`
- `0dbe885` `phase(141): backfill plan and memo commit trace`

- `abc038e` `phase(142): split chunk parser entry modules`

- `712dfd1` `phase(143): split card attachment parsers`

## Notes

- 无
