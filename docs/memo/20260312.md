# 20260312 Memo

## Item

- [15:22] Item: 启动 Phase 209，收敛语音“连接中”交互；本轮只处理灰态动态声波按钮、中性等待文案和轻量连接提示，不改语音主链逻辑。
- [15:34] Item: 完成 Phase 209，语音连接态已改为灰态动态声波按钮，补充轻量“连接中……”提示，并将等待文案从“请允许麦克风权限”改为中性表述；`bun test`、`bun run typecheck`、`bun run build` 通过。
- [15:42] Item: 启动 Phase 210，继续收敛语音连接中交互；本轮改为单行输入框连接态、移除用户语音自动回填，并把右侧声波从旋转改成真实高低起伏动画。
- [15:55] Item: 完成 Phase 210，语音连接中已改回单行输入框，用户语音不再回填 textarea，右侧声波切为逐柱高低起伏动画并复用更紧凑的柱状符号；`bun test`、`bun run typecheck`、`bun run build` 通过。
- [16:06] Item: 启动 Phase 211，给已连接语音面板里的小柱状声波接真实麦克风电平；本轮不改语音连接态布局，只补本地 mic level 采样、UI 驱动和最小回归测试。
- [16:18] Item: 完成 Phase 211，已用 LiveKit 本地 microphone track 的 audio analyser 驱动语音面板内小柱状波，讲话时柱条会抬升，静音/断开后回落；新增 mic level 映射与控制层派发回归测试，`bun test`、`bun run typecheck`、`bun run build` 通过。
- [09:25] Item: 启动 Phase 198，排查 `AI Memory & Vector DB Comparison` 中正文 inline citation pill 缺失问题；已确认上游返回了 `citation_card` 与 `inlineCitations`，当前是前端主动忽略正文 citation 并降级到底部 `Key Citations`。
- [09:42] Item: 完成 Phase 198，已将 `citation_card` 展开为正文域名 pill，并为 host-only markdown 链接补 pill 样式；新增相邻重复 citation 去重与 pill 渲染回归测试，`bun test`、`bun run typecheck`、`git diff --check` 全部通过。
- [09:45] Item: 记录 Phase 198 提交 `d0d02db`，对应 inline citation pill 渲染、正文/底部 citations 共存和回归测试已提交。
- [09:55] Item: 启动 Phase 199，修复底部 `Key Citations` 对同 URL 的 inline citation 仍按 `cardId + citationId` 去重、导致重复来源展示两条的问题。
- [10:01] Item: 完成 Phase 199，已将底部 citations 改为按 URL 去重，并新增“不同 cardId 指向同一 URL 只保留一条”的回归测试；定向 `bun test`、`bun run typecheck`、`git diff --check` 通过。
- [10:03] Item: 记录 Phase 199 提交 `7fc186b`，对应底部 `Key Citations` 去重修复与回归测试已提交。
- [18:20] Item: 启动 Phase 200/201，开始把语音功能从 UI 壳接到现有原生 LiveKit 链路；本轮边界收敛为 `session_id` 主链、LiveKit 原生静音、session-event 上报和 textarea transcript/文本回显，不引入 `voice-gateway realtime/ws_pcm/gateway_tts`。
- [03:55] Item: 完成 Phase 200，已补齐 `VoiceService` / `GatewayVoiceService` / `LivekitSessionController` 的 `session_id` 主链、`voice_gateway_session_id` 透传、LiveKit 文本事件解析和会话事件上报；定向 `bun test`、`bun run typecheck`、全量 `bun test`、`bun run build` 均通过。
- [04:08] Item: 记录 Phase 200 提交 `f0f62fb`，语音控制层、LiveKit 协议 helper 与 voice token/session-event 契约已按 `session_id` 主链提交。
- [04:12] Item: 完成 Phase 201，`ChatInput` 已接入真实 `LiveKit` 会话控制，`ChatShell` 已落语音 final 文本到当前会话与 anchors，语音模式 Enter 不再误走普通 `/v1/responses`。
- [04:15] Item: 记录 Phase 201 提交 `01c836c`，`ChatInput` / `ChatShell` 语音接线、voice final 文本落库与语音输入主链已提交。
- [06:48] Item: 完成 Phase 202，补齐 `GatewayVoiceService` 对 `/v1/responses` 配置的根路径归一化，并使用真实本机网关完成文本建会话 -> voice token -> session-events smoke test；验证同一 `session_id` 可复用到语音链路，`session_connected` 与 `session_closed(manual_close)` 均返回成功。
- [06:50] Item: 记录 Phase 202 提交 `5f6fdaf`，真实语音 smoke test、voice endpoint 根路径归一化修正及回归测试已提交。
- [06:58] Item: 完成 Phase 203，语音 assistant 非 final 文本事件已复用现有 `streaming_assistant` 渲染，并增加“首个 delta 源胜出”策略，避免 `response.audio_transcript.delta` 与 `response.text.delta` 对同一轮文本重复叠加。
- [07:01] Item: 记录 Phase 203 提交 `0e8f15a`，语音 assistant 实时 delta 渲染、首源胜出去重 helper 与回归测试已提交。
- [07:16] Item: 启动 Phase 204，排查桌面端点击语音后无反馈问题；已确认 `ChatInput` 只有在 `voiceEntryState === active` 时才渲染语音面板，启动失败又会被本地 `catch` 直接收回 UI；同时当前 macOS bundle `Info.plist` 缺少 `NSMicrophoneUsageDescription`。
- [07:31] Item: 完成 Phase 204，语音入口在 `connecting` 阶段即展开，失败后会在输入框下方显示错误文案；同时已新增 `src-tauri/Info.plist` 并通过 debug 打包确认 macOS bundle 包含 `NSMicrophoneUsageDescription`。
- [07:33] Item: 记录 Phase 204 提交 `f528e4b`，语音入口反馈修复、错误文案映射、macOS 麦克风权限声明与回归测试已提交。
- [07:42] Item: 启动 Phase 205，用户反馈桌面端语音报错 `Load failed` 且不希望错误直接显示在 UI；已确认语音网关请求仍在直接使用浏览器 `fetch`，与聊天主链的 `runtimeFetch` 不一致，高概率是桌面端请求失败源头。
- [08:04] Item: 完成 Phase 205，语音 token / session-event 已改走 `runtimeFetch`，输入框不再渲染语音失败文案；新增前端日志桥接命令，`voice.prepare` / `voice.connect` / `voice.issue_token` / `voice.session_event` 失败会写入 app log 的 `frontend.log`，并通过全量测试、前端构建与 Tauri debug 打包验证。
- [08:07] Item: 记录 Phase 205 提交 `081d739`，语音错误已统一落到 app log，桌面端语音网关请求已改走 Tauri runtime fetch。
- [08:22] Item: 启动 Phase 206，排查“无麦克风权限弹窗、UI 自动回退、对方只回文字无声音”问题；已确认当前语音链路在 `room.connect()` 后才开麦，失败时未强制断房，且远端 audio track 订阅后没有执行 `attach()`。
- [08:44] Item: 完成 Phase 206，语音入口点击时已先做 `getUserMedia` 预检以触发系统权限/设备错误；`LivekitSessionController` 已补远端 audio track attach/detach、隐藏播放容器与媒体失败强制断房；新增 `livekit-session.test.ts` 覆盖权限预检、远端音频挂载和开麦失败清理，并通过全量测试、typecheck 与前端构建验证。
- [08:47] Item: 记录 Phase 206 提交 `423e2ca`，语音媒体链路已补齐点击手势预检、远端音频播放挂载与失败断房清理。
- [09:18] Item: 启动 Phase 207，基于 `frontend.log` 复核语音闪退根因；已确认主因是 `/api/v1/voice/token` 返回 `400 Unknown session_id`，并非麦克风权限本身，恢复需要沿用上游 `conversation_id` 而不是本地会话 id。
- [09:36] Item: 完成 Phase 207，语音 token 已支持 `conversation_id + strict_resume` 恢复、legacy/纯文本错误消息解析，以及本地 `conv_*` 会话 id 过滤；新增 `gateway-voice-service.test.ts` 与 `chat-shell-helpers.test.ts` 回归，并通过全量 `bun test`、`bun run typecheck`、`bun run build`、`git diff --check`。
- [06:06] Item: 启动 Phase 208，按真实网关重查 `session_id` 主链；已确认出问题的旧 conversation 在 `/v1/sessions/{session_id}/state` 返回 `session_not_found`，而新鲜 canonical `session_id` 的 chat -> voice 探针可正常下发 voice token。
- [06:17] Item: 完成 Phase 208，为语音入口增加 chat session 存活检查；若本地 conversation 绑的是失效 `session_id`，则本地清空旧 `sessionId/lastResponseId` 后再进入语音，不再直接闪退。新增 `grok-chat-service.voice-session.test.ts` 与 stale anchor helper 回归，全量 `bun test`、`bun run typecheck`、`bun run build` 通过。
- [06:19] Item: 记录 Phase 208 提交 `cdc7844`，语音入口 stale session 校验、本地锚点摘除与回归测试已提交。

## Commits

- `d0d02db`: `phase(198): render inline citation pills`
- `7fc186b`: `phase(199): dedupe repeated key citations`
- `f0f62fb`: `phase(200): implement livekit voice controller`
- `01c836c`: `phase(201): wire voice sessions into chat shell`
- `5f6fdaf`: `phase(202): validate live voice gateway contract`
- `0e8f15a`: `phase(203): stream voice assistant deltas`
- `f528e4b`: `phase(204): surface voice connect feedback`
- `081d739`: `phase(205): route voice errors to app logs`
- `423e2ca`: `phase(206): repair livekit voice media flow`
- `cde32a2`: `phase(207): recover stale voice session anchors`
- `cdc7844`: `phase(208): guard voice entry against stale sessions`

## Notes

- 无
