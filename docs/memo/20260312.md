# 20260312 Memo

## Item

- [09:25] Item: 启动 Phase 198，排查 `AI Memory & Vector DB Comparison` 中正文 inline citation pill 缺失问题；已确认上游返回了 `citation_card` 与 `inlineCitations`，当前是前端主动忽略正文 citation 并降级到底部 `Key Citations`。
- [09:42] Item: 完成 Phase 198，已将 `citation_card` 展开为正文域名 pill，并为 host-only markdown 链接补 pill 样式；新增相邻重复 citation 去重与 pill 渲染回归测试，`bun test`、`bun run typecheck`、`git diff --check` 全部通过。
- [09:45] Item: 记录 Phase 198 提交 `d0d02db`，对应 inline citation pill 渲染、正文/底部 citations 共存和回归测试已提交。
- [09:55] Item: 启动 Phase 199，修复底部 `Key Citations` 对同 URL 的 inline citation 仍按 `cardId + citationId` 去重、导致重复来源展示两条的问题。
- [10:01] Item: 完成 Phase 199，已将底部 citations 改为按 URL 去重，并新增“不同 cardId 指向同一 URL 只保留一条”的回归测试；定向 `bun test`、`bun run typecheck`、`git diff --check` 通过。
- [10:03] Item: 记录 Phase 199 提交 `7fc186b`，对应底部 `Key Citations` 去重修复与回归测试已提交。
- [18:20] Item: 启动 Phase 200/201，开始把语音功能从 UI 壳接到现有原生 LiveKit 链路；本轮边界收敛为 `session_id` 主链、LiveKit 原生静音、session-event 上报和 textarea transcript/文本回显，不引入 `voice-gateway realtime/ws_pcm/gateway_tts`。
- [03:55] Item: 完成 Phase 200，已补齐 `VoiceService` / `GatewayVoiceService` / `LivekitSessionController` 的 `session_id` 主链、`voice_gateway_session_id` 透传、LiveKit 文本事件解析和会话事件上报；定向 `bun test`、`bun run typecheck`、全量 `bun test`、`bun run build` 均通过。

## Commits

- `d0d02db`: `phase(198): render inline citation pills`
- `7fc186b`: `phase(199): dedupe repeated key citations`

## Notes

- 无
