# AGENTS.md

## 适用范围

- 本文件适用于仓库根目录及其所有子目录。

## 指令优先级与冲突处理

1. 平台/运行时强约束优先于本文件。
2. 开发者即时指令优先于本文件。
3. 当开发者指令与本文件冲突时，AI 必须先提示冲突点并请求开发者确认；仅在确认后方可例外执行。
4. 发生例外执行时，必须在当日计划文档 `docs/plan/YYYYMMDD.md` 记录：冲突条目、确认结论、执行时间。

## 项目约束

- 当前项目重写已完成，UI、路由、数据层、网络层、状态管理、存储层均以现有实现为基线持续迭代。
- 模型调用统一走网关 HTTP 接口；当前未引入 AI SDK，也未引入 `openai` 原生 SDK。
- 工具链统一使用 Bun + Tauri CLI，不保留 Node 兜底路径。

## 写入规则

1. 变更必须最小化，只修改与当前任务直接相关的文件。
2. 计划文档统一写入 `docs/plan/YYYYMMDD.md`（年月日），按天归档；本次计划、明日计划、任务存档均落该目录。
3. 所有 `YYYYMMDD` 日期一律以 `Asia/Shanghai`（UTC+8）自然日计算，不使用本机系统时区。
4. 接口、模型路由、存储 schema 的变更必须附带类型定义或文档说明。
5. 任何删除性重构都要先确认替代路径可运行，再移除旧实现。
6. 文档默认使用中文；路径、命令、代码标识使用英文原文。

## 计划文档实施方法

1. 技术实施时，必须分 Phase 执行，哪怕只有一个 Phase。
2. 必须严格按照计划文档进行：分 Phase 执行、分 Phase 提交、分 Phase 验收。
3. 每个 Phase 必须在计划文档中有明确的目标、实施项、验收项、状态。

## 计划文档模板

- 文件路径：`docs/plan/YYYYMMDD.md`
- 可复用模板：`docs/plan/TEMPLATE.md`
- 必填结构：
  - `# 今日目标`
  - `## 范围`（In / Out）
  - `## Phase 计划`
  - `## Phase 验收记录`
  - `## 测试记录`（命令 + 结果）
  - `## 提交记录`（commit hash + 说明）
  - `## 例外记录`（若无写“无”）
  - `## 明日计划`

## Memo 规则

1. Memo 文档统一写入 `docs/memo/YYYYMMDD.md`，按天归档；`YYYYMMDD` 一律使用 `Asia/Shanghai`（UTC+8）自然日。
2. 每日 Memo 必须基于模板创建，使用 `Item` 逐轮记录当轮改动内容，风格类似 ChangeLog。
3. 每一次 commit 后都必须在当日 Memo 追加对应记录（至少包含 commit hash 与改动摘要）。
4. 每一轮自动 compact 之后，开始后续工作前必须先读取最新 Memo（`docs/memo/` 中日期最新的文件）。
5. 使用 `/new` 开启新对话时，开始工作前也必须先读取最新 Memo，再继续执行任务。

## Memo 模板

- 文件路径：`docs/memo/YYYYMMDD.md`
- 可复用模板：`docs/memo/TEMPLATE.md`
- 必填结构：
  - `# YYYYMMDD Memo`
  - `## Item`
  - `## Commits`
  - `## Notes`（若无写“无”）

## Phase 提交规则

1. 每个 Phase 完成后必须至少有 1 个 commit。
2. 不允许跨 Phase 混合提交（一个 commit 只对应一个 Phase）。
3. commit message 必须带 phase 标识，格式建议：
   - `phase(<n>): <summary>`
   - 示例：`phase(3): bootstrap tauri shell with bun scripts`
4. Phase commit 前必须完成该 Phase 的验收项自检（以当日 `docs/plan/YYYYMMDD.md` 记录为准）。
5. 若 Phase 过大可拆分多个 commit，但最后必须有一个明确的“Phase 完成”提交。

## 例外流程

1. 紧急修复：允许先修复后补文档，但必须在同一工作日补齐 `docs/plan/YYYYMMDD.md` 的 Phase、测试、提交记录。
2. 纯文档/注释/文案改动：可简化为单 Phase，测试可仅执行必要校验（如格式/类型检查）；省略项需在测试记录说明原因。
3. 开发者明确要求“本次不提交”：可不创建 commit，但仍需完成 Phase 执行与验收记录。
4. 任何例外都必须写入 `## 例外记录`。

## 测试

1. 当前不引入 e2e 测试。
2. 涉及逻辑变更时，至少执行并记录：`bun test`、`bun run typecheck`。
3. 若改动影响构建链路（打包、资源、路由入口等），追加执行并记录：`bun run build`。
4. 新增或修改核心逻辑时，必须新增或更新对应单元测试；若暂无法补测，必须在计划文档记录原因与补测计划。

## 质量底线

- 保持可构建、可运行状态，不提交明显不可执行的中间状态。
- 新增依赖要说明用途，并优先复用现有依赖栈。
- 错误处理和日志至少覆盖主链路失败场景（网络失败、鉴权失败、流中断）。

## AI 输出格式

1. 变更摘要：本次改了什么、为什么改。
2. 验证结果：执行了哪些命令，结果如何。
3. 风险与未完成项：当前已知限制、阻塞、待补事项。
4. 下一步：给出可执行的后续动作（如有）。
